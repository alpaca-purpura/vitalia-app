[project]
name = "luana-core-analytics-engine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "sqlalchemy>=2.0",
    "fastapi>=0.115",
    "structlog>=24.0",
    "httpx>=0.27",
    "arq>=0.26",
    "redis>=5.0",
    "luana-core-platform",
    "luana-core-iam",
    "luana-core-copilot",          # Story 6 T-16 — copilot_provider/ unlift requires copilot ports
    "google-auth-oauthlib>=1.2.0",
    "google-api-python-client>=2.118.0",
    "google-analytics-data>=0.20.0",
    "facebook-business>=22.0",
]

[project.entry-points."nicolify.copilot_providers"]
analytics = "luana_core_analytics_engine.copilot_provider:provider"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/luana_core_analytics_engine"]

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
