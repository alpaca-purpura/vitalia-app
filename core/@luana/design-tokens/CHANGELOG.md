# @luana/design-tokens — CHANGELOG

## 0.2.0 — 2026-06-08 (core-ds-foundation)
### Added
- Full token scale: `spacing` (Tailwind 4px-base), `radius`, `typography`, `color-names` — shared NAME contract (values stay per-brand, RN-5). Previously the package exported only `z-index`.
