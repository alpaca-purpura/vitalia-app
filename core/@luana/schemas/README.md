# @luana/schemas

**Version:** 0.0.1-alpha  
**Lift origin:** `AISALESHT/frontend/src/lib/zod-schemas/`  
**Lift commit:** `3282768` (feat(luana-ts-batch2): lift @luana/ui-kit, @luana/api-client, @luana/schemas)

## Overview

Shared Zod validation schemas for cross-feature form validation. Currently a
placeholder — schemas that import from `@/features/*` are module-coupled and
will be lifted into this package when their consumer features lift in later
stories.

## Key exports

- `src/index.ts` — placeholder (empty export); full schema catalog pending
  Story 3+ feature lifts
