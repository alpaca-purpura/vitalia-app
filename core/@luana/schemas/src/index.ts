// Placeholder — populated in Stories 4/5 when module Zod schemas are lifted.

// AutosaveContract — primitiva de autoguardado compartida (ADR-012, build-autosave-primitive-luana T-1)
export type {
  AutosaveStatus,
  UseAutosaveOptions,
  UseAutosaveReturn,
} from "./autosave";
