# @luana/eslint-config — CHANGELOG

## 0.1.0 — 2026-06-08 (core-ds-foundation · net-new package)
### Added
- `no-arbitrary-value` ESLint rule (flat-config): errors on Tailwind arbitrary values in locked axes {spacing, radius, font-size, color-hex}; allowlists sizing (w/h/min/max); named escape `// ds-lock-allow:`. Opt-in per brand (vitalia pilot). RuleTester SC-1/SC-2/SC-4.
