# Sales Agent Brand Voice

> **Slim pointer (W1-Phase2 eviction 2026-06-09 · tier: project).** Cuerpo operativo + ex-always-on body (creep guard ×5) en `sales-agent-expert` skill → `references/sales-agent-brand-voice.md`.

Trigger: tocás voz/prompts/specialists de `sales_agent` (engine o brand extension).

No-skip 1-liner: SSoT voz = `personality_profiles.system_instruction` (compiler v2, slot 5 `BRAND_VOICE` cache prefix) — NUNCA mirror/fine-tuning/rewriter-pass/hardcodeo de voz. Voseo: `spanish-text.md` NO aplica al output sales_agent (respeta voz tenant).
