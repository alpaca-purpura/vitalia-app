# Data Reliability Verification

> **Slim pointer (W1-Phase2 eviction 2026-06-09 · tier: project).** Cuerpo operativo + ex-always-on body (4 layers + status targets single-brand) en `metrics-expert` skill → `references/data-reliability.md`.

Trigger: modificás provider/stage-service/DTO/component del pipeline analytics.

No-skip 1-liner: cada cambio de data pipeline corre su layer de verificación (0 ETL · 1 source probe · 2 pipeline · 3 UI fidelity) — no existe "small data pipeline change".
