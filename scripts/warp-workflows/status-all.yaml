name: status-all
description: Dashboard del repo vitalia-app — branch + dirty/clean + last commit + Docker vitalia.
command: bash scripts/git/status-all.sh
tags:
  - luana
  - git
shells:
  - bash
  - zsh
