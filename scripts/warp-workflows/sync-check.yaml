name: sync-check
description: T1/T2 sync check para canónicos Luana — fetch origin/main + auto-FF si trivial + advisory.
command: bash scripts/git/check-sync.sh
tags:
  - luana
  - git
  - worktree
shells:
  - bash
  - zsh
