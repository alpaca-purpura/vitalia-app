[project]
name = "luana-client-simulator"
version = "0.2.0"
description = "Client simulator engine for Luana sales_agent quality testing (cross-brand). Scenarios live per-brand in {brand}/backend/tests/agentic_evals/simulator/scenarios/."
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100
