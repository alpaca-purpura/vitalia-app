# Harness Backlog (vivo)

> Issue-tracker liviano del harness. Captura sin fricción (`/harness-issue` o "anotá al harness backlog: X"). Estados: `reported → triaged → ratified → applied → verified`. Severidad: 🔴 silent-killer · 🟡 quick-win · 🔵 decision · 🟣 wave. Proceso: `docs/process/harness-lifecycle.md`. Detalle de items grandes vive en el catálogo (`docs/learnings/tooling/harness-audit-2026-06-01.md`) — acá solo el tracking.
>
> **Convención de estado:** `verified` = efecto confirmado (gate/live/re-read independiente). `applied` = commiteado pero el efecto no se ejerció aparte (típico de staleness de docs). `ratified` = Chris aprobó, falta aplicar.
>
> **★ Este backlog es el carril L1 del CIL** (`docs/process/continuous-improvement.md` · proceso v5 §5.7). El stop semanal `/harnesses-improvement` lo lee junto a L2 (learnings) · L3 (`tech-debt.md`) · L4 (cap-desfasada auto-detect). Opcional: taggear un ítem con `[L1]` en el texto (default L1).

| id | fecha | sev | item | estado | ref |
|---|---|---|---|---|---|
| HB-1 | 2026-06-01 | 🔴 | contract-guard.js muerto en worktrees (hardcode `/luana-platform/`) | **verified** | 17bf3c62 |
| HB-2 | 2026-06-01 | 🔴 | QW-1 comentario voseo-allowed línea-1 rompe registro skills (handoff/worktree-protocol/po) | **verified** | 17bf3c62 |
| HB-3 | 2026-06-01 | 🔴 | grep-bot self-id "Nicolify" | **verified** | 17bf3c62 |
| HB-4 | 2026-06-01 | 🟡 | voseo enforcement acotado a UI/agentic (descope harness) | **verified** | 17bf3c62 |
| HB-5 | 2026-06-01 | 🟡 | `make install-hooks` roto en worktrees → `git rev-parse --git-path hooks` | **verified** | 1a0dc598 (make -n) |
| HB-6 | 2026-06-01 | 🟣 | Wave 1 quick-wins (QW-3..QW-20: model IDs, containers visionarias_*, 04→06 tickets, WIP caps, overlay caps) | **verified** | 2388a13a (machinery-check) |
| HB-7 | 2026-06-01 | 🟣 | Wave 2 gates: #37 DoD + #33 CONN en templates + 5 globs de rules + tessl cleanup | **applied** | dc6a94fa · 32c01fcd · 46633718 · 3a9c53f2 — residual PII → HB-18 |
| HB-8 | 2026-06-01 | 🔵 | D-2 retirar git-manager · D-3 deprecar ux-disruptivo/ux-flow-architect/02-design-ui | **verified** | ab647839 (frontmatter línea-1 + bash -n) |
| HB-9 | 2026-06-01 | 🔵 | D-6 bump caps CLAUDE.md (270/165) · D-10 self-fix v4.2 canónico | **applied** | D-6 verified 2388a13a · D-10 propagación a verificar → HB-19 |
| HB-10 | 2026-06-01 | 🔵 | D-11 tessl: refs `tessl__*` → guía inline/`tessl-context` | **verified** | 3a9c53f2 (gates preservados) |
| HB-11 | 2026-06-01 | 🟡 | limpiar comentarios `voseo-allowed` heredados (ruido inofensivo) | **deferred** | **count real = 380 archivos** (no ~73 — estimate corregido 2026-06-02); ~76 en `docs/`+`.claude/`, resto = story docs vitalia. Removal blanket = alto-churn/cero-beneficio + product-code user-facing DEBE conservarlo (residual spanish-text.md). NO drenar salvo pedido explícito |
| HB-12 | 2026-06-01 | 🟣 | Wave 3 staleness masiva paths/vocab + `isolation: worktree` builders | **verified** | Wave3 96aa9559/ab647839 · isolation 2119d0c1 |
| HB-13 | 2026-06-01 | 🔵 | D-1: ¿`model:` campo válido de skills frontmatter? | **verified** | claude-code-guide vs docs oficiales → SÍ, turn-scoped (mantener) |
| HB-14 | 2026-06-01 | 🟣 | Wave 4: ADR-002/003/004/010/011 addenda + PARADIGM §7 DoD + D-4 banner + fix offer-preset test ref. (ADR-001/005/007/008/012/013 ya en Wave 3 → reconciliación ADR COMPLETA) | **applied** | cbc736ce (3 claims fácticas spot-checked; ADR-011 overestimate) |
| HB-15 | 2026-06-01 | 🔵 | D-5: crear scripts dev-app-up.sh + cloudflared-setup.sh + e2e-preflight.sh (referenciados, nunca existieron; `make dev-app-{brand}` roto) | **verified** | 22dacb4c (bash -n + shellcheck + e2e-preflight corrió verde live) |
| HB-16 | 2026-06-01 | 🟣 | Wave 5: agent runtime hints `isolation:worktree`(builders) + `background:true`(grep-bot) + `memory:user`(auditores/architect) + rule #37 honesty (tunnel credencial pendiente) | **applied** | 2119d0c1 — memory:user es enabler, activación → HB-17 |
| HB-17 | 2026-06-01 | 🟣 | **Wave 5b**: (a) `context:fork` análisis (b) `when_to_use` pilot (c) activar `memory:user` body-instructions | **applied** (cont. 4) | (a) **DEFERRED** — análisis vs docs CC oficiales (claude-code-guide): NO hay candidatos limpios (todos los skills son interactivos/ratify, ya delegan a Haiku, o son reference sin task → fork retorna vacío). (b) **applied** — pilot architect/auditor/dev-team/playwright (description↔when_to_use split, combined ≤1500; CC docs confirman when_to_use se appendea a description). (c) **applied** — `<memory>` body-instructions en 4 agents (auditor-{be,fe,agentic}+architect-orch). Pendiente commit ratif. |
| HB-18 | 2026-06-01 | 🟡 | PII scanners (`scan_seed_pii.py`/`scan_goldens_pii.py`) + `pii-sanitisation.md` stub — pre-commit §8-9 degradan a WARNING (gate PII inerte) | **verified** (cont. 4) | scanners en `scripts/` (lib compartida `_pii_scan_lib.py`) — email-denylist + intl-phone, NO flaggea DNIs sintéticos. **Live-tested:** exit 0 en goldens reales + core copilot; exit 1 en gmail+`+51` fabricado; whitelist suprime; ruff verde. pre-commit §8/§9 resolución → `scripts/` primero + legacy fallback. `.claude/rules/pii-sanitisation.md` stub + CLAUDE.md #11 repoint. Pendiente commit ratif. |
| HB-19 | 2026-06-01 | 🔵 | Verificar D-10 self-fix v4.2 propagado en todos lados (auditor SKILL · ADR-007 · rules-detail/auditor-self-fix-policy) | **applied** (cont. 4) | auditor SKILL: report template `/3 /4`→`/4 /5` + banner SUPERSEDED en sección v4.1 + caps anotados + ref `v4.1`→`v4.2`. rules-detail: 6 cap mentions anotadas `[v4.1 — v4.2: 5/4]` (sin borrar). **ADR-007 = OVERESTIMATE** (ya tenía bitácora v4.2 completa, líneas 150-181 — verify-first confirmó). Pendiente commit ratif. |
| HB-20 | 2026-06-01 | 🟡 | inconsistencia nombre workflow auditoría: comment del .js dice invocar `harness-audit`, `meta.name` dice `harness-audit-2026` | **applied** (cont. 4) | comment L1-2 alineado a `harness-audit-2026` (== meta.name, el invocable real); filename queda `harness-audit.js` por compat con refs HLP/backlog. Pendiente commit ratif. |
| HB-21 | 2026-06-01 | 🔵 | **operacional (Chris)**: tunnel Cloudflare + `DEV_APP_TEST_PASSWORD` | **verified** (cont.5 2026-06-02) | **RESUELTO.** TÚNEL: los 3 (vitalia/nicolify/comunify) provistos + connector docker UP + DNS routed; vitalia verificado live (`/`→307 + `/api/health`→200). API tokens (cfat_) gitignored en `{brand}/deploy/cloudflared/.credentials/cf-api.env`; `cloudflared-setup.sh` reescrito no-interactive (verificado). CREDS TEST: `DEV_APP_TEST_*` + `DEV_APP_CHRIS_*` seteados en `vitalia/.env.dev` (gitignored), **passwords verificados vía Clerk `verify_password`→true** (dr.demo + hola@). **Política cross-brand** de creación/almacenamiento/verificación de usuarios+claves test cementada en rule #37 (nombres de key compartidos, valores per-brand). lupulo = placeholder |
| HB-22 | 2026-06-01 | 🟡 | cola MEDIUM/LOW del catálogo sin wave explícita | **verified** (cont. 5) | cockpit README path (cont.4) + en B3/B6: cockpit README counts (route/lib/test) · playwright-expert body+frontmatter Nicolify→Luana · README v0.7. **NO aplicados (verify-first):** pre-push dynamic brands (las 10 marcas tienen dir capabilities/, 6 placeholders → discovery sin beneficio; hardcoded-4 correcto) · bidirectional dedup (diseño tiered intencional: pre-commit ADVISORY + pre-push HARD, no es bug). **DECISIÓN Chris pendiente:** settings.json StopFailure (cambia comportamiento) |
| HB-23 | 2026-06-02 | 🟣 | **Cierre B1-B6** (closeout reconciliado `harness-closeout-2026-06-02.md`): B1 10 bugs funcionales · B2 brand-overlay rules · B3 quick-wins+hooks · B4a templates · B4b process+rules · B5 skills HIGH · B6-residual | **verified** | 7 commits `aaf1041e`·`316f8ce8`·`42c52a78`·`ea9ad657`·`65e573fe`·`9cd049d6`·`1f162789`. Re-audit 6 superficies (read-only): B1-B5 DONE-verified + **0 regresiones**; 20/20 residuales cerrados en B6. 47 skills frontmatter L1 OK |
| HB-24 | 2026-06-02 | 🔵 | **DEFERRED → RE-SCOPED (investigación profunda 2026-06-23)**: skill file-size extraction. **Hallazgo:** la mayoría del tamaño de las 4 skills CORE (architect 983 · auditor 818 · dev-team 903 · po-ux 596) es **load-bearing o machinery-pinned**, NO extraíble sin degradar. Clasificación: **architect** = el ÚNICO con duplicación real (~360 líneas: el bloque inline `04-validators.yaml` 259-521 + `05-guidelines.md` 535-635 son copias verbatim de `docs/specs/templates/04-validators-template.yaml`+`05-guidelines-template.md`, que ya existen y son SUPERSET). **dev-team** (spawn-prompts operacionales, pin-densos AWAIT_CHRIS_VERIFY/PRODUCTOR/PISO HARD), **auditor** (CHECKPOINTS C1-C5, 67 líneas marginal), **po-ux** (Step 3 = guía in-context del PO, sin duplicación) → NO tocar. **GO-slice (único viable):** de-dup los 2 templates inline de architect → skeleton+pointer. Machinery-safe (CHECK 19/32 pinean `technical_gates.mutation`/`seam_coverage` por PRESENCIA, viven en prosa 233/255/525 fuera del bloque). **Pero:** el bloque ES el template que el architect produce inline (línea 257 "Template:") → reemplazarlo por pointer = read-indirection en la skill keystone del pipeline; verificable SOLO con un dry-run real del architect (producir un ready package post-extracción) que confirme cero pérdida funcional. Verify: `machinery 0 fallos` + read-through. **APLICADO 2026-06-23:** los 2 bloques inline reemplazados por skeleton (keys top-level = la forma) + pointer a la SSoT (`docs/specs/templates/0X-*-template.*`). Verificado: el archivo template es **superset estricto** del inline (cero pérdida — los archivos traen MÁS keys que el inline). architect SKILL.md **983→648 líneas** (−335 dup). machinery 72/0 (pins CHECK 19/32 sobreviven en prosa). El patrón pointer ya estaba probado vivo en Step 7 (06-tickets sin bloque inline). dev-team/auditor/po-ux confirmados sin duplicación extraíble segura (pin-denso/operacional/in-context) → quedan as-is por diseño. | applied | refactor skills CORE; GO-slice (architect 2-template de-dup) APLICADO 2026-06-23; payoff = brevedad (−335 líneas dup); resto de skills sin extracción segura (confirmado). |
| HB-25 | 2026-06-02 | 🔵 | skill-listing budget 1% excedido (27 descriptions dropped · `/doctor` warning) | **verified** | Trim ratificado Chris: `disable-model-invocation: true` en architect-{be,fe,agentic} (instruction-docs que el orchestrator LEE por path) + 6 pm-* stubs sin bootstrapear → salen del auto-listing del modelo, **siguen user-invocables**. 0 costo recurrente, 0 capacidad perdida. Verificado: registry re-renderizó, skills antes-dropeadas (tessl-context/offer-type-preset-expert/migrate/explore-module) volvieron al budget. Alternativas descartadas: subir skillListingBudgetFraction (+~13k tok/sesión) · aceptar drop |
| HB-26 | 2026-06-02 | 🔵 | **cockpit visualiza el Harness Backlog** (Vía B ratificada Chris): vista `/harness` = board read-only que parsea `harness-backlog.md` por estado (reported→verified + deferred) + tooltip por columna (qué significa + qué acción tomar). Nav transversal "core" separado de las vistas de marca. El `.md` sigue SSoT (cockpit solo LEE; captura/transición = HLP) | **verified** | parser puro `lib/harness-backlog.ts` + 9 tests · `/api/harness` · `components/harness/HarnessView` · 7 tooltips `harness_*`. tsc + **110 tests** verde · API live (26 items reales) · `/harness` 200 sin overlay. **Hover/kanban visual confirmado live por Chris 2026-06-02.** Built en wip/vitalia (excepción ratificada · `SCOPE_GATE_SKIP`) · `68db9ff8` |
| HB-27 | 2026-06-02 | 🔵 | **cockpit visualiza stories platform/core** (Vía A): pseudo-marca "Platform" read-only en el selector → el board lee `docs/product/stories/` (root, owner /pm-luana, ej. `empleados-ia-auto-extension`) con badge "CORE"; nav de marca (roadmap/arq/drift/map) gateado para platform. Complementa HB-26 (Vía B). NB: las stories platform SIGUEN el SDD de producto (10 estados) — esto solo las HACE VISIBLES, no cambia su ciclo | **verified (PURGADO — overestimate, YA construido)** | ✅ verify-first 2026-06-05: el código YA EXISTE — `lib/platform-context.ts` (`PLATFORM_SLUG`/`PLATFORM_LABEL`/`isPlatform`/`PLATFORM_VIEWS`=board+learnings, resto gateado), `BrandSwitcher.tsx` renderiza `⬡ Platform · core`, `workspace.ts::getSelectableBrands()` appendea platform si existe `docs/product/`, comment del archivo dice literal "Vía A · HB-27". El backlog decía "NO arrancado" = drift del propio backlog. Cerrado |
| HB-28 | 2026-06-03 | 🔵 | **Clerk dev-FAPI scaling para suites e2e largas** (cross-brand, playwright-expert): bajo carga acumulada (~144 page-loads autenticados ×3) el Clerk dev-FAPI / `next dev` se throttlea → los tests de autosave que corren TARDE necesitan un `getToken()` a mitad y el PATCH no dispara a tiempo. **Pasan aislados + temprano; fallan tarde.** NO es bug de producto ni de selector. Origen: `estabilizar-harness-e2e-lisa-marca` (suite 94% ×3; residuales `large-dataset:92`+`voice-warning:127` + el keystone flaqueó 1× hoy). Decisión Chris 2026-06-03: **aceptar retries=1 como verde-de-infra-externa** (estándar Playwright para transients) + abrir este item. Candidatos: reuso de token/context entre specs (storageState compartido), tier Clerk pago, o shard que baje page-loads/worker. ⚠️ tocar el auth fixture = riesgo alto → sesión dedicada **APLICADO 2026-06-23 (causa real diagnosticada + documentada · fix de config = acción Chris):** la investigación profunda CORRIGIÓ el diagnóstico — NO es `setupClerkTestingToken` (fetch one-time cacheado en `process.env`, sólo un `context.route()` interceptor → ~0 carga FAPI) ni `next dev` compile. El load real = el `getToken()` de la app (`useAuth().getToken()` en ~110 src files, incl. cada autosave hook como `useIdentityAutosave.ts:59`) minteando el **JWT default de sesión (~60s TTL)**; en suites largas el JWT cacheado expira → round-trip a dev-FAPI `/tokens` → Clerk dev rate-limit → los autosave specs tardíos throttlean. **Fix real (durable, no retry-mask) = subir el Session token lifetime del Clerk dev instance (dashboard → Sessions, 60s→max) — config, por marca, SOLO Chris.** Fallback sin dashboard: sharding (additive, sin tocar fixture). Rechazados: memoizar getToken en fetchClient (110 prod sites, enmascara expiry real) · tocar el auth fixture (es el no-op de alto riesgo que el item advertía). Documentado en `playwright-expert/references/clerk-auth-deep-dive.md § HB-28` + `vitalia/.env.dev.template`. | applied | NO arrancado. Afecta a todas las marcas con suites e2e largas. Owner natural: `playwright-expert`. Causa real + doc 2026-06-23; **acción Chris pendiente: dashboard TTL por marca.** **★ PROBADO POR API 2026-06-23 (con la key real de vitalia):** el session-token TTL default NO es settable por Backend API — `GET /v1/instance` no expone el campo, `PATCH /v1/instance` tampoco; el único lever API es un JWT template (solo aplica a `getToken({template})`, no a los ~110 call-sites default → cargo-cult). Es **dashboard-only by design** (Clerk). Capturado definitivo en `playwright-expert/references/clerk-auth-deep-dive.md § HB-28` (con la evidencia API) + pointer en `scripts/e2e-preflight.sh` (se surface al momento del dolor). Cerrado a nivel harness: mitigación `retries:1` aceptada (Chris 2026-06-03) + doc anti-re-misdiagnóstico; el dashboard-TTL queda como upside opcional (requiere login al dashboard, que la key NO da). Ya NO se re-investiga. |
| HB-29 | 2026-06-04 | 🟡 | **override shell-template drift vs raíz**: `vitalia/docs/specs/templates/01-spec-shell-template.md` no tiene § Mapa funcional + § Matriz de cobertura (capa humana cementada 2026-05-31 en `spec-mapa-funcional.md`); el override es anterior y nunca se sincronizó. Una shell-story refinada con él pierde la capa que Chris lee + la § Matriz que /auditor Phase D cruza con la gherkin-matrix | **verified** | resuelto este turn: § Mapa funcional (§5.5) + § Matriz de cobertura (§6.5) agregados al override, espejo del raíz. Drift-check enforcement → HB-30 (deferred). Origen: handoff a0ec7194+fa722b56 |
| HB-30 | 2026-06-04 | 🔵 | **no hay enforcement de drift override↔raíz spec-templates** (meta-gap de HB-29): cuando el template raíz `docs/specs/templates/01-spec-template.md` recibe una cementación, los overrides por marca (vitalia shell-template) no se sincronizan automático — el handoff 2-firmas fue 100% manual | **verified** | RESUELTO: CHECK 9 en `scripts/validate_machinery_consistency.py` (concept-based · corre en pre-commit/machinery-check) — `MANDATORY_SPEC_CONCEPTS` enforce que raíz + overrides por marca lleven los conceptos cementados. Negative test confirma que caza el drift de HB-29. Owner: /pm-luana al cementar concepto nuevo → agregarlo a la lista |
| HB-31 | 2026-06-04 | 🔴 | Dos /dev-team autónomos sobre el MISMO single-hub colisionan: el module-bucket lock (code:crm vs code:inbox) NO protege archivos FE shared (crm-shared/types.ts, ChannelBadge.tsx, globals.css, arch-tests) que ambas stories tocan; + la sesión paralela usó `git add -A`/`commit -a` y barrió 15 archivos de T-BE-1 a SU commit (mislabeled) — índice compartido + 3 fallos de commit por race. Fix: bucket `code:fe-shared` o lock a nivel-archivo + pre-commit que rechace `git add -A/-u/.` cuando hay >1 lock en .session-locks/ y exija pathspec | **applied** | ✅ 2026-06-04: Chris REVOCÓ la prohibición "no 2 /dev-team" (acelerar). Sweep-guard cableado: `scripts/git/multi-session-scope-guard.sh` + call en `scripts/git-hooks/pre-commit` (symlink, activo ya) BLOQUEA commit que stagea scope exclusivo de ≥2 sesiones vivas (story-folder + módulo). 4/4 tests (PASS single · BLOCK sweep · override · neutro). M15 en parallel-safety. Fail-OPEN + `MULTI_SESSION_ACK=1`. Pendiente residual: el race de archivos FE-shared concurrentes (no-sweep) sigue cubierto solo por M8 read-first. origen: 1ra corrida autónoma; ⚠️ era silent-killer |
| HB-32 | 2026-06-04 | 🔴 | Builder agents (builder-backend, builder-agentic) auto-crean worktrees aislados desde `main` stale → el trabajo NO aterriza en el hub + rompe la cadena de deps (worktree desde main no ve ticket previo committeado en wip/{brand}). Contradice parallel-safety M9 (sub-agents in-place, NO worktrees). builder-frontend SÍ trabaja in-place (inconsistente). Workaround que funcionó: instruir paths ABSOLUTOS del hub in-place. Fix: alinear builders con M9 o documentar patrón absolute-path + extracción post-ticket | **applied** | ✅ 2026-06-08 (harness-audit): bullet IN-PLACE (NO worktree/branch from `main` · M9) agregado al STRICT SCOPE de `builder-backend.md` + `builder-agentic.md`; además fix de los comentarios de branch en builder-agentic (`wip/{brand}` hub, NO `wip/{story}-{ticket}`). origen adrian-embudo; era ⚠️ silent |
| HB-33 | 2026-06-04 | 🟡 | Pre-commit Story closure gate (Section 12 Layer 4) NO es module-aware: bloqueó commits de embudo (módulo crm) porque inbox (módulo inbox) estaba developed, aunque ADR-009 permite paralelo cross-módulo. Requirió STORY_CLOSURE_GATE_SKIP=1 por commit. Fix: hacerlo module-aware (consistente con rule slim story-closure-gate.md v2 module-scoped) | **applied** | ✅ 2026-06-08 (harness-audit): `checks/12-story-closure.sh` ahora lee `module:` de cada checkpoint (open + staged) y SOLO bloquea si comparten bucket; cross-módulo (crm vs inbox) permitido (ADR-009). Conservador si el módulo es desconocido. origen adrian-embudo |
| HB-34 | 2026-06-04 | 🟡 | Section 5b cap-verification exige cap YAML re-staged en CADA commit que incluya checkpoint con cap_change_type:new, aunque el cap ya esté en HEAD sin cambios → fuerza touch artificial. Fix: 5b acepta cap YAML ya en HEAD (no exigir re-stage si existe) | **applied** | ✅ 2026-06-08 (harness-audit): `checks/05b-cap-advisory.sh` resuelve `cap_target`→path (`resolve_cap.py`) y si el cap YAML existe en HEAD NO bloquea (cero touch artificial). origen adrian-embudo (3×) |
| HB-35 | 2026-06-04 | 🟡 | builder-frontend corta antes del lint-pass final (tsc/eslint) — en los 3 tickets FE de adrian-embudo vitest quedó GREEN pero dejó 1-2 errores lint/tsc que el orchestrator limpió (Carril A: cast unknown, any RHF+zod documented, unused var). Fix: builder corre tsc+eslint+vitest ANTES del result, o el orchestrator asume el lint-pass como paso propio | reported (DOWNGRADE → adherencia, no def) | origen adrian-embudo T-FE-1/2/3. **✅ verify-first 2026-06-05: el agent def YA es correcto** — `builder-frontend.md:38` dice "implementation isn't 'done' until /test-frontend blocker steps (tsc + eslint `src/` + vitest) report green". El gap no es la definición sino la ADHERENCIA en runtime (el builder no corrió su propio gate). Fix real = enforcement (orchestrator corre tsc+eslint como paso propio post-builder, o gate-runner obligatorio antes del result), no reescribir el def |
| HB-36 | 2026-06-04 | 🟡 | pathspec enumeration miss: commit T-FE-2 omitió archivo transitivamente-requerido (types/embudo-schema.ts) que compilaba en disco pero el commit aislado no — recuperado en T-FE-3. Lección/Fix: al commitear por pathspec incluir deps transitivas, o un check pre-commit de imports no-committeados **APLICADO 2026-06-23:** pre-commit Section 21 (`21-pathspec-transitive.sh` + helper `scripts/check_pathspec_transitive.py`) — para cada `.ts/.tsx` staged extrae imports LOCALES (`./ ../ @/`), resuelve a ruta repo (tsconfig `@/*`→`src/`), y BLOQUEA si la dep resuelta está untracked o modified-unstaged (fuera del snapshot del commit). Bare specifiers (`react`/`zod`/`next`) + `@luana/*` (workspace pkgs) + no-resuelve-a-archivo → skip (warn-not-block). Fail-OPEN + `PATHSPEC_TRANSITIVE_SKIP=1`. Tests ×3 (untracked→block · ambos-staged→pass · bare+committed-clean→no-false-pos). | applied | origen adrian-embudo. Gate 2026-06-23. |
| HB-37 | 2026-06-04 | 🟡 | Tooling paths desactualizados en docs: real es container `luana-dev-vitalia_backend_dev-1` + workdir `/workspace/vitalia/backend` + `/workspace/.venv/bin/alembic` (NO `luana-vitalia-backend-dev`/`/app`/`alembic` como dice debugging.md + overlay vitalia). Fix: actualizar `.claude/rules/debugging.md` + `vitalia/CLAUDE.md` | **applied (platform+vitalia · nicolify/comunify ROUTED)** | ✅ 2026-06-08 (harness-audit): fix de CLASE (no solo vitalia) — `debugging.md:29` (`/app`→`/workspace/{brand}/backend` + venv) + `AGENTS.md` L16 (alembic one-liner) + `vitalia/CLAUDE.md` L59/72/73/154 (container `luana-dev-vitalia_backend_dev-1`). `nicolify/CLAUDE.md` + `comunify/CLAUDE.md` tienen el mismo drift pero son OTRA marca (scope gate M13 desde wip/vitalia) → ROUTED a `/pm-nicolify` + `/pm-comunify`. origen adrian-embudo live-smoke. **✅ ground-truth fijado 2026-06-05 (vs `vitalia/docker-compose.dev.yml` + container vivo):** container real = `luana-dev-vitalia_backend_dev-1`; workdir backend = `/workspace/vitalia/backend` (compose L48); venv = `/workspace/.venv/bin/alembic` (L32). DRIFT confirmado en 2 lugares: `vitalia/CLAUDE.md` L59/72/73/154 usa `luana-vitalia-backend-dev` (nombre MAL) + `debugging.md:29` usa `/app` (workdir MAL, nombre OK). Fix listo para W2 |
| HB-38 | 2026-06-03 | 🔴 | **Critical Rule #37 (live-verify dev-app) es PAPER RULE — enforcement layers 2/8/9 ⏳ PENDING, NO cableados en skills.** dev-team Step 5 cierra `developing→developed` con SOLO validators-green + gherkin-local-coverage; NO bloquea por `dod_live_verified`/`dod_evidence`/`demo-script` ("NO cerrar por tests verdes mockeados" es texto, no código). auditor Phase D declara el check en SKILL.md pero `auditor-frontend/backend.md` NO tienen auto-FAIL ni corren Chrome MCP. architect lo trata "recomendado" (dirigido a sí mismo) + `04-validators-template` SIN `playwright_visual_scope` + `assignment` FE sin `chrome-devtools-verify` forzado. Único gate real = `pm-{brand}` Fase F merge REFUSE (demasiado tarde + saltable, las stories ni llegan). Fix: gate duro en developed-boundary (dev-team) + auto-FAIL auditor + architect hard-instruction. | **applied** | ca615ba6 (efecto se ejerce en próxima corrida de story funcional) · ⚠️ silent-killer; diagnóstico /pm-luana 2026-06-03 (4 sondas) · learning `docs/learnings/tooling/2026-06-03-paper-rule-enforcement-gap.md` |
| HB-39 | 2026-06-03 | 🔵 | **auditor no es "auditor real"** (pedido Chris): (a) no corre live E2E él mismo, confía en self-report; (b) no empoderado a hacerse cargo del fix — self-fix policy lo manda a Carril B/C sin reflex; (c) sin mecanismo para responsabilizar al architect cuando el root cause es upstream (04-validators no declaró `verification_nature` / no cableó el skill). Decisión Chris: modelo de ownership del auditor (correr ≥1 write live + finding `## Upstream deficiency` + auto-backlog). | **applied** | ca615ba6 · Chris ratificó "arregla TODO incl. build" (Carril R default) + 2 escapes (stake-asimétrico → Carril C · feature-entera → PLAN). diagnóstico /pm-luana 2026-06-03 |
| HB-40 | 2026-06-03 | 🔴 | **Falta el REFLEX de auto-hardening.** Cuando dev-team/auditor/pm detecta que un gate de Critical Rule fue saltado, NO captura automático un harness-backlog entry + learning. El loop de mejora del harness depende de que Chris lo note a mano ("lo descubro yo en mi día a día"). Fix: regla "gate-skip detection → MUST append HB + learning antes de cerrar turn", cableada especialmente en auditor. | **applied** | ca615ba6 (reflex en auditor SKILL + self-fix-policy v5 + rule #37 layer 11) · ⚠️ meta-gap; diagnóstico /pm-luana 2026-06-03 |
| HB-41 | 2026-06-03 | 🟡 | `04-validators-template.yaml` NO tiene campo `playwright_visual_scope` (solo en `architect/references/autonomous-mode.md`) → dev-team no lo recibe salvo que architect lo agregue a mano. + `assignment` block FE sin `must_load_skills` forzado de `chrome-devtools-verify`. Fix: portar `playwright_visual_scope` al template + default FE assignment incluye `chrome-devtools-verify`. | **applied** | ca615ba6 (playwright_visual_scope + dev_app_verified en template; hard-step architect) · diagnóstico /pm-luana 2026-06-03 |
| HB-43 | 2026-06-04 | 🟡 | **Cap-as-locator dormido: el pipeline NO lee las caps (dev_preview/code_ref) que ya tienen los punteros a los archivos → ahorro de tokens no se banca.** Diagnóstico /pm-luana 2026-06-04 (Chris): (1) `architect-orchestrator` NUNCA lee `capabilities/{m}/{cap}.yaml` como input; (2) trigger builder "If `06-tickets.yaml` declares `cap_target`" roto: `cap_target` vive en **checkpoint.md** (0/3 en 06-tickets); (3) footgun raíz: `cap_target`→path **no resoluble** (formas mixtas path/functional_area/área). **2 fixes:** (A) cablear architect-orchestrator + context-builder + builders a leer la cap; (B) resolver determinístico del footgun slug→path. | **verified** | ✅ 2026-06-04 (misma convo, mandato Chris "que REALMENTE funcione"). **Resolver:** `scripts/resolve_cap.py` (TIERS preciso/fuzzy · maneja path-style/functional_area/ÁREA-multi-cap · `--extract` dev_preview/code_ref/scenarios). **2 footguns cazados en verificación que habrían roto el cable en prod:** (i) slug→path no-determinístico (`adrian.inbox`→2 caps de área, `lisa.doctores`→`clinics/lisa-doctores.yaml`); (ii) cap YAML tiene **>1 doc `---`** con cola malformada → `safe_load` reventaba → split-por-chunk+try/except. **Gate relajado** de `cap_change_type∈{fix,extend,derive}` → `cap_target` no-null (agujero: `lisa.doctores` es `new` PERO ya tiene main_component real multi-sesión → el gate lo saltaba). **Cableado 5 superficies** (builder-backend/frontend · context-builder §4.5+step_3b · architect-orchestrator Step1 · architect SKILL). **Anti-rot:** CHECK 10 en `validate_machinery_consistency.py` (corre en pre-commit) asegura resolver existe + 5 superficies lo referencian — **negative-test con dientes** (quité el ref → CHECK 10 FALLA exit 1 → restaurado). 12/12 tests resolver · ruff verde · machinery 21/21 0 fallos · e2e real adrian-embudo(fix)→main_component+route. Pendiente commit ratif |
| HB-44 | 2026-06-04 | 🟡 | **2da instancia del contrato-imaginado (HB-42) — el registry manual + live-verify por-superficie incompleta lo dejaron pasar.** Chris (live-verify de SU demo, dev-app) pegó un runtime crash en `/{tenant}/adrian/embudo/{leadId}/resumen` → `ResumenView.tsx:184 autonomy.canDo.join` undefined. FE imaginó `AutonomyInfo {canDo, needsApproval, currentMode}`; BE emite `{operated_by, can, needs_ok}`. **Por qué pasó el gate "developed + dod_live_verified=true":** (1) la contract-parity test de HB-42 es **registry-based manual** → solo `LeadCardDTO` registrado; el DTO anidado `AutonomyInfo` nunca se agregó (el límite que HB-42 mismo declaró); (2) `board-live.spec` (la única live-verify) cubría board+writes pero **NUNCA el lead-detail Resumen** → live-verify por-superficie incompleta; (3) el unit `use-lead-detail.test.ts` **mockeaba la forma imaginada** (canDo/...) → falso-verde clásico. Fix aplicado este turn: FE realineado a BE (`can`/`needsOk`) + guard `?? []` + **AutonomyInfo agregado al CONTRACT_PAIRS** (gate) + fixture del unit corregido a la forma real + **`resumen-live.spec.ts`** (live-verify del Resumen, real-backend authed, base.ts) → verde contra dev-app (`GET /detail 200`, sin burbuja, lead `0d2313ff` real). **Sistémico abierto:** (a) contract-parity manual → cualquier view-model FE nuevo contra un DTO BE no-registrado es invisible (fix real = auto-discovery de pares, no manual); (b) la live-verify debe cubrir **toda superficie user-reachable** de la story (board + nuevo + resumen + historial + recuperar), no solo la principal. | **applied** | ✅ 2026-06-04: bug + guard cerrados (`embudo.types.ts` + `ResumenView.tsx` + `test_fe_be_contract_parity.py` + `use-lead-detail.test.ts` + `resumen-live.spec.ts`). Live-verified dev-app. reflex /pm-vitalia · extiende [[verification-real-not-200]] + HB-42 |
| HB-42 | 2026-06-03 | 🔴 | **Contract test FE↔BE ausente del gate → drift snake↔camel invisible hasta live.** embudo: FE construido contra contrato IMAGINADO (camelCase + campos que el BE no manda: `lastActivityDescription`/`lastActivityAt`); los 8 hooks hacen `fetchClient<CamelType>()` sin transformar el JSON snake del BE `crm` → board crashea LIVE (`LeadCard.tsx:88 buyingSignals.slice` undefined, burbuja Next). "6/8 GREEN" era 100% mockeado: component tests con fixtures camelCase + BE unit tests snake **nunca se encontraron**. Lo cazó la live-verify (base.ts anti-burbuja), NO el gate de unit. Fix: gate de contrato FE↔BE (type-gen desde OpenAPI / schemathesis / contract test que falle si el view-model FE referencia un campo que el DTO BE no expone) + architect declara el mapeo REAL BE↔FE en 03-arch (no "imaginado"). | **applied** | ✅ 2026-06-04: `vitalia/backend/tests/architecture/test_fe_be_contract_parity.py` (arch-fitness, corre en el gate): importa el pydantic DTO BE → `model_fields` camelizados, parsea la interface FE → asserta FE ⊆ BE; FALLA si el FE declara un campo que el BE no expone. **Verificado con dientes** (negative test: inyectar `imaginedGhostField` → RED). Registry-based (1 par: LeadCardDTO; pares futuros BoardKpis/BoardColumn/TransitionDTO = registro manual — límite de cobertura honesto). reflex /pm-vitalia 2026-06-03 · complementa [[verification-real-not-200]] |
| HB-45 | 2026-06-03 | 🟡 | **validator false-green**: `04-validators` (plantilla + instancias) usa `--project=smoke` para specs que viven en `e2e/regression/` → Playwright matchea **0 tests = falso verde**. Detectado en `nicolify-r0-sitemap-completo` (`nav_walk_v3`/`empty_states_all`/`visual_empty_state_n3`). El gate "pasa" sin correr nada | **applied** | ✅ 2026-06-08 (harness-audit): doble fix — (1) `04-validators-template.yaml` integration_e2e usa `--project=regression` (coincide con el dir del spec) + `expect_min_tests: 1`; (2) SISTÉMICO `gate-runner.md` (parse_gates + regla #13): "0 tests matched = FAIL, nunca PASS" para pytest/vitest/playwright (mata toda la familia falso-verde HB-42/44/45). origen story nicolify-r0-sitemap-completo FLAG #5. Riesgo: suite "verde" que no ejerció nada (rompe DoD #37). El auditor de esa story corrió manual `--project=regression` (warm 33/0). Fix candidato: `/architect` deriva el `--project` del dir del spec (regression vs smoke) + gate-runner/auditor tratan `0 tests matched` como **FAIL** (no PASS). ¿otras marcas igual? |
| HB-46 | 2026-06-03 | 🔵 | **validator over-spec**: `04-validators eslint_zero` usa `npx eslint src/ --max-warnings 0`, pero el gate REAL enforced (gate-runner `test-fe-{brand}`) = `npx eslint src/` (0 errores · warnings toleradas por ratchet arch-fitness). nicolify FE tiene ~102 warnings PRE-EXISTENTES (jsdoc/next-image/sonarjs) → `--max-warnings 0` inalcanzable sin cleanup masivo fuera de scope | **verified (PURGADO)** | origen story nicolify-r0-sitemap-completo FLAG #1. Fix: alinear el template `04-validators` `eslint_zero` → `npx eslint src/` (0 errores) + validator opt-in separado `eslint_zero_warnings` solo para stories de cleanup. Generalizar: revisar over-spec en otras marcas. **✅ verify-first 2026-06-05 (PURGADO — overestimate): el TEMPLATE ya está correcto** — `04-validators-template.yaml:122` usa `npx eslint src/features/{m}/ --cache` SIN `--max-warnings 0`; el único `--max-warnings 0` vivo es una nota de texto libre en `vitalia/.../adrian-embudo/04-validators.yaml:137`, no un validator real. La over-spec que HB-46 describe NO existe en el template. Cerrado |
| HB-47 | 2026-06-04 | ✅ applied | **DoD #37 live-verify ENMASCARA bugs (estado sembrado + URL de conveniencia)** — sesión `nicolify-r1-abel-icp-buyer` (renumber: era HB-33 en la lineage nicolify; colisionaba con el HB-33 de vitalia tras el squash). La live "verde" ocultó ~7 bugs reales que Chris destapó en dev-app: slug-vs-UUID (X-Tenant-ID) · cold-start/empty · orphan-mount (modal `<UniversalIntake>` sin montar) · contrato FE↔BE 422 (extract camel↔snake) · burbuja 404. | **applied** | Graduado a `.claude/rules/definition-of-done-live-verify.md § Anti-masking` (3 trampas: routing exacto slug+cuenta-Chris · cold/empty · orphan-mount = ejercer el CLIC real, no solo API/unit). Builder que debilita assert o desactiva failOnRuntimeError sin reemplazo → CHANGES_REQUESTED. |
| HB-48 | 2026-06-04 | 🟡 in-progress | **E2E Playwright FLAKY (nicolify)** — sesión `nicolify-r1-abel-icp-buyer` (renumber: era HB-32 en la lineage nicolify). retries:1 local esconde · fullyParallel+workers vs mismo backend/tenant · specs `E2E_*_ID` seeded · `next dev` hot-reload. | in-progress | RESUELTO para la suite **abel** (journeys self-provisioning + serial + retries:0). Pendiente: aplicar la misma hardening a la suite **r0-shell** (4 specs fallan serial — `nicolify/docs/observed-bugs/2026-06-04-r0-shell-regression-failures.md`). |
| HB-49 | 2026-06-03 | 🔵 | **cockpit traga parse-error de checkpoint mudo** (renumber: era HB-30 en la lineage nicolify) — `tools/luana-cockpit/app/api/stories/route.ts` gray-matter no tira excepción con YAML inválido → story cae silenciosa a `state='idea'` (columna equivocada, sin aviso). | **verified (PURGADO — ya fixeado)** | ✅ verify-first 2026-06-05: `app/api/stories/route.ts:52-72` YA tiene `catch (err)` que NO silencia — rescata `state` via regex + marca `parse_error` → board pinta badge rojo "⚠ checkpoint inválido" (el comment dice literal "Antes esto retornaba null… o caía a state=idea… — confusión silenciosa"). gray-matter/js-yaml SÍ tira `YAMLException` en key duplicada (confirmado por HB-51 G7), así que el catch dispara. La clase exacta de HB-49 está cubierta. Cerrado |
| HB-50 | 2026-06-04 | 🔴 | **3ra instancia verificación-≠-200: endpoint devuelve 200 pero NO commitea → write perdido en silencio; los unit con sesión mockeada son ciegos al commit faltante.** `vitalia-fase2-adrian-inbox`: TODAS las mutaciones del inbox (mode/pause/send/proactive/nudge + retract) usaban `Depends(get_async_session)` (que por diseño NUNCA commitea — delega al caller) + ningún service del inbox llama `session.commit()` → `PATCH /mode` + `POST /pause` daban **200 pero `pause_until` quedaba NULL / `handler_mode` sin cambiar en DB**. El optimistic-update de React Query enmascaraba la no-persistencia en la UI; pm9 declaró "pausa funciona · POST /pause 200" verificando SOLO el 200. Lo cazó el golden e2e (botón pausa flippea a "Pausado" solo si commitea+refetchea) + DB query directa. Fix: 6 factories de mutación → `get_async_session_committing` (la sesión que YA usaban los endpoints CRM por el MISMO bug; el 6º=retract lo cazó auditor-backend). | applied | gate `vitalia/backend/tests/architecture/test_mutation_handlers_commit.py` (cross-file Depends-closure · 7 inbox resueltos · 11 baselined AUDIT-PENDING → HB-80) 2026-06-16. Bug+fix cerrados (router.py · 103 BE tests verdes · golden 7/7 + DB-verified). **Sistémico abierto:** (1) arch-fitness que detecte handler mutación (POST/PATCH/PUT/DELETE) cuyo factory NO use `get_async_session_committing` y cuyo service NO tenga `.commit()` → FAIL; (2) los router-tests con sesión mockeada NO cazan commit faltante → toda mutación necesita ≥1 test de PERSISTENCIA (sesión real / live-verify con read-back DB). reflex autónomo /pm-vitalia · extiende [[verification-real-not-200]] + HB-42/44/47 · graduado a `inbox.adrian-inbox` cap business_rule `inbox-mutation-commits` |
| HB-51 | 2026-06-04 | ✅ BUILT 2026-06-05 | **bidirectional code↔cap roto para headers en forma `functional_area` (FE) vs `module.slug` (BE).** El índice `_code-index.json` mapea el string literal del header `# cap:`/`// cap:` a un cap_id `{module}.{slug}`. Pero el FE usa DOS convenciones: `crm.adrian-embudo` (module.slug → resuelve a `crm/adrian-embudo.yaml` ✅) y `adrian.inbox`/`lisa.marca`/`mateo.agenda` (forma `functional_area`/`agent.subtab`, **68/53/47 archivos** → NO resuelve a ningún `{module}/{slug}.yaml`). Consecuencia: el architect que grepea el cap_id canónico `inbox.adrian-inbox` NO encuentra los archivos FE (dicen `adrian.inbox`). Para `inbox.adrian-inbox` lo mitigué con `code_pointers` explícitos (cap→code) + `functional_area: adrian.inbox` (resuelve vía resolve_cap.py), pero el code→cap por header sigue sin resolver al cap_id. | **DECIDIDO (b) + SOLUCIÓN MÁXIMA · spec lista — Chris 2026-06-05 "resolver de una vez por todas"** | Base: (b) `functional_area` como alias del cap_id (cero churn de producto, reusa `resolve_cap.py` HB-43). **Solución fuerte/máxima = enforcement determinístico end-to-end de 8 capas → diseño COMPLETO en `docs/process/cap-deterministic-enforcement.md`:** (1) resolver único two-way `cap_id↔functional_area` SSoT; (2) **generator** `make new-cap` (scaffold schema-valid, Claude no hand-authorea); (3) schema validation pydantic pre-commit; (4) **6 gates HARD** (G1 header-resuelve · G2 área-viva-tiene-cap · G3 cap-tiene-hogar · G4 paths-existen · G5 superseded-válido · G6 map-coverage) — G1+G2 hubieran cazado ESTE incidente (header huérfano + caja Inbox vacía); (5) resolver único elimina la lógica paralela map-vs-índice (causa raíz); (6) skill = puntero al generator, NO describe el formato; (7) backstop pre-push + harness-audit; (8) `make cap-doctor` health report. Acceptance: negative test por gate + reproducir el origen (borrar la cap → G1+G2 RED). **Tanda harness dedicada verify-first, NO mid-feature.** Spec: `docs/process/cap-deterministic-enforcement.md`. Relacionado HB-43 + HB-50 (advisory≠enforcement). **★ BUILT 2026-06-05** (verify-first): 8 capas en `scripts/{resolve_cap,new_cap,validate_caps_schema,validate_code_cap_bidirectional,cap_doctor,generate_code_to_cap_index}.py` + hooks §5e/§4e (HARD vitalia+comunify · advisory nicolify/lupulo) + `make {new-cap,cap-doctor,caps-schema-check,cap-gates}` + cockpit `GET /api/capabilities/doctor` + machinery CHECK 11 (anti-rot). G1-G6 con negative test c/u + test repro (borrar cap inbox → G1+G2 RED). Backfill vitalia→0 (1 header re-apuntado + 6 paths de cap). **★ Corrección de diseño verificada:** `functional_area` NO es 1:1 — es la AREA del cockpit, 1:N (admin→5 caps); G1/G2 = ≥1, no ==1 (ver § Estado de construcción del spec). **★ G7 agregado 2026-06-05 (verify-the-real-consumer):** Chris reportó la caja Inbox vacía AUNQUE el gate Python estaba verde → **15 caps vitalia tenían claves YAML duplicadas** (`last_modified` ×14 + `map_box` ×1) que PyYAML tolera pero gray-matter/js-yaml del cockpit RECHAZA → caps ILEGIBLES → cajas vacías. Deduplicadas + **G7 cockpit-readable** (strict parse, HARD) cierra la clase: el validador ahora es tan estricto como el cockpit. Pendiente nicolify: 2 G6 = deuda rebuild de `/pm-nicolify`. |
| HB-52 | 2026-06-05 | 🔵 | **El cockpit muestra ruido-de-proceso en vez de función: "✨ Qué puedo hacer" leakea el estado interno del harness.** Al clickear una cap sin `scenarios` materializados (~la mitad de las 74 de vitalia · las v3.1 pre-v3.2), `ScenariosSection.tsx:21` renderiza *"Cap todavía v3.1 · migrará a v3.2 cuando la próxima story la toque (Fase F.3 enforce)"* en vez de decirle a Chris **qué hace la cap**. Root: "Qué puedo hacer" está cableado SOLO a los Gherkin scenarios; no hay fallback a la descripción funcional (la cap SÍ tiene `description`/`main_component`/`dev_preview`). El versionado v3.1/v3.2/F.3 es maquinaria interna del SDD — NO debe aparecer en la vista funcional/producto que Chris lee. Síntoma del tema más grande: la cap acumuló capas de proceso (v3.x, ledger, F.3 enforce, functional_area, code_ref) y perdió su trabajo #1 = decir en una frase qué tengo. | **verified (W1 · CHECK 12)** | origen Chris 2026-06-05 (sesión harness-polish). **DECIDIDO (C) — Chris 2026-06-05:** (A) AHORA = fallback de display (cap sin scenarios muestra `description`/`what_you_can_do` + caja + estado real, **CERO jerga de versión** v3.x/F.3/migrará) — barato, ~1 archivo `ScenariosSection.tsx`; (B) GRADUADO = backfill de `what_you_can_do`/`scenarios` en las ~39 caps v3.1. **Regla invariante:** "✨ Qué puedo hacer" SIEMPRE tiene respuesta funcional, nunca versión. Lidera W1 (tema-madre). Relacionado HB-43 (cap-as-locator) + HB-51 (cap-deterministic, BUILT). Aplicación = tras juntar todos los comentarios de la sesión (no mid-collection) |
| HB-53 | 2026-06-05 | 🟣 | **Rediseño de coherencia spec→done (anti-Frankenstein · re-secuenciar, no apilar).** Origen: Chris 2026-06-05 — historias gigantes dejan funcionalidad sin implementar + invisible; docs quedan stale y el auditor revierte lo que Chris pidió; no hay loop de prueba de Chris pre-auditor; los aprendizajes no se trasladan (backlog = solo notas). **Decisiones RATIFICADAS Chris 2026-06-05:** (1) **G · Chris-verify loop** entre `developed` y `reviewing` — default **pausa-y-ofrece** el kit (demo-script+dod_evidence+dev-app, que dev-team YA genera en developed/Layer 10); excepción = Chris pide "autónomo hasta done" → reúsa flag `autonomous_mode`. Sin estado nuevo: `state:developed` + `phase:AWAIT_CHRIS_VERIFY`. (2) **Scope dinámico EN G** (no slicing pre-emptivo): todo lo pedido se cumple; escenarios fuera-de-scope se deciden ítem-por-ítem (implementar-ahora si corto+necesario · o spawnear historia(s) nueva(s) en el verify con lo avanzado); **piso HARD: funcionalidad nueva → core/happy-path DEBE estar construido, nunca diferido**; el **ledger de cobertura vivo** marca ✅construido/→historia/⏳ahora (= la vista "qué NO está implementado"). (3) **R · Reconcile**: `/pm-{brand}` reconcilia 01-spec/03-arch/04-validators/cap a la realidad+cambios ratificados ANTES del auditor (formaliza el `scope_amendment` manual de inbox). (4) **Auditor lee docs RECONCILIADOS + `chris_verify_signoff`** — guardián de arquitectura, NO revierte scope que Chris ratificó. (5) **CIL · Ledger de Mejora Continua** (owner /pm-luana, transversal `docs/process/`, sin worktree especial; homologa en stop semanal merge→main→sync): 4 carriles **L1 harness** (=harness-backlog tipado) · **L2 producto/skills-arq** · **L3 deuda técnica** · **L4 capability-desfasada** (auto-detect cap_doctor + cap-anterior-a-cement-date); cada entrada CARGA el aprendizaje (problema→causa→fix→refuerzo→carril); story-closure RUTEA al carril (relaja learnings.md per-story a "un lugar donde se acumulen"); ritual `/mejora-semanal` lee los 4 carriles + reúsa board cockpit `/harness` (HB-26) extendido. | **applied (W2+W4+W5 · CHECK 13-18,22-27 · proceso v5 BUILT)** | umbrella del rediseño de proceso (esta sesión harness-polish). Aplicación = wave dedicada tras cerrar el diseño con Chris (NO mid-collection). Toca: lifecycle.md · story-closure-gate(.md+detail) · rule #37 · capability-protocol · skills {dev-team,auditor,pm-{brand},architect,po-ux} · 04-validators template · harness-backlog→CIL · cockpit. SSoT final = doc de proceso consolidado (entregable al término) |
| HB-54 | 2026-06-05 | 🔵 | **Mutation testing como gate diff-scoped (de Uncle Bob harness-sdd · mecaniza nuestra herida falso-verde).** Origen: Chris compartió `betta-tech/harness-sdd@uncle-bob-harness` 2026-06-05; verify-first del repo real (no del resumen). Mapeo: ya hacemos 5/6 de los agentes UB (spec_partner≈po-ux-debate · gherkin_author≈po-ux-R2 · tdd_craftsman≈dev-team · judge≈auditor-PhaseD · craftsman_lead+handoff≈pm+architect+cadena-artefactos); **el único gap = mutation testing first-class**. UB lo hace bien y barato: muta SOLO líneas nuevas/modificadas (lee qué tocó la feature), umbral 100% sobre nuevas, survivors en código HEREDADO = deuda documentada (no bloquea), loop-back por artefacto (reporte línea+mutación+test-gap-en-prosa → implementador escribe test RED). **Reúsa nuestros slots, 3 cosas:** (1) gate diff-scoped en límite `developed` para superficies que `/architect` marca mutation-críticas vía `verification_nature` (commit/persistencia HB-50 · dinero/pricing · gates PHI · state machines · transforms contrato HB-42/44); tool **mutmut** (BE)/**Stryker** (FE) NO su mutador toy; crítico=hard · resto=advisory (anti-costo). Vive en rule #37 §2 technical_gates + test-design-doctrine. (2) survivor→reporte=CHANGES_REQUESTED al fix-loop existente (mata "tests verdes mockeados"; endurece el kit de G). (3) **★ survivors heredados → carril L4 del CIL (capability-desfasada)** — mutar el diff de una story nueva destapa auto test-debt de las caps que toca → stop semanal. Cero fase nueva, cero agente nuevo. | **applied (W3 · CHECK 19-21 · degrade verificado; hard pending mutmut/Stryker install)** | recomendación /pm-luana — Chris ratifica. Refuerza familia verificación≠200 (HB-42/44/47/50) + se enchufa a HB-53 (§2 technical_gates antes de G · L4 del CIL). NO usar el `mutate.py` custom de UB; robar su DISCIPLINA (diff-scope + new-lines-threshold + survivor-loopback-artifact) |
| HB-55 | 2026-06-06 | 🟡 | **Cockpit `next start` sirve build STALE tras cambios de source → el efecto del harness se ve VIEJO aunque el código esté arreglado.** Origen: sweep cap-coherence L4 (`/pm-luana` 2026-06-06). El cockpit corriendo en :4002 (uptime 7h45m, `next start -p 4002` = build de PRODUCCIÓN) mostraba en el cap-drawer de `lisa-marca` la jerga **"Cap todavía v3.1 · migrará a v3.2… (Fase F.3 enforce)"** — EXACTAMENTE el ruido que HB-52 (W1) ya arregló en el SOURCE. `next start` NO recompila ante cambios; el string vive solo en `.next/dev/server/chunks/ssr/*.js` (build pre-HB-52), NO en el source ni en cap YAML ni en `/api/capabilities/status`. Verify-first DOBLE lo destapó: FS/source limpio + live stale = la verdad (sin live-verify se concluye "todo bien"; sin chequear source se culpa a la cap). **Riesgo sistémico:** cualquier fix de UI del cockpit (HB-27/49/51/52…) queda INVISIBLE para Chris si la instancia corriendo es un `next start` viejo → "lo arreglé" pero Chris ve lo de antes (familia DoD #37 "verde≠lo-que-el-usuario-ve", aplicada al PROPIO cockpit). | **applied (P0 daemon 2026-06-06)** | **★ P0 RESUELTO — `scripts/cockpit-daemon.sh {start\|stop\|status\|restart}`:** TRUE detach via `setsid nohup pnpm dev >LOG 2>&1 </dev/null &` + pidfile → sobrevive el cierre de terminal Y el reaping de Claude (verificado: `start` en un shell Bash → `status` UP desde un shell Bash SEPARADO = terminal-close analog). Port-check ahora SOLO LISTEN via `ss` (primario · `lsof` quedó **ciego** en este entorno, devuelve vacío aunque `ss` vea el socket) con `lsof -sTCP:LISTEN` fallback → mata el footgun ESTABLISHED. Health real = HTTP code de `/api/sessions` (no `/api/watch`, que es SSE y colgaría curl). Idempotente (start sano = no-op). `make cockpit-{up,down,status,restart}`. PIDFILE/LOG gitignored. **★ 2ª causa de caída cazada en P7 live-verify:** el dev server moría con **V8 heap OOM** en recompiles HMR pesados (log = `Factory::AllocateRaw` death-stack) — literalmente "se cae a cada rato" — el daemon detecta DOWN correcto pero no auto-revivía. Fix root-cause: `NODE_OPTIONS=--max-old-space-size=4096` en el start. Recovery si vuelve a caer: `make cockpit-restart`. Remediación previa (stale-build) verificada live: relancé `PORT=4099 scripts/cockpit-up.sh` (`next dev` Turbopack, source actual) → cap-drawer ahora muestra `user_facing_description` + "Disponible en:" (fallback HB-52), **0 jerga**. Fix candidato: (1) `make cockpit-up` debería preferir `next dev` (recompila) o, si sirve `next start`, **rebuild antes** + estampar el SHA/commit del build en el footer del cockpit para detectar staleness de un vistazo; (2) `scripts/cockpit-up.sh` port-check usa `lsof -ti` que matchea conexiones ESTABLISHED (un socket residual de Chrome retuvo el bind de :4002) → debería filtrar solo LISTEN. Evidencia: `docs/process/cap-coherence-audit-2026-06.md § F3-1` + screenshot. Relacionado HB-52 (source fix · este es el deployment) |
| HB-56 | 2026-06-06 | 🟡 | **El cap NO se "auto-genera": el CONTENIDO (scenarios/business_rules) es autoría manual de skill, sin gate de presencia.** Origen: Chris pidió verify-first de la afirmación "el dato auto-emerge + lo mantienen gates" (`cockpit-capability-levels-proposal.md`). Traza real idea→done: `new_cap.py` scaffold-ea el cap con `scenarios: []`/`business_rules: []` **vacíos (`# TODO`)** ("llená los TODO de CONTENIDO, NO el formato"); el contenido lo escribe `/po-ux`/`/po` en `01-spec.md` y `/pm-{brand}` lo **"Embed scenarios verbatim"** al merge (Fase F paso **manual #2**, instrucción de skill, NO gate). **NO existía gate "cap live+user_visible ⇒ ≥1 scenario/business_rule"** (grep vacío en validate_code_cap_bidirectional/cap_doctor/validate_caps_schema). El "≥1 scenario al merge" de new_cap.py + skill = **paper-rule**. ⚠️ **Corrección de dato (verify-first 2026-06-06):** el cuadrante peligroso (live+`user_visible`+0 scenarios) = **0** en las 3 marcas, NO 39 — los 39 vitalia sin scenarios son **28 deprecated + 9 planned + 2 live-infra(`user_visible:false`)**, ninguna es cap de valor live+visible. El gap era la AUSENCIA de gate (no violaciones actuales) → por eso G8/G9 entran HARD sin grandfather. | **applied (F2a 2026-06-06)** | **Corrige mi sobreestimación**: lo gateado REAL es la ESTRUCTURA (cap_doctor G1-G7 HB-51 HARD vitalia/comunify) + el LINK (cross_check_3) + COBERTURA en audit (gherkin-matrix Phase D, story-scoped). El CONTENIDO de PRESENCIA ahora es mecánico: **★ G8** (live+visible sin `user_facing_description`→drift) + **★ G9** (live+visible sin ≥1 scenario→drift) BUILT en `validate_code_cap_bidirectional.py` + dispatcher + cap_doctor GATE_IDS + machinery CHECK 11 (g8/g9 + negative tests `test_g8_red`/`test_g9_red` con dientes) + HARD vía pre-commit §5e `--cap-gates-hard` (vitalia/comunify). 0 violaciones hoy (vitalia G8/G9 = 13/13 pass). machinery 72/0, gate suite 46/46. **Pendiente:** G10 (cada `business_rule` ↔ ≥1 scenario) = nice-to-have, deferido. = **F2a de `cockpit-capability-levels-proposal.md`**. Relacionado HB-52 (display) + HB-51 (estructura) + HB-57 (accuracy, abierto) |
| HB-57 | 2026-06-06 | 🔴 | **Ningún gate verifica que el TEXTO del scenario siga reflejando el comportamiento real (accuracy drift silencioso).** `cross_check_3` valida SOLO que `e2e_test` **path existe + contiene patrones de test** (línea 183: "verify path exists + contains test patterns") — NO que el test PASE, NO que el given/when/then COINCIDA con lo que el test/código hace. La `R·reconcile` (Fase F.3) que debería realinear el cap a la realidad es un **paso MANUAL de `/pm-{brand}`** y corre SOLO cuando una story TOCA ese cap. Consecuencia: si un refactor cross-cutting o una story de un cap vecino cambia el comportamiento sin tocar ESE cap, o si yo (corriendo el skill de merge) olvido reconciliar, el cap-drawer muestra un caso de uso **lindo pero MENTIROSO** sin que nada lo cace. Es la familia "verde≠realidad" (HB-42/47/50) aplicada al PROPIO cap. | **partial (P1 mecanismo 2026-06-06 · backfill→L4)** | Fix candidato: (1) gate que detecte cap cuyo `e2e_test` linkeado cambió (git-diff) pero cuyo scenario no se tocó → flag stale; (2) o atar el cap a la live-verify (DoD #37): un scenario `status: live` exige evidencia de que su `e2e_test` PASA en el último run, no solo que exista; (3) el mutation-gate diff-scoped (HB-54) sobre el surface del cap destapa scenarios sin test real. NO sobre-construir: empezar por (2) que reusa infra existente. Relacionado HB-50/47 + cross_check_3. **★ Plan F2b (Chris ratificó vía mutation HB-54 · 2026-06-06 · PASO DEDICADO, no mid-sesión):** (a) `uv add --dev mutmut` + Stryker FE; (b) cablear `mutation_gate.py` diff-scoped al surface del cap (architect marca scenarios mutation-críticos vía `verification_nature`); (c) interim barato camino (2): scenario `status:live` ⇒ su `e2e_test` PASÓ en el último run. **Precondición ya hecha:** F2a (G8/G9 presencia) cerró que el cap TENGA casos de uso; F2b cierra que sean VERDAD. Doc: `cockpit-capability-levels-proposal.md` §4 F2b. **★ P1 BUILT (2026-06-06):** (a) `uv add --dev mutmut` (3.5.0 · `mutation_gate.tool_for(BE)` ya NO degrada) — **Stryker FE deferred-advisory** (decisión verify-first: el degrade-advisory ya cubre FE sin romper; `tool_for` FE asume `node_modules` root que no calza con el layout per-brand-frontend; mutmut cubre las superficies accuracy-CRÍTICAS = BE pricing/PHI/state/persistencia; FE = step dedicado cuando architect marque un surface FE mutation-crítico); (b) `mutation_gate.py --cap <cap_id>` resuelve el surface del cap vía `_code-index.json::cap_to_files` + lo INTERSECTA con el diff (diff-scoped real · lisa-marca→58 archivos); (c) camino-(2) **HONESTO**: gate HARD "live⇒e2e PASÓ" es **data-blocked** (verify-first: vitalia tiene 139 scenarios live, **135 sin `verified_real` (solo 4/139 poblado) + 33 sin `e2e_test`** → un gate hard inventaría 135 evidencias = el anti-patrón). En su lugar `cap_doctor.py --accuracy` MIDE la deuda (advisory, exit 0) → ruta al carril L4 del CIL (backfill incremental, sin mass-invent). El accuracy de CÓDIGO sí está cableado (mutation `--cap`). **Estado: partial-applied** (mecanismo real listo; backfill de scenarios = L4). |
| HB-58 | 2026-06-06 | 🔵 | **`verified_real` es campo decorativo: 0 lecturas en scripts/skills + ni siquiera está en el type `CapScenario` del cockpit.** Varias caps (lisa-marca) anotan `verified_real: true` por scenario, pero ningún gate/loader lo lee → un badge "✅ verificado en vivo" construido sobre él (como propuse en F1 de cap-levels) **MENTIRÍA**. | **applied (P2/P3 2026-06-06)** | **★ RESUELTO con OPCIÓN (a): cableado, NO dropeado.** `verified_real` ahora tiene DOS lectores reales: (1) `cap_doctor.py --accuracy` (P1, mide la deuda) + (2) el **badge de verdad N1** del cap-drawer (P3). Agregado al type `CapScenario` (`verified_real?: CapScenarioVerifiedReal | boolean | null`, shape real `{at, how}` — 4/4 en vitalia son objetos, no booleanos). El badge deriva del dato real (lógica pura testeada `lib/cap-badges.ts::scenarioTruth` · 4 tests): ✅ verificado-live (verified_real presente) · 🟠 con-test (e2e_test, sin vr) · ⚪ sin-test (ninguno · deuda L4). `readCapability` YA pasaba el scenario completo (gray-matter parsea todo el YAML) → no requirió passthrough nuevo, solo el type. El backfill de `verified_real` en los 135 scenarios que no lo tienen = carril L4 (no se inventa). Fix original: o (a) cablearlo a la evidencia real de DoD #37 (`dod_evidence`/`dev_app_verified`) — se setea `verified_real: true` SOLO cuando la live-verify de ese scenario quedó registrada; o (b) dropearlo y derivar el badge F1 de `scenario.status` + "e2e_test pasa". **Corrección a la propuesta**: el badge de verdad de F1 NO debe usar `verified_real` hasta cablearlo. Relacionado HB-57 + `cockpit-capability-levels-proposal.md` §2 (badge de verdad) |
| HB-59 | 2026-06-06 | 🟡 | **`cross_check_4` no puede atribuir el enforcement de un endpoint que vive en un god-file `# cap: __shared__`.** Origen: P6 (fix de seguridad del audit log de compliance, 2026-06-06). El gap de access-control era REAL y se ARREGLÓ (`require_brand_owner_access` en `GET /medical-compliance/{events,export-csv}` · test RED→GREEN 16/16 · 403 a roles forbidden). PERO `cross_check_4` sigue flaggeando `compliance.hipaa-lite-defensive-stack` con `no_enforcement_found` porque resuelve los archivos del cap vía `_code-index.json::cap_to_files` (headers `# cap:`), y el endpoint vive en `vitalia/.../api/routes.py` tagueado `# cap: __shared__` (monolito que sirve booking/consent/compliance/…) → excluido del scan per-cap. **NO es un agujero de seguridad** (el 403 está enforced + testeado); es una limitación de ATRIBUCIÓN del validador para endpoints en god-files. **APLICADO 2026-06-23 (Opción B · fix sistémico, no band-aid):** `cross_check_4` ahora (B.1) lee `resolved_cap_to_files` (keyea el cap_id canónico → cierra el false-drift del header alias `lisa.servicios`) + (B.2) escanea los god-files `__shared__` **per-endpoint** (matchea el route-decorator por el último segmento del path + corre `detect_enforcement` SOLO en el cuerpo de ESA función → cero false-attribution del gate de un endpoint vecino). Drift cc4 vitalia 2→0. Tests ×4 (shared con/sin gate · no-false-attribution · resolved-alias). Opción A (extraer endpoints) descartada: band-aid brand que deja `offer.lisa-servicios` rojo + invita recurrencia. | applied | Fix candidato (cualquiera cierra el SOFT-drift): (a) extraer los endpoints de compliance a `vitalia/.../compliance/api/router.py` con header `# cap: compliance.hipaa-lite-defensive-stack` + registrar en `main.py` (arquitectónicamente correcto · rompe el `__shared__` god-file · es un refactor, no un hotfix → story dedicada); o (b) enseñar a `cross_check_4` a resolver el `path` del entry_point → archivo que SIRVE la ruta (incluyendo `__shared__`) en vez de solo `cap_to_files` (validator enhancement · apply-pipeline). El drift es SOFT (no bloquea commits). Relacionado P6 + `routes.py` `# cap: __shared__` · ★ triage 2026-06-16: SOFT non-blocking; candidato (b) AST route-resolver = FP-prone 80-120 líneas (deferido apply-pipeline) · candidato (a) refactor = story dedicada. Known-limitation aceptada. |
| HB-61 | 2026-06-08 | 🔴 | **Tools de mantenibilidad declaradas pero NUNCA cableadas (BE+FE) → false-green "8/8" / "13/13".** Origen: consulta Chris sobre `fallow` (dead-code/dup TS). Auditoría honesta: FE `test-fe-{brand}` corría 3/8 gates (jscpd/knip/madge sin config/deps/scripts; builder-frontend.md L387 lo admitía "⏳ pendiente"); BE `test-{brand}` corría 4 (pytest/ruff/format/mypy) pero builder-backend.md declaraba 13 (jscpd/interrogate/pip-audit gates 11-13 NUNCA invocados); BE **sin dead-code alguno** (ruff solo unused imports/vars). = deuda P0-4 del harness-closeout, expandida a BE. | **verified** | **RESUELTO esta sesión (verify-first + real-run + negative-test).** Decisión sobre evidencia: **FE→fallow** (Rust 0.1-0.2s, baseline/threshold/json nativos, reemplaza knip+jscpd+madge), **BE→jscpd+vulture+interrogate+pip-audit** (fallow es JS/TS-only; **deptry descartado** = 1318 falsos-positivos en uv workspace). Entregado: `scripts/quality/code-health.sh` (baseline-ratchet shrink-only) + `.fallowrc.jsonc` ×3 + baselines ×4 + `pip-audit-ignore.txt` + `make code-health[-baseline]` + shortcut gate-runner `code-health-<brand>` + parser + builder-{fe,be}.md false-green CORREGIDO + SSoT `docs/process/code-health-gate.md`. **Live: vitalia/nicolify/comunify = PASS; negative-test vulture 31→30 = FAIL (ratchet con dientes); fallow dupes 11.3%→2.9% al excluir e2e.** lupulo = placeholder (docstrings<80%, NO enforced). **Deuda de seguridad RESUELTA misma sesión:** bumpeados workspace-wide (`uv lock --upgrade-package`) los 11 CVEs con fix — pyjwt 2.12.1→2.13.0 (×4 auth) · starlette 1.0.0→1.2.1 · aiohttp →3.14.1 · idna →3.18 · langchain-openai →1.2.2 (+litellm 1.88/openai 2.41). Solo queda `py` PYSEC-2022-42969 (sin fix upstream, deprecado) en allowlist. **Companion fix** (engine, mecánico): pyjwt ≥2.13 valida JWKS URI scheme en `PyJWKClient.__init__` → `core/luana-core-iam/.../auth.py` crasheaba al import con `CLERK_ISSUER` ausente; agregado `jwt.exceptions.PyJWKClientError` al `except` ya existente (semántica intacta). **Verificado:** pip-audit 11/12 cleared · core-llm/core-iam/vitalia-arch GREEN · vitalia `test_auth_stub_env_gate` 4/4 · negative-test ratchet. Pre-existentes NO causados por bump (flagged, no fixed): `treatment_plans.notes` PHI BYTEA (arch) · sales-agent 38 personality fails (story-8 lift) + snapshot `tests.modules` import · copilot/e2e env-missing Settings. Menor: jscpd `%` display cosmético · fallow/jscpd→root devDeps offline · rollout pre-commit opcional. |
| HB-60 | 2026-06-08 | 🟡 | **Design System Canon — drift recurrente canon↔skills↔código (ítem CIL mantenido).** Chris ratificó el showcase `vitalia-ds-showcase` (8 rondas /po-ux) → cementado `docs/architecture/luana-platform/design-system-canon.md` (contratos + ejemplos de código) + bindeado HARD en `frontend-visual-fidelity.md` + `/po-ux` + `/dev-team` + `/architect` (builder/auditor-frontend heredan vía la rule). **Punto de partida nuevo (2026-06-08): toda hoja nueva/tocada se homologa al canon; lo existente se MODIFICA.** Reforzar `/harnesses-improvement` = chequear en cada stop que: (1) los bindings del canon siguen en las 4 superficies (po-ux/dev-team/architect/rule) — `grep "design-system-canon"`; (2) la build story consolidada `core-ds-foundation` (Fase 0+1+2 en una, ratificado Chris 2026-06-08) avanza a materializar el canon en `@luana/ui-kit` + lint no-arbitrary + arch-test no-div-layout; (3) la adopción comprehensiva por marca (`{brand}-ds-adoption`) no se estanca. | **partial (bindings applied 2026-06-08 · build+gates mecánicos = stories en pipeline)** | Bindings de proceso APLICADOS (canon doc + 4 superficies). Pendiente (carril L4 + stories de build, NO mid-feature): gates mecánicos REALES (eslint `no-arbitrary-value` + arch-test FE `no-div-layout`/`no-native-select` + `/showcase` route) = los materializan `core-ds-*` (Track A Fase 0-2) vía /architect→/dev-team; hasta entonces el enforcement es por-rule (criterio reforzado), no 100% mecánico. Doctrina `ADR-014` + proposal `2026-06-07-design-system-homologation`. Relacionado HB-42/47/50 (familia "verde≠realidad": acá "homologado≠mecánico" hasta cerrar los gates). |
| HB-81 | 2026-06-04 | 🔴 | (renumber HB-68→HB-81 en integration-checkpoint 2026-06-16: colisionaba con HB-68 verde-fantasma 2026-06-11; era HB-50→55→68 lineage nicolify) (renumber ×2: era HB-50 lineage nicolify → HB-55 → HB-68 al sync con main 2026-06-09 [HB-55 tomado por cockpit-stale-build]) **`isolation: worktree` en builders CONTRADICE M9 v2 + ADR-009 single-hub** → trabajo varado. El hint (agregado a propósito en HB-16/Wave 5, commit 2119d0c1) hace que `builder-{backend,frontend,agentic}` corran en worktree aislado (`luana-platform/.claude/worktrees/agent-*`), uncommitted, FUERA del hub `wip/nicolify`. Detectado en build de `nicolify-r1-abel-icp-buyer`: T-BE-1 construyó el módulo `abel` completo pero quedó varado en worktree aislado + el agente se cortó (budget) sin commit/result, y **SendMessage no está disponible** al orchestrator → no se puede continuar el agente cortado. M9 v2 (`parallel-safety.md`) dice explícito "Sub-agents NO crean worktrees. Trabajan in-place sobre cwd del caller" — HB-16 violó M9. NB: relacionado con HB-32 (vitalia, builders auto-crean worktrees) — /pm-luana reconciliar. | **applied** (pendiente ratif Chris + sesión nueva) | Fix: removido `isolation: worktree` de los 3 builder agent defs (reconcilia con M9 v2 · revierte hint de HB-16). ⚠️ **Registry snapshot**: toma efecto en **sesión nueva** (agent defs se snapshotean al inicio · ver `claude-agents-frontmatter-must-be-line-1`). Sub-item ABIERTO: SendMessage no expuesto al orchestrator → recovery de agente cortado = re-spawn-from-tree o consolidación manual. |

## Verify-first sweep · 2026-06-05 (sesión harness-polish · mandato Chris "revisá qué hace el código, no contar con lo que no existe")

Sweep de los 14 items ABIERTOS contra el FS/código real (HLP §6.1 "la auditoría sobreestima"). Resultado: **3 fantasmas purgados · 1 downgrade · 2 con números/ground-truth corregidos · 8 confirmados REAL.**

| Verdict | Items | Evidencia |
|---|---|---|
| **PURGADO (fantasma)** | HB-27 · HB-46 · HB-49 | HB-27 ya construido (`platform-context.ts`+BrandSwitcher+getSelectableBrands) · HB-46 template ya correcto (L122 sin `--max-warnings 0`) · HB-49 ya fixeado (route.ts:52 parse_error badge) |
| **CORREGIDO** | HB-24 (números crecieron 967/824/863/567) · HB-37 (ground-truth container/workdir/venv fijado) | — |
| **DOWNGRADE** | HB-35 (def ya manda lint-pass; gap = adherencia runtime, no def) | builder-frontend.md:38 |
| **REAL confirmado** | HB-32 · HB-33 · HB-34 · HB-37 · HB-45 · HB-48 · HB-50 · HB-52 | HB-33 Section 12 story-level no module · HB-34 5b HARD check · HB-45 template:175 smoke→regression · HB-50 grep arch-fitness commit-enforce vacío |
| **REAL no-code** | HB-28 (Clerk infra) · HB-36 (lección pathspec) | no verificable por código; se mantienen |

cap_doctor vitalia = **✅ 0 drift code↔cap** (paths existen, headers resuelven) → nada que purgar a nivel cap-path. Los ~39 caps sin scenarios = estado `stub` en `/drift` view = target de **backfill HB-52(B)**, NO caps muertas. NB: el overestimate cont.5 "cockpit BrandSwitcher NO implementado" quedó stale — se construyó junto a HB-27 entre 2026-06-02 y hoy (no se reescribe el histórico, solo se anota acá).

**Queue REAL para waves (post-sweep):** W1 HB-52 · W2 HB-32/33/34/35/37 · W3 HB-50/45 · W4 HB-49✓(cerrado)→ solo platform-board pulido si surge · W5 HB-28/48 · diferidos HB-24/HB-11.

## Próxima acción

**★ Rediseño de coherencia spec→done (HB-52/53/54) — DISEÑO listo para tu lectura final:** `docs/process/process-coherence-v5-2026-06.md`. Consolida G·Chris-verify + R·reconcile + auditor-guardián + mutation-gate-diff-scoped + CIL(4 carriles)+stop-semanal + cap=qué-tengo. Anti-Frankenstein (7 re-usos + 1 artefacto que consolida; cero eje/estado/agente/fase nueva). Orden de waves W1-W5 en §7 del doc. Aplicación = wave dedicada tras tu ✓ (NO mid-feature).

Waves 1–5 + D-1..D-11 + D-5 scripts + reconciliación ADR + **cierre B1-B6 (cont. 5, HB-23)** = **cerradas** (23 commits, 17bf3c62..1f162789). **Continuación 5** (2026-06-02): apply-pipeline §6 end-to-end — 6 batches (B1 opus directo · B2 opus · B3 hooks-opus+workflow-sonnet · B4a/B4b workflows-sonnet · B5 opus+workflow · B6-residual workflow) → re-audit 6 agentes read-only (B1-B5 DONE + 0 regresiones, 20 OPEN) → B6 cerró los 20 → commit por pathspec (Haiku). **Abierto:** HB-24 (file-size extraction, deferred-dedicada) + HB-11 (voseo cleanup, defer salvo pedido). **Operacional Chris:** HB-21 (provisión tunnel Cloudflare = B6 del closeout) + settings.json StopFailure (DECISIÓN). Correr `/harness-audit-2026` fresco cuando "huela a drift".

**Overestimates registrados cont. 5** (señal de calidad del catálogo — Chris los quiere anotados): "8-gate FE" no existe (jscpd/knip/madge sin cablear) · pre-push dynamic-brands sin beneficio (placeholders tienen dir capabilities/) · offer catalogs reales=6 no 8 (biz_type=enum, ladder_hints inexistente) · dev-team live-verify ya parametrizado · anti-dup L25 redundante además de roto · context-builder sin refs mcp__ (WebFetch directo) · cockpit BrandSwitcher NO implementado (roadmap correcto) · T-impl-log/T-result/T-review-Cat12-14/REVIEW-final-Verificación-live YA estaban (waves previas) · bidirectional dedup intencional · 17 "Opus 4.7" residuales = históricos/append-only (no se reescribe historia). **cont. 4:** HB-19 ADR-007 ya tenía bitácora · HB-11 "~73"→380 archivos.
| HB-62 | 2026-06-08 | 🟡 | **`builder-frontend` subagent revienta por contexto ("Prompt is too long") en paquetes grandes — lee `pnpm-lock.yaml` (20.688 líneas ≈ 250k tokens) en startup.** Origen: `core-ds-foundation` T-3..T-9, el subagent `builder-frontend` murió ×4 consecutivas al tocar `core/@luana/ui-kit`/`vitalia/frontend` (tool_use 3-6, 0 output tokens). Causa: alguna acción de startup del agente lee el lockfile completo. `general-purpose` (Sonnet) con guardrail explícito "NUNCA leas pnpm-lock/node_modules/files>800 líneas" funcionó perfecto (T-4..T-9 + refactor). | **applied** | ✅ 2026-06-08 (harness-audit): guardrail HARD agregado a `.claude/agents/builder-frontend.md` (sección "HARD context guardrail (HB-62)") — NUNCA Read lockfiles/node_modules/.next/archivos >800 líneas; para confirmar dep, grep package.json. |
| HB-63 | 2026-06-08 | 🟡 | **Docker dev frontend usa anonymous-volume `node_modules` con pnpm-store SEPARADO del host → skew de versión host↔container en deps recién agregadas rompe la live-verify repetidamente.** Origen: `core-ds-foundation` agregó `@tanstack/react-virtual` (ui-kit) + `@luana/ui-kit` (vitalia dep). El symlink compartido `core/@luana/ui-kit/node_modules/@tanstack/react-virtual` resuelve a `.pnpm` distintos en host (3.13.24) vs container (3.14.2); cada `pnpm install` (host o `docker exec`) repunta el symlink, rompiendo el otro lado → /showcase 500 (EntityPicker no resuelve react-virtual). Ping-pong: costó ~6 ciclos de restart durante el live-verify + audit. | open | Fix sugerido: (a) compose monta `node_modules` host (no anonymous-volume) O (b) un solo store compartido O (c) `make dev-app-{brand}` corre `pnpm install --frozen-lockfile` en el container tras cualquier dep nueva. Regla operativa durante live-verify: NO correr `pnpm install` en el host mientras se verifica contra el container (repunta el symlink). Lockfile commiteado = correcto; el skew es artefacto local. |
| HB-65 | 2026-06-08 | 🔵 | **6 domain-expert skills son PRE-REORG legacy wholesale** (copilot/sales-agent/manychat/metrics/brand/offer-expert). Sus descriptions + bodies asumen la topología single-backend pre-multibrand (2026-05-15): paths `api/`, `domain/`, `application/`, `shared/`, `docs/refactors/`, `docs/domains/{brand,offer}/INDEX.md` que ya no existen. La description de brand-expert dice "post-refactor field-contract-platform"; offer-expert "cómo Nicolify maneja offers". No es un puntero suelto — es deriva sistémica de 6 skills enteros. | **applied** | ✅ 2026-06-09 (sesión integración main): topology-alignment 6/6 EJECUTADO contra filesystem real — copilot (tabla capas→engine, tests retirados→suite real, comandos smoke-collected 1640+42, tech-debt CIL L3, CONTRACT purgado), metrics (+`make extraction-contract` RESTAURADO al Makefile, contract doc→engine/docs/, stage_services→application/services/, FE registry anotado no-existe), brand (anotación PURGADOS precisada), offer (7 catálogos→luana-core-offer-studio verificados, tests retirados→dag_smoke+field_contract_endpoint), sales-agent (2 guards vivos verificados + 7 invariantes con guard retirado listados honestos — recrearlos=pase dedicado), manychat (8 paths→luana-core-connections verificados, FE pre-reset anotado, description engine-framing). Dead-refs 44→2 (los 2 = citas deliberadas en anotaciones PURGADO). Detectado en harness-audit 2026-06-08 (sweep de punteros). NO se boil-the-ocean mid-sesión (alto-churn en skills de dominio que rara vez se tocan + riesgo de romper su pedagogía). Los punteros workspace-rooted muertos quedan BASELINED (anti-rot CHECK 28). Fix real = pase dedicado de topology-alignment por skill (description + paths a `core/luana-core-*` / `{brand}/backend/src/modules/{brand}/`). Owner: `/pm-luana`. |
| HB-66 | 2026-06-08 | 🟣 | **Decomposición del pre-commit god-file (2105 líneas / 22 secciones) + gate anti-rot de punteros** (ratificado Chris 2026-06-08 en harness-audit). Defend-the-process: SRP por check + observabilidad + el rot deja de crecer. | **verified (BUILT)** | ✅ 2026-06-08: (1) `scripts/git-hooks/pre-commit` → DISPATCHER de 160 líneas que SOURCEa `scripts/git-hooks/checks/NN-*.sh` (22 checks aislados) en orden explícito. **Faithfulness probada** (concat bodies byte-idéntico a orig[132..2105]) + bash -n 22/22 + equivalencia behavioral orig-vs-new (mismo exit+output) + machinery 73/0. HB-33/34 cayeron como edits localizados en sus check files. CHECK 8/11 de machinery actualizados a leer el hook SYSTEM (dispatcher+checks/). (2) `scripts/scan_harness_pointers.py` (workspace-rooted + skill-relative resolution) + baseline `scripts/machinery/harness-pointer-baseline.txt` (29 residual) + machinery **CHECK 28 ADVISORY** (warn(), no bloquea) + `make harness-pointers[-baseline]`. Negative-test: ref roto nuevo → ⚠ advisory + exit 0 (teeth sin friccionar). |
| HB-67 | 2026-06-09 | 🟡 | **`form-runtime-array.md` references MIRROR divergente entre `brand-expert` y `offer-expert`** (dos copias con contenido DISTINTO — `diff` ≠ 0; detectado durante W1-Phase2 eviction). Viola anti-duplication (un concern, un home). El ex-always-on body se appendeó SOLO a la copia de brand-expert; el stub de la rule apunta ahí como primaria. | **verified** | ✅ 2026-06-09 (sesión integración main): consolidado a UNA copia canónica en brand-expert (deltas reconciliados: mockup ref anotado histórico — `array-field-unified.html` no existe en NINGÚN path post-reorg; ex-always-on body conservado) + offer-expert = pointer 3 líneas + stub rule actualizado. Owner: `/pm-luana`. |
| HB-64 | 2026-06-08 | 🟢 | **Pre-commit hook cross-brand-stagea `.fallowrc.jsonc` (HB-61) durante un commit scoped a otra marca/platform.** Origen: `core-ds-foundation` (platform/@luana + vitalia) — `nicolify/frontend/.fallowrc.jsonc` + `comunify/frontend/.fallowrc.jsonc` aparecieron tracked en el rango de la story sin que ningún commit mío los tocara (replicación de template por hook). Contenido legítimo (HB-61 code-health config), NO pollution de feature, pero un hook que stagea archivos de OTRAS marcas durante un commit scoped viola el espíritu de M15 (scope per commit). | **verified (NO-BUG · misdiagnosis)** | ✅ verify-first 2026-06-08: NINGÚN hook auto-stagea `.fallowrc` — `grep -rn 'fallowrc\|git add' scripts/git-hooks/` = 0 hits de auto-stage de fallowrc. Los `.fallowrc.jsonc` cross-brand se commitearon como parte del setup de HB-61 (commit `e4007707`), NO por un side-effect de pre-commit. La hipótesis "el hook stagea cross-brand" es falsa. Son config legítima per-marca, tracked intencionalmente. Cerrado. |
| HB-68 | 2026-06-11 | 🔴 | **e2e suite verde-fantasma: asserts imposibles pasaron como PASS en la gherkin-matrix del hardening — falta gate que detecte shell-colgado/DOM-vacío en runs axe/visual.** Origen: auditor-frontend de `platform-lift-shell-chrome-ui-kit` (upstream deficiency del predecesor `vitalia-shell-core-hardening`). El edge-redirect del lift destapó que varios runs "verdes" del hardening escaneaban un SHELL COLGADO (DOM "Cargando" casi vacío por el flake `Rendered more hooks`, learning next16-softnav): axe reportaba 0-violations sobre nada + 2 asserts eran IMPOSIBLES contra la app real — (1) dark vía `classList.contains("dark")` cuando la app usa `attribute="data-theme"` desde F1-S1 (la clase `.dark` jamás existe); (2) Ribbon `<a href>` cuando el Ribbon es `button+router.push` desde F1-S7. La gherkin-matrix del hardening los marcó PASS = sobre-reporte del builder T-7. Un `axe` verde sobre un shell desmontado es un falso-negativo: el harness no tiene guard "el DOM está realmente montado antes de correr axe/visual asserts". | applied | `assertShellMounted(page)` helper en `e2e/fixtures/base.ts` (fail-fast: shell montó + ≥1 hijo real + NO 'Cargando') + convención HARD en `e2e-testing.md` (axe/visual sobre shell DEBE assertShellMounted antes) 2026-06-16. Fix sugerido: gate en `fixtures/base.ts` (o helper de specs visual/axe) que ASSERT el shell montó (`[data-shell-ready="true"]` + ≥1 elemento de contenido real, NO "Cargando") ANTES de correr axe/`toHaveScreenshot`/contraste — un axe-pass sobre DOM-vacío debe FALLAR el spec, no pasar. Además: cuando un assert cita un selector que NO existe en la app real (`.dark`, `<a href>` del Ribbon), el spec debe fallar-ruidoso en setup (selector-not-found), no pasar silencioso. Owner: `/pm-luana` (harness) + builder-frontend al tocar la suite shell. NO bloquea el merge de `platform-lift-shell-chrome-ui-kit` (su suite ejerce el contrato real sobre DOM montado — button+push, data-theme, edge-redirects deterministas). |
| HB-69 | 2026-06-11 | 🟡 | **Tests crm slice-1 con patrón asyncio roto en Python 3.12 — falsos RED en la suite.** `tests/modules/vitalia/crm/test_lead_repository.py::test_get_by_id_raises_without_tenant_id` usa `asyncio.get_event_loop().run_until_complete()` → `RuntimeError: no current event loop` en 3.12 (en aislamiento). `test_router_conversation_detail.py::test_get_conversation_detail_404_slice1` usa `MagicMock` donde el resolver es async → `TypeError: MagicMock can't be used in 'await'`. Producto correcto; el TEST es el roto. Detectado por /auditor de embudo (la suite crm para en el 1er fallo y enmascara los 28 tests verdes del embudo). | verified | RESUELTO: los 2 tests crm (lead_repository + router_conversation_detail) = 7 passed (asyncio.run + AsyncMock ya aplicados). 2026-06-16. Fix: migrar a `@pytest.mark.asyncio` + `await` directo (pytest-asyncio mode=AUTO ya activo) + `AsyncMock` para resolvers async. Owner: `/pm-vitalia` story de saneo slice-1 (NO embudo). |
| HB-70 | 2026-06-11 | 🔴 | **`treatment_plans.notes` PHI definido TEXT en vez de BYTEA (pgcrypto) — módulo fidelizacion.** Arch test `test_pgcrypto_phi_columns::test_no_phi_column_uses_text_or_varchar_unencrypted` FAIL. `notes` de un treatment_plan es PHI clínica real (hipaa-lite: PHI columns DEBEN usar pgcrypto BYTEA). Origen fidelizacion T-4 (`df92ede3`), pre-existe al embudo (módulo distinto, lead es non-PHI). Stake-asimétrico (PHI + migración TEXT→BYTEA + cifrado) → Carril C, NO auditor self-fix. Detectado por /auditor de embudo. | applied | FALSO POSITIVO (NO deuda PHI): `treatment_plans.notes` ES BYTEA (migración 005); el regex `notes\s+TEXT` matcheaba el meta-comentario `(NOT 'notes TEXT')` de la migración 038 → fix: lookbehind `(?<![\w'])` excluye prosa quoteada (pgcrypto 15 passed). 2026-06-16. Fix: migración idempotente que convierte `treatment_plans.notes` a BYTEA + cifrado pgcrypto KEK (+ encrypt/decrypt en el repo PHI) — story dedicada fidelizacion. Owner: `/pm-vitalia` (ratifica scope) → `/architect`. **HIGH: deuda PHI real, no falso-positivo de regex.** |
| HB-71 | 2026-06-12 | 🔴 | **10ª instancia de contrato-imaginado FE↔BE en la MISMA story (lisa-doctores delta v3) pese a 9 fixes previos — y esta vez el diseño upstream lo habilitó:** `03-arch-delta.md §6.2` declara el StructuredProfileEditor "inline + autosave" pero la tabla de endpoints §6.1 NUNCA declaró el write (PATCH save del public_profile). El builder FE inventó `PATCH /doctors/{id}/public-profile` (no existe → 404 live-reproducido por auditor) + tipos FE que MIENTEN sobre el wire (experiencia `{cargo,desde,hasta,descripcion}` vs BE `{puesto,lugar,anios}`; certificaciones/idiomas objetos vs BE `string[]`). El hook shippeó con CERO test (ni siquiera mock-test que cementara la URL) y la dod_evidence nunca ejerció el write del editor → todos los gates verdes. Refuerza HB-42 (contract-test FE↔BE) con causa-raíz nueva: el gate debe incluir "todo componente con autosave declarado en arch DEBE tener su write endpoint en la tabla de endpoints del arch" (lint de arch package) + "todo hook de mutation nuevo DEBE tener test que asserte method+URL contra el router BE real (no contra sí mismo)". Detectado por auditor-frontend DELTA-FE (Carril C' — endpoint compound nunca diseñado). | applied | architect REFUSE gate (editable+autosave sin write endpoint declarado · SKILL §v4.3) + `test_http_contract_parity.py` (app.routes authoritative + FE fetchClient/vitaliaFetch URLs · 17 gaps reales baselined → HB-82) 2026-06-16. Fix: (a) architect checklist: superficie editable+autosave sin write endpoint declarado → REFUSE ready; (b) HB-42 contract-test: generar tabla rutas BE (FastAPI routes dump) y assert que cada URL citada en hooks FE matchea method+path — mecánico, mata la clase entera (10 instancias en una story). Owner: `/pm-luana` (harness) + `/architect`. |

## Sesión harness-audit (2026-06-08 · `/pm-luana` · mandato Chris "resolver todo + defender el proceso, no apilar capas")

Verify-first de TODOS los items abiertos contra el código real (HLP §6.1) → resolución + 1 refactor estructural ratificado. **No se hand-fixearon 25 punteros individuales** (boil-the-ocean en domain-skills); en su lugar se arreglaron los platform-owned claros + se instaló el **gate anti-rot** (la única "capa" que RESTA trabajo).

| HB | Verdict | Qué se hizo |
|---|---|---|
| HB-32 | ✅ applied | builders IN-PLACE (M9) + fix branch comments agentic |
| HB-33 | ✅ applied | `checks/12-story-closure.sh` module-aware (lee `module:`) |
| HB-34 | ✅ applied | `checks/05b` acepta cap YAML en HEAD (resolve_cap.py) |
| HB-35 | ⬇️ addressed-by-design | def YA manda gate-runner como veredicto (builder-frontend.md:38/354); gap era adherencia runtime, el contrato existe |
| HB-37 | ✅ applied (class) | debugging.md + AGENTS.md + vitalia/CLAUDE.md; nicolify/comunify ROUTED |
| HB-45 | ✅ applied | template project↔dir + `expect_min_tests` + gate-runner regla #13 "0 tests = FAIL" (mata familia falso-verde) |
| HB-62 | ✅ applied | guardrail HARD lockfile en builder-frontend.md |
| HB-64 | ✅ verified (no-bug) | misdiagnosis: ningún hook auto-stagea `.fallowrc` |
| HB-66 | ✅ BUILT | pre-commit decomposition (2105→160 + 22 checks) + anti-rot CHECK 28 |
| HB-65 | 🔵 reported (nuevo) | 6 domain-skills pre-reorg legacy → pase de topology-alignment dedicado |
| HB-36 | 🟡 sigue open | lección pathspec deps transitivas (sin fix mecánico barato) |
| HB-59 | 🟡 sigue open | cross_check_4 god-file attribution (validator enhancement) |
| HB-63 | 🟡 sigue open | docker node_modules skew (infra/operacional, dedicada) |
| HB-28 / HB-57 / HB-60 | sin cambio | Clerk infra / accuracy-L4 / DS-canon-build (cada uno su pista) |

**Punteros (`cada puntero de cada skill/agent/rule`):** scanner productizado → 175 hits crudos = ~80% falsos-positivos (skill-relative `references/` + module-shorthand). **Genuine workspace-rooted rotos = 29** → 6 platform-owned arreglados (process-learnings→learnings, cierra-limpio path, generate_core_modules→pendiente-anotado, field-contract→purgado, cobertura-learning→repoint, brand/offer INDEX→purgado) + 23 baselined (pre-reorg domain paths, deferred e2e-tests.yml, nicolify-reset refs, ejemplos ilustrativos). El **CHECK 28 advisory** impide la próxima tanda.

**Refactor estructural (defend-the-process):** el pre-commit era un god-file de 2105 líneas (SRP roto · HB-33/34 = síntomas). Decompuesto en `checks/NN-*.sh` (1 check = 1 archivo testeable + observable vía `PRECOMMIT_VERBOSE=1`), faithfulness byte-probada + equivalencia behavioral. La familia "falso-verde" (HB-42/44/45) recibió el principio **fail-closed** centralizado en gate-runner (0 tests = FAIL). El rot de punteros recibió un ratchet shrink-only.
| HB-72 | 2026-06-12 | HIGH | Excepción reset --hard post-squash (git-safety detail) no protege working tree compartido del hub — borró 73 files WIP de sesiones paralelas (recuperados ~95% vía blobs staged). Gate propuesto: stash-first obligatorio si tree dirty + script safe-reset-postsquash.sh | sesión doctores squash 2026-06-12 | docs/rules-detail/git-safety.md § Excepciones + learning 2026-06-12-reset-hard-post-squash |
| HB-73 | 2026-06-15 | 🟡 | Chrome DevTools MCP no aislado por sesión → colisión SingletonLock. userDataDir global era per-worktree (`luana-vitalia`) no per-sesión → 2 sesiones same-hub (ADR-009) chocan el lock de Chrome → toda tool-call de la 2ª falla. Fix aplicado: `userDataDir=…/luana-vitalia-${LUANA_LANE:-solo}` (reusa convención LUANA_LANE). Residual: aislamiento depende de export manual de LANE — si ambas quedan sin LANE → ambas "solo" → colisión vuelve. Hardening: default único por-PID o doc en parallel-safety. | verified | RESUELTO (fix `~/.claude.json` userDataDir per-`LUANA_LANE` + learning, en main). docs/learnings/tooling/2026-06-15-chrome-devtools-mcp-per-session-isolation.md |
| HB-74 | 2026-06-15 | 🔴 | **FE omite headers REQUERIDOS del BE (X-Clinic-ID/X-User-ID/X-User-Role) y los tests con MSW lo ocultan — 4ª recurrencia** (lisa/staff X-User-ID · embudo imagined-contract · agenda 422 · tenant-from-clerk-org). BE multi-tenant/HIPAA-lite exige headers actor; FE manda solo X-Tenant-ID → 422/403 LIVE, pero los tests mockean el BE del propio surface → verde-fantasma. El gate contract-test FE↔BE (HB-42) sigue sin existir. Propuesta: (a) arch-test FE que para cada `vitaliaFetch`/`fetchClient` a `/api/v1/...` verifique que viajan los headers actor cuando el endpoint los exige; **o** (b) gate fail-closed: un surface NO se acepta verificado si su única prueba mockea su propio backend (exigir ≥1 e2e real-backend por surface PHI). Mata la familia entera. | sesión bugfix agenda 2026-06-15 (vitalia-bugfix-agenda-actor-headers-422) | docs/learnings/{2026-06-04-embudo-imagined-contract-never-integrated,2026-06-11-mocked-service-tests-hide-repo-contract,2026-05-31-e2e-mockeado-verde-falso}.md · HB-42 |
| HB-75 | 2026-06-15 | 🟡 | **Fuente de rol del actor inconsistente FE:** SSR lo lee de `/iam/users/me` (rol global) · client del tenant-store (rol per-tenant). Para el mismo usuario salen distintos (dr.demo: SSR `doctor`, client `owner`). No rompió porque ahora ambos roles están permitidos en agenda, pero footgun (un surface puede pasar RBAC en SSR y fallar en client o viceversa). Unificar a UNA fuente canónica del rol per-tenant (tenant-store, o `/me` que devuelva el rol per-tenant resuelto por X-Tenant-ID y SSR/client lean lo mismo). | sesión bugfix agenda 2026-06-15 (builder-backend T-2 nota) | vitalia/frontend/src/hooks/useActorHeaders.ts · features/mateo/api/agenda-server.ts |
| HB-76 | 2026-06-16 | 🔴 | **commit-worker (git-haiku-delegation) editó CONTENIDO sin autorización.** Al commitear el ready package de nicolify-r0-design-system-adoption, el worker Haiku flipeó `cap_change_type: new → fix` en checkpoint.md por su cuenta (para esquivar el 5b gate, razonando "adopción cero-creación"). Corrupción silenciosa de campo SSoT — el flip SATISFACÍA el gate con dato errado → habría shippeado `fix` si no lo reviso. Un commit-task NO debe editar contenido (solo `git add <pathspec>` + `git commit`); hook que bloquea → REPORTAR, no "arreglar". Fix: guardrail verbatim en `haiku-delegation.md` que PROHÍBE explícitamente cualquier edit de contenido + el spawn de commit nunca pide "arreglá para que pase el gate". | applied | guardrail '⛔ NEVER edit content' + 2 anti-patterns en haiku-delegation.md 2026-06-16. sesión ds-adoption 2026-06-15 · flip 3bbbabea · revert b5deb3d8 · .claude/skills/commit-push/references/haiku-delegation.md |
| HB-77 | 2026-06-16 | 🟡 | **architect-orchestrator cerró `ready` con `cap_change_type: new` SIN crear el cap YAML → 5b gate bloqueó.** Su Step 8 dice "Si new: crea cap YAML schema v2 completo" pero no lo hizo; el gate HARD 5b (new exige cap YAML staged/HEAD) frenó el commit → corrí `make new-cap` a mano + fix infra (user_visible:false, agent_owner:plataforma-tecnica). Causa raíz de HB-76 (el package nació inconsistente con el gate → tentó al worker a flipear). Fix: el orchestrator corre `make new-cap` (cap planned) al cerrar `ready` cuando cap_change_type ∈ {new,extend}. | applied | architect SKILL Step 8 manda `make new-cap` para new/derive (no hand-author) 2026-06-16. sesión ds-adoption 2026-06-15 · .claude/skills/architect/SKILL.md Step 8 · checks/05b · HB-34/HB-76 |
| HB-78 | 2026-06-16 | 🔵 | **FE dev-server (Next webpack dev, memory threshold) entra en memory-restart loop → `ERR_SOCKET_NOT_CONNECTED` intermitente + enmascara con stale cache.** Live-verify nicolify (Chrome MCP): `luana-dev-nicolify_frontend_dev-1` (Up 12h) repetía "approaching used memory threshold, restarting" al compilar rutas nuevas; cache stale seguía referenciando un `EmptyState.tsx` ya borrado (restart limpió). NO bug de prod (BE 200/201/PATCH-200). Hace la live-verify flaky. Decisión: bump `--max-old-space-size` del dev-server / evaluar turbopack / restart-on-deploy del dev-stack / nota debugging.md. Dev-infra cross-brand (mismo webpack en las 4 marcas). | applied | `ENV NODE_OPTIONS=--max-old-space-size=3072` en `vitalia/frontend/Dockerfile` dev target (3GB heap < 4G container) — toma efecto en próximo `make dev-vitalia` rebuild. 2026-06-16. sesión ds-adoption 2026-06-15 · debugging.md · docker compose dev FE. **★ 2026-06-17 corrección:** turbopack evaluado a fondo (sesión RAM 3-marcas) — front-loadea el grafo de TODO el proyecto en RAM (vitalia 4.5GB vs webpack 1.7GB; nicolify al revés: turbopack 1.3 < webpack 2.4). Política nueva = **bundler de dev por tamaño de marca** (vitalia→webpack, chicas→turbopack), NO "turbopack para todas". `NODE_OPTIONS=--max-old-space-size` NO acota la memoria nativa (Rust/tokio) de turbopack — el OOM real era cgroup del contenedor. SSoT: `docs/learnings/tooling/2026-06-17-dev-bundler-by-brand-size.md` + `make dev-active`. |
| HB-79 | 2026-06-16 | 🟡 | **`04-validators.yaml` no reconciliado al descope (G) + arch validators con path brand-style inexistente en core pkgs.** Story `copilot-chat-mountable` (platform/core): `reconciled: true` pero 04-validators seguía listando 6 validators `must_pass: true` para scope DEFERIDO (T-4 end-state C: `v_no_global_settings_import` 49 hits, `be_all_touched_pkgs_*`; T-5: `v_chat_401_200` con test nunca creado, `be_downstream_4brands_arch`) → verde-fantasma latente. Además `be_platform_arch`/`be_copilot_arch` corrían `pytest tests/architecture/` — dir que NO existe en core pkgs (arch-fitness es inline; el architect copió el template brand-style). Lo reconcilió el auditor (Carril R: must_pass:false + tag `deferred:`). **Dos fixes upstream:** (1) Fase R de `/pm-{brand}` DEBE reconciliar 04-validators al scope post-G (no solo checkpoint). (2) template/architect 04-validators: technical-story core usa el layout real de arch-fitness del paquete (inline en la suite), no `tests/architecture/` brand-style. | applied | Fase R reconcilia 04-validators (deferred→`must_pass:false`) + template core-arch note 2026-06-16. /auditor story platform/copilot-chat-mountable 2026-06-16 · 04-validators.yaml · story-closure-gate.md Fase R · 04-validators-template.yaml |
| HB-80 | 2026-06-16 | 🔴 | **11 mutation handlers sin committing session en su Depends-closure (baseline del gate HB-50) — write-durability SIN confirmar.** El gate `test_mutation_handlers_commit.py` los baselineó shrink-only (AUDIT-PENDING). Algunos son no-write (OAuth-init, transcribe) = inocuos; otros (`complete_onboarding` 'commits brand', marketing approve/reject/undo) PUEDEN ser silent-write-loss como el origen HB-50. Fix: /pm-vitalia live-verify cada uno (¿persiste el write? read-back DB) → committing factory o quitar del allowlist. | **verified** | ✅ 2026-06-18: auditados los 11 por code-read del write-path. **8 eran silent-write-loss REAL → fixeados a `get_async_session_committing`:** wizard start_draft/confirm_slot/extract_tenant_context/complete_onboarding (draft via `SqlAlchemyOnboardingProgressRepository.save`=flush-only "Caller commits"; fix en el provider `get_onboarding_progress_repo`) · marketing approve/reject/undo_recommendation (status transition + audit INSERT, ninguno commiteaba) · scheduling send_appointment_reminder (audit HIPAA-lite sync INSERT). **3 eran no-write genuino:** simulate_personality (stubs read-only, sale del allowlist — su closure ya alcanza el committing vía el provider compartido) · transcribe_audio (Whisper, session solo auth-read) · connect_channel (OAuth-init) → quedan allowlisted con justificación precisa (allowlist 11→2). **Live read-back (start_draft):** POST 200 → GET en request SEPARADO 200 (404 pre-fix) + fila en `vitalia_onboarding_progress` confirmada. Gate verde (allowlist 2) · ruff verde · 145 passed (notify/agenda-rbac/wizard-routes/marketing; overrides de tests apuntados al dep committing). `test_mutation_handlers_commit.py::KNOWN_NON_COMMITTING_MUTATIONS` · extiende HB-50 |
| HB-82 | 2026-06-16 | 🔴 | **17 gaps reales FE→BE que el gate HB-71 surfaceó en su 1ra corrida — FE llama paths que el BE no sirve (404 live o código FE muerto).** Causas vistas: drift de spelling (FE `fidelization` vs BE `fidelizacion`), drift de prefix (FE `/vitalia/offers` vs BE `/offer`; FE `/notify` vs BE `/scheduling`), endpoints faltantes (`GET` booking único, `POST /treatments`). Baselined en `KNOWN_CONTRACT_GAPS` (shrink-only) → el gate bloquea NUEVOS imagined-contracts hoy. Fix: /pm-vitalia triagea cada uno (¿404 real? ¿renombrar FE/BE? ¿hook FE muerto? ¿router no montado?) → fix-or-remove + drop del allowlist. | **triaged → routed /pm-vitalia** | ✅ 2026-06-18: los 17 diagnosticados contra rutas BE vivas (openapi) + hook+type FE. **Veredicto: NO son typos de URL — es la enfermedad imagined-contract (HB-44/HB-71) al por mayor.** El feature `fidelizacion` + los hooks `features/vitalia/*` (offers/treatments/booking) se construyeron contra un contrato imaginado que diverge del BE en TRES capas a la vez (URL + body + response shape), así que un rename de URL solo cambiaría un 404 por un shape-mismatch SILENCIOSO (parece conectado, sigue roto = peor). Probado: fidelizacion/summary BE devuelve `items`/snake_case, FE espera `events`/camelCase; re-engagement (5 POST) el BE quiere `patient_id`+`clinic_id` en el BODY, el FE lo manda en el PATH. Diagnóstico per-gap (buckets: fidelizacion-reads · re-engagement-body · nps/responses-missing · vitalia/offers cross-módulo+missing · treatments-POST missing · booking-GET-single missing · brand-studio sin-BE · notify endpoint distinto · trust-signals method PUT≠POST) cementado en el comentario de `KNOWN_CONTRACT_GAPS`. **Requieren reconciliación FE↔BE per-feature CON live-verify = stories /pm-vitalia (no es paso mecánico de harness — el gate existe justo para no shippear esto a ciegas).** Gate sigue bloqueando NUEVOS imagined-contracts. **★ 2026-06-18: diagnóstico LIVE completo (vivo-vs-oculto contra dev-app :8002/:3002).** Veredicto re-engagement (5 mutations + 3 reads fidelización): **OCULTA/PREMATURA** — BE vivo+cableado (`modules/vitalia/fidelizacion`, 10 rutas openapi-verificadas), FE construido pero NUNCA montado (cero ruta/nav, contrato imaginado URL+body+headers, cap `fidelizacion.re-engagement`=planned, hooks tagueados a cap deprecada `patients.nps-tracking`, origen slice viejo). **★★ 2026-06-18 (Chris): la story contenedor `vitalia-fidelizacion-reengagement-contract-reconciliation` fue BORRADA** (era idea, redundante) — el diagnóstico re-engagement quedó como **notas a considerar (NO mandato)** dentro de `vitalia-fase2-camila-reactivar` (home real: contrato BE real + 2 decisiones /architect [call_result enum propio · BE resuelve PHI por patient_id] + regla anti-código-muerto: lo que el rediseño no reuse, borrarlo) + cruce NPS en `vitalia-fase2-camila-reputacion`. Cada problema vive ahora en la story que lo toca. **Las 9 restantes — ESTE backlog es su SSoT:** (B-zombie → cleanup+remove) `features/vitalia/offers` GET/POST + `brand-studio/sections` GET/PATCH = código muerto, superseded por `/offer/servicios/*`+`/lisa/marca/*` LIVE; (C-defer → missing-endpoint, superficie no montada) `POST /vitalia/treatments`, `GET /vitalia/bookings/{id}`; (D-bugfix) `POST notify/reminder` = bug LIVE en mateo/agenda AppointmentDrawer (404 real, URL≠BE `/scheduling/appointments/{id}/notify`) + `PUT lisa/marca/trust-signals` método≠ (BE POST+DELETE, triage lisa/marca). Spin-offs sugeridos: 1 bugfix (notify) + 1 cleanup (zombie hooks). `test_http_contract_parity.py::KNOWN_CONTRACT_GAPS` · extiende HB-71/HB-42/HB-44 |
| HB-83 | 2026-06-16 | 🟡 | **El venv (`.venv` editable installs) puede driftear a un worktree path STALE → enmascara fallas del arch suite.** Hallado en sesión harness: los editable installs de las 4 marcas apuntaban a `~/Proyectos/luana-core-copilot-mountable/` (worktree viejo) en vez de `luana-platform` → `import luana_core_iam/observability` fallaba → el arch suite ERROReaba en collection y `-x` paraba ANTES de llegar a fallas reales (HB-70 pgcrypto + 65 tests DB-dependientes quedaban invisibles = falso verde por masking). Fix: `uv sync` re-apunta los editables. Gotcha: correr `uv sync` tras churn de worktrees; un arch suite que ERRORea en collection NO es verde. | verified | `uv sync` ejecutado — editables re-apuntados a luana-platform; core packages importan; arch suite corre (destapó HB-70 FP + HB-84 real). 2026-06-16. `uv sync` 2026-06-16 · afecta a quien corra arch tests local |
| HB-84 | 2026-06-16 | 🔴 | **Dos modelos SQLAlchemy LIVE mapean la MISMA tabla `vitalia_lucas_recommendations` con esquemas EN CONFLICTO → `InvalidRequestError: Table already defined` rompe el arch suite (64 fails en orden default) + riesgo de datos.** `agentic/lucas/persistence/models/stage_recommendation.py::LucasStageRecommendationModel` (18 cols · migración 009 · `status String(16)` · `tenant_id` SIN index) vs `marketing/infrastructure/models/lucas_recommendation_model.py::LucasRecommendationModel` (24 cols, superset · migración 028 · `status String(32)` · `tenant_id` indexed). AMBOS vivos (agentic: daily-analysis graph + weekly worker + tool; marketing: service + repo). El que registra 2º en el proceso pytest revienta. `extend_existing` NO sirve — enmascara el conflicto con esquema no-determinístico. Causa raíz de la flakiness del arch suite (destapada por el fix venv HB-83). | verified | RESUELTO vía /architect (canónico = modelo superset corregido al DDL real, MOVIDO a `infrastructure/models/lucas_recommendation_model.py` shared-infra; ambos repos importan de ahí; 2 duplicados borrados; status→16, title/projected→Text, drop phantom index=True, priority→50, index names=DDL real). SIN migración (modelo alineado a tabla existente). arch suite 361 passed (det+3 seeds, era 64-fail) · marketing 116 + agentic 102 + schema-mirror 34 passed · ruff clean. **Residual (architect, fuera de scope):** marketing `RecommendationStatus` enum no tiene `undone`/`skipped_budget` (statuses agentic) → `_model_to_entity` ValueError si marketing leyera fila agentic (write-paths separados hoy). 2026-06-16. **/pm-vitalia + /architect · stake-asimétrico (datos · 2 features)**. Fix: architect elige canónico (prob. marketing=tabla real post-028) → migrar repo agentic a usarlo O renombrar tabla + reconciliar status-length + index (¿`String(16)` agentic = truncación?). Diagnóstico sesión infra 2026-06-16 |
| HB-85 | 2026-06-17 | 🔴 | **`make dev-{brand}` bindea el worktree EQUIVOCADO en silencio.** El proyecto compose `luana-dev` es ÚNICO cross-worktree + los binds son relativos al CWD; correr `make dev-{brand}` desde otro worktree RECREA los containers apuntando a ese CWD → el último que lanza gana, sin señal. Caso real: stack de nicolify servía código VIEJO de main (otra sesión corrió `make dev-nicolify` desde `~/Proyectos/luana-platform`) → horas perdidas, los fixes "no funcionaban" porque nunca se deployaron. El `dev-lock-check.sh` era stub (avisaba "ya corriendo" — y con grep ROTO `^luana-${BRAND}-` que nunca matcheaba `luana-dev-${brand}_`). | verified | Fix: `dev-lock-check.sh` compara worktree bindeado (docker inspect mount `/workspace`) vs `git rev-parse --show-toplevel` → BLOQUEA si difieren (escape `FORCE_REBIND=1`); fail-OPEN sin docker; `--self-check` verde. Nuevo `make dev-which` (read-only) lista qué worktree sirve cada marca. Demo live: cazó vitalia+comunify bindeados a main. Platform-scoped (`scripts/dev-lock-check.sh` + `Makefile`). 2026-06-17 |
| HB-86 | 2026-06-17 | 🔵 | **Protocolo "poné al día a las demás" — propagar mejoras core/harness a todos los worktrees con UN gesto, sin entanglear el WIP de marca.** Doctrina (ya parcialmente cementada): un cambio compartido (core/ o harness) se commitea como commit AISLADO brand-free (cero `{brand}/**`) → se promueve a main por **cherry-pick del SHA** (NO squash del branch: hoy `wip/vitalia` está 95 commits adelante de main con WIP de marca; squashear arrastraría todo) → cada otro worktree corre `sync-from-main.sh` (FF, STOP-on-dirty, nunca pierde WIP). El miedo "sincronizar y perder algo" ya está cubierto: sync-from-main es FF/merge-only. **Falta el gesto único:** (1) `scripts/git/sync-all.sh` + `make sync-all` = loop sobre los worktrees corriendo sync-from-main + tabla de status (~30 líneas); (2) helper `promote-to-main <sha>` = cherry-pick a main + push, guardando que el commit toque cero `{brand}/**`. Pendiente dirección Chris (¿construir helpers ahora, o cherry-pick manual cuando haga falta?). **APLICADO/VALIDADO 2026-06-23:** los helpers YA existen y se ejercieron end-to-end esta sesión — `scripts/git/promote-to-main.sh` (`make promote-to-main SHA=...`: guard brand-free + cherry-pick a main + push) + `scripts/git/sync-all.sh` (`make sync-all`: loop FF-safe con tabla de status). Promovidos 4 commits hoy (ff8fab30, 68bca379, +HB-98/bump) → main + sync de comunify/nicolify/protocol. **Limitación descubierta:** `promote-to-main` RECHAZA commits que tocan `{brand}/**` (guard correcto), pero un **dep-bump** toca inherentemente `{brand}/frontend/package.json` + el lockfile shared → no es brand-free puro. Workaround validado hoy: cherry-pick MANUAL a main (`git -C $MAIN_WT cherry-pick <sha>` con `LUANA_ALLOW_MAIN_COMMIT=1`) + resolver el conflicto append-only de `harness-backlog.md` tomando la versión de main + re-aplicando el edit. Regla emergente: los cambios shared de harness se commitean brand-free (→ make promote-to-main); los que tocan package.json/lockfile cross-brand van por cherry-pick manual o esperan el squash-merge de marca. | applied | Doctrina = triple-branch + `sync-from-main.sh` existentes; orquestador CONSTRUIDO + validado 2026-06-23. Diseño en respuesta de sesión 2026-06-17 |
| HB-87 | 2026-06-18 | 🔴 | **`dev-lock-check` no detecta binding MIXTO FE-aquí/BE-en-otro-worktree → stack sirve código mezclado, BE corre main stale → 500 silencioso en endpoints DB.** El guard solo compara `bind(backend)` vs `cwd` al correr `make dev-{brand}`; NO detecta que FE y BE quedaron bindeados a worktrees DISTINTOS (FE `luana-vitalia`, BE `luana-platform`/main). Caso real: `dev-app.vitalialat.com` daba "Estamos teniendo problemas conectando con el servidor" porque BE=main (sin commit `f421ad81` platform 0.5.1 → `Settings()` con 14 campos required → ValidationError → 500 en `get_engine`/`get_settings`) mientras FE=vitalia esperaba el endpoint funcionando. `/health` lo enmascaró (no toca DB). Fix manual: `FORCE_REBIND=1 make dev-vitalia`. Sugerencia: `make dev-{brand}` valida coherencia `bind(FE)==bind(BE)` (y postgres) por marca ANTES de `up`, advierte/bloquea mismatch; `dev-lock-check --which` ya lista binds → extender a chequear el par FE↔BE. | **verified** | ✅ 2026-06-18: `scripts/dev-lock-check.sh` ahora chequea AMBOS servicios — `_fe_root()` deriva el worktree del FE (`/app/core` Source → dirname; fallback `/app/{brand}/frontend`); precondición de `make dev-{brand}` bloquea si BE **o** FE sirven otro worktree (verdict simétrico) + `_decide_pair` detecta binding MIXTO; `--which` muestra columnas BACKEND+FRONTEND+COHERENCIA con flag `⚠ MIXTO`. self-check 8/8 (incl. 4 casos `_decide_pair`) · `--which` live (cazó comunify bound a luana-platform) · block message live. `vitalia/docker-compose.dev.yml` · sesión 2026-06-18 |
| HB-88 | 2026-06-18 | 🟡 | **`POST /onboarding/drafts` (start_draft) tira 500 crudo en duplicado en vez de 409.** Encontrado verificando HB-80 (2do start_draft del mismo tenant): `repo.save` flushea → `UniqueViolationError` en `uq_vitalia_onboarding_progress_tenant_user` (1 draft por tenant+user) que se filtra como `IntegrityError`→500. Product bug (robustez de error-mapping), NO del fix de sesión-committing de HB-80. | **verified** | ✅ 2026-06-18 (TDD): `create_draft` cachea `IntegrityError`→`DraftAlreadyExistsError` (la constraint DB es la verdad, cubre el race check-then-insert) + `start_draft` la mapea a HTTPException 409. 2 tests RED→GREEN (service: IntegrityError→domain-error · route: 409) + happy regression. Live: 1er POST 200, duplicado 409 "Ya existe un borrador…" (era 500). ruff verde. Capturado como harness a pedido de Chris |
| HB-89 | 2026-06-19 | 🟡 | **`/pm-vitalia` (y cualquier lane que no sea el de Chris) no puede self-serve la live-verify DoD #37 → toda la evidencia la ejerce Chris a mano.** En lisa-servicios G round, los 4 findings los cazó la live-verify de Chris pero el lane de Claude no pudo reproducirlos: (1) el perfil Chrome del lane no tiene sesión Clerk (sin `CLERK_TESTING_TOKEN_VITALIA` armado en el userDataDir per-LANE) → no puede ejercer writes autenticados; (2) el FE OOMea (host ~3G libres) al levantar el stack para verificar. Resultado: el gate DoD depende de Chris, no se puede cerrar autónomo. Fix propuesto: (a) lane-auth — sembrar el Clerk testing token + storageState en el perfil Chrome del lane (`luana-vitalia-${LUANA_LANE}`); (b) headroom RAM del FE dev (ver HB-78/HB-86 dev-bundler-by-brand). **APLICADO 2026-06-23 (a · lane-auth):** `scripts/lane-auth.sh <brand>` + `scripts/lane-auth.mjs` siembran una sesión Clerk autenticada en el perfil Chrome del lane (`~/.cache/chrome-devtools-mcp/luana-${brand}-${LANE}`) vía `chromium.launchPersistentContext` + `@clerk/testing` (mismo ticket-flow que `clerk.setup.ts`) → los cookies se flushean nativos, el MCP arranca ya autenticado → los writes (POST/PATCH) NO redirigen a /sign-in. Guard: rechaza si el MCP tiene el profile-lock; freshness 4h. `make lane-auth-{vitalia,nicolify,comunify}` + paso en `chrome-devtools-verify` skill. **(b · RAM) = infra/host (no code-fix):** lever ya existe (vitalia=webpack + `make dev-active` single-brand FE ~2.7GB); la cura real es RAM física (32GB) — aceptado como host-bound. ⚠️ lane-auth necesita verificación live por Chris (stack + lane + creds reales). | applied | Origen: vitalia-fase2-lisa-servicios RECONCILE-HANDOFF § Gap infra. Complementa HB-73 (Chrome per-LANE) + HB-74 (verde-fantasma e2e mockeado). docs/process/harness-backlog.md |
| HB-90 | 2026-06-21 | 🔵 | **Caps forward-declared (`status: planned`/`idea`) sobreviven al feature real sin reconciliarse → cajas fantasma en el map del cockpit.** Caso: vitalia `offer_studio/medical-services-offer-preset` (planned, bootstrap 2026-05-15) describía la MISMA capability que `offer/lisa-servicios` (live, 2026-06-19) — mismo `agent_owner: lisa`, misma `functional_area: lisa.servicios`, misma route `/lisa/servicios`, mismo engine EP-2. Nunca se marcó `superseded_by`/`dropped` al shippear la cap real → 2 cajas en la misma área del cockpit. `cap-doctor` NO lo caza porque las caps `status: planned` están exentas del check bidireccional code↔cap. Gap de proceso: ni la fase R (reconcile, `/pm-{brand}`) ni F.3 (merge) obligan a reconciliar placeholders forward-declared preexistentes en la misma `functional_area` cuando una story shippea la cap real. **Decisión Chris:** ¿auto-drop o flag-only? Fix candidato: en F.3/R, grep caps con misma `functional_area` + `status ∈ {planned, idea}` → forzar `superseded_by` + `status: dropped`; **o** gate `cap-doctor` que flagee >1 cap `user_visible` en la misma `functional_area` con ≥1 en `planned`-stale. **APLICADO 2026-06-23 (decisión: FLAG-ONLY, no auto-drop):** gate **G10** (`gate_g10_stale_forward_decl` en `validate_code_cap_bidirectional.py`, registrado en `run_cap_gates` + `cap_doctor` GATE_LABEL/IDS) flagea un área con cap live + cap forward-declared (planned/idea/partial/wip) user_visible non-superseded. ADVISORY (excluido del hard-set `--cap-gates-hard`, mismo principio "no se prende HARD con fallas conocidas" hasta el backfill cross-brand). Auto-drop descartado: superseded-vs-dropped es juicio de `/pm-{brand}`, no de un gate determinístico (anti-default-flip). Tests g10 ×3 (single-live PASS · phantom-box RED · superseded PASS). cap_doctor vitalia ahora muestra `❌ G10 → lisa.servicios`. ⚠️ DATA RECONCILE pendiente (brand): agregar `superseded_by: offer.lisa-servicios` a `medical-services-offer-preset.yaml` (commit vitalia aparte → cierra la caja fantasma + G10 a 0). | applied | Origen: auditoría cockpit map 2026-06-21 (sesión vitalia). Cap fantasma + código muerto del bootstrap ya eliminados (14 files). Gate preventivo 2026-06-23. Complementa [[user-story-no-es-ssot]] · story-closure-gate § Fase R. docs/process/harness-backlog.md |
| HB-92 | 2026-06-22 | 🟡 | **Architect ready-package afirmó "el engine ya honra [INSTRUCCION DEL OPERADOR]" SIN verificar el read-side → integration gap (imagined contract, 5ª recurrencia familia HB-44/74/82).** En `vitalia-fase2-adrian-canal-inbound`: 01-spec § Instrucción step 3 + 03-arch-agentic § 1.2 trataron el `metadata_info["operator_instructions"]` (persistido por T-BE-3) como ya-inyectado por el supervisor. NO lo es: el engine `conversation_pipeline.prepare_messages_and_intent` inyecta `[INSTRUCCION DEL OPERADOR]` desde `checkpoint.resume_objective` (one-shot, lo limpia tras leer) y NO lee NADA de `metadata_info`. T-BE-3 persistió a una key que el engine nunca lee → la instrucción nunca llegaba al turno. Lo cazó el cross-module audit (NO-NEW-LAYER) de builder-agentic en T-AG-1, no el spec; cerrado con un bridge brand-side (`OperatorInstructionBridge` espeja `metadata_info[operator_instructions]`→`resume_objective` por turno, persistente, cero engine edit). Fix candidato: `/architect` (anti-orphan-integration) — toda claim "el engine ya honra/lee/inyecta X" en el ready-package DEBE citar el path:line del READ-side (grep), no solo el WRITE-side; un "ya existe" sin grep del consumidor = imagined contract. **APLICADO 2026-06-23:** `/architect` SKILL.md § Integration design (CONN) + checklist ready ahora exigen el **Read-side gate** — todo claim "el engine/supervisor YA honra/inyecta/lee [X]" cita su read-site EXACTO (`archivo:función` grep-verificado); sin read-site = contrato imaginado → la integración (bridge/wiring) es deliverable, NO supuesto. (Instancia original ya cerrada con OperatorInstructionBridge.) | applied | Origen: builder-agentic T-AG-1 cross-module audit 2026-06-22 (vitalia). Live-verified bridge end-to-end vs Postgres real. Complementa HB-44/HB-71/HB-74/HB-82 (familia imagined-contract) + anti-orphan-integration § Integration design. Regla preventiva 2026-06-23. docs/process/harness-backlog.md |
| HB-91 | 2026-06-21 | 🟡 | **Fase F (merge, story-closure-gate) archiva la story pero NO flipea `state: done` en el checkpoint → quedan campos stale en `{brand}/docs/archive/`.** 3 instancias confirmadas en vitalia archive, todas con `07-merge.md` + signoff APPROVED pero el checkpoint frontmatter en estado pre-merge: `root-login-redirect-softnav` (developed/AWAIT_CHRIS_VERIFY), `shell-nav-scroll-errors` (reviewing/REVIEWING_AWAITING_DEMO_SIGNOFF), `copilot-tools-impl` (developed/AWAITING_AUDITOR_AGENTIC). El cockpit (filesystem-as-DB) pinta "developed/reviewing" sobre algo `done` → confunde a Chris (no quiere firmar algo que cree pendiente cuando ya está merged+firmado). Fix candidato: paso explícito en Fase F del `/pm-{brand}` (o gate pre-commit) que asegure `state: done` + `phase: MERGED` en el checkpoint en el MISMO commit del `git mv` a archive (R2). **Bonus silent-killer:** el pre-commit frontmatter-discipline NO escanea checkpoints ya archivados hasta que se los toca → la key duplicada `ratified_by_chris` en `copilot-tools-impl` pasó sin detección desde 2026-05-18 (la cazó recién al editarlo hoy). Extender el glob del gate a `**/archive/**/checkpoint.md` (al menos en pre-push/ci-parity). **APLICADO 2026-06-23:** pre-commit Section 20 (`20-story-yaml-valid.sh` + helper) ahora BLOQUEA archivar un `checkpoint.md` bajo `docs/archive/.../stories/` con `state` ≠ `done` → fuerza a Fase F a flipear `state: done` en el MISMO commit del git mv. (Las 3 stale ya estaban flipeadas + dup removida; esto previene recurrencia.) ⚠️ Pendiente menor: el backstop corre sobre archivos STAGED (cubre el git mv); un barrido `**/archive/**` en pre-push queda como hardening opcional. | applied | Origen: sesión vitalia 2026-06-21 — scan archive (49 stories) tras review root-login. 3 stale ya flipeadas + dup removida (commits 4aa5482a, 35260155). Complementa HB-90 · story-closure-gate § Fase F + brand-docs-schema § R2. Gate preventivo 2026-06-23. docs/process/harness-backlog.md |
| HB-93 | 2026-06-22 | 🔴 | **Un `checkpoint.md` con frontmatter YAML estructuralmente inválido llegó a `state: developed` sin que ningún gate lo cazara → el cockpit (alpaca, Go-yaml) falla al parsear la historia y NO renderiza sus artefactos (demo-script incluido).** Caso: `vitalia-fase2-adrian-canal-inbound` — `/dev-team` escribió `pending_chris_G:` (clave de mapa) indentado 2 espacios DENTRO de la lista `dod_evidence:`, mezclando item `-` con clave al mismo nivel (YAML inválido). Error del cockpit: `did not find expected '-' indicator` apuntando a `line 22` cuando el error real está en `line 30` (Go-yaml reporta la línea del item previo — UX menor). Todos los gates de build pasaron GREEN; el checkpoint roto pasó invisible hasta que Chris abrió el cockpit. Fix candidato: gate `yaml.safe_load` sobre el frontmatter de todo `*/checkpoint.md` tocado en el commit (pre-commit + pre-push/ci-parity + opcional en `cap-doctor`); fail si inválido. **APLICADO 2026-06-23:** pre-commit Section 20 (`scripts/git-hooks/checks/20-story-yaml-valid.sh` + helper `scripts/validate_story_yaml.py`) corre `yaml.safe_load` sobre el frontmatter de `checkpoint.md` staged → bloquea con file:line + override `STORY_YAML_SKIP=1`. Tested: caso pending_chris_G-en-lista BLOQUEA, frontmatter válido PASA. Cubre HB-102 (mismo gate, mismos paths). | applied | Origen: sesión vitalia 2026-06-22 (loop sales_agent graph). Complementa HB-91 (glob `**/checkpoint.md` del frontmatter-discipline gate). Resuelto pasada /dev-team harness 2026-06-23. docs/process/harness-backlog.md |
| HB-94 | 2026-06-23 | 🔴 | **"Cubierto" ≠ "test verde mockeado" — falta la doctrina + gate de seam-testing.** 9 bugs de integración pasaron el unit suite (2900+ verde) en `vitalia-fase2-mateo-nueva-cita` porque cada test mockeaba el colaborador del otro lado de la costura. Doctrina durable (cementar en `test-design-doctrine.md` + `definition-of-done-live-verify.md`): un test que mockea la costura bajo prueba NO cuenta como cobertura de esa costura. + tabla costura→tipo-de-test (código↔DB→integration real-DB · FE↔BE→contract · service↔router→router test · código↔Clerk→live-verify · componente↔shell→render-en-contenedor). | applied | Diseño SSoT: docs/learnings/2026-06-23-coverage-means-real-collaborator-seam-testing.md. Aplicado pasada seam-testing 2026-06-23 (test-design-doctrine + DoD #37). Subsume/avanza HB-42/HB-74 (familia imagined-contract). |
| HB-95 | 2026-06-23 | 🔴 | **`/architect` `test_construction_plan` no declara la COSTURA ni el tipo-de-test real por escenario.** Fix: cada SC del plan declara `seam` (code-db|fe-be|router|auth|component-shell) + `test_type` requerido (integration-realdb|contract|router|e2e-live|live-verify) + `target`. Prohibir "unit (mocked)" para un escenario de costura. Habilita el gate HB-96. | applied | Aplicado pasada seam-testing 2026-06-23: clave `seam_coverage` en 04-validators-template + architect SKILL (Step 5 + validation gate) + gate `scripts/check_seam_coverage.py`. (Clave `seam_coverage`, no `scenario_to_test` — colisión con la existente.) |
| HB-96 | 2026-06-23 | 🔴 | **Phase D (dev-team local + auditor) cuenta "existe un test", no "existe un test del tipo que ejerce la costura".** Fix: el gate gherkin-matrix marca `MOCK-ONLY` (=`MISSING`) un escenario de costura cubierto solo por test mockeado. El auditor rechaza cobertura mock-only de escenarios de costura. | applied | Aplicado pasada seam-testing 2026-06-23: `scripts/check_seam_coverage.py` (mock-scan grep) cableado en dev-team Step 4.5 + auditor Phase D + machinery CHECK 32. Self-check prueba mock-DB → MOCK-ONLY. |
| HB-97 | 2026-06-23 | 🔴 | **technical_gates declarados (mutation HARD + schemathesis) se saltaron SILENCIOSAMENTE — el builder reportó solo conteo unit como "GREEN".** En mateo, `04-validators` declaró `mutation: diff-scoped-HARD` sobre 4 targets + `schemathesis: enabled`; mutmut 3.5.0 instalado (sin excusa de degrade); ningún result-file reportó score → nunca corrieron. Fix: `/dev-team` DEBE correr los technical_gates declarados y el result-file DEBE incluir su salida; `/auditor` verifica que corrieron (si `enabled: true` y no hay evidencia de corrida → CHANGES_REQUESTED). | applied | Aplicado pasada seam-testing 2026-06-23: dev-team DoD § technical_gates (result-file DEBE incluir salida) + auditor Phase D (enabled+sin-evidencia → CHANGES_REQUESTED). |
| HB-98 | 2026-06-23 | ✅ | **Runner E2E Playwright roto — SOLO vitalia (NO platform-wide). Causa real: `vitalia/frontend/node_modules` stale con copia física DUPLICADA de `playwright`, heredada de la migración in-place `node-linker=hoisted` (HB-78 2026-06-17) que nunca se clean-reinstaló.** El runner (`.pnpm/@playwright+test/.../cli.js`) bindeaba `.pnpm/playwright@1.60.0/.../lib/common`; los specs (`import @playwright/test` → copia hoisted root) bindeaban `node_modules/playwright/.../lib/common` — **distinto realpath-string, mismo inode** (hardlink; Node deduplica por path-string, NO por inode) → cada copia con su `currentlyLoadingFileSuite` → "did not expect test()". **Corrige el diagnóstico previo de esta fila (3 hipótesis refutadas empíricamente):** (a) NO es bug del loader 1.60 — trivial spec en `/tmp` con el MISMO binario PASA, dentro del paquete FE falla; (b) el "dual-version descartado" era FALSO — el realpath check previo miró `@playwright/test`+`playwright-core` pero NO el `playwright` (runner) que divergía entre la copia hoisted root y la de `.pnpm`; (c) `setup.describe.configure({mode:'serial'})` top-level es **LEGAL** (colecta perfecto con loader sano) — síntoma, NO causa → **clerk.setup.ts NO se tocó**. nicolify+comunify nunca estuvieron rotos (clean-instalados). **Fix:** clean reinstall canónico (`rm -rf **/node_modules && pnpm install` — lo que el `.npmrc` exige tras cambiar node-linker) → vitalia colecta 1043 tests/215 files, nicolify 22, comunify 26. **+ Bump 1.60.0→1.61.1** (3 marcas; changelog fixea "ESM loader resolution issues with pnpm workspaces" → reduce recurrencia; `pnpm dedupe` purgó el físico 1.60.0 residual que reintroducía el dual-version). **Hardening:** `@playwright/test` devDep directa en vitalia (cierra phantom-dep; 215 specs lo importan). Verificado: tsc 0, vitest 2662/2662, chromium 149 launch OK. ⚠️ host gap: falta `libavif16` (no-fatal — chromium lanza; `sudo apt-get install -y libavif16` silencia el warn de `playwright install`). | applied | Origen: vitalia-fase2-mateo-nueva-cita T-FE-4. Resuelto pasada /dev-team HB-98 2026-06-23 (commits e5d84794+69da2d43). Learning: docs/learnings/tooling/2026-06-23-pnpm-hoisted-stale-nodemodules-playwright-dual-instance.md. Files: vitalia+nicolify+comunify/frontend/package.json + pnpm-lock.yaml (clerk.setup.ts INTACTO). |
| HB-99 | 2026-06-23 | 🔴 | **El dispatcher `scripts/git-hooks/pre-commit` quedó con marcadores de conflicto git (`<<<<<<< Updated upstream` … `>>>>>>> Stashed changes`, +1655 líneas del god-file viejo) sin commitear en wip/vitalia → un `bash` con conflict-markers rompe el hook (syntax error) y barrió el sweep-guard M15 (HB-31) → contaminación cross-sesión observada.** Causa: `git stash pop` mal resuelto que mezcló el monolito pre-decomposición con el dispatcher nuevo. Silent-killer: el symlink activo apuntaba a la copia limpia de main, pero cualquier branch que committee ese file rompe sus commits. Fix (esta pasada): descartar el working-tree garbage (`git checkout` restaura el dispatcher committeado, `bash -n` OK) + `make install-hooks`. | applied | Origen: turn 1 sesión vitalia-fase2-mateo-nueva-cita; nunca HB-eado formal. Aplicado en pasada seam-testing. |
| HB-100 | 2026-06-23 | 🟡 | **`scripts/git/new-session.sh` no soportaba el pseudo-brand `protocol` (solo vitalia\|nicolify\|comunify\|lupulo\|core) y el scope-gate (`13-scope-branch.sh:79`) sugería `new-session.sh protocol exp <slug>` — comando inexistente.** El scope-gate YA reconoce branches `wip/protocol-*`, pero no había forma de CREAR ese worktree con la herramienta. Fix (esta pasada): pseudo-brand `protocol` + TYPE=`work` → `~/Proyectos/luana-protocol-{slug}` en `wip/protocol-{slug}`; hint corregido a `protocol work <slug>`. | applied | Origen: setup de la pasada seam-testing (KICKOFF §3). |
| HB-101 | 2026-06-23 | 🟡 | **El `click()` de Chrome DevTools MCP es sintético y NO dispara el `onClick` de React en botones con handler `onClick` puro (ej. FreeDoctorsList, "Crear paciente", "Crear cita") → el click "funciona" sin error pero no hay POST; se pierden turnos creyendo que es bug de la app.** Los pickers/combobox Radix SÍ responden al MCP-click; los botones onClick-puro NO. Workaround verificado: `evaluate_script` con `el.click()` nativo SÍ dispara React. Fix (esta pasada): documentado en la skill `chrome-devtools-verify`. | applied | Origen: live-verify de mateo (~15 turnos perdidos). Aplicado en pasada seam-testing. |
| HB-102 | 2026-06-23 | 🟡 | **Un `04-validators.yaml` puede ser YAML estructuralmente inválido y llegar a `developed` sin que ningún gate lo cace (extensión de HB-93, que solo cubre el frontmatter de `checkpoint.md`).** Caso descubierto esta pasada: `vitalia-fase2-mateo-nueva-cita/04-validators.yaml:56` — `SC-concurrent-availability:{ graders: ...` SIN espacio después del `:` → PyYAML no lo parsea (flow-mapping inválido). El gate `yaml.safe_load` de HB-93 debe extenderse a `*/04-validators.yaml` + `*/06-tickets.yaml` (no solo checkpoint.md). `scripts/check_seam_coverage.py` ya degrada con mensaje claro ante YAML inválido en vez de tracebackear. **APLICADO 2026-06-23:** pre-commit Section 20 valida los 3 (`checkpoint.md` frontmatter + `04-validators.yaml` + `06-tickets.yaml` staged) con `yaml.safe_load`. Tested contra el caso real de mateo (`04-validators.yaml:56` flow-mapping inválido) → BLOQUEA con file:line. ⚠️ El archivo de mateo SIGUE inválido en disco (story developing en otra sesión — NO se tocó); el gate lo cazará cuando esa sesión lo re-stagee. | applied | Origen: pasada seam-testing 2026-06-23 (corriendo el gate seam contra mateo). NO tocar mateo (developing). Complementa HB-93. Resuelto 2026-06-23. docs/process/harness-backlog.md |
| HB-103 | 2026-06-24 | 🟡 | **Doctrina design-system de nicolify stale — contradice el canon Storybook-first (canon §5, cement 2026-06-22).** `nicolify/.claude/rules/shell-mockup-per-component.md` (ADR-nicolify-003) todavía MANDA el modelo mockup-HTML `_shared.css` per-componente que `.claude/rules/frontend-visual-fidelity.md § Storybook` ya declaró MUERTO en favor de Storybook (`@luana/ui-kit`, 82 stories) como único SSoT visual; y el skill `nicolify-design-system §6` sigue citando "Mockup-per-component (hereda ADR-vitalia-003)" — el análogo de vitalia ya se actualizó 2026-06-22, el de nicolify no. Fix = alinear ambos artefactos brand al canon Storybook-first (matar `_shared.css`, partir de Storybook, proponer+promover net-new), espejo de la actualización de vitalia. **Fase 0 (prerequisito doctrina) del plan "todo sale del storybook" en nicolify** (status+plan 2026-06-24, Chris eligió doctrina-primero + manifiesto-consultable cross-brand). | applied | NO mid-feature (ds-adoption en G, abel en reviewing). Precede a la story de baseline de inventario brand. Owner harness batch. **Applied 2026-06-23:** 5 artefactos alineados a Storybook-first → (1) `nicolify/.claude/rules/shell-mockup-per-component.md` reescrita (Storybook = SSoT visual, `_shared.css` MUERTO, bucle partir+proponer+promover); (2) `.claude/skills/nicolify-design-system/SKILL.md` §4 wrapper + §6 gate + §8 checklist + frontmatter → Storybook-first; (3) `ADR-nicolify-003` marcada `status: Superseded` + banner + changelog 1.1 (no borrada, registro histórico); coherencia: (4) `nicolify/.claude/rules/README.md` filas, (5) `shell-feature-architecture.md` G1. Grep `_shared.css` en `nicolify/.claude/` + skill = solo histórico/superseded, cero gate activo. ⚠️ Observación: la *rule* + *ADR* de **vitalia** (`shell-mockup-per-component.md` / `ADR-vitalia-003`) siguen con el modelo viejo (solo el *skill* de vitalia fue alineado 2026-06-22) → candidato HB de paridad cross-brand. |
| HB-104 | 2026-06-24 | 🟡 | **El story-closure-gate (pre-commit Section 12, Layer 4) over-fires sobre una story `idea` NUEVA que comparte `module:` con una story open (developed/reviewing) — bloquea el commit aunque `idea` NO consume el bucket WIP.** El gate solo mira "el commit toca files de OTRA story del mismo módulo que una open", sin leer el `state:` de la story nueva. Pero el WIP-cap module-scoped es `≤1 en developing/developed/reviewing` — una story `idea` (cap ∞) no compite. Caso: `nicolify-r0-storybook-inventory` (idea, handoff) bloqueado por compartir `module: design-system` con `nicolify-r0-design-system-adoption` (en G). Resuelto con `STORY_CLOSURE_GATE_SKIP=1` ratificado por Chris (commit a60a77b6). **Fix propuesto:** el gate debe leer el `state:` del checkpoint de la story nueva del commit y NO bloquear si ∈ {idea, parked, dropped} (no consumen bucket). Reduce overrides ratificados innecesarios. | reported | Origen: handoff nicolify-r0-storybook-inventory 2026-06-24. SSoT: `.claude/rules/story-closure-gate.md` + pre-commit Section 12. |
| HB-105 | 2026-06-24 | 🟡 | **Migración Storybook-first incompleta cross-harness (deep sweep de HB-103).** Tras alinear nicolify (HB-103), 3 subagentes Opus barrieron el harness: el canon Storybook-first (canon §5, cement 2026-06-22) dejó stale (a) **vitalia entero** — rule `shell-mockup-per-component.md` + `ADR-vitalia-003` (`status: Accepted`) + README + `DESIGN-SYSTEM-ENFORCEMENT.md` + `01-spec-shell-template.md` §7 + `shell-feature-architecture-mandatory.md` + skill `vitalia-design-system §8`, todos MANDABAN el modelo mockup-HTML/`_shared.css`/`ratified_visual_by_chris`/golden-vs-mockup como gate ACTIVO (solo el *skill* vitalia se había actualizado 2026-06-22); (b) **2 FE-agents con gates muertos** — `builder-frontend` UX-FIRST GATE spawnea `nicolify-ux-designer` + lee `prompts/0a-ux-designer-start.md` (**ambos inexistentes**), y `auditor-frontend` `UX_HANDOFF_MISSING`/`UX_NOT_APPROVED` FAILean sobre `UI-SPEC.md`+`design.md` que el flujo v4 NUNCA produce (**landmine: FAILea toda story UI v4**); (c) **templates raíz** `01-spec-template.md` (mockup-kit/`/showcase`/`mockups/{screen}.html`); (d) **canon auto-contradictorio** (L127 'cita el mockup-kit' vs L149 'mockup-kit SUPERSEDED'); (e) **vocabulario pre-v4 muerto** `CONTRACT.md`/`UI-SPEC.md` (~25 refs en builder/auditor/context-builder; v4 usa `03-arch.md`/`01-spec.md`). **APLICADO 2026-06-24:** 14 artefactos harness-doc alineados a Storybook-first → vitalia a paridad con nicolify (rule reescrita + ADR Superseded + README + enforcement-doc + template + shell-feature-mandatory + skill); FE-agents re-cableados al gate v4 (`mockup_final_signed` + `03-arch §FE` cita la story de Storybook, fuera `nicolify-ux-designer`/`UI-SPEC.md`/`design.md`); templates + canon coherentes; vocab renombrado. Grep de gates activos `_shared.css`/`mockup-per-component`/`UI-SPEC.md` = 0. | applied | Deep sweep 3 subagentes Opus (vitalia · actor-skills/agents · templates/hooks/canon). NO mid-feature. Product test-infra spun off → HB-106/107. |
| HB-106 | 2026-06-24 | 🟡 | **Gates DS mecánicos cross-brand son vitalia-only (vapor en nicolify/comunify).** El canon §5 enmarca `no-div-layout` + `no-native-select` (arch-tests FE, ratchet shrink-only) como cross-brand, pero solo existen en `vitalia/frontend/src/__tests__/architecture/`. nicolify tiene eslint `no-arbitrary-value` wired pero NO los 2 arch-tests; comunify no tiene NINGÚN gate DS (ni eslint no-arbitrary ni los tests) → una hoja con `<div>` de layout o `<select>` nativo pasa el gate en esas marcas. **Diseño:** (1) replicar `test-no-div-layout.test.ts` + `test-no-native-select.test.ts` a `nicolify/frontend/src/__tests__/architecture/` con baseline shrink-only (correr vitest para fijar el count); (2) idem comunify + wire eslint `no-arbitrary-value` en `comunify/frontend/eslint.config.mjs`; (3) recién entonces emitir los gates como entries reales en `04-validators-template` (hoy = comentario-pointer, anti-vapor). **APLICADO 2026-06-24:** arch-tests `no-div-layout` (baseline 39/11 nicolify · 34/25 comunify) + `no-native-select` (1/1 ambos) replicados + `_ds-lock-scanner` brand-local portado; 7 tests GREEN por marca; commits nicolify `825ec59a` + comunify `7d9bb690`. ⚠️ Eslint `no-arbitrary-value` en comunify **NO cableado** a propósito: comunify nunca tuvo ds-adoption → `flip=error` rompería su lint (necesita su propia story de migración, no un flip ciego — diferido). vitalia ya tenía los 2 arch-tests. | applied | Origen: deep sweep HB-105 (Agent C wiring). Product-test-infra ejecutado 2026-06-24 (cross-brand vía worktree comunify). Pendiente: eslint no-arbitrary comunify (su ds-adoption). |
| HB-107 | 2026-06-24 | 🟡 | **'Net-new shared promovido a `@luana/ui-kit` + story' no tiene gate mecánico — solo prosa.** El canon §5 + `/auditor` Cat 16 + `/architect` exigen promover una primitiva shared net-new al kit (no dejarla local en `features/{m}/`), pero ningún hook/arch-test lo detecta: una pieza shared dejada local pasa todos los git-hooks; solo la caza el auditor si lee la prosa. **Diseño (heurístico, no trivial):** arch-test FE que flaggee componentes en `features/{m}/components/` que (a) son autocontenidos (no importan de otra `features/`) y (b) se referencian desde ≥2 features distintas → candidato 'shared sin promover' → WARN ratchet. Alternativa simple: exigir header `// promote-candidate: <reason>` en componentes nuevos de `features/*/components/` sobre cierto LOC. Validar señal (riesgo falsos positivos) antes de cablear. **APLICADO 2026-06-24:** `test-no-local-kit-primitive.test.ts` (heurística CONSERVADORA: basename EXACTO = primitiva del kit en `features/<m>/components/`, baseline 0, ratchet shrink-only — domain-prefixed `IcpCard`/`CohortRosterTable` exentos → cero falsos positivos medidos en las 3 marcas). En nicolify (`825ec59a`) + comunify (`7d9bb690`). El diseño original (import-graph ≥2 features) se descartó: ESO ya lo cubre el FSD-boundaries eslint; la señal real del bug es la **primitiva reinventada local** (basename), que es lo que este test caza. vitalia parity diferida (worktree con WIP activo de otra sesión — no interferir). | applied | Origen: deep sweep HB-105 (Agent C). Backstop prosa (auditor Cat 16) queda; ahora mecanizado. Ejecutado 2026-06-24. |
| HB-108 | 2026-06-23 | 🟢 | **`scripts/scan_harness_pointers.py` (machinery CHECK 28) flaggeaba como "rot" los punteros R3 a OUTPUTS auto-gen gitignored (PORTFOLIO/BACKLOG/INFRA-MATRIX) → 12 falsos-positivos en un worktree fresco (los outputs nunca se materializan).** Causa raíz: el scanner no tenía conciencia de `.gitignore` — un pointer correcto a un archivo «edita la fuente + regen» se veía roto. **APLICADO 2026-06-23:** helper `_is_gitignored(target)` (`git check-ignore -q`, matchea por pattern aunque el archivo no exista) → el scanner salta los gitignored. Mata la clase entera de FP para todas las marcas presentes y futuras. NO se editó ningún SKILL.md (los 12 punteros eran correctos). machinery 72/0/**0 advisory** (antes 1). | applied | Origen: /harnesses-improvement 2026-06-23 (el minor de CHECK 28). Investigado a fondo: 12/12 = falsos-positivos gitignored. docs/process/harness-backlog.md · ★ renumerado de HB-103→HB-108 2026-06-24 (colisión con HB-103 design-system de sesión paralela; ver HB-103). |
| HB-109 | 2026-06-24 | 🔴 | **Bugs reales (cursor pagination rota → dup-key React → bloqueo crear-paciente, validación prematura) llegaron a Chris pese a tests "verdes" — porque los tests mockean el backend/repo.** CAUSA RAÍZ triple: (1) **unit del REPO mockea el repo mismo** (`test_patient_search_typeahead.py` usa `AsyncMock`) → el SQL real (ORDER BY/WHERE del cursor) NUNCA se ejecuta → un cursor roto (`id > :cursor` sobre `ORDER BY created_at DESC` = campo-cursor ≠ campo-orden + dirección invertida) shipó sin que ningún test lo cazara; (2) **`vitalia_test` DRIFTA de `vitalia_dev` en la MISMA `version_num` alembic (052)** — `vitalia_test` no tiene `marketing_opt_in/opt_out/opt_out_reason/opt_out_at` (dev sí) → fue **stampeada, no migrada secuencialmente** → los integration tests corren contra un schema EQUIVOCADO (silencioso); (3) **e2e mockea su propio backend** (`page.route`) → falso-verde sobre data real (el dup-key venía de paginación con >20 filas, jamás ejercida por el mock que devolvía 1 paciente). PREVENCIÓN: (a) todo repo con SQL no-trivial (paginación/cursor/filter/join) DEBE tener ≥1 test **real-DB** (la costura, no mock del repo) — refuerza seam-testing HB-94..98; guard nuevo ya agregado: `test_patient_search_pagination_realdb.py` (probado RED sobre el cursor viejo, GREEN sobre el fix); (b) **gate schema-parity**: verificar `vitalia_test` schema == migrations-head (rebuild desde migraciones en preflight/CI o diff test-vs-dev) — el drift a igual version_num es peligroso y ya rompió mi primer test por columna inexistente; (c) extender el gate anti-mock-de-su-propio-backend (HB-74) a e2e: surface con flujo de data real (paginación/create) NO se acepta verde solo con e2e mockeado. | reported | Origen: vitalia-fase2-mateo-nueva-cita live-verify 2026-06-24. Fixes: cursor pagination (4126f840) + EntityPicker dedup (0de9625a). Complementa HB-42/74/94..98. · ★ renumerado de HB-108→HB-109 al sync 2026-06-24 (colisión con HB-108 main = scan_harness_pointers gitignore). |
