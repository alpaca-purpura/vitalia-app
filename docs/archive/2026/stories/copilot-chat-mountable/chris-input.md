# chris-input — copilot-chat-mountable

> Technical-story (engine fix). Sombrero CTO: `/architect` propone, Chris decide.

## ✍️ Firmas

- **Approach (2026-06-16):** ✅ **C — full lazy refactor** (`get_settings()` en ~55 call-sites, sin global mutable; shim back-compat deprecado durante transición para semver minor). Firmado Chris. Rechazados A (lazy module-boundary) y B (router factory).

## 💬 Conversación

- 2026-06-16 · `/architect` · **✓ APLICADO** — Recon del codebase antes de pedir firma: 55 archivos engine usan el global `settings`; NO existe app standalone del motor (riesgo "no romper standalone" = moot); ningún código de prod monta el `/chat` hoy (solo tests); root cause = 2 usos eager a module-load (`config:317` + `database:22-59` engine+redis). Presenté 3 approaches (A lazy-boundary recomendado / B factory / C full-refactor). **Chris eligió C** (end-state limpio, acepta R3 ancho).
- 2026-06-16 · `/architect` · **✓ APLICADO** — Ready-package cerrado (state ready). 5 tickets: T-1 root lazy (config+database)+shim · T-2 migrar import-path chat (driver=test de efecto) · T-3 contract+bump+CHANGELOG+doc · T-4 completar C (~48 off-path)+deprecar shim · T-5 R3 4 marcas+/chat 401/200. DAG T-1→T-2→{T-3∥T-4}→T-5. Insight clave: el unblock NO requiere los 55 — solo el import-path transitivo de chat.py; el shim cubre el resto. T-1+T-2+T-3 ya desbloquean comunify; T-4 completa C. **El orchestrator (Opus) crashó por socket a los 288s sin escribir nada — produje el ready-package directo (ya tenía el recon decisivo).** Próximo: Chris ratifica el diseño (blast-radius luana_core_platform) → /dev-team build. autonomous_mode: false.
- 2026-06-16 · `/dev-team` · **✓ APLICADO** — Build T-1/T-2/T-3 DONE + pushed (bb8e34f8/13665a80/680144a0). UNBLOCK logrado y verificado: driver subprocess GREEN (chat.py importable env multibrand-only) + R3-light (vitalia/nicolify/comunify importan limpio) + suites tocadas GREEN. Camino difícil: 4 builders sub-agente murieron por infra (socket/500/timeout) → terminé el debug + fixes de tests yo (polución lru_cache + sys.modules → driver reescrito a subprocess; ~8 test-mocks actualizados por globals module-level removidos). **Decisión pendiente Chris (G):** T-4 (completar C = migrar ~47 settings + ~61 database shim-users en 12 paquetes + retirar shim) es GRANDE y deferrable (el shim funciona). ¿Shipear el unblock + deferir T-4 a story follow-up, o seguir T-4 ahora?
- 2026-06-16 · `/auditor` · **✓ APLICADO** — **Verdict: APPROVED** (CHECKPOINTS.md). Re-corrí todo desde el worktree: ruff clean · driver subprocess GREEN (la acceptance) · `test_lazy_settings_no_eager` 6✓ · suites copilot 1641/25s · platform 290 · events 76 · assets 58 · iam✓ · R3 import 3 marcas OK (lupulo placeholder) · CHANGELOG+semver (platform 0.5.0/copilot 0.3.0). Review de código: lazy refactor correcto, shim PEP 562 preserva back-compat, graceful-degrade (redis None + `_LazySessionLocal` deferido) intacto, cero cambio de comportamiento. **0 findings de código.** 1 finding doc-coherence (Carril R, lo arreglé yo): `04-validators.yaml` no estaba reconciliado al descope (6 validators de scope T-4/T-5 deferido seguían `must_pass:true`) + `be_{platform,copilot}_arch` apuntaban a `tests/architecture/` inexistente en core pkgs → reconciliados a `must_pass:false`+`deferred:`. Upstream deficiency → HB-79 (Fase R debe reconciliar 04-validators, no solo checkpoint). Auto-handoff /pm-luana para el merge (es core).
- 2026-06-16 · `/pm-luana` · **✓ APLICADO** — Merge a main (core/engine, owner /pm-luana). Cerré la story: 07-merge.md (technical-story: §§ Gherkin/Playwright/caps N/A, verificación = validators + R3) + `docs/core-modules/copilot.md` NEW (contrato `/chat` brand-mountable — arregla el link roto del README index) + checkpoint state→done + `git mv` story a `docs/archive/2026/stories/` (R2) + squash-merge wip/core-copilot-mountable→main. ci-parity full-Docker 4-marcas = advisory (sentinel) + redundante para un refactor core con engine-suites+R3 ya GREEN → confié en el gate de pre-commit (cross_check_3 HARD corre en el commit del merge). **Pendiente handoff (NO esta sesión):** proposal `2026-06-16-copilot-chat-brand-mountable` → `migrated` la marca /pm-comunify al re-montar (vive en wip/comunify); comunify-shell-organism T-agentic v2 desbloqueada; T-4 follow-up para /pm-luana cuando convenga.
