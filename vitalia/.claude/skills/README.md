# Vitalia — skills overlay

Skills brand-specific que extienden `.claude/skills/` core.

**Filosofía:**
- Skills técnicos (`copilot-expert`, `backend-expert`, `auditor`, etc.) son globales en `.claude/skills/` core.
- Skills brand-specific (si surgen) viven aquí. Ej: `/vitalia-domain-helper` con knowledge profundo del vertical.
- Por ahora vacío. Se llena solo si emerge necesidad.

`/pm-vitalia` vive en `.claude/skills/pm-vitalia/` core (no aquí) — es estructural del paradigma.
