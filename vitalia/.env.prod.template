# vitalia/.env.prod.template
# CHECKED IN — copialo a vitalia/.env.prod (gitignored) en servidor de produccion.
# vitalia/.env.prod NUNCA debe commitearse (.gitignore ya lo excluye).
#
# S-DOCKER-DEV-MULTIBRAND T-7 — 2026-05-15

# === Clerk (Auth) ===
CLERK_PUBLISHABLE_KEY=pk_live_REPLACE_ME
CLERK_SECRET_KEY=sk_live_REPLACE_ME
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_REPLACE_ME

# === Database ===
DATABASE_URL=postgresql+asyncpg://REPLACE_ME:REPLACE_ME@REPLACE_ME:5432/vitalia_prod

# === Redis ===
REDIS_URL=redis://REPLACE_ME:6379/1

# === Qdrant ===
QDRANT_HOST=REPLACE_ME
QDRANT_PORT=6333
QDRANT_COLLECTION_PREFIX=vitalia_

# === Brand ===
BRAND_SLUG=vitalia
ENVIRONMENT=production

# === LLM ===
OPENAI_API_KEY=sk-REPLACE_ME
LITELLM_PROXY_URL=REPLACE_ME

# === Cloudflared ===
TUNNEL_TOKEN=REPLACE_ME

# === Next.js ===
NEXT_PUBLIC_API_URL=https://api.vitalialat.com
