/**
 * Smoke test — verifies the vitest suite runs for vitalia frontend.
 * A4 scaffold acceptance criterion.
 */
describe("vitalia frontend scaffold", () => {
  it("suite runs without error", () => {
    expect(true).toBe(true);
  });
});
