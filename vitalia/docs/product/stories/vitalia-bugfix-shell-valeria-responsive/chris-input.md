# chris-input — vitalia-bugfix-shell-valeria-responsive

> Protocolo `docs/process/chris-input-protocol.md`: input verbatim de Chris + verdicts que cada skill appendea al cierre de su turno.

---

## Input inicial (Chris · 2026-06-04 · creación de la story)

> crea un story para mejorar el responsive de valeria que toca el shell, agrega estas cosas:
> 1. Por default el rail del historial debe estar collapsed
> 2. el 50-50 de valeria y lo demás se reduce a 30 Valeria - 70 los demás (por default), pero de igual forma se sostiene que es resizable
> 3. Revisar y proponer como trabajar para tamaños como tablet.

### Contexto / origen (de dónde sale)

Durante el cierre de `vitalia-fase2-adrian-inbox` (2026-06-04), la inspección live (Chrome DevTools MCP) destapó que **Valeria 'full' (~50%) exprime el contenido del agente**: en viewport 1280 con Valeria 'full', el `inbox-desktop` queda en 696px → el thread del inbox (`flex-1 min-w-0`) se exprime a **56px** (0px en sesión Playwright fresca) → ilegible. Esto bloquea la verificación live de AC-4/5/7 del inbox + afecta TODAS las sub-tabs de agente (Lisa/Mateo/Adrián/Lucas/Camila), no solo el inbox.

Chris confirmó el diagnóstico y fijó la dirección: **mejorar el responsive del shell para que Valeria reduzca su tamaño / colapse cuando se llega a estos anchos**. Esta story implementa esa dirección con sus 3 puntos.

**Repro + causa raíz + opciones de fix:** `vitalia/docs/observed-bugs/2026-06-04-shell-valeria-squeeze-plus-darkmode.md` (BUG #1).

### Alcance (qué SÍ / qué NO)

- **SÍ** (esta story): responsive de Valeria — (1) rail del historial collapsed por default, (2) default 30/70 (Valeria/agente) en vez de 50/50, conservando resizable, (3) propuesta de comportamiento tablet.
- **NO** (sibling, story aparte): el **dark mode half-applied** (inbox `vt-*` sin variante `[data-theme="dark"]`) — BUG #2 del mismo observed-bug. Es token-audit, concern separado. Tracকear como story hermana.
- **NO**: ContactSidebar colapsable — ya existe (`ContactSidebarToggle` en ThreadHeader). BUG #3, no es trabajo nuevo.

### Restricción de coordinación

Hub compartido con sesión(es) concurrente(s). Esta story toca el shell-organism (`src/components/shared/shell-organism/` + `src/stores/shell-store.ts` + `useViewportGuard.ts`) → cross-tab. Coordinar bucket lock `code:shell` (o similar) al construir. NO arrancar build con otra story del shell en `developing`.

## 💬 Conversación

### 2026-06-06 · 🤖 claude · `/pm-vitalia` · ⚠️ DUDA
Contexto: Chris ratificó el lift FE/shell a `@luana/ui-kit` (promotion proposals `accepted` 2026-06-06, commit 256517a3) y pidió cerrar las stories abiertas ANTES del lift (árbol limpio). Esta story (`shell-valeria-responsive`) es **la ruta crítica del lift** — es el único colisión a nivel de archivo (el shell mismo). Está **code-complete + live-verified** (commits 29451ef6 + 614bfbd5, dod_live_verified=true); su `next_action` marca que falta **el demo #37 de Chris ANTES de /auditor** (gate DoD #37 / story-closure v5 fase G). Pendiente de Chris: hacer el demo en dev-app (1280/1024/800) → firma `chris_verify.signoff` → yo corro /auditor → merge → shell limpio → arranca el lift. Recalibración propuesta: ruta crítica = esta story (no las 4 en serie); embudo/lisa son features acopladas que cierro en paralelo. Espera decisión de Chris sobre arrancar los demos.

### 2026-06-06T19:44 · 👤 Chris · nuevo input (regresar a plan · scope expandido)
> quiero continuar la historia vitalia-bugfix-shell-valeria-responsive pero necesito regresar al plan antes de desarrollar ya que haya algunas solicitudes y revisar bien ya con la nueva forma de trabajo que tenemos para volver a armarla bien, por ejemplo quiero que veamos también esto:
> 1. ese botón "agentico" que cambia la vista a web y agentico en la parte superior ya no es necesario
> 2. el boton con dropdown de cambio de tenant debe estar pegado a la derecha, no a la derecha.
> 3. valeria actualmente ocupa 50/50 con el contenido de la aplicación en si, debe ser una propoción de aproximadamente 30/70
> 4. actualmente el tamaño mínimo de valeria "rompe" el responsive de valeria, debemos arreglarlo
> 5. El rail del historial por default debe estar colapsado
> 6. Valeria debe tener su propio boton de colapsar, el que esta abajo en el rail de conversacion no es ni visible ni util. Cuando valeria se colapsa se colapsa tmb el historial si estuviera abierto y cuando todo este colapsado al "abrir" valeria debería abrirse solo valeria y si quiero ver el hsitorial abro el historial y empuja a valeria, tal como funcionaba antes en el legacy.
> 7. Necesito agregar por diseño el comportamiento del 3er tab para casos lista / detalle, como lo hicimos en staff en valeria o icp en Nicolify

### 2026-06-06T19:44 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
Regresé la story `developing → refining` (Chris-directed) para re-armarla con proceso v5. Capturé los 7 puntos en `checkpoint.md § Re-planning 2026-06-06` (4 nuevos: 1 quitar toggle web/agéntico · 2 tenant-switcher pegado a la derecha · 6 botón propio de colapsar Valeria + push legacy del historial · 7 patrón N3 list/detail tipo staff/icp; 2 ya construidos: 3 el 30/70 + 5 rail-collapsed; 1 refinamiento: 4 min-width rompe responsive). El build previo (614bfbd5 + 29451ef6) queda como **BASELINE** — no se descarta. Marqué que el `dod_live_verified` actual cubre SOLO el baseline → NO mergeable con esa evidencia hasta re-verificar los puntos nuevos. Step 0 GREEN (la única OPEN es lisa-doctores, módulo distinto). Encadeno a `/po-ux` ahora (R1 interrogatorio + mapa funcional + DUDAS → R2 ejecutable + mockups).
Necesito de Chris (lo resolverá /po-ux en R1, lo dejo flaggeado): (a) **punto 2 typo** — "pegado a la derecha, no a la derecha": ¿pegado al borde derecho del topbar (vs centrado/separado)? (b) **flag de proceso**: esta story es ruta crítica del lift shell→`@luana/ui-kit`; expandir scope lo retrasa — el sequencing rework-vs-lift se decide en `ready` (refinar ahora es safe, no toca código).

### 2026-06-06T19:50 · 🤖 claude · `/po-ux` · ✓ APLICADO
RONDA 1 del proceso v5 escrita en `01-spec.md` (v1, `spec_round: 1`). Antes de redactar exploré el shell real + prior-art y corrí el interrogatorio gate con 2 batches de preguntas. Decisiones cerradas con Chris: **(1)** eliminar modo web entero (chip `ShellModeToggle` + `shellMode` del store + acople D2 `collapsed↔web`) → shell siempre agéntico. **(2)** switcher de tenant al extremo derecho del topbar (`[🌙 tema][▼ tenant]`) — hoy está a la izquierda junto al logo; resuelve el typo. **(3)** 30/70 (ya construido). **(4)** split inline ≥1024 con Valeria clamp min ~320px (el min legacy 620px era la causa del "rompe"); <1024 drawer. **(5)** default = Valeria abierta en chat, historial colapsado. **(6)** legacy-push 3 estados: A cerrada (tira 44px avatar V) / B chat 30/70 / C +historial que EMPUJA (ensancha Valeria, angosta agente); botón colapsar propio en cabecera; colapsar Valeria colapsa historial; abrir historial estando cerrada abre Valeria; rail de 60px eliminado. **(6t)** tablet/móvil <1024 mantiene drawer. **(7)** documentar/cementar el patrón N3 list/detail (EntitySubNavBar — ya construido en lisa/staff + adrian/embudo + nicolify abel/icp) DENTRO de esta story como contrato.
El spec tiene § Mapa funcional (happy path + árbol de bifurcaciones Bif-1..7 + RN-1..11 + AC-1..10) + wireframes ASCII borrador de los 3 estados + topbar + N3 + § Prior art applied. Reclasifiqué `type: bugfix → ui-story` (el scope dejó de ser bugfix lite).
**Necesito de Chris (firma 1 + 4 dudas):** (1) historial estado C ¿ancho fijo ~260px o resizable? (2) tira fina ¿siempre borde izquierdo? (3) punto 7: ¿documentar + flaggear divergencias entre staff/embudo/icp, SIN refactorizar las 3? (4) ¿confirmás reclasificación bugfix→ui-story? → Con tus respuestas + "firmo RONDA 1" escribo el mockup FINAL + RONDA 2 (Gherkin + matriz) y cierro a `refined`.

### 2026-06-06T20:05 · 🤖 claude · `/po-ux` · ✓ APLICADO
Chris preguntó si nicolify tiene el mismo shell y cuál arquitectura es mejor. Comparé `nicolify/frontend/src/components/shared/shell-organism/` vs vitalia (read-only, sin tocar nicolify). Hallazgo: **nicolify = port re-skinneado del shell de vitalia** (Valeria→Luana), misma máquina de estados (renombró `rail`→`history`; `railWidth = full?280:60` idéntico), mismo `ShellModeToggle`/`shellMode` muerto, mismo `EntitySubNavBar`/`useViewportGuard`. Tres diferencias materiales: (a) **ninguna de las dos hace el "push" del historial** → el punto 6 legacy-push es nuevo para ambas; (b) nicolify declara un `splitState` enum semi-vestigial (no copiar); (c) **nicolify está mejor en el punto 7**: tiene `EntityWorkspaceLayout` = wrapper reusable del N3 list/detail (EntitySubNavBar + children + skeleton SSR-safe G2), mientras vitalia cablea EntitySubNavBar a mano por layout. Decisiones derivadas, registradas en `01-spec.md § Prior art › Nicolify comparison`: punto 6 = diseñar modelo limpio nuevo (`closed/chat` + `historyOpen` aditivo) que supere a ambas, NO copiar el conflado; punto 7 = portar el `EntityWorkspaceLayout` de nicolify como contrato canónico vitalia. Flag cross-brand: ambas casi gemelas + shell aprobado para lift a `@luana/ui-kit` (256517a3) → el punto 6 + EntityWorkspaceLayout + quitar web-mode deberían converger ambas marcas y alimentar el lift; es decisión `/pm-luana` (yo no edito nicolify), lo escalo post-merge.

### 2026-06-06T20:20 · 🤖 claude · `/po-ux` · ✓ APLICADO
Chris dijo "aprendamos lo que nicolify hizo bien" + respondió las 4 dudas. (1) **Learning capturado:** `vitalia/docs/learnings/2026-06-06-n3-entity-workspace-layout-from-nicolify.md` (promotable: candidate, target `@luana/ui-kit`) + pointer en MEMORY.md — el patrón N3 list/detail tiene 2 niveles de factorización; nicolify (port re-skinneado) evolucionó `EntityWorkspaceLayout` reusable que vitalia no tiene; adoptarlo + meta-learning "comparar cross-brand antes de rediseñar shell compartido = también adoptar la mejor versión, no solo no-duplicar". (2) **Respuestas registradas en `01-spec.md`:** historial estado C = ancho FIJO ~260px (chat resizable) → RN-7/AC-6 · tira fina borde izquierdo · reclasificación bugfix→ui-story confirmada · **punto 7 = REFACTOR REAL** (portar EntityWorkspaceLayout + migrar staff+embudo, "dejemos todo bien"), no solo documentar → AC-9 + out-of-scope actualizados.
⚠️ **DECISIÓN PENDIENTE (secuencia punto 7):** el refactor cruza módulos con stories ABIERTAS — `lisa/staff`=`vitalia-fase2-lisa-doctores` (developing, module clinics) + `adrian/embudo`=`vitalia-fase2-adrian-embudo` (developed, module crm). Refactorizarlos desde esta story (module shell) colisiona M14. Preguntando a Chris cómo secuenciar (bundle-aquí vs story-dedicada vs fold-en-las-abiertas) antes de cerrar scope + firmar RONDA 1.

### 2026-06-06T20:35 · 🤖 claude · `/po-ux` · ✓ APLICADO
Chris eligió **bundle 1-7 en esta story**. Registrado: `01-spec.md § Coordinación punto 7` documenta la decisión + las 4 preconditions que `/pm-vitalia` DEBE ejecutar ANTES del build (bumpear type/module cross-`[shell,clinics,crm]` · pausar lisa-doctores · plegar embudo · adquirir 3 bucket locks). Frontmatter: `input_spec_signed: true` (RONDA 1 intent cerrada — 7 decisiones + 4 dudas + secuencia resueltas) · `cross_module_scope: [shell, clinics, crm]` · `po_ux_version: 2`. Riesgo aceptado por Chris (story grande, serializa 3 módulos, reestructura 2 stories) a cambio de dejar el N3 canónico + funcional en un paso.
**Mockup FINAL interactivo creado:** `mockups/shell-valeria-states.html` (tokens verbatim de globals.css; toggles estados A/B/C con historial-empuja fijo 260px, viewport 1280/1100/800 con clamp 320 + drawer, N3 directorio↔workspace, dark). **Pendiente firma 2** (`mockup_final_signed`).
**Necesito de Chris:** abrí el mockup (`python3 -m http.server 8888` en el dir mockups/) → "firmo el mockup". Con firma 2 escribo RONDA 2 (Gherkin 4 base + sub-categorías a11y/i18n/network/empty/large + matriz de cobertura Bif/RN→SC→verificación REAL) → cierro `refined` → handoff `/architect`. ⚠️ Recordá: antes del BUILD, `/pm-vitalia` ejecuta las 4 preconditions de coordinación cross-módulo.

### 2026-06-06T20:50 · 🤖 claude · `/po-ux` · ✓ APLICADO
Chris revisó el mockup low-fi y pidió 2 ajustes de comportamiento, aplicados al mockup + al spec: **(1)** estado A (cerrada) — reabrir Valeria = clic en la **imagen/avatar** (no flecha), + atractivo (avatar destacado + dot estado + label "Valeria" + hover) → RN-12 + AC-5 + Bif-2a/2b + wireframe. **(2)** cabecera de Valeria gana un botón **"+" nueva conversación** que limpia el chat (estado vacío) y **archiva la conversación actual al historial** (aparece en la lista) → RN-13 + AC-11 + Bif-3c + wireframe; el mockup lo simula (prepend a la lista de historial + empty-state + toast). Mockup actualizado en `mockups/shell-valeria-states.html` (server sigue en :8888, refrescar).
**Necesito de Chris:** refrescá el mockup, probá el avatar (estado A) + el "+" (estado B/C) → **"firmo el mockup"** (firma 2). Con eso escribo RONDA 2 (Gherkin + matriz) y cierro `refined`.

### 2026-06-06T20:55 · 🤖 claude · `/po-ux` · ✓ APLICADO
Chris confirmó (firma 2 ✓) + caveat "seguir los lineamientos UI vigentes, no arruinar lo que está bien". Caveat cementado como **§ Design system constraint (HARD)** en `01-spec.md`: el mockup es behavior-fi NO pixel-fi; la implementación usa átomos `components/ui/` + tokens `globals.css` + SHELL-DESIGN-CONTRACT + avatar real del catálogo (no el placeholder "V"); dark mode no se toca; regresión cero en sub-tabs done. **Story CERRADA a `refined`** (spec v3): RONDA 2 escrita — 19 scenarios (4 base + a11y/i18n/network_failure/empty_state/large_dataset; race/concurrent con not_applicable_reason ratificable) + § Matriz de cobertura (Bif-1..7 + RN-1..13 → SC, 0 huecos / 0 huérfanos) + estados visuales + componentes (reuse>new, NEW justificados: EntityWorkspaceLayout port + tira-avatar) + microcopy neutro + responsive + a11y. Checkpoint: `state: refined`, ambas firmas + `ratified_visual_by_chris: true`, type bumpeado bugfix→ui-story, `cross_module_scope: [shell, clinics, crm]`.
**Próximo (decisión Chris):** handoff a `/architect` para el ready package. ⚠️ PERO esta story es cross-módulo (bundle 1-7) y toca staff(`lisa-doctores` developing) + embudo(`adrian-embudo` developed) — `/pm-vitalia` debe decidir/secuenciar (pausar lisa-doctores + plegar embudo + bucket locks) antes del BUILD. Opciones: (a) `/architect` ahora (diseña el ready package; la coordinación cross-módulo se ejecuta al build); (b) `/pm-vitalia` primero coordina las 2 stories abiertas y luego `/architect`. Recomiendo (b) si querés evitar reestructurar al final; (a) si querés avanzar el diseño ya.

### 2026-06-06T21:00 · 🤖 claude · `/po-ux` · ✓ APLICADO
Chris eligió **(b)**: `/pm-vitalia` coordina las 2 stories abiertas (pausar `lisa-doctores` developing + plegar `adrian-embudo` developed + bucket locks `code:{shell,clinics,crm}`) ANTES de `/architect`. Story `refined` lista — handoff a `/pm-vitalia` para la coordinación cross-módulo del bundle 1-7. /po-ux cierra su parte.

### 2026-06-06T21:10 · 🤖 claude · `/pm-vitalia` · ⚠️ DUDA
Coordinación cross-módulo (Step 0 + lectura de las 2 stories). **Dato nuevo que cambia el plan:** `lisa-doctores` (developing) y `adrian-embudo` (developed) NO son early-stage — están a **1-2 pasos de done**: lisa-doctores espera ratificación visual de Chris (V-VIS-1..4) + unos RED → /auditor → merge; embudo tiene dod_live_verified=true y su bloqueo (la story inbox) **ya está archivada/done** → /auditor → merge. ⇒ **Pausar/parkear esas 2 (la precondition original del spec) es ahora la PEOR opción** (tira trabajo casi terminado + reabre stories sanas). Replanteo la secuencia del bundle 1-7:
- **(b1) RECOMENDADO — secuencia-después:** cerramos lisa-doctores + embudo (→ done) PRIMERO; luego shell-valeria-responsive corre bundle 1-7 (/architect→build) sobre código MERGEADO + estable. Cero parking, módulos libres, el punto 7 refactoriza superficies estables. shell queda `refined` esperando (es ruta crítica del lift igual, pero el lift ya espera a esta story de todas formas).
- **(b2) split — puntos 1-6 ya:** shell-valeria-responsive corre puntos 1-6 ahora (/architect→build, solo `module:shell`, sin esperar a nadie); el punto 7 (refactor N3) queda como follow-up tras mergear las 2. Más rápido para Valeria/topbar; el N3 se cementa después.
- **(b3) pausar ambas + shell primero:** lo que decía la precondition original — ahora descartado (desperdicia su estado casi-done).
**Necesito de Chris:** ¿b1 (terminar las 2 → luego bundle 1-7) o b2 (puntos 1-6 ya, punto 7 después)? Recomiendo **b1** si querés el bundle íntegro sin rework raro; **b2** si querés ver Valeria/topbar arreglados ya. (b3 lo descarto salvo que insistas.)

### 2026-06-07T · 🤖 claude · `/pm-luana` · ✓ APLICADO
b1/b2 **RESUELTO → b1** (sin pregunta extra): al abrir la iniciativa design-system homologation (ADR-014 + proposal accepted) se destapó que el outcome `luana-core-ui-foundation` YA gateaba "no arrancar el lift/homologación encima de las 4 stories abiertas (shell-valeria-responsive, lisa-doctores, adrian-embudo, nicolify abel-icp)". Ese gate ES b1: cerrar las abiertas → liftear shell → homologar. Esta story (refined) sigue su curso normal de cierre (build de los puntos 1-7) como parte de las "4 abiertas"; su punto 7 (EntityWorkspaceLayout) es el primer consumidor del patrón de layout-primitives de la iniciativa. SSoT de la secuencia: `docs/architecture/luana-platform/design-system-homologation-HANDOFF.md`. La homologación NO se bundlea acá (queda enfocada en su responsive + N3).
