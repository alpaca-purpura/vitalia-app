---
story_id: vitalia-fase2-adrian-canal-inbound
created_at: 2026-06-04T00:00:00-05:00
last_modified: 2026-06-04T00:00:00-05:00
notes_count: 0
refs_count: 0
conversation_count: 1
---

# chris-input.md · vitalia-fase2-adrian-canal-inbound

> **Qué es este archivo:** acá Chris escribe notas + referencias + Claude responde con verdicts. Es la cocina de la story (la conversación) — separada del spec/design/arch (los outputs ratificados).
>
> **3 secciones secuenciales** (mantener el orden + emojis para que parser + cockpit funcionen):
> - 💭 Notas — Chris escribe en lenguaje natural antes/durante refinement
> - 📎 Referencias — links, imágenes, story-refs, learning-refs, doc-refs
> - 💬 Conversación — turn-by-turn cronológico Chris ↔ Claude con verdicts
>
> Doc canónico: `docs/process/chris-input-protocol.md`.

## 💭 Notas

### 2026-06-04
Chris: el agente LangGraph que responde mensajes de WhatsApp/Instagram/Telegram, el que nutre el inbox como funcionaba antes el sales_agent. Reutilizar al máximo lo que ya existe, mejorarlo, y seguir haciendo de este sistema algo genial. Debe estar conectada con las capabilities del inbox → probablemente modifica → su alcance incluye las pruebas de regresión del inbox.

## 📎 Referencias

- **📖 story-ref** · `vitalia-fase2-adrian-inbox` — superficie del inbox + modo por-conversación que este loop respeta y nutre
- **📄 doc** · `core/luana-core-sales-agent/` — runtime LangGraph (cerebro, read-only, consumir vía import)
- **📄 doc** · `vitalia/docs/product/capabilities/sales_agent/{inbox-handler-mode-occ,adrian-3-tools-mvp}.yaml` — caps slice-1 (deprecated) del loop shipped

## 💬 Conversación

> Append-only · turn-by-turn cronológico.
> Verdict labels: ✓ APLICADO · ⚠️ DUDA · ❌ REFUTADO · 💡 PROPONE.

### 2026-06-04 00:00 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
Story creada en state=idea con cap_target=`adrian.inbox` y cap_change_type=`extend`. Naturaleza: NO se construye el agente (ya existe en engine `core/luana-core-sales-agent` + extensión `vitalia/.../sales_agent/` — se reutiliza al máximo y se mejora). Scope = cablear el loop inbound→grafo→outbound que nutre el inbox: receivers webhook WhatsApp/IG + Telegram adapter nuevo + trigger del runtime respetando el modo por-conversación del inbox + feed del activity stream. Conectada a las caps del inbox → modificadora → **regression scope del inbox dentro del alcance** (regression_guard: tests inbox shipped pasan sin modificarse). Antecedente: loop shipped en slice-1, deprecado en reorg → re-home + mejora. Deps hard: adrian-inbox (debe aterrizar primero). Próximo: /po refina 01-spec agentic-story → /ux-agentico flujo turn-by-turn; ANTES verificar si slice-1 dejó código huérfano recuperable (extend re-home) vs borrado.

### 2026-06-04 22:30 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
Investigación pre-refinement completa (Chris pidió revisar core + slice-1 + legacy sales_agent + LLM gateway antes de refinar). Resultado consolidado en `00-research.md`. Hallazgo decisivo de "slice-1 código huérfano recuperable": **el cerebro YA está en el engine** (`core/luana-core-sales-agent` orchestrator/chat.py + smart_debounce + graph + telegram handler) y **la extensión de marca `vitalia/.../sales_agent/` está in-tree + registrada vía EP-3** (tools+personas+state_overlay+observability+lead_screening). PERO **el loop inbound NUNCA se cableó**: `webhook_routes.py` L251/473/525/621 son stubs literales ("stub in T-be-8 scope") y ningún código vitalia importa el orchestrator del core. → `extend` confirmado: no hay que rescatar código borrado, hay que **enchufar** lo que ya existe + agregar lo que falta (adapter Telegram nuevo + dispatch real + honor-modo del inbox). Vitalia tiene 2 superficies agénticas: `sales_agent/` (Adrián inbound = core de esta story) y `agentic/` (Lucas growth + guardrails + compose.py = REUSE) — `/architect` resuelve el solapamiento de tools. Legacy "state of the art" = sus gems ya están mayormente en core (smart debounce, PR-8 campaign recognition, tool-call dedup, stage-gated registry) — confirmar en /architect, no recrear. **Keys almacenados** (gitignored): LLM gateway centralizado (`deploy/litellm/.env` creado en este worktree — faltaba; footgun per-worktree) + Telegram bots + AI_PROVIDER/MODEL Chinese-first en `vitalia/.env.dev`. MEMORY pointer `llm-gateway-litellm`. **Mejorar el gateway "al máximo" = `/pm-luana`** (toca core/luana-core-llm + config.py + root compose + observability pricing) — esta story lo CONSUME, no lo posee; proposal SSoT `2026-06-04-llm-gateway-chinese-first.md`.

### 2026-06-04 22:30 · 🤖 claude · `/pm-vitalia` · ⚠️ DUDA
Antes de invocar `/po` (Chris gateó: "revisa si necesitas algo de mi antes de refinar"). 4 preguntas: (1) **Dep hard adrian-inbox aún `developing`** — refino el spec en paralelo (docs bucket) pero el BUILD no puede aterrizar hasta que inbox cierre. ¿OK refinar ya, o esperás? (2) **Mejora del gateway LLM** = `/pm-luana` (core), separada de esta story. ¿La agendamos como item /pm-luana aparte, o querés que la atienda DENTRO del scope de canal-inbound (rompería el boundary engine)? (3) **Solapamiento `sales_agent/tools` vs `agentic/tools`** (reschedule/payment/followup duplicados aparentes) — ¿lo resuelve `/architect`, o ya sabés cuál es el canónico? (4) **WhatsApp/IG inbound** — ¿los incluimos en esta story o la acotamos a **Telegram-first** (lo único probable hoy) y WhatsApp/IG quedan para cuando tengas la API? Acotar a Telegram-first reduce riesgo y te da el loop verificable ya.

### 2026-06-04 22:35 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
Chris ratificó las 3 decisiones blocking: (1) **Telegram-first** (WhatsApp/IG = follow-up sin API), (2) **mejora del gateway LLM = item `/pm-luana` aparte** (esta story lo consume tal cual), (3) **refinar ya en paralelo** (build espera dep hard adrian-inbox). Q3 (solapamiento tools sales_agent vs agentic) la resuelve `/architect`. Transición `idea → refining` aplicada en checkpoint (channel_scope=telegram-first, gateway_improvement=out-of-scope, scope receivers acotado a Telegram, decisiones registradas). WIP cap refining OK (1/3). Encadeno a `/po` para producir `01-spec.md` (agentic-story Telegram-first) → luego `/ux-agentico` diseña el flujo turn-by-turn inbound + state machine + honor-modo del inbox.

### 2026-06-04 22:55 · 🤖 claude · `/po` · ✓ APLICADO
`01-spec.md` v1 escrito (agentic-story, Telegram-first). Cargué `sales-agent-expert` (invariantes + §3 surfaces protegidas: el loop **consume** `handle_telegram_webhook`/debounce del engine, NO los reescribe) y la story `adrian-inbox` como **autoridad de modos** (el engine ya tiene Telegram: `api/dto/telegram.py` + `BaseChannel` + `message_handler` port). Hallazgo de diseño clave: el inbox firmó un modelo **2-modos** (`Adrián decide` / `Adrián consulta`) + **pausa** (manual eliminado) — el loop DEBE honrarlo, no inventar. Spec cubre: 12 RN + 11 AC + 6 scenarios (happy `decide` / negative `consulta`-no-envía / edge pausa+ráfaga-debounce / adversarial PHI-firewall+prompt-injection / security webhook+idempotencia / isolation cross-tenant) con graders agénticos (tool_calls, state_check trace/conversación/telegram_outbound, voice_fidelity, vertical-medical-fidelity, no-hallucination) + matriz de cobertura + regression_guard del inbox + verificación live por Telegram real (bot dev). NET-NEW acotado a: canal Telegram de marca + ruta webhook (reemplaza stubs T-be-8) + bridge honor-modo.

### 2026-06-04 22:55 · 🤖 claude · `/po` · ⚠️ DUDA
Ratificación del spec v1 — 3 decisiones de producto antes de cerrar (Q1-Q3 en `01-spec.md § Open questions`): **Q1** default de conversación nueva (`Adrián decide` autónomo vs `Adrián consulta` hasta calificar) · **Q2** alcance de tools en el loop (solo responder+calificar vs ya mandar payment_link/reagendar) · **Q3** en `consulta`, el grafo corre para armar borrador (consume tokens) vs no corre hasta pedido humano. Respondé y cierro spec → state queda `refining` hasta que `/ux-agentico` diseñe el flujo turn-by-turn.

### 2026-06-04 23:05 · 🤖 claude · `/po` · ✓ APLICADO
Chris ratificó: Q1 **Adrián decide** (default) · Q2 **tools de acción EN scope** (payment_link + reagendar dentro del loop, más de lo que propuse — `tool_groups`: screening/payment/booking, con guarda por modo) · Q3 **el grafo corre en consulta** (deja borrador armado, cero outbound auto). Spec actualizado a **v2** (`ratified_by_chris: true`): RN-3 + RN-3b (tools de acción + guarda por modo) + AC-12 + SC-7 (happy-action: decide manda payment_link/reagenda · consulta → borrador) + matriz actualizada + § Decisiones ratificadas reemplaza Open questions. Story queda en `state: refining`, `phase: SPEC_RATIFIED_AWAITING_DESIGN`. Próximo: `/ux-agentico` produce `02-design-agentic.md` (flujo turn-by-turn inbound + state machine honor-modo 2-modos/pausa + prompt slots + eval policy personas/rubrics/pass^k) → al ratificarlo, `refining → refined`.

### 2026-06-04 23:25 · 🤖 claude · `/po` · ✓ APLICADO
Chris pidió **rescatar del legacy** la "instrucción al agente por-conversación": en modo automático, el campo de texto deja dar indicaciones de cómo tratar a ESE lead → cuando el lead escribe, Adrián enruta según la indicación (el lead no la ve). Agregado al spec **v3**. **Hallazgo: ≈100% reuse** — el engine `supervisor_routing.j2:31` YA honra `[INSTRUCCION DEL OPERADOR]` con prioridad máxima; vitalia YA tiene `override_context_wire.py` (cap `sales_agent.adrian-override-context`, de embudo) que inyecta contexto en `agent_state_checkpoints.metadata_info` JSONB SIN cambio de schema; legacy `closer_studio/command_service.py` + `MessageInput.tsx` = la UX de referencia. Agregado: RN-13 (instrucción por-conversación) + RN-14 (semántica del composer) + RN-15 (audit/glass-box) + AC-13 + SC-8 + § Instrucción del operador (detalle mecánica 4 pasos) + bifurcaciones + matriz. **El legacy lo modelaba así:** en modo AI el composer = instrucción a Adrián; en modo humano (= pausado) = mensaje directo al lead. Eso reconcilia limpio con el inbox 2-modos PERO **cambia el comportamiento del composer del inbox en `decide`** (hoy: escribir = mensaje humano + pausa). Por eso → DUDA abajo.

### 2026-06-04 23:25 · 🤖 claude · `/po` · ⚠️ DUDA
Una decisión que toca tu inbox firmado: en `Adrián decide`, ¿cómo se comporta el composer? Opción A (fiel al legacy): escribir = **instrucción a Adrián**; para mensaje directo al lead → pausás. Opción B: **toggle explícito** en el composer ("Mensaje al lead | Instrucción a Adrián") y el operador elige cada vez. A es más limpio (un composer, depende del estado de Adrián) pero cambia el comportamiento actual del inbox en decide; B es más explícito pero agrega un control. Confirmá A o B → cierro el spec v3 y encadeno a `/ux-agentico`.

### 2026-06-04 23:35 · 🤖 claude · `/po` · ✓ APLICADO
Chris eligió **Opción A** (fiel al legacy): un solo composer, modo según estado de Adrián (decide=instrucción a Adrián, el lead no la ve · pausado=mensaje directo al lead). RN-14 fijada como definitiva (Opción B descartada). **Spec `01-spec.md` v3 COMPLETO y ratificado** (`ratified_by_chris: true`): 15 RN + 13 AC + 8 scenarios (happy/negative-consulta/edge-pausa+ráfaga/adversarial-PHI+injection/security-webhook/isolation/happy-action-tools/instrucción-operador) + matriz de cobertura + § Instrucción del operador + regression_guard del inbox + DoD live Telegram. Story `state: refining` · `phase: SPEC_RATIFIED_AWAITING_DESIGN`. **Próximo:** `/ux-agentico` → `02-design-agentic.md` (flujo turn-by-turn inbound + state machine honor-modo 2-modos/pausa + prompt slots [incl. slot instrucción operador + slot honor-modo] + eval policy personas/rubrics/pass^k). Al ratificar el diseño → `refining → refined` → `/architect`.

### 2026-06-05 00:05 · 🤖 claude · `/ux-agentico` · ✓ APLICADO
`02-design-agentic.md` v1 escrito. **Norte: CERO edición de engine** — el flujo reusa todo lo existente (orchestrator `handle_incoming_webhook`, debounce, grafo, honor-modo `handle_human_mode`, `[INSTRUCCION DEL OPERADOR]` del supervisor, slots `compose.py`, patrón `override_context_wire`). NET-NEW de marca = receiver webhook Telegram + adapter connections + **gate honor-modo al outbound** + endpoint set-instruction + wire de inyección. Diseñado: § boundary engine/brand · happy path 3 turnos (incl. instrucción del operador steereando) · state machine (honor-modo = gate al OUTBOUND; pausa = no corre grafo; decide/consulta corren el mismo grafo, difieren en envío) · tools (screening/payment/reschedule stage-gated) · **slot architecture: la instrucción del operador va en SLOT 7 VOLÁTIL (post cache-boundary), NUNCA en prefix cacheable** (sería silent invalidator) → surface como `[INSTRUCCION DEL OPERADOR]` (prioridad máxima, ya en engine) · voice (voz tenant + medical rails) · recovery matrix · eval (4 personas incl. lead-instrucción-VIP + 5 rubrics + goldens honor-mode/PHI/instrucción) · cost (Chinese-first gateway) · observabilidad. Sin deltas estructurales del spec.

### 2026-06-05 00:05 · 🤖 claude · `/ux-agentico` · ⚠️ DUDA
Una decisión de diseño (D1, § 11 del design): **¿la instrucción del operador es persistente o one-shot?** El legacy la inyectaba en "el próximo mensaje" (one-shot). Propongo **mejorarla a persistente por-conversación** (steerea todos los turnos siguientes hasta que la recepción la cambie/limpie) + chip "Instrucción activa: …" editable. Más útil para tratar al lead de forma sostenida ("es VIP", "ofrécele 10%"). Confirmá persistente o one-shot → ratifico el diseño y transiciono `refining → refined` + encadeno `/architect`.

### 2026-06-05 01:00 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
Chris preguntó cómo Adrián entiende la necesidad del paciente + presenta al especialista disponible (match first+callbacks) si no hay servicios creados ni doctores cableados al agente. **Investigación** → `00-research-data-foundation.md`. Hallazgos: (1) la plomería ya existe (`TenantKnowledgeBuilder` lee Offer Studio + team); (2) servicios = **cero creados** → viven en Offer Studio, los construye `lisa-servicios` (idea); (3) doctores ricos para presentar (bio_public 3-secc + adjuntos + disponibilidad) pero **sin link a servicios** (lo trae lisa-servicios) y **★ NO cableados al cerebro de Adrián** (hoy ve el `team` de Brand Studio, no el roster clínico `clinics.Doctor`). **Decisiones Chris:** (a) **secuenciar servicios PRIMERO** (canal-inbound nace con el match completo); (b) **servicios = Offer Studio (escalera de valor)**; (c) match servicio→especialista pasa a **in-scope** de canal-inbound. **Aplicado:** dep hard NEW `lisa-servicios` (+ soft `lisa-doctores`); spec v4 § Match (RN-16/17/18 + AC-14 + tool brand-level `match_service_and_specialist`); design + tool; checkpoint § decisión. **Gap pendiente:** hogar del wiring doctores→agente (lisa-servicios / slice propio / canal-inbound) → `/architect`/Chris.

### 2026-06-05 01:00 · 🤖 claude · `/pm-vitalia` · 💡 PROPONE
Próximo paso lógico tras "secuenciar servicios primero": **refinar `vitalia-fase2-lisa-servicios`** (state=idea) — el desbloqueador. Es UI-story (sub-tab Servicios de Lisa: Catálogo Offer Studio + Escalera de valor + link servicio↔doctor) → `/po-ux`. canal-inbound queda `refined` esperando. **Pero ojo:** lisa-servicios como está scopeada es solo la UI de gestión de Lisa; el **cableado servicios+doctores → Adrián** (el tool `match_service_and_specialist` + que el agente lea el roster clínico) es agentic y necesita hogar — propongo decidirlo al refinar lisa-servicios (sumarlo a su scope) o como slice agentic propio. ¿Arranco refinando lisa-servicios, o querés primero discutir dónde vive el cableado?

### 2026-06-05 01:10 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
Chris eligió **pausar acá**. Todo registrado. Estado: `canal-inbound` = `refined`, BLOQUEADO en `lisa-servicios` (dep hard NEW) para el match servicio→especialista. Próximo (cuando Chris retome): refinar `lisa-servicios` (idea, /po-ux) + decidir hogar del cableado doctores+servicios→Adrián. Artefactos sin commitear (docs tracked del story-folder + keys en gitignored env). No se commitea sin pedido (otras sesiones en el árbol → commit por pathspec).

### 2026-06-05 00:15 · 🤖 claude · `/ux-agentico` · ✓ APLICADO
Chris eligió **persistente** (D1). Diseño fijado: la instrucción del operador steerea todos los turnos hasta editar/limpiar (chip "Instrucción activa: …"); RN-13 del spec + § 11 del design actualizados. **`02-design-agentic.md` v1 ratificado.** Story transicionada **`refining → refined`** (`ratified_by_chris: true`, `phase: AGENTIC_DESIGN_RATIFIED`). Refinamiento COMPLETO: `01-spec.md` v3 + `02-design-agentic.md` v1. **Próximo:** `/architect vitalia` → ready package (03-arch + 04-validators + 05-guidelines + 06-tickets). Notas para architect (en next_action del checkpoint): (1) **target CERO edición de engine** — gap real → escalar `/pm-luana`; (2) FE delta = composer instrucción-mode sobre componente shipped del inbox → decidir si requiere mockup-per-component (ADR-vitalia-003); (3) el **BUILD espera la dep hard `adrian-inbox`** (developing) — el ready package se puede preparar en paralelo.

### 2026-06-21 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
Chris retoma canal-inbound (deps reportadas cumplidas: staff + servicios construidos) y pide validar si Adrián ya puede "agendar reuniones" desde el loop. **Verdict (grep verbatim):** el **motor** de agendar EXISTE y es LIVE (`BookingService.create_booking` @ `application/services/booking_service.py:176` + advisory locks · cap `booking/prepaid-booking-advisory-locks` live · endpoint `api/routes.py:376`) — **pero NO está cableado a Adrián**. El grafo `sales_agent` solo expone screening/payment_link/reschedule; ninguna crea turno (`reschedule` mueve existente, `send_payment_link` REQUIERE appointment_id existente). El `tool_groups: booking` de RN-3b está hueco del lado de Adrián. Existe `propose_and_book` (`agentic/tools/appointment_reschedule_with_doctor.py`) que SÍ agenda vía el mismo BookingService, pero cableada al agente `agentic`/copilot (Story 11), no al de Adrián. **Decisión Chris: el book va DENTRO de canal-inbound.** Fix cero-dup: tool fina `book_appointment` en `sales_agent/tools/` que delega vía DI a `BookingService.create_booking` + bindeo al grafo. Flujo prepago: match→list_slots→book(hold)→payment_link(seña)→confirma. **Aplicado:** checkpoint `refined→refining` (`ratified_by_chris: false`), scope-add documentado, next_action→`/po`. Encadeno `/po` para plegar el tool al spec.

### 2026-06-21 · 🤖 claude · `/pm-vitalia` · 💡 PROPONE
La fuente de **slots disponibles por doctor** que el book necesita toca el modelo de disponibilidad de `lisa-doctores`, que cambió desde 2026-05-27 (recurrencia/Google, vista-mes) — flaggeado en la story `vitalia-scheduling-mateo-review` (idea) que spawneé hoy. Al refinar el book, `/po`/`/ux-agentico` deben fijar de dónde lee `list_slots` los huecos reales (no inventar disponibilidad). Si la agenda de Mateo ya expone los slots consultables, el book los consume; si no, es un gap a hogarar.

### 2026-06-21 · 🤖 claude · `/po` · ✓ APLICADO
Chris ratificó en conversación: book **dentro de canal-inbound** · plomería de scheduling **todo junto** (no se parte) · hold con vencimiento, **TTL configurable desde Adrián** (param config per-tenant, default sugerido 30min, no hardcoded). **Hallazgo honesto que le di:** las piezas existen pero el agendado NO está armado — 3 glues brand-local (cero engine): (a) slots-libres = disponibilidad rrule (`AvailabilityProjectionService`, lisa-doctores) − ocupación (BookingRepository) → hoy `get_available_slots` api/routes.py:337 es STUB que devuelve []; (b) vencimiento del hold sin-pago = NO existe sweep/TTL; (c) book en el grafo de Adrián = la tool rica `appointment_reschedule_with_doctor` (list_slots+propose_and_book) tiene cuerpo pero su EP-3 es `_not_implemented_yet`. El hold atómico (`BookingService.create_booking`) SÍ está vivo. **Spec `01-spec.md` → v5** (`ratified_by_chris: false` pendiente re-ratify): + RN-19..25 (agendar=hold sin-pago · vencimiento config Adrián · guarda-modo · carrera→re-propone · slots reales · idempotencia · glass-box) + AC-15..19 + SC-9..12 (happy-book · hold-vence · consulta-propuesta · carrera) + bifurcaciones + matriz. Flujo: match→list_slots(reales)→book(hold sin-pago)→payment_link(seña)→confirma al pagar. Checkpoint `phase: SPEC_V5_AWAITING_RERATIFY`.

### 2026-06-21 · 🤖 claude · `/po` · 💡 PROPONE
Próximo: re-ratificás el spec v5 (mirá sobre todo RN-20 TTL-config-Adrián + RN-23 slots-reales) y encadeno `/ux-agentico` para foldar el paso agendar al flujo turn-by-turn + state machine honor-modo + eval goldens (slots reales / hold vence / carrera). Recordá: el BUILD sigue esperando deps duras `lisa-servicios` + `adrian-inbox` — confirmá su `state: done` antes del handoff a `/architect` (no asumir aunque estén "construidos").

### 2026-06-21 · 🤖 claude · `/po` · ✓ APLICADO
Chris pidió revisar el contrato del agendado para NO duplicar (booking depende de disponibilidad, disponibilidad depende de doctor, doctor depende de servicio). **Mapeé la cadena real — toda viva:** Servicio (`offer.lisa-servicios` live) → Servicio↔Doctor (`offer_service_specialist_links` migr 045 live) → Doctor↔Disponibilidad (`clinics` rrule → `availability_slots` 90d con flag `has_confirmed_appointment`, `lisa-doctores` live) → Crear turno (`scheduling/create_appointment_service` **`origin="proactivo_adrian"` ya existe** en el enum). **Landmine de duplicación cazado:** mi v5 nombró `BookingService`/`vitalia_bookings` como el motor a reusar — **MAL: está `status: deprecated`**, lo reemplazó la agenda de Mateo (`valeria-agenda`). Escribir ahí = isla (Mateo no lo ve) + slot no se marca. **Corregido el spec a v5.1:** el turno de Adrián aterriza en el lane VIVO `scheduling` origin=proactivo_adrian; slots-libres = `availability_slots` con `has_confirmed_appointment=False` filtrados por doctores del servicio (la resta disp−ocupación YA está modelada, no es net-new); `BookingService` queda como referencia-de-patrón (advisory-lock+hold), no store. RN-19/20/22/23/24/25 reescritas + **RN-26 nueva (cero-isla: aparece en agenda Mateo + marca el slot)** + graders de SC-9..12 apuntan a `scheduling_appointment`+`availability_slot` (no vitalia_bookings) + matriz. **2 gaps reales flaggeados al architect:** (a) `create_appointment_service` HOY no marca `has_confirmed_appointment` → crear turno no bloquea el slot = **posible bug de doble-booking en la agenda de Mateo a mano** → cerrar en este scope; (b) la agenda viva no tiene hold-TTL (portar el patrón del deprecado). Net-new real = chico, todo en `scheduling`, cero engine.

### 2026-06-21 · 🤖 claude · `/po` · ⚠️ DUDA
El gap (a) — que crear un turno en la agenda NO marca el slot ocupado — puede ser **bug existente de Mateo** (no solo de Adrián). Lo dejé in-scope de canal-inbound (RN-26) + flaggeado para coordinar con `vitalia-scheduling-mateo-review` (la story que abrí hoy, misma agenda). ¿Lo cerramos acá (canal-inbound lo necesita sí o sí) o preferís que el fix del marcado de slot viva en mateo-review y canal-inbound lo consuma? Mi recomendación: cerrarlo acá (es precondición del agendado de Adrián) y que mateo-review lo verifique en su re-validación. Decidí y sigo: ¿ratificás v5.1 + disparo `/ux-agentico`, o ajustamos algo más del contrato?

### 2026-06-21 · 🤖 claude · `/po` · ✓ APLICADO
Chris **ratificó el spec v5.1** (book_appointment, lane scheduling vivo, anti-dup). Spec `ratified_by_chris: true`; story sigue `refining` hasta que /ux-agentico ratifique el diseño. Pidió: ver cómo será la experiencia de agendamiento para el usuario + recomendación de si lanzar /ux-agentico, SIN perder lo avanzado (cero duplicación). **Recomendación dada:** sí, /ux-agentico es el skill correcto porque en un agendamiento agéntico la experiencia ES la conversación (no hay UI nueva — el paciente agenda dentro del chat Telegram; el turno aparece en la agenda de Mateo que YA existe). **Guardrails anti-dup para el handoff a /ux-agentico:** (1) EXTENDER `02-design-agentic.md` v1 (NO reescribir) — el flujo inbound, state machine honor-modo, persona y slots ya están diseñados; solo se suman los turnos de booking; (2) cero UI nueva — superficies = el chat (inbox/composer de adrian-inbox) + la agenda viva de Mateo; (3) reusar engine orchestrator + persona Adrián + bio_public del doctor (scope-add 2026-06-11, link perfil público ya in-scope) + slots de la disponibilidad viva (RN-23); (4) decisión de canal: slots como texto vs inline-keyboard de Telegram (native, sin UI nueva). Encadeno /ux-agentico.

### 2026-06-21 · 🤖 claude · `/ux-agentico` · ✓ APLICADO
Diseñé la experiencia de agendamiento, **extendiendo** `02-design-agentic.md` v1 → v2 (sin reescribir — anti-dup). Chris eligió **slots como TEXTO** (no botones inline): cero engine, el lead responde natural y el agente mapea al slot por razonamiento (no parser/if). Agregado: **§2.1 sub-flujo de agendamiento** (turn A match+presenta doctor+lista slots reales · turn B lead elige→book proactivo_adrian+seña · turn C pago confirma→agenda Mateo · branch C' hold vence→re-ofrece · branch ninguno-sirve · branch carrera→re-propone · guarda por modo) + 2 tools NEW al §4 (`list_available_slots` lee clinics availability viva · `book_appointment` → create_appointment_service origin=proactivo_adrian, NUNCA el BookingService deprecado) + recovery (carrera/no-sirve/hold-vence) + eval goldens (book-happy selección-por-razonamiento · consulta · race · hold-expira) + persona lead-quiere-agendar + **D2** (slots texto, rationale cero-engine, botones=mejora futura) + **D3** (lane scheduling vivo, anti-dup). Bar no-`if`s explícito: la selección de slot es razonamiento del agente, no regex.

### 2026-06-21 · 🤖 claude · `/ux-agentico` · 💡 PROPONE
Diseño v2 listo. Si lo ratificás, transiciono la story `refining → refined` y encadeno `/architect` (prepara el ready package: 03-arch + 04-validators + 05-guidelines + 06-tickets). Recordatorio honesto: el **BUILD** sigue esperando deps duras `lisa-servicios` + `adrian-inbox` `state: done` — el architect puede preparar el paquete en paralelo, pero antes de buildear confirmo esos states (no asumo aunque estén "construidos").

### 2026-06-21 · 🤖 claude · `/ux-agentico` · ✓ APLICADO
Chris pidió cruzar el modelo dirigido-a-objetivo contra TODO el sales_agent del engine + nuestra extensión ("seguro hay mucho que reutilizar"), ser muy detallista, usar subagentes, y armar un buen spec para el architect sin rehacer lo bueno. **Hice auditoría profunda con 4 subagentes Explore paralelos** (engine graph/supervisor/specialists · routes/objections/state/profile · workers/knowledge/registry · brand extension). **Hallazgo grande: reuso ≈90%.** El engine YA es un agente de ventas dirigido-a-objetivo completo (supervisor que razona→qualifier/product_expert/closer + 13 rutas semánticas incl. objeciones/dolor/deseo/señales + objection_history + Aikido + signal_accumulator + lead_score/stages). **Mi "modelo nuevo" = ponerle nombre a eso.** Y **el engine tiene un subsistema completo de agendamiento agéntico (S8)**: tools get_available_slots/create_booking_link/verify_booking_status sobre patrón SchedulerProvider(Strategy) + estado scheduled_meetings + workers appointment_reminder_engine (T-24h/T-1h/post, **no-show GRATIS**) + verify_pending_bookings (reconcilia hold→confirmado→no-show) + follow_up_engine/frozen_detection (momentum/se-enfría). Y los **guardrails éticos médicos YA existen** (slot_4_medical_safety_rails no-diagnóstico + personas forbidden_phrases/emergency_protocol + screening_outcome OK/DERIVAR_DOCTOR/DERIVAR_EMERGENCIA). **Reescribí `02-design-agentic.md` → v3** (16 secciones, detallista): modelo dirigido-a-objetivo MONTADO sobre el engine + mapa de reuso con path:line + escenarios reales del lead + sub-flujo agendado sobre S8/VitaliaSchedulerProvider + delta brand explícito §15. **Spec → v6:** RN-27 (screening ético precede al book, emergency corta) + RN-28 (dirigido-a-objetivo, no flujo fijo, no dark patterns) + nota de reuso S8 + matriz. **El NET-NEW se achicó a:** VitaliaSchedulerProvider (Strategy, envuelve lane scheduling vivo) + match_service_and_specialist (doctor) + share_doctor_profile (URL existente) + extender state_overlay + tuning persona. CERO engine.

### 2026-06-21 · 🤖 claude · `/ux-agentico` · 💡 PROPONE
Diseño v3 + spec v6 listos para tu firma. Si ratificás: transiciono refining→refined + encadeno `/architect` (con todo el mapa de reuso en el next_action para que aterrice el delta brand SIN rehacer lo bueno). Dos cosas que el architect deberá decidir y conviene que sepas: (a) `tuning.py` (umbrales de stage/fatiga/scoring) es del ENGINE — si querés tunearlos para contexto médico (ej. cerrar a score más bajo por urgencia, fatiga a 2 preguntas no 3), eso es per-tenant config o un lift /pm-luana, no edición directa; (b) si `MeetingEntry` necesita `doctor_id`/`service_id` y eso toca el engine, va por /pm-luana. ¿Ratificás v3/v6 y disparo /architect?

### 2026-06-21 · 🤖 claude · `/ux-agentico` · ✓ APLICADO
Chris **ratificó diseño v3 + spec v6**. Story transicionada **`refining → refined`** (`ratified_by_chris: true`, `phase: AGENTIC_DESIGN_RATIFIED`, `last_artifact: 02-design-agentic.md`). Refinamiento COMPLETO: 01-spec v6 + 02-design-agentic v3. Chris pidió el prompt para disparar `/architect` hasta `done` en una conversación nueva → entregado (lleva embebido el mapa de reuso + las 10 constraints duras anti-dup/cero-engine/ético/deps para que la sesión nueva no pierda lo avanzado). Próximo (sesión nueva): `/architect vitalia vitalia-fase2-adrian-canal-inbound` → ready package → /dev-team → G (Chris-verify live Telegram) → /auditor → /pm-vitalia merge → done. NO autonomous (agentic+PHI = stake-asimétrico, gate G obligatorio). BUILD espera deps duras lisa-servicios + adrian-inbox `done`.

### 2026-06-21 · 🤖 claude · `builder-backend` · ✓ APLICADO
T-BE-1 BUILT: Telegram inbound channel. POST /api/v1/connections/telegram/webhook (TelegramWebhookAck). Secret validation (RN-9/V-NF-1) + update_id dedup (migration 047: vitalia_telegram_update_dedup) + tenant resolution + engine dispatch (ChatOrchestrator.handle_telegram_webhook). TelegramAdapter brand-local shim. 16/16 tests GREEN (V-NF-1+V-FN-5+V-FN-6). 361/361 arch tests GREEN. Commits: e27f6cbd 22192d83. Live-verify pending (needs VITALIA_TELEGRAM_WEBHOOK_SECRET + VITALIA_TELEGRAM_DEFAULT_TENANT_ID env + tunnel + migration applied). FORBIDDEN: cero engine edits, cero BookingService, cero cross-brand.

### 2026-06-21 · 🤖 claude · `builder-frontend` T-FE-1 · ✓ APLICADO
T-FE-1 BUILT: Composer modo-instrucción (effectiveMode extension). `getEffectiveMode("ai")` → `"instruction"` (label "🤖 Instrucción a Adrián", hint "el paciente no la verá", CTA "Dar instrucción", POST /api/v1/adrian/conversations/{id}/instruction via `useOperatorInstruction` hook). `getEffectiveMode("human")` → `"direct"` (composer sin cambios, RN-14). `InstructionChip` (RN-13 persistente): muestra instrucción activa con Editar/Limpiar. Tenant via `useTenantId()` — NUNCA `useAuth().orgId`. Commit: `6375cb0d`. Files: 14 (8 new, 6 modified). Gates: tsc=0 errors · eslint=0 errors · vitest=2589/2589 PASS. Playwright V-VIS-1 smoke escrito (exec pendiente dev-stack). `regression_guard.excepcion_coordinada` aplicado (5 tests ComposerArea actualizados por cambio intencional RN-14). Cap header `// cap: adrian.inbox` en todos los archivos nuevos. Live-verify (DoD #37) diferido a fase G per `autonomous_mode: false`.

### 2026-06-21 · 🤖 claude · `builder-backend` T-BE-2 · ✓ APLICADO
T-BE-2 BUILT: scheduling plomería (slot-marking + hold-TTL + sweep). Cierra RN-26 gap. `create_appointment_service` ahora acepta `hold_service` inyectable + `slot_id`/`tenant_config` opcionales — backwards-compatible (Mateo walk_in no pasa hold_service). `SchedulingHoldPort` (ABC): `mark_slot_confirmed` / `set_hold` / `list_expired_holds`. `SchedulingHoldService` (book_with_hold: mark slot True + set hold_pending_payment + TTL tenant_config.adrian_hold_ttl_minutes default 30). `HoldExpirySweepService` (idempotente: lista expirados → cancela appointment → libera slot False → emite activity). `SchedulingHoldRepository` (raw SQL IF EXISTS dual-filter). Migration 047 (hold_status VARCHAR(24) + hold_expires_at TIMESTAMPTZ + hold_created_by_agent BOOLEAN + partial index WHERE hold_status='hold_pending_payment'). ARQ worker scaffold `hold_expiry_sweep_job.py`. 17 tests nuevos GREEN + 195 scheduling tests GREEN + 361 arch tests GREEN. Ruff format PASS. FORBIDDEN cumplidos: cero engine, cero BookingService, dual-filter tenant+clinic. Live-verify pending (necesita Postgres + migration applied + turno real creado).

### 2026-06-21 · 🤖 claude · `/architect` · ⚠️ DUDA
Ready package cerrado (5 artifacts: 03-arch consolidado + 3 per-surface, 04-validators, 05-guidelines, 06-tickets, dispatch-plan). Deps verificadas `state: done` (lisa-servicios + adrian-inbox, archivadas). `autonomous_mode: false`. **PERO el prior-art re-scan (verificando cada path:line del diseño v3 §2 contra el código real) chocó 3 muros de engine que el diseño asumió resueltos y NO lo están** — porque el loop inbound NUNCA se cableó, así que esos seams nunca se ejercieron:
- **ESC-1** · `scheduler_provider_for_tenant` (`providers.py:447-448`) hace `_ = tenant_id # reserved for future per-tenant routing` y SIEMPRE devuelve `SCHEDULER_PROVIDERS["internal"]` → registrar `VitaliaSchedulerProvider` no rutea nunca.
- **ESC-2** · el grafo despacha por `TOOL_REGISTRY.get(tool_name)` (`nodes.py:402`), un dict ESTÁTICO del engine SIN merge de las tools EP-3 de marca (y los handlers EP-3 hoy son `_not_implemented_yet`) → `book_appointment`/`match_service_and_specialist`/`share_doctor_profile` no pueden despachar por el grafo.
- **ESC-3** · `STAGE_TOOL_SCOPE` (`registry.py:56`) es hardcoded engine → los nombres de tools nuevos nunca entran a un stage.
Los 3 obligan a editar `core/luana-core-sales-agent/src/` → **constraint 1 = STOP + /pm-luana (promotion gate)**. NO generé tickets de builder para book/match/share; los escalé a **T-LIFT-1**. Partí el DAG en **carril brand-local buildable-ya** (Telegram loop + plomería scheduling + honor-modo + state_overlay + instrucción-operador + composer FE — cero engine, valor demoable live) y **carril lift-gated** (book/match/share = el valor estrella). Verifiqué los 3 muros yo mismo (no confié en el subagente): reales. Decisión tuya cómo seguir (ver pregunta).

### 2026-06-22 · 🤖 claude · `builder-agentic` T-AG-1 · ✓ APLICADO
T-AG-1 BUILT (AGENTIC · flagship R23 · CERO engine): (1) EXTEND `state_overlay` con 4 booking keys (`recommended_doctor_id`, `recommended_service_offer_id`, `candidate_slots` ★volátil-only nunca cacheable, `doctor_profile_shared_at`, total=False). (2) **Operator-instruction agentic WIRING** — cerré un integration gap real (familia imagined-contract): T-BE-3 persistió la instrucción en `metadata_info[operator_instructions]` pero el engine inyecta `[INSTRUCCION DEL OPERADOR]` desde `checkpoint.resume_objective` (one-shot) y NO lee `metadata_info` → la instrucción nunca llegaba al turno. Lo cerré con `OperatorInstructionBridge` (+ adapter raw-SQL) que espeja `metadata_info[operator_instructions]`→`resume_objective` por turno (persistente RN-13, cero engine edit, slot 7 volátil post-CACHE_BOUNDARY). Wired al `OperatorInstructionService` (dep opcional, back-compat) + router. (3) Persona tuning médico-descubrimiento en `adrian_persona_base.md` (slot 5 base, guardrails HONRADOS no tocados). (4) **5 goldens BUILDABLE** (must_pass:true, 5/5 dims c/u vs grader real): honor-mode · screening-gate (DERIVAR_EMERGENCIA→no book+deriva+escala) · objection-trust · ethical (no dark patterns) · operator-instr (SC-8). Runner count gates 17→22 / 16→21. **Gates:** sales_agent module 70 + agentic_evals 58 = 128 passed · regression inbox+crm 429 · arch 361 · ruff clean · cap cross_check_3 130/130 0-drift. **Live-verify (DoD #37, Postgres real, código real no mocks):** bridge.apply_for_turn→True, resume_objective espejado, persistente 2do turno, cross-tenant decoy NULL (isolation), 5 goldens 5/5 GREEN. ⚠️ engine-turn end-to-end no exercisable aún (tabla `agent_state_checkpoints` no migrada en vitalia_dev — pipeline inbound aún no deployado; verifiqué contra tabla schema-idéntica en Postgres real). **CERO engine edit** (`git status core/` vacío). **Upstream:** capturé HB-92 (architect afirmó "el engine ya honra X" sin grepear el read-side → imagined contract). Commit por pathspec, push wip/vitalia.

### 2026-06-21 · 🤖 claude · `/dev-team` · ✓ APLICADO
**OLA 1 construida (carril cero-engine) → pausa en G.** Buildé los 5 tickets buildables + los pusheé: T-BE-1 (canal Telegram: receiver+route+secret+update_id dedup, `e27f6cbd`) · T-BE-2 (scheduling: marca availability_slot + hold-TTL + sweep, cierra el gap RN-26, `ad039b0e`) · T-BE-3 (honor-modo bridge decide/consulta/pausa + endpoint set-instruction, `cdfbaaa6`) · T-AG-1 flagship (EXTEND state_overlay + wiring instrucción-operador + tuning persona + 5 goldens, `52dab5b6`) · T-FE-1 (composer modo-instrucción, `6375cb0d`). **Incidentes que resolví:** (1) los 2 builders BE paralelos minteron la MISMA revisión alembic `047_vitalia` (two-heads) → re-encadené telegram dedup a `048`. (2) T-BE-3 murió por error de conexión DESPUÉS de quedar verde (24/24) → finalicé yo (ruff fix import-sort + creé la cap `sales_agent.honor-mode-bridge` que el builder referenció sin crear + commit). (3) **HB-92 (capturado por T-AG-1):** el architect asumió "el engine ya honra la instrucción" sin grepear el lado de lectura — el engine lee `checkpoint.resume_objective`, NO el `metadata_info` que persistió T-BE-3 → T-AG-1 cerró el gap con `OperatorInstructionBridge`. Misma clase de error que los muros ESC. **Verde combinado:** BE arch 361 + connections + scheduling + sales_agent + inbox/crm regression 429 · FE 2589 · tsc clean · CERO engine edit. **Live-verify:** los 5 goldens agentic corrieron contra Postgres real (honor-mode·screening-gate·objection-trust·ethical·operator-instruction). **Falta tu verify en G:** el round-trip Telegram real (SC-1) + el sweep scheduling live — necesitan el bot dev + tunnel + migración 047/048 aplicada (runbook en `demo-script.md`). Story → `developed` + `phase: AWAIT_CHRIS_VERIFY`. NO encadené al auditor (autonomous_mode:false). Ejercé live → firmá `chris_verify.signoff`. En paralelo (OLA 2): `/pm-luana T-LIFT-1` (ESC-1/2/3) en worktree core efímero desbloquea book/match/share.

### 2026-06-22 · 🤖 claude · `/dev-team` (G live-verify) · ⚠️ DUDA
**Armé el live-verify Telegram y tu mensaje LLEGÓ — pero el grafo no puede correr en vitalia (3 muros de engine MÁS).** Lo que funciona ya (real): canal Telegram (POST desde Telegram 91.108.5.15) · resolución de tenant (e69a691d) · dedup por update_id · webhook registrado vía túnel cloudflared. **Arreglé en el camino:** (1) Redis estaba caído (`luana_redis_dev` exited 2 semanas) → el buffer de debounce hacía `NoneType.rpush` → lo levanté + reinicié el backend (ahora `telegram_webhook_dispatched` OK); (2) la tabla `channel_connections` NO existía en vitalia_dev (módulo connections nunca migrado) → la creé + seedié la conexión del bot dev; (3) registré el webhook. **PERO al despachar al grafo, 3 muros NUEVOS de engine (`core/luana-core-*`), misma clase que ESC-1/2/3 — el grafo sales_agent NUNCA se ejerció en un proceso de marca, así que bugs latentes salen ahora:**
- **ESC-4** · `core/luana-core-platform/.../infrastructure/models/crm.py:209` — `LeadModel.messages = relationship("MessageModel")` string pelado → ambiguo entre el MessageModel del engine y el de `vitalia/.../crm` (mismo Base). SQLAlchemy: *"use a fully module-qualified path"*. Rompe el mapper init → "Could not fetch tenant" → tenant=None cascada.
- **ESC-5** · `core/luana-core-sales-agent/.../infrastructure/prompts/base.py:32` — `templates_dir` hardcodeado `"src/modules/sales_agent/..."` (layout monolítico pre-multibrand; el real es `modules/vitalia/sales_agent/`) → `TemplateNotFound: message_completeness.j2`.
- **ESC-6** · `core/luana-core-sales-agent/.../infrastructure/models/prompt_version_model.py` — `PromptVersion` sin `tenant_id` → falla la carga de prompts desde DB.
**Consecuencia:** Adrián NO responde live hasta el lift. **El T-LIFT-1 de /pm-luana crece: ESC-1..6** (era scheduler+tools+stage; ahora suma grafo-multibrand: relationship qualified + prompt path + PromptVersion.tenant_id). El código de marca de OLA-1 es correcto, pero su valor (un Adrián que contesta) está gated por hacer el engine sales_agent multibrand-capable. `dod_live_verified` sigue `partial` (honesto: canal+dispatch+buffer live; grafo blocked). Recomendación: el lift (ahora 6 ESC) es el camino crítico — sin él el grafo no corre en ninguna marca. ¿Lo abrimos en /pm-luana (worktree core efímero) o querés que primero confirme si ESC-4/5/6 tienen mitigación brand-side antes de escalar?

### 2026-06-22 · 🤖 claude · `/pm-luana` · 💡 PROPONE
Abrí la promotion proposal del lift consolidado: `docs/promotion-protocol/proposals/2026-06-22-sales-agent-multibrand-graph-runtime.md` (state: `under_review`). Documenta ESC-1..6 con path:line + el contrato que expone para cablear marcas sin más engine edits + verificación por-ESC + el bar runtime real (grafo corre end-to-end en vitalia) + downstream regression ×4 marcas. Framing: NO es brand-pattern-lift, es **engine-hardening** — el grafo sales_agent nunca se ejerció en un proceso de marca; vitalia es la 1ra y falla en 6 puntos del engine. Semver minor (additivo + bugfix + columna `prompt_versions.tenant_id`), migración requerida (ESC-6). Toca 2 packages: `core/luana-core-sales-agent` + `core/luana-core-platform` (ESC-4). Recomendación /pm-luana: **APPROVED** (camino crítico — sin esto el grafo no corre en NINGUNA marca; OLA-1 brand ya construida esperándolo). NO lo marqué `accepted` (necesita tu ratificación). Si ratificás: worktree core efímero `wip/core-sales-agent-multibrand` → `/architect` engine (ready package del lift) → `/dev-team` build → downstream regression → migrate → vitalia ejerce el grafo live (cierra el G) + desbloquea OLA-2.

### 2026-06-22 · 🤖 claude · `/pm-luana` · ✓ APLICADO
Chris **ratificó el lift** (proposal `accepted`, `ratified_by: Chris`). Apliqué **phasing** (pedido de testeo de Chris): **Phase 1 = ESC-4/5/6 (runtime)** → grafo corre + Adrián responde = TESTEABLE (mensaje Telegram → reply); **Phase 2 = ESC-1/2/3 (features)** → book/match/share (OLA-2). Chris testea tras Phase 1, NO hace falta esperar los 6. Creé el worktree core efímero `~/Proyectos/luana-core-sales-agent-multibrand` (branch `wip/core-sales-agent-multibrand`, venv symlinked, desde origin/main b4da136). Respuesta a "¿ya puedo probar?": **no todavía** — hoy el mensaje llega pero el grafo crashea (sin reply). Tras Phase 1 mergeada a main + sync a vitalia + restart stack → el webhook/Redis/conn que armé persisten → escribís al bot → Adrián responde. Próximo: `/architect` (engine) en el worktree core para el ready package del lift (Phase 1 primero). Recomiendo sesión fresca dedicada (core lift = esfuerzo enfocado 3-5 días, parallel-safety).

### 2026-06-22 · 🤖 claude · `/pm-vitalia` · ✓ APLICADO
**Phase 1 del lift ADOPTADA en vitalia + governance cerrada. Falta solo TU round-trip Telegram + firma.** Hecho este turno: (1) **sync** `wip/vitalia ← main` (merge real limpio, trajo el engine hardened ff0b9345; cero conflicto en `core/luana-core-sales-agent` — mi WIP DS toca `core/@luana/ui-kit`, disjoint). (2) **migración 049** `049_vitalia_prompt_versions_tenant_id.py` autoreada (DDL verbatim de `migration_notes.md`: CREATE TABLE IF NOT EXISTS `prompt_versions` + `tenant_id` UUID + índices, backfill no-op) + aplicada (`alembic current` → `049_vitalia`) + verificada (columna `tenant_id` live en vitalia_dev). (3) **muros Phase 1 verificados caídos:** el engine que CORRE en el container tiene `PromptVersion.tenant_id` (footgun venv: el `.pth` resolvía a main, pero el container monta mi worktree sincronizado); 6/6 ESC arch tests GREEN vs engine sincronizado (PYTHONPATH override). (4) **proposal** `2026-06-22-sales-agent-multibrand-graph-runtime` → `state: migrated` **scopeado a Phase 1** (`migrated_scope: phase-1`, `migrated_pr: ff0b9345`, adopción vitalia anotada) + **carve-out explícito Phase 2** (ESC-1/2/3 `status: pending`, re-promoción brand-first — sin el carve-out un scan creería los 6 ESCs done). (5) checkpoint canal-inbound: `engine_lift_phase1: adopted`, el G destrabado. **⚠️ Lo que NO cerré (es tuyo, stake-asimétrico):** el round-trip Telegram real (D1-D5 demo-script) + `chris_verify.signoff` — agentic funcional, no firmo por vos. **Gap de setup para tu demo:** el router lee `VITALIA_TELEGRAM_DEFAULT_TENANT_ID` + `VITALIA_TELEGRAM_WEBHOOK_SECRET`, pero `.env.dev` provee `TELEGRAM_DEV_BOT_TOKEN`/`TELEGRAM_SANARE_TENANT_ID` (naming mismatch) → seteá esos 2 + tunnel + setWebhook antes del round-trip (el webhook/Redis/conn que armó /dev-team el 2026-06-22 persisten). **2 cambios sin commitear** (migración 049 + edits docs) — no commiteo sin que lo pidas. Próximo: tu demo → firmás → `/pm-vitalia reconcile` (R) → `/auditor` → merge slice 1. PASO 6 (Phase 2 ESC-1/2/3) cuando quieras, brand-first desde acá.

### 2026-06-22 · 🤖 claude · `/pm-vitalia` (debug live) · ❌ REFUTADO (mi propia marca anterior)
**Escribiste al bot, NO respondió → el grafo NO corre, y mi "G destrabado" del entry anterior era PREMATURO.** Caí en el trap arch-verde≠runtime: los 6/6 ESC arch tests pasan, pero el bar real del propio proposal (§5: "el grafo corre end-to-end, NO arch verdes") NO se cumple. Diagnóstico de logs: **ESC-7 — muro NUEVO de engine, misma clase onion que ESC-4/5/6.** `core/luana-core-platform/.../crm.py:214` → `LeadModel.appointments = relationship("AppointmentModel", foreign_keys="AppointmentModel.lead_id")` apunta a una clase `AppointmentModel` que **NINGUNA marca registra** (vitalia tiene `AppointmentClinicMapModel`/`AppointmentPaymentModel`/`VitaliaAvailabilitySlotModel`, ninguna con `lead_id`). `configure_mappers()` falla → *"name 'AppointmentModel' is not defined"* → crashea TODO el ORM init → cascada "Could not fetch tenant" → grafo muerto. La relación NO se usa en el grafo (grep vacío); el comentario del engine lo confirma: *"AppointmentModel remains stub-targeted (Story 8 lift pending)"* — lift que nunca aterrizó. Antes ESC-4 crasheaba primero y lo enmascaraba; el lift Phase 1 lo destapó. **Brand NO puede fixear** (es core → /pm-luana). Fix mínimo: borrar la relación `appointments` colgada del `LeadModel` del engine (unused + dangling), después ejercer el grafo hasta que Adrián responda — **puede haber más capas onion** (ej. `SaleModel.offer→ProductModel` por orden de import; solo se ven cuando ESC-7 caiga). **Correcciones aplicadas este turno:** proposal `migrated → accepted` (lift incompleto, bar runtime no cumplido) + bloque `esc_7` documentado + `phase_1.runtime_bar: BLOCKED`; checkpoint `engine_lift_phase1: code-merged-runtime-BLOCKED`, G SIGUE bloqueado. **Ruteo:** ESC-7 (+ posibles capas) → `/pm-luana` en el worktree core que ya existe (`~/Proyectos/luana-core-sales-agent-multibrand`); la definición de DONE del lift NO es arch-verde, es el grafo ejercido hasta que Adrián conteste live. Recomendación: NO marco más nada "listo" hasta ver el reply real. ¿Abro /pm-luana para el fix de ESC-7 o lo tomás vos en el worktree core?

### 2026-06-22 · 🤖 claude · `/pm-vitalia` (self-paced lift loop) · ✓ APLICADO + ⚠️ NUEVO BLOQUEADOR
**El grafo YA corre y Adrián responde live — pero descubrí que NINGÚN tool de marca se ejecuta (ESC-17). El round-trip de TEXTO está destrabado; el de TOOLS no.** Este turno (continuando el lift, todo en el hub `wip/vitalia`, `SCOPE_GATE_SKIP` in-hub ratificado):

**Confirmado live (no arch-verde — ejercí el grafo):** Tier 1 (ESC-7/8/13/15/16, `44e1d4af`) ya estaba mergeado y **funciona**: disparé un webhook Telegram sintético → `semantic_check_result is_complete=True` → `llm_call` → `processing_response_chunks chunks_count=4` (Adrián contestó sobre blanqueamiento dental: *"El blanqueamiento de…"*), único traceback = el 400 ESPERADO de `sendMessage` a un chat sintético inexistente. **El grafo corre end-to-end.** Así que para el carril OLA-1 (texto/honor-mode/screening/objection-trust/operator-instruction) ya podés ejercer el round-trip real (demo-script.md) y firmar `chris_verify.signoff`.

**Tier 2 seam COMPLETADO (3 commits, TDD + review adversarial + live-verify cada uno):**
- **2.1** `e43015ee` — el prompt ahora le **advierte al LLM los tools de marca** (`# Herramientas de marca`, slot cacheable, stage-independent → cache-safe; advertised==dispatchable). 8 tests.
- **2.2** `3aff15af` (ESC-1) — `scheduler_provider_for_tenant` **routea por `tenants.config_json['scheduler_provider']`** (vitalia sigue `internal`, sin cambio de comportamiento; resolver resiliente). 6 tests.
- **2.3** `833fece3` — resueltos los huérfanos: `state_overlay.py` CONSERVADO con docstring corregida (las keys viven en el state dict / metadata_info, NO hay `engine.register_state_extension` — contradecía el contrato §4 ratificado); borrados `sales_agent/prompts/` + `personas/` (huérfanos reales, voz viva = BrandVoicePort DB).

**⚠️ ESC-17 — el bloqueador real del END-STATE (capturado `fe481dac`):** haciendo el pre-flight de OLA-2 (verificar-antes-de-construir) encontré que el **ABI de los handlers EP-3 está roto**. El engine despacha `tool_fn(state, db=...)` (sync, lee args del state) y todos los tools del engine calzan; pero los **9 handlers "reales" de vitalia son StructuredTools de LangChain** (async, args Pydantic). Empíricamente `screening_questions(state, db=None)` → `TypeError: 'StructuredTool' object is not callable` → el `except` del dispatcher devuelve `{"status":"error"}` → **el tool nunca se ejecuta**. Ironía: los 4 placeholders `_not_implemented_yet` (funciones planas) SÍ son llamables y degradan limpio — los placeholders andan, los reales no. El *"13 tools dispatchable"* de `846388a6` verificó presencia-en-registry, no callability-bajo-ABI: **registrado ≠ ejecutable** (arch-verde≠runtime, misma familia que el embudo y los ESC). Por eso Adrián, hoy, NO puede recomendar/agendar/compartir con un tool real.

**No lo arreglé en este turno a propósito** (regla del loop: si excede lo seguro, PARÁ y proponé sub-fases — esto es agentic+HIPAA con un footgun de event-loop en el puente async→sync). Lo dejé **sub-faseado + capturado**: design-doc § ESC-17 + revised Tier 2.4 (**2.4a** = arreglar el ABI: la marca registra adapters sync `(state,db)->dict` que extraen args del state + puentean al service async; test de EJECUCIÓN + arch test "todo handler EP-3 es callable sync"; **owner builder-agentic flagship**) → **2.4b** = `VitaliaSchedulerProvider` + match/share/book + goldens. Learning cross-brand `docs/learnings/2026-06-22-ep3-tool-handler-abi-mismatch.md`. Proposal: ESC-17 anotado, `phase_2` NO mergeable-para-ejecución hasta el fix (sigue `accepted`, no `migrated`).

**Cómo ejercés la demo HOY:** el stack está listo (postgres + `luana_redis_dev` + `luana_litellm_dev` + cloudflared + fastembed 0.5.1 + modelo en cache + tenant `e69a691d` `can_use_platform_keys=true`). Mandá un mensaje al `nicolify_dev_bot` → Adrián **responde** (carril texto, firmable). **Aún no** verás un tool de marca ejecutarse — eso es 2.4a. Pendiente además (governance): migraciones 049-pattern para las tablas que el grafo consulta + seed gobernado `can_use_platform_keys` + `uv lock` del pin fastembed; y al final promote core SHAs a main + sync-all + downstream ×4 → proposal migrated.

### 2026-06-22 · 🤖 claude · `builder-agentic` (autonomous-dispatch follow-up) · ✓ APLICADO
**El end-state diferido de esta historia — Adrián despacha share/match SOLO en conversación — quedó RESUELTO + promovido a main.** El F-path había concluido "el LLM no emite el tool-call (problema agéntico/persona)"; midiéndolo con instrumentación directa resultó ser **3 bugs de cableado** del native-calling, no comportamiento:
1. `MultiRoleLLMRouter` (el servicio que corre en el grafo vivo) **no overrideaba `generate_with_tools`** → heredaba el fallback texto de `BaseLLMService` → `bind_tools` nunca corría live (el ~30% medido antes era 100% fallback de texto).
2. Los tools EP-3 namespaced `{brand}.{tool}` tienen **punto** → DeepSeek/OpenAI los rechazan con 400 (`^[a-zA-Z0-9_-]+$`) → fallback silencioso a texto. Fix: sanitizar `.`→`-` en el schema + revertir antes del dispatch.
3. La métrica de dispatch sub-contaba (el tool sólo logueaba en el path data-success; un `not_found` es dispatch igual). Fix: log de seam durable `sales_agent.tool_dispatched`.
**Medido live (eval repetible nueva `core/luana-core-sales-agent/scripts/live_dispatch_eval.sh`):** 0 → **~1.0 dispatch por conversación** en 3 fraseos (20/20, 12/12, 12/12; share-focused 8/12), modelo REASONING por defecto, **sin** tocar persona/routing/modelo. Promovido a main: `d8c737c4` (fe02df19 mecanismo native) + `7a326124` (a24604c0 sanitize+router+seam-log) + sync-all (comunify+nicolify). Tests regresión añadidos. Proposal `2026-06-22-sales-agent-multibrand-graph-runtime` actualizada (deferred autonomous-dispatch → RESOLVED). Learning `docs/learnings/2026-06-22-tools-advertised-executable-not-dispatched.md` § fifth pass. **Lección durable:** "isolated bind_tools 3/3" ≠ "corre live" — el servicio vivo puede ser un wrapper/router sin el método native, y un nombre de tool que un unit test nunca manda 400ea en prod; instrumentá la llamada real antes de concluir "es la persona/el grafo".

### 2026-06-22 · 🤖 claude · `/pm-vitalia` (auditoría profunda doc-vs-reality) · ✓ APLICADO + 💡 PROPONE
**Auditoría a fondo del avance paralelo (3 sub-auditorías read-only + git). Veredicto: lo construido es real y sólido — pero el checkpoint subreportaba 2 gaps de cableado live + tenía 1 nota stale. Reconciliado.** SSoT del estado real escrito en `RECONCILE-2026-06-22-doc-vs-reality.md`.

**Confirmado real (no humo):** ESC-17 (adapter sync `(state,db)->dict` + arch-test) · run_async main-loop bridge (`tool_bridge.py`, wired en `main.py:75`) · ESC-18 (relación muerta removida) · share/match LIVE (DB real, URL real) · lift proposal `migrated` · 11 SHAs citados existen.

**Drift corregido en docs:**
1. **autonomous-dispatch** — el checkpoint lo listaba DEFERIDO (F-path "LLM no despacha"); en realidad está **RESUELTO + promovido a main** (`d8c737c4`+`7a326124`, 0→~1.0; HEAD `a1a95e1e`). Era 3 bugs de cableado, no agéntico. Checkpoint corregido.
2. **GAP-1 (high, buildable ya):** 6 de 9 tools dispatchan pero **erroran** — sus DI resolvers nunca se cablearon en `register_all`. Solo share/match/book (native) ejecutan lógica real. "dispatch ~1.0" ≠ "tool funciona". V-FN-8 deferred-reason actualizado (ESC-17 ya no es el blocker; el blocker es el resolver).
3. **GAP-2 (high):** el loop inbound **NO honra `consulta`** (borrador/0-outbound). HonorModeBridge solo gate del endpoint operator-instruction, sin pre-send interceptor en el loop. decide+pausa sí. V-FN-2 (must_pass) flaggeado RED.
4. **GAP-3 (high):** el loop **NO emite activity event** al inbox ("nutre el inbox glass-box" del Goal = no cumplido en runtime; escribe solo audit_log). V-FN-1 (must_pass) flaggeado RED en el sub-clause `+activity`.
5. **GAP-4 (blocked):** book ESC-19 confirmado — dos tablas appointment (engine `appointments` write vs brand `vitalia_appointments` read=grilla Mateo) + `AgendaGridRepositoryImpl` sin `create()`. Decisión dominio scheduling, bien escalada.

**Por qué importa:** V-FN-1/V-FN-2 son must_pass y el live-wiring no está → NO pasan /auditor hasta GAP-2/3. La firma OLA-1 cubrió el round-trip de TEXTO + goldens (nivel eval), NO el cableado live de consulta-mode/activity-feed. Sin este audit, esos dos gaps llegaban al auditor como verde-fantasma.

**PROPONGO decisión Chris (continuar la historia):**
- **(A) Construir OLA-2:** (1) cablear los 6 DI resolvers — *buildable ya, cero engine, alto valor* → /dev-team; (2) `/architect` para GAP-2 (consulta pre-send) + GAP-3 (activity emit) — el send/flow vive en engine, decide si EP brand o seam engine; luego /dev-team. book (GAP-4) queda detrás de tu decisión de scheduling.
- **(B) Mergear slice OLA-1** (texto, ya firmado) + parkear OLA-2 (resolvers + consulta + activity + book) como story/sub-fase aparte.

Mi recomendación: **(A) arrancando por los 6 resolvers** (barato, destraba V-FN-8 + hace reales los tools que ya dispatchan), y en paralelo abrir /architect para GAP-2/3. **4 docs cambiados sin commitear** (reconcile nuevo + checkpoint + 04-validators + este chris-input) — no commiteo sin que lo pidas. ¿Vas (A) o (B)?

### 2026-06-22 · 🤖 claude · `/dev-team` (OLA-2 build resume — dir A) · ✓ APLICADO
**Chris eligió (A) construir OLA-2. Arrancado.** Step 0: módulo `sales_agent`, sin colisión WIP (mateo-nueva-cita = otro módulo). Story `developed/AWAIT_CHRIS_VERIFY` → `developing/BUILD_OLA2_GAP1` (OLA-1 firmada persiste; la story mergea post-OLA-2 por slice_decision). GAP-1 confirmado mecánicamente: los 5 `set_*_service_resolver` existen, **cero call sites** → los tools wrapped erroran al dispatch.
- **Lanzado builder-agentic (flagship) para GAP-1:** cablear los resolvers en `extensions.py::register_all` + test de EJECUCIÓN (dispatch real vía ABI `node_tool_executor`, no registry-presence) RED→GREEN + arch test (todo resolver cableado) + live-verify real. Boundary: cero engine.
- **Lanzada investigación de routing GAP-2/3:** consulta-gate + activity-emit tocan el flow del engine (`chat.py`/`conversation_pipeline`) → decidir si hay EP brand viable o requieren seam engine (→ /pm-luana). NO se buildea hasta tener la ruta.
- **GAP-4/book (ESC-19):** parkeado, tu decisión de dominio scheduling.
Reporto cuando ambos terminen. autonomous_mode:false → pauso para vos tras GAP-1 + la decisión de ruta GAP-2/3.

### 2026-06-22 · 🤖 claude · `/dev-team` (OLA-2 — GAP-1 built + GAP-2/3 routed) · ✓ APLICADO + 💡 PROPONE
**GAP-1 construido y verificado; GAP-2/3 resultaron engine-seam (→ /pm-luana, no brand). Pauso para tu decisión.**

**GAP-1 ✅ BUILT (commit `0fd45f31`, builder-agentic flagship):** 5 DI resolvers cableados vía nuevo `sales_agent/composition.py::wire_sales_agent_tool_resolvers()` llamado en `extensions.py::register_all` (cada resolver abre AsyncSession main-loop-bound + UoW commit/rollback/close; tenant/clinic authoritative del state). Cero engine. TDD: execution test RED(5 fail)→GREEN(6) + arch guard. 454 sales_agent+arch PASS, inbox regression 103 PASS, ruff clean. Live-verify vs dev Postgres: `tool_resolvers_wired` log + screening/retract reachan el service (retract corrió query real → not_retractable, NO resolver-error). Ahora payment/screening/reengagement/retract ejecutan lógica real al dispatch (antes error-dict). Round-trip Telegram full = tu G. **GAP-5 NUEVO (medium):** reschedule reacha el service pero el lane scheduling `update_slot` es stub → degrada elegante; mismo dominio que book/ESC-19 (scheduling, no sales_agent).

**GAP-2/3 → engine-seam (investigación de routing):** NINGUNO es brand-buildable. EP-13 pre_send = signature-only/unwired; `ChatOrchestrator` singleton hardcoded sin mode-resolver injection; engine `handler_mode` solo conoce `human`; `AuditEmitter` sin brand-listener. Ambos = seams ADITIVOS en `core/luana-core-sales-agent` (mismos 2 files: `chat.py` + `conversation_pipeline.py`/`audit_emitter.py`):
- GAP-2: generalizar `handle_human_mode`→`resolve_mode` + `draft_only` en `deliver_response`; brand inyecta el `HonorModeBridge` ya construido.
- GAP-3: emitir `AgentTurnCompletedEvent` (solo IDs+stage, sin PHI) por el outbox EventBus existente; el subscriber que escribe `vitalia_activity_events` = brand puro. Land emit+listener juntos (anti-orphan).
Backward-compatible → blast radius downstream mínimo (×4 marcas).

**PROPONGO:** (1) abrir **1 sola promotion proposal /pm-luana** para los 2 seams (GAP-2+GAP-3) → core worktree efímero → /architect engine → build → migrate → brand wirea HonorModeBridge + activity-subscriber. (2) GAP-4/book + GAP-5/reschedule = decisión de dominio scheduling (coordinar `vitalia-scheduling-mateo-review`). Story queda `developing/OLA2_GAP1_DONE_AWAIT_CHRIS`; NO auto-handoff auditor (autonomous_mode:false + slice_decision = auditor único post-OLA-2). **6 docs/code sin commitear de mi lado** (GAP-1 ya está en `0fd45f31`; lo no commiteado = checkpoint/04-validators/reconcile/chris-input del audit). ¿Abro la proposal /pm-luana para GAP-2/3? ¿GAP-4/5 los tratamos ahora o después?

### 2026-06-23 · 🤖 claude · `/pm-luana` (lift GAP-2/3 in-hub) · ✓ APLICADO
**Vos: "hacer con /pm-luana todo aquí con skip 1". Abierto el proposal + lanzado el build engine.** Proposal nuevo `docs/promotion-protocol/proposals/2026-06-23-sales-agent-inbound-mode-activity-seams.md` (state: **accepted**, ratificado vos, in-hub wip/vitalia + SCOPE_GATE_SKIP=1, semver minor — 2 seams ADITIVOS backward-compatible). Contract-complete para que el builder no improvise sobre engine.
- **Seam 1 (GAP-2):** mode-resolver inyectable + `draft_only` en `deliver_response` (consulta → 0 outbound + borrador, post-ainvoke = cache-safe). Brand inyecta el HonorModeBridge ya construido.
- **Seam 2 (GAP-3):** `AgentTurnCompletedEvent` (IDs+stage, CERO PHI) por el outbox EventBus existente; subscriber brand escribe `vitalia_activity_events`. Land emit+listener juntos (anti-orphan).
- **Lanzado builder-agentic (flagship, background):** ambos seams en `core/luana-core-sales-agent` + wiring vitalia, TDD (default-off = comportamiento idéntico, regression ×4), live-verify real. Default sin opt-in = byte-idéntico (corre en 4 marcas).
`migrated` SOLO cuando: suite engine + ×4 green + vitalia ejerce ambos seams live (consulta→0 outbound+borrador+activity; decide→reply+activity) + cero PHI en el payload. Reporto al terminar el build. Round-trip Telegram full = tu G.

### 2026-06-23 · 🤖 claude · `/pm-luana` (lift build — infra-blocked) · ⚠️ DUDA
**El build del lift GAP-2/3 está bloqueado por Anthropic API 529 (overload server-side), 2 intentos a 0 tokens. NO es un problema del código ni del plan.** Estado preservado intacto: proposal `2026-06-23-sales-agent-inbound-mode-activity-seams` accepted + contract-complete; GAP-1 ya commiteado (0fd45f31); tree engine/sales_agent limpio (cero partial). El builder-agentic (flagship) no llegó a arrancar (529 antes del 1er tool-use). Per la regla de >2 retries del /dev-team → pauso en vez de quemar intentos ciegos. Para reanudar: decime "reintentá el lift" (re-spawneo el mismo builder) cuando la infra se normalice (status.claude.com). El prompt del builder + el proposal son el SSoT — el reintento es 1 spawn, cero re-trabajo.

### 2026-06-23 · 🤖 claude · `/pm-luana` (lift GAP-2/3 BUILT — pausa para tu G) · ✓ APLICADO + ⚠️ DUDA
**Los 2 seams construidos, testeados y live-verified (orchestrator-level). Falta SOLO tu G (round-trip Telegram real) + el auditor.** Commit `5bd0dd1b` (14 files): engine `inbound_mode_seam.py` + `AgentTurnCompletedEvent` (events.py) + `conversation_pipeline.py` rama CONSULTA + `audit_emitter.py` emit + vitalia `inbound_seam_adapter.py` + composition wiring. TDD 13 engine + 13 brand RED→GREEN; downstream ×4 GREEN (comunify 144 · nicolify 20 · engine obs 36 + platform events 19); **seam default-off = byte-idéntico** (las 4 marcas sin opt-in no cambian). Live-verify vs `vitalia_dev` real: DECIDE→activity row · CONSULTA→draft row (0 body, 0 outbound) · sin PHI. Proposal actualizado (build landed; migrated pendiente tu G + auditor).

**⚠️ Race shared-index (importante, no es pérdida de datos):** el builder hizo un commit NO-pathspec en el hub compartido y barrió los archivos staged de la sesión paralela mateo → commit `9feb302c` quedó con el CONTENIDO de mateo (migración 052 notes_internal + T-BE-4) bajo el SUBJECT de mi lift. Se autocorrigió commiteando sus 14 files como `5bd0dd1b`. Ambos están PUSHED a origin → NO se reescribe (git-safety: force-push/amend prohibidos). **Acción para vos/sesión mateo:** el T-BE-4 notes-fix de mateo YA está en `9feb302c` (subject equivocado) — repointar el SHA en el checkpoint de mateo-nueva-cita. **Harness gap:** el sweep-guard M15 no frenó el commit non-pathspec → recomiendo `/harness-issue` (builder-agentic debe commitear SIEMPRE por pathspec en hub; el guard no cubrió este caso). Lo dejo capturado acá, no freno el feature para arreglar harness (HLP).

**Próximo (tu cancha):** (1) G — round-trip Telegram live (consulta→borrador+0-outbound · decide→reply+activity · tools share/match/payment ya dispatchan). (2) Tu OK para commitear los docs OLA-2 sin commitear (5: reconcile + proposal nuevos · checkpoint/04-validators/chris-input M). (3) Luego R (reconcile /pm-vitalia) → /auditor (single pass post-OLA-2) → merge. NO auto-avanzo (autonomous_mode:false, agentic+PHI stake-asimétrico).
