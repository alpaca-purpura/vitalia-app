# Vitalia — architecture (ADRs locales)

Decisions arquitectónicas brand-specific. Patrón ADR (Architecture Decision Records).

Archivo por decisión: `{seq}-{slug}.md` con frontmatter:

```yaml
id: ADR-vitalia-001
date: YYYY-MM-DD
status: proposed | accepted | superseded
context: ...
decision: ...
consequences: ...
```

ADRs platform (cross-brand) viven en `docs/architecture/` core.
