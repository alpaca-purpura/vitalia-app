# CONTEXT-BRIEF for estabilizar-harness-e2e-lisa-marca (bugfix, T-1..T-4)
> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder
> Modules: brand_studio
> Faithfulness flag: partial  (sealed — MEDIUM items M1/M2/M3 in §11; 0 HIGH; not blocking)
> Audit log: context-builder-logs/iter-1-2026-06-03T00:11:00Z.log
> Validator pass: CONTEXT-BRIEF-validation.md  (adversarial probe ran in-process — no Agent tool; 0 HIGH, 1 new MEDIUM)

## 1. PR summary

**Story:** `estabilizar-harness-e2e-lisa-marca` · type `bugfix` (lite, ADR-011) · F2 · module `brand_studio` (sub `lisa/marca`) · cap_target `lisa-marca` · cap_change_type `fix` · agent_owner `lisa` · `adr_004_compliance: bugfix-lite-na` (NOT a sub-tab — 9-section ADR-vitalia-004 gate does NOT apply). `verification_nature: ambas`. **state: `developing` · phase `BUILD_T3`** (checkpoint:5-6) — build started on the BE ticket. ratified_by_chris: true.

**What it does (one line):** make the lisa-marca E2E suite *honest* (real backend, not mocking the backend-under-test) and *deterministic* (web-first asserts, no dangling listeners), + close 2 acotated auth-header sub-bugs. NO new UI.

**Origin:** follow-up of `arreglar-guardado-voz-y-tono` — that bug shipped because the suite mocked the backend ("verificación teatro"). Learning: `vitalia/docs/learnings/2026-05-31-e2e-mockeado-verde-falso.md`.

**Re-repro 2026-06-02 (against HEAD, post-`14af22b2`) — root-cause REVISED:**
- The old Clerk auth-readiness race is **RESOLVED/stale** → directions #1 (Clerk-ready gate) + #2 (resilient query) are OBSOLETE, do NOT pursue.
- **B1** mock of backend-under-test (false-green): `vitalia-fase2-lisa-marca/fixtures/lisa-marca.fixture.ts:137,252` `route.fulfill()` over `/api/v1/lisa/marca/{identity,visuals,personality}`; 10 specs inherit.
- **B2** anti-burbuja not adopted: 11 specs import `@playwright/test` direct, not `e2e/fixtures/base.ts`.
- **B3** non-web-first assert (the REAL determinism fix): `poms/voz-tono-section.pom.ts:180 getSelectedArchetype()` once-reads `data-selected` → deterministic null.
- **B4** dangling listener: `voz-arquetipo-autosave.spec.ts:251` `page.on("response", () => void ...)` w/o `page.off` → "Target page has been closed".
- **B5** sub-bug #2 (HIPAA-lite actor): `marca-voice-api.ts:113` sends `X-User-ID: opts.tenantId` (org UUID as audit actor, not Clerk userId).
- **B6** sub-bug #1 (prohibited-phrases 422): GET requires `X-User-ID` header that `fetchClient` never injects → 422 in browser; e2e masks it by injecting the header manually.

**4-ticket DAG:** `T-3 (BE: #1 header optional + #2b actor resolver)` → `{ T-2 (FE-prod: actor real), T-1 (FE-tests: de-mock + web-first) }` → `T-4 (docs cap re-cable)`. T-3 first because the de-mocked suite (T-1) runs against the real BE; SC-6/SC-7 need the header optional (#1) + the real actor (#2b).

## 2. Contract decisions

From `03-arch.md` (consolidated) — verbatim-faithful summary. The arch doc is split into PARTE A/B/C/D by surface.

**Existing-systems audit (03-arch § 57-94) — already done by architect with self-ran greps (paths verified live in §7 below):**

| System | Path | State | Architect decision |
|---|---|---|---|
| Forwarding to real BE (inline) | `arreglar-guardado-voz-y-tono/voz-{arquetipo,bloque}.spec.ts` | active, **inline duplicated** | **LIFT** → `e2e/fixtures/real-backend-forward.fixture.ts` (shared) |
| Anti-burbuja gate | `e2e/fixtures/base.ts` | active | **ADOPT** via `mergeTests` composition (do NOT recreate) |
| Mock of backend-under-test | `vitalia-fase2-lisa-marca/fixtures/lisa-marca.fixture.ts::setupLisaMarcaMocks` | active — **is the bug** | **REPLACE** with forwarding (de-mock) |
| Audit actor resolver (clerk→UUID) | `iam/.../clinic_resolver.py::_resolve_user_uuid` | active | **CONSUME** (do NOT recreate) |

**PARTE C — BE sub-bug #1 (T-3, marca_router.py):** `get_prohibited_phrases` change `user_id: str = Header(alias="X-User-ID")` → `user_id: str | None = Header(alias="X-User-ID", default=None)`. `X-Tenant-ID` stays **required** (tenant-isolation). `response_model=ProhibitedPhrasesListDTO` **kept** (arch gate `test_response_model_required.py` + PII allowlist). `user_id` is NOT parsed/used (read by tenant+country). It's a GET → no audit_log write → no actor needed.

**PARTE C/D — sub-bug #2 architecture decision (D2-be-resolves-actor):** the audit actor must be a **UUID** (`audit_writer.py:105` does `CAST(:user_id AS uuid)`; PATCH parses `UUID(user_id)` at `marca_router.py:374,241,292`). Clerk `useAuth().userId` = `user_2abc...` is NOT a UUID → plumbing it raw → 422/CAST fails. The HIPAA-lite-correct actor = IAM `users.id` UUID resolved from `users.clerk_id == clerk_sub`.
- **T-2 (FE-prod, primary):** `marca-voice-api.ts::updatePersonality` STOP sending `X-User-ID: opts.tenantId`; send `X-User-ID: opts.userId` (real Clerk userId).
- **T-2b (BE companion, in T-3):** PATCH `/personality` (+ marca PATCHes that audit) **resolve actor from `clerk_sub` of the JWT** via `_resolve_user_uuid` (CONSUME) → `users.id` UUID. `X-User-ID` stops being the actor source. RBAC `require_brand_owner_access` keeps reading `X-User-Role` (orthogonal).
- **§ 16 open question for PM:** Chris ratified "scope completo · sub-bug #2 in-story" but tagged it FE-prod; the hard constraint forces an FE+BE pair (small BE touch inside `brand_studio`, no core, no other brand). Architect surfaced this as a minor scope divergence — see §11.

**No migrations. No default-flag flips** (03-arch § 9 + § 9.5).

**CONN (integration design):** no new user-reachable surface; "consumer" is the verification suite. Cap `lisa-marca` re-cabled `e2e_test` → honest specs. No island.

## 3. UI spec decisions

**N/A — no UI design doc exists** (no `02-design-ui.md`/`02-design-agentic.md` in folder). This is a harness bugfix + 2 auth-header fixes; `01-spec.md § Secciones UI estándar` is explicitly **N/A (justified):** `not_applicable_reason: "bugfix-lite de harness E2E + contrato de auth; cero superficie UI nueva"`. No wireframes, no visual states, no microcopy, no brand-voice changes.

**Acceptance criteria (01-spec § AC) that drive the build:**
- AC-1: suite ×3 against real BE → 0 flaky, 0 failed (`--repeat-each=3 --workers=1`).
- AC-2: static honesty — grep finds no mock-of-backend-under-test, no direct `@playwright/test` import, no `getSelectedArchetype`-once / `page.on` dangling.
- AC-3: sub-bug #2 — a marca mutation records the real Clerk userId as actor in `vitalia_audit_log`.
- AC-4: cap re-cabled — every `status:live` scenario → honest spec + `verified_real`.
- AC-5 (in-scope, ratified Chris): sub-bug #1 — GET `/lisa/marca/prohibited-phrases` returns 200 from real browser.

**8 Gherkin scenarios** SC-1..SC-8 (mix técnica/funcional). Full validator matrix in §9.

## 4. Module current-state extracts

**`vitalia/docs/product/modules/brand_studio.md`** (last_updated 2026-05-27) — Capabilities:
- `brand-studio-medical-sections` (live) — 4 sections enabled via config.
- `lisa-marca` (**live**) — Sub-tab Marca de Lisa shell-organism, 3 sub-sub-tabs N3-static (Identidad/Voz/Presencia), 4 salud archetypes (Caregiver/Sage/Healer/Hero), prohibited phrases soft warning, trust hybrid catalog per país.

Consumes `core/luana-core-brand-studio` (engine) with 4 sections enabled (identity, contact, team, testimonials). T-4 modifies the `lisa-marca` cap YAML only (re-cable `e2e_test` + `verified_real`); no narrative change to `brand_studio.md`.

**Cross-module consumer note (sub-bug #2b):** the `brand_studio/marca_router` (T-3) will CONSUME the `iam` module's `ClinicResolver._resolve_user_uuid` — this is a *same-brand* cross-module read. Per `backend-ddd.md` cross-module is "default forbidden" except via port/event; the architect's decision is to call the iam resolver. **Nuance the builder must resolve:** `_resolve_user_uuid` is a PRIVATE method (see §7) — calling it cross-module may trip a DDD boundary arch test. The public entry is `ClinicResolver.async_resolve(token, session, tenant_id_str, clinic_id_str)`.

## 5. Relevant rules

Loaded for `<phase>=builder` + modules brand_studio (+ vitalia overlay). Extracts of the No-skip/Prohibido parts:

| Rule | Hard rule relevant to this story |
|---|---|
| `tenant-isolation.md` | Every query filters `tenant_id`. **X-Tenant-ID stays required** in prohibited-phrases GET (T-3). FE tenant from `useTenantId()`, NEVER `useAuth().orgId`. |
| `hipaa-lite.md` (vitalia) | Audit log row for PHI ops; **actor = real user (the whole point of sub-bug #2)**. NOTE: these flows are brand-config (no-PHI) per arch doc; sub-bug #2 improves HIPAA-lite "quién" fidelity. `audit_log` actor MUST be a UUID. |
| `anti-duplication.md` | Match in core → EXTEND via import. **CONSUME `_resolve_user_uuid` (iam), do NOT recreate.** Forwarding LIFT to shared fixture (no inline copy ×11). |
| `tdd-mandatory.md` | RED first. BE: GET sans X-User-ID → 200 RED (against 422). Bug fix → regression test reproduces bug first. |
| `backend-ddd.md` | Domain→infra→app→api. `response_model=` kept. Cross-module default forbidden (the iam consume is the nuance — see §4/§7). `FastAPI(redirect_slashes=False)`. |
| `e2e-testing.md` | SSoT = `playwright-expert`. NATIVE only, NEVER `make e2e*` (OOM). `E2E_BASE_URL=http://localhost:3002`. |
| `definition-of-done-live-verify.md` (#37) | No `done` without Claude exercising live (Chrome MCP) + `dod_evidence`. Anti-burbuja gate `base.ts` mandatory. GET 200 ≠ verified. demo-script.md required (`demo_required: true`). |
| `test-design-doctrine.md` | Verificación REAL ≠ HTTP 200 — exercise the write + read logs + confirm effect. |
| `anti-default-flip-audit.md` | N/A — no flag flip (03-arch § 9.5 confirms). |
| `spanish-text.md` | UI/agentic only; **harness/tests NOT scanned** (acotado 2026-06-01). The de-mocked specs already assert neutro copy. |
| `frontend-fsd.md` / `frontend-quality.md` | api-layer stays in `features/lisa/api/`; no cross-feature import; `tsc` strict + eslint 0. |
| `anti-orphan-integration.md` | T-4 cap re-cable is the "On-the-map" anchor; fixture consumed by specs; no island. |
| **playwright-expert hard invariants** | #6 web-first asserts ONLY (the determinism fix); #5 `getByRole>getByLabel>getByText>getByTestId`, NEVER CSS/XPath; #2 `setupClerkTestingToken({page})` per fixture; #8 tenant via `addInitScript`; #1 NATIVE; #10 no perma-skip. |
| `shell-feature-architecture-mandatory.md` (vitalia) | `adr_004_compliance: bugfix-lite-na` — 9-section ADR-vitalia-004 gate does NOT apply (not a sub-tab). |

## 5.5 Domain skill invariants (SSoT extracts)

| Skill | Hard rules / SSoT highlights | Source |
|---|---|---|
| `brand-expert` | • `identity.voice_tone` is **DEPRECATED** — NEVER touch (use `brand_personality`/`PersonalityProfile`). <br>• PersonalityProfile 3-pilar engine NEVER bypassed (dimensions + linguistic_patterns + sample_exchanges, all 3). <br>• Cross-module brand read ONLY via `core/luana-core-platform/.../links/ports/brand.py::BrandDataPort` — never import `luana_core_brand_studio.domain` directly cross-module. <br>• personality archetype = Jung 12 (salud subset: caregiver/sage/healer/hero). <br>• Engine path: `core/luana-core-brand-studio/`; brand extension: `vitalia/backend/src/modules/vitalia/brand_studio/`; FE schemas: `vitalia/frontend/src/features/lisa/types/marca/*.schema.ts`. | brand-expert/SKILL.md:46-69, 159-172, 343-354 |
| `playwright-expert` | • **Web-first assertions ONLY** (`await expect(locator).toHaveAttribute/toHaveValue/toBeVisible`), NEVER `expect(await locator.x()).toBe()` — this IS the determinism fix (B3). <br>• Locator priority `getByRole > getByLabel > getByText > getByTestId`, NEVER CSS/XPath. <br>• `setupClerkTestingToken({page})` per per-test fixture (else Turnstile blocks mid-test). <br>• storageState in `playwright/.clerk/user.json` (gitignored, never edit by hand). <br>• Multi-tenant header auto via `auth.fixture.ts` `addInitScript` → `localStorage.x-tenant-id` — never hardcode. <br>• NATIVE only; NEVER `make e2e*`. No perma-`test.skip`. <br>• Skill must NOT modify `frontend/src/**` to fix a flaky test — fix the test (NOTE: T-2 modifies prod api-layer, but that's a real prod bug, not a test fix). | playwright-expert/SKILL.md:184-220 |
| `backend-expert` | (not deep-read; cross-cutting) — `response_model=` mandatory (arch test), tenant filter, SQLA 2.0 `select().where()`, structlog. The T-3 change keeps `response_model=ProhibitedPhrasesListDTO`. | via backend-quality.md + 03-arch § 12 |

`frontend-expert` not deep-read (cap ~80 lines/skill; the visual-fidelity D1/D2/D3 disciplines do not apply — no UI). All 3 builders load their own skills when reasoning per `must_load_skills` in 06-tickets.

## 6. Git diff summary

`git diff main..HEAD --stat`: **8 files changed, 1689 insertions, 5 deletions — ALL docs/ready-package** (`01-spec.md` through `dispatch-plan.md` + `checkpoint.md` + `chris-input.md`). **ZERO production or test code touched yet** — the build has NOT written any code despite `checkpoint.md::phase: BUILD_T3`. (The phase field is aspirational/planning; the actual T-3 implementation hasn't started.)

Branch commits (`main..HEAD`):
- `22f25e4d` docs(vitalia): re-repro + ready package estabilizar-harness-e2e-lisa-marca
- `e78b3c9e` docs(vitalia): rescope estabilizar-harness-e2e-lisa-marca — relevancia revisada, NO resuelta

Working tree dirty (story scope): `checkpoint.md` + `chris-input.md` modified (uncommitted); `CONTEXT-BRIEF.md` untracked (this file). No scope source code dirty.

**Implication for the builder:** this is a clean greenfield build against the ready package. The forwarding fixture (`real-backend-forward.fixture.ts`) does NOT exist yet (verified) — T-1 creates it. `base.ts` DOES exist. No partial/abandoned implementation to reconcile.

## 7. Existing systems detected (NO-NEW-LAYER scan)

All paths VERIFIED LIVE by context-builder greps (not just trusting the arch doc). Exact `file:line`:

### BE surfaces (T-3)

| What | Verified path:line | State / evidence |
|---|---|---|
| **marca_router.py** | `vitalia/backend/src/modules/vitalia/brand_studio/api/routers/marca_router.py` | active |
| **`get_prohibited_phrases`** (sub-bug #1) | `marca_router.py:577-602` (decorator @577, `async def`@582) | `tenant_id: str = Header("X-Tenant-ID")`@583; **`user_id: str = Header("X-User-ID")`@584 — REQUIRED but NEVER used in body** (body only uses `tenant_uuid`@594 + `country`@585+599). Fix: line 584 → `user_id: str \| None = Header(alias="X-User-ID", default=None)`. `response_model=ProhibitedPhrasesListDTO`@579. |
| **`patch_personality`** (sub-bug #2b) | `marca_router.py:413-437` (decorator @413, `async def`@418) | `user_id: str = Header("X-User-ID")`@421; **`user_uuid = UUID(user_id)`@428** (will 422 if not UUID). `_role: str = _brand_owner_required`@422 (RBAC, orthogonal). Calls `bundle.marca.patch_personality(user_id=user_uuid)`@433-436. |
| Other PATCHes that do `UUID(user_id)` | `marca_router.py:241` (patch identity), `:292` (patch visuals), `:325` (logo), `:377` (delete logo), `:428` (personality), `:479` (contact) | all parse `user_uuid = UUID(user_id)` — same actor pattern. |
| **`ClinicResolver._resolve_user_uuid`** (CONSUME target) | `vitalia/backend/src/modules/vitalia/iam/application/services/clinic_resolver.py:151` | **PRIVATE method** (`_` prefix). `select(UserModel.id).where(UserModel.clerk_id == clerk_sub)`@164. Public entry: `async_resolve(token, session, tenant_id_str, clinic_id_str)`@93 (decodes JWT→`clerk_sub`@123, calls `_resolve_user_uuid`@129). Raises `UserNotFoundError` if no match. |
| **audit_writer** (how actor written) | `vitalia/backend/src/modules/vitalia/audit/audit_writer.py:105` | `CAST(:user_id AS uuid)` in `INSERT INTO vitalia_audit_log` (sync `db.execute`). Second writer @:203. **Actor MUST be UUID — confirms why raw Clerk userId fails.** `AsyncAuditWriter` is the class injected into `marca_service` (`brand_studio/application/services/marca_service.py:31,84,258`). |
| **vitalia_audit_log model** | `vitalia/backend/src/modules/vitalia/infrastructure/models/medical_audit_log_model.py` + repo `.../repositories/medical_audit_log_repository.py` + `_shared/repositories/audit_log_repository.py:124` | active (table from migration 013_vitalia per arch doc). |
| **marca_service.patch_personality** | `brand_studio/application/services/marca_service.py:227` (user_id: UUID @227, `self._audit.write`@258) | active — receives `user_id: UUID` from router, writes audit. |

### FE surfaces (T-2)

| What | Verified path:line | State / evidence |
|---|---|---|
| **marca-voice-api.ts** | `vitalia/frontend/src/features/lisa/api/marca-voice-api.ts` | active |
| **`updatePersonality`** (sub-bug #2) | `:101-124` | **`"X-User-ID": opts.tenantId`@113** (with comment @109-111 explaining it's a "stable placeholder" tenant UUID). `"X-User-Role": opts.userRole ?? "owner"`@115 (RBAC). **`userId?: string \| null` field ALREADY exists in ApiOpts@34** ("sent as X-User-ID header for mutation audit log") — so the FE already has the userId plumbed in the type, just sends the wrong value. |
| **`getProhibitedPhrases`** | `:128-135` | sends only `opts` — **no X-User-ID** → 422 from browser (the B6 contract bug). |

### FE-test surfaces (T-1)

| What | Verified path:line | State |
|---|---|---|
| **Parent forwarding (inline, NOT shared)** | `arreglar-guardado-voz-y-tono/voz-arquetipo-autosave.spec.ts:81-89` (`page.route("**/api/v1/**")` → `page.request.fetch(targetUrl)`), `BACKEND_API_URL`@58-61, `TENANT_ID`/`E2E_TENANT_ID`@50; same in `voz-bloque-autosave.spec.ts:76-84` | active, **inline duplicated in 3 specs** → LIFT target. ⚠️ `voz-arquetipo:137 "X-User-ID": TENANT_ID` (the B6 mask). |
| **Parent POM (web-first template)** | `arreglar-guardado-voz-y-tono/poms/voz-tono-section.pom.ts` | `getSelectedArchetype()` once-read @180 (the B3 defect); `waitForLoaded()`@132 (only waits for static card); `getAutosaveStatus()`@227 (`getAttribute`@230); **`waitForAutosaveSaved()`@257 — the CORRECT polling template**. |
| **5 `authTest.fixme`** (des-quarantine) | `voz-arquetipo-autosave.spec.ts:236`, `voz-bloque-autosave.spec.ts:178`, `voz-autosave-error.spec.ts:126,178,229` | quarantined assuming the resolved Clerk race. |
| **Dangling listeners** | `voz-arquetipo-autosave.spec.ts:161,261`, `voz-bloque-autosave.spec.ts:118,209` (4× `authedPage.on("response", ...)`) | the B4 defect (`:261` is the fire-and-forget without `page.off`). |
| **lisa-marca suite** | `vitalia-fase2-lisa-marca/` — **11 specs** + 4 POMs (`voz-tono`, `identidad`, `presencia`, `lisa-marca-page`) + fixtures (`lisa-marca.fixture.ts`, `large-dataset.fixture.ts`, `network-failure.ts`, `voice-preview-mock.ts`) | active. |
| **`setupLisaMarcaMocks`** (the bug, REPLACE) | `vitalia-fase2-lisa-marca/fixtures/lisa-marca.fixture.ts:255` (`page.route`@266,300,368 + `route.fulfill`@272,289,306,322,341,356,374,390,405,422,437,453) | mocks identity/visuals/personality/contact/trust → false-green. Comment @137 "API mock base data ... NO real BE calls". |
| **Per-spec inline mocks** | `lisa-marca-autosave-timeout.spec.ts:116,155` (identity), `concurrent-owners:95,118,181` (personality), `voice-warning:52,133,177` (personality), `identidad-autosave:58,112` (identity), `logo-upload-size:51,93,135` (visuals) | route over the 3 backend-under-test endpoints in specs too (not just the fixture). |
| **`base.ts` anti-burbuja gate** (ADOPT, exists) | `vitalia/frontend/e2e/fixtures/base.ts` (5559 b) | `page.on("pageerror")`@73, `nextjs-portal` overlay check@105, `failOnRuntimeError`@138, `export const test = base.extend(...)`@137, `export { expect }`@150. **Provides ONLY the runtime gate** — composes with a SEPARATE `auth.fixture.ts` via `mergeTests` (per its own header @22-23). |
| **`auth.fixture.ts`** (the 'authed' to compose) | `vitalia/frontend/e2e/auth.fixture.ts` | exists. |
| **`real-backend-forward.fixture.ts`** (NEW, T-1 creates) | `vitalia/frontend/e2e/fixtures/real-backend-forward.fixture.ts` | **does NOT exist yet** (verified) — correct, T-1 creates it. |

### Grep counts (SC grep-gate baselines — IMPORTANT, see §11)
- `route.fulfill` in lisa-marca: **53 total**; matching single-line `route.fulfill.*lisa/marca/(identity|visuals|personality)`: **0** (mock is multi-line: `page.route("...identity")` on one line, `route.fulfill()` lines later).
- `from "@playwright/test"` in lisa-marca `*.spec.ts`: **12** (11 specs + 1 extra).
- `page.route("**/api/v1/lisa/marca/{identity,visuals,personality}")`: present in fixture @266,300 + 5 specs (the REAL mock signal).

## 7.5 Anti-duplication inventory cross-reference

Checked against `.claude/rules/anti-duplication.md` § "Inventario engine abstractions (SSoT)":

| Subsystem in PR scope | In `anti-duplication.md` engine inventory? | Canonical path | Recommendation |
|---|---|---|---|
| `_resolve_user_uuid` (clerk→users.id) | **NO** (not in the engine inventory table; it's a vitalia brand-local iam service, NOT a `core/luana-core-*` abstraction) | `vitalia/backend/src/modules/vitalia/iam/application/services/clinic_resolver.py::ClinicResolver._resolve_user_uuid` | **CONSUME** (it already exists in the brand; do NOT recreate). Arch doc + guidelines mandate this. |
| `real-backend-forward.fixture.ts` (e2e forwarding) | NO (e2e harness, per-brand) | n/a (new shared fixture) | **NEW shared fixture (LIFT inline)** — within-brand de-duplication, not an engine lift. |
| `base.ts` anti-burbuja gate | NO (e2e harness, per-brand) | `vitalia/frontend/e2e/fixtures/base.ts` | **ADOPT** (exists, compose via mergeTests). |
| `audit_writer` / `vitalia_audit_log` | NO — vitalia has its **OWN** audit_log (hipaa-lite.md explicitly forbids reusing root Luana audit_log; extra cols `clinic_id`, `payload_redacted`) | `vitalia/backend/src/modules/vitalia/audit/audit_writer.py` | **CONSUME** existing brand writer (no change to writer; only the actor *value* passed to it changes). |
| `sanitize_payload` (PII) | **YES** (engine inventory) | `core/luana-core-observability/.../recording/sanitization.py::sanitize_payload` | not directly touched, but audit_writer already routes payload through `sanitize_phi_payload` (`audit_writer.py:91-95`) — confirm preserved. |

**Severity flagging:**
- No subsystem in PR scope is listed in the engine inventory AND proposes a NEW core file → no HIGH escalation on engine-lift grounds.
- The `_resolve_user_uuid` consume is brand-local; the only nuance (private method / DDD boundary) is a MEDIUM note for the builder/auditor (§11), not an anti-dup violation.
- Forwarding LIFT to shared fixture is the CORRECT anti-duplication move (was inline ×3, would become ×14 if copied) — confirmed by `arch_no_new_forwarding_duplication` validator in 04-validators.

## 8. EXTEND vs NEW recommendations

Mechanical decision rule applied (§7 + §7.5 evidence). Architect already decided these; context-builder confirms via mechanical rule.

| Surface PR proposes | Existing system ≥80% overlap (§7) | In anti-dup inventory? (§7.5) | Recommendation | Reason (path:line) |
|---|---|---|---|---|
| Resolve audit actor (clerk_sub→users.id) | `ClinicResolver._resolve_user_uuid` (100% overlap) | NO (brand-local) | **EXTEND/CONSUME** | `clinic_resolver.py:151` already does exactly this. Builder decides public-vs-private access (see §11). |
| Forward e2e to real BE | parent inline forwarding (100% overlap) | NO | **EXTEND (LIFT to shared)** | `voz-arquetipo-autosave.spec.ts:81-89` is the verbatim template. |
| Anti-burbuja runtime gate | `base.ts` (100%) | NO | **EXTEND/ADOPT** | `e2e/fixtures/base.ts` exists; compose via `mergeTests`. |
| Audit write of marca mutation | `audit_writer` + `marca_service` (100%) | NO | **EXTEND/CONSUME** (no writer change) | `audit_writer.py:105`; only the actor *value* changes. |
| `X-User-ID` optional on a GET | n/a (signature change on existing endpoint) | n/a | **MODIFY in place** | `marca_router.py:584` required→optional. |
| FE send real userId | `updatePersonality` (existing fn, `userId?` field already exists) | n/a | **MODIFY in place** | `marca-voice-api.ts:113` tenantId→userId. |

**No `NEW` from-scratch production layer.** The only NEW file is `real-backend-forward.fixture.ts` (a test fixture lifting existing inline code — anti-dup compliant). **No `REPLACE` of contracts.** Everything is EXTEND/CONSUME/MODIFY-in-place. The architect's NO-NEW-LAYER audit (03-arch § 57-94) matches this mechanically.

## 9. Architecture fitness gates

Gates `gate-runner` will run against this diff (from 04-validators.yaml — the validators are the executable SC translation):

**BE (T-3) baseline:**
- `cd vitalia/backend && ${WS}/.venv/bin/pytest tests/architecture/ -x -q` — must stay GREEN. Key tests: `test_response_model_required.py` (kept `response_model=`), `test_brand_studio_module_ddd.py`, tenant-isolation, `test_phi_dual_filter.py`, `test_audit_log_sync_write.py`. **⚠️ the iam-consume in marca_router may trip a cross-module DDD boundary test** — see §11.
- `ruff check src/modules/vitalia/brand_studio/` + `ruff format --check`
- `mypy --strict src/modules/vitalia/brand_studio/api/routers/marca_router.py`
- new_coverage: `pytest tests/modules/vitalia/brand_studio/ -k prohibited_phrases` (RED: GET no X-User-ID → 200) + `-k audit_actor_real`.

**FE (T-1/T-2):**
- `cd vitalia/frontend && npx tsc --noEmit` (strict)
- `npx eslint e2e/ src/features/lisa/api/ --max-warnings 0`
- `npx vitest run src/__tests__/architecture/` — incl `test-no-clerk-organizations.test.ts` (api-layer keeps `useAuth().userId`, not `orgId`).
- coverage_update: `usePersonalityAutosave.test.ts` (assert X-User-ID ≠ tenantId).

**Functional (need `make dev-vitalia` UP, BE :8002 + FE :3002):**
- `sc1_suite_determinista`: lisa-marca suite `--repeat-each=3 --workers=1` → 0 flaky.
- `sc2/sc3a/sc5a`: grep-gates (anti-regression). **⚠️ sc2 pattern needs adjustment — see §11.**
- `sc3b/sc5b`: runtime base.ts gate + repeat-each=3.
- `sc4`: parent suite `--repeat-each=5` (5 fixme des-quarantined).
- `sc6_audit_actor_real`: state_check DB `SELECT user_id,tenant_id FROM vitalia_audit_log WHERE resource_type='personality' ORDER BY occurred_at DESC LIMIT 1` → user_id = users.id ≠ tenant_id.
- `sc7_prohibited_phrases_browser_real`: state_check backend_log GET 200 from real browser.
- `live_verify`: Chrome DevTools MCP — PATCH personality real + GET prohibited-phrases 200 + 0 burbuja Next. `dod_evidence` in checkpoint.

**Architectural validation:** `arch_no_new_forwarding_duplication`, `arch_no_cross_brand_imports`, `arch_no_core_edit` (`! git diff --name-only main...HEAD | grep '^core/'`).

Iteration policy: `max_iterations: 8`.

## 10. Implementation log highlights

No `T-N-impl-log.md` exists yet (build just started — checkpoint `phase: BUILD_T3`, no impl log written). The closest thing is `checkpoint.md § Re-repro 2026-06-02` (the live re-repro evidence) which is the impl-relevant payload:

- **Direction OBSOLETE (do not pursue):** #1 Clerk-ready gate, #2 resilient query. The auth race is resolved by `14af22b2` + `retry:5`.
- **Phase (a) re-repro DONE:** block persists 5/5, archetype 5/5 (with polling assert). Zero "No se pudo cargar" in ~30 runs.
- **The real determinism fix is web-first asserts** (B3), not auth infra.
- **05-guidelines.md** carries 7 REQUIRED patterns (P1 forwarding-via-shared-fixture, P2 base.ts composition, P3 web-first asserts, P4 no dangling listeners, P5 round-trips vs canned seed, P6 BE X-User-ID optional, P7 actor real FE+BE) + a FORBIDDEN list + a files-in-scope whitelist per ticket.
- **dispatch-plan.md** exists (not deeply read) — `autonomous_mode: false`; Chris ratifies at `/dev-team` spawn.

## 11. Faithfulness gaps + validator findings

All ready-package source files present and non-empty. All BE/FE paths the architect cited were verified live against HEAD. No source lost to compression. Items below are *findings for the builder/architect*, not summary failures.

**MEDIUM:**
- **M1 — SC-2 grep-gate is vacuously passing.** The 04-validators `sc2_no_mock_backend_bajo_prueba` pattern (`route\.fulfill` filtered by `lisa/marca/(identity|visuals|personality)` on the same line) returns **0 today** despite 53 `route.fulfill` mocking those endpoints — because the mock is multi-line (`page.route("...identity")` then `route.fulfill()` later). As written, this gate would pass without removing any mock. The REAL signal is `page.route("**/api/v1/lisa/marca/{identity,visuals,personality}")`. **Architect/builder must fix the grep-gate pattern.** (Severity MEDIUM — could mask incomplete de-mock.)
- **M2 — iam `_resolve_user_uuid` is a PRIVATE method, cross-module.** Sub-bug #2b (T-3) must consume it from `brand_studio` → calling a `_`-prefixed method of the `iam` module may trip a DDD cross-module boundary arch test (`backend-ddd.md`: cross-module default forbidden except port/event). The public `async_resolve` returns a `ClinicContext` whose `.user_id` is the Clerk sub *string* (not the UUID), and requires tenant/clinic strings + does full role resolution — not a clean drop-in. Builder must choose an approach (extract public helper / inject service / port). Flagged so the arch-fitness GREEN isn't assumed.
- **M3 — mock scope under-covers `trust-signals`/`contact`/`voice-preview`** (validator adversarial finding D1). RN-1 / SC-2 scope the backend-under-test mock to identity/visuals/personality, but the suite ALSO mocks `contact` (`empty-state.spec:102`, `lisa-marca.fixture:368`), `trust-signals` (`empty-state.spec:124`, `large-dataset.fixture:136`), and `voice-preview` (`voice-preview-mock.ts:115,202`). A de-mock limited to the 3 named endpoints could leave the suite partially mocked. Builder/architect: decide if RN-1 extends to `contact`/`trust-signals` (likely yes — real reads) vs `voice-preview` (possibly a legitimate mock). Severity MEDIUM (completeness, not correctness).

**LOW:**
- **L1 — Brief exceeds 5-8k token target (~9k).** Intentional: caller requested exact file:line across 7 subsystems on both BE and FE surfaces. Not a fidelity loss.
- **L2 — `chris-input.md` + `dispatch-plan.md` not deep-read** (skimmed via refs). Low risk — they're chris-ratification + agent-assignment, already reflected in 06-tickets `assignment` blocks.
- **L3 — Line-number drift vs arch doc (cosmetic):** arch doc cited dangling listener @`voz-arquetipo-autosave.spec.ts:251`; actual fire-and-forget `.on` is @261 (surrounding test block starts earlier). Arch cited prohibited-phrases endpoint @584 for the header (correct) and patch UUID @374/241/292 (verified at 428/241/292 — the 374 is the delete-logo path). No material impact.

**Domain skill SSoT:** loaded for brand_studio (`brand-expert` + `playwright-expert`). Not skipped → no auto-`partial` on that ground.

**Validator (context-validator adversarial probe):** ran — `CONTEXT-BRIEF-validation.md`. Verdict: brief FAITHFUL, 0 HIGH, 1 new MEDIUM (M3 above). All 3 spot-checked §7 claims confirmed; §15 FastAPI claim corroborated by in-codebase precedent (`marca_router.py:181`). NOTE: `context-validator` subagent could not be spawned (no Agent/Task tool in this harness function set); the adversarial probe was executed in-process by context-builder with different/synonym keywords and the artifact written, per R24/R28.

## 12. Raw paths consulted

**Ready package (read full):** `01-spec.md`, `03-arch.md`, `04-validators.yaml`, `05-guidelines.md`, `06-tickets.yaml`, `checkpoint.md`. (Not read: `chris-input.md`, `dispatch-plan.md` — skimmed via git/refs only.)

**BE code (read/grepped, paths verified):**
- `vitalia/backend/src/modules/vitalia/brand_studio/api/routers/marca_router.py` (lines 413-437, 577-602 read)
- `vitalia/backend/src/modules/vitalia/iam/application/services/clinic_resolver.py` (lines 71-170 read)
- `vitalia/backend/src/modules/vitalia/audit/audit_writer.py` (lines 80-129 read)
- `vitalia/backend/src/modules/vitalia/brand_studio/application/services/marca_service.py` (grepped)
- `vitalia/backend/src/modules/vitalia/infrastructure/models/medical_audit_log_model.py` (located)

**FE code (read/grepped):**
- `vitalia/frontend/src/features/lisa/api/marca-voice-api.ts` (lines 101-145 read)
- `vitalia/frontend/e2e/fixtures/base.ts` (grepped)
- `vitalia/frontend/e2e/auth.fixture.ts` (located)
- `vitalia/frontend/e2e/regression/arreglar-guardado-voz-y-tono/{voz-arquetipo-autosave,voz-bloque-autosave,voz-autosave-error}.spec.ts` + `poms/voz-tono-section.pom.ts` (grepped)
- `vitalia/frontend/e2e/regression/vitalia-fase2-lisa-marca/` (11 specs + 4 POMs + fixtures, grepped) incl `fixtures/lisa-marca.fixture.ts`

**Modules + skills:** `vitalia/docs/product/modules/brand_studio.md`, `.claude/skills/brand-expert/SKILL.md`, `.claude/skills/playwright-expert/SKILL.md`.

**Rules:** auto-loaded set (tenant-isolation, anti-duplication, backend-ddd, tdd-mandatory, e2e-testing, definition-of-done-live-verify, test-design-doctrine, hipaa-lite[vitalia], shell-feature-architecture-mandatory[vitalia], spanish-text, frontend-fsd).

## 13. Verbatim grep + WebFetch commands executed

```bash
WS=/home/chalreme/Proyectos/luana-vitalia
# A — marca_router locate + prohibited-phrases + X-User-ID
grep -rn "prohibited-phrases\|X-User-ID\|X-Tenant-ID" $WS/vitalia/backend/src/modules/vitalia/brand_studio/api/routers/marca_router.py
grep -n "@router\.\|async def \|UUID(\|response_model\|personality" .../marca_router.py
# B — iam ClinicResolver._resolve_user_uuid
grep -rn "_resolve_user_uuid\|class ClinicResolver\|clerk_id\|get_by_clerk" $WS/vitalia/backend/src/modules/vitalia/iam/ $WS/core/luana-core-iam/src/
find .../iam .../luana-core-iam -name "*resolver*.py"
# C — audit_log model + audit_writer
grep -rln "vitalia_audit_log\|class.*AuditLog\|audit_writer" $WS/vitalia/backend/src/modules/vitalia/
grep -rn "CAST.*user_id\|:user_id" .../vitalia/ | grep -iE "audit|cast"
# D — FE marca-voice-api.ts
grep -n "X-User-ID\|updatePersonality\|opts.tenantId\|opts.userId\|prohibited" .../features/lisa/api/marca-voice-api.ts
# E — parent forwarding fixture + POM + fixme + page.on
grep -rn "page.route\|page.request.fetch\|X-User-ID\|E2E_TENANT_ID" .../arreglar-guardado-voz-y-tono/
grep -n "getSelectedArchetype\|waitForSelectedArchetype\|waitForAutosaveSaved\|waitForLoaded" .../poms/voz-tono-section.pom.ts
grep -rn "authTest.fixme\|page.on\|authedPage.on" .../arreglar-guardado-voz-y-tono/
# F/G/H — lisa-marca specs + mock + grep-gate baselines
ls .../vitalia-fase2-lisa-marca/*.spec.ts                                       # 11 specs
grep -n "setupLisaMarcaMocks\|route.fulfill\|page.route" .../fixtures/lisa-marca.fixture.ts
grep -rEn 'route\.fulfill' .../vitalia-fase2-lisa-marca/ | grep -E 'lisa/marca/(identity|visuals|personality)' | wc -l   # → 0 (multi-line!)
grep -rEn 'route\.fulfill' .../vitalia-fase2-lisa-marca/ | wc -l                # → 53
grep -rEn 'from "@playwright/test"' .../vitalia-fase2-lisa-marca/*.spec.ts | wc -l  # → 12
# I/J — base.ts + shared fixture existence
ls .../e2e/fixtures/base.ts                                                     # exists
ls .../e2e/fixtures/real-backend-forward.fixture.ts                            # NOT YET
# K/L — auth.fixture + git diff
find .../e2e -name "auth.fixture.ts"
git diff main..HEAD --stat                                                      # 8 files, all docs
```

**WebFetch executed (§15):** see §15 table. No general-research web access used.

## 14. Free-form notes — what I'd tell a smart colleague

This ready package is unusually good — the architect already did a self-ran existing-systems audit with real path:line refs, and I verified every one of them against HEAD. They hold. So you're not flying blind; my job was mostly to confirm and surface the gotchas the templates don't.

**Build T-3 first, and watch the iam consume.** The single highest-friction thing here is sub-bug #2b: the BE must resolve the audit actor from the JWT `clerk_sub` via `ClinicResolver._resolve_user_uuid`. That method is **private** (`_` prefix) and lives in the `iam` module, while you're editing `brand_studio`. Calling a private method cross-module will very likely trip a DDD boundary arch test (`backend-ddd.md` says cross-module is default-forbidden except via port/event). The clean public entry is `async_resolve(token, ...)` but that wants tenant/clinic strings and does the full role resolution + returns a `ClinicContext` (whose `.user_id` is the Clerk sub string, NOT the UUID — see `clinic_resolver.py:143`). So neither path is a clean drop-in. Decide early: extract a small public resolver, or inject the iam service. Don't let this bleed into iter-8.

**The grep-gates have a real bug.** SC-2's pattern (`route.fulfill.*lisa/marca/(identity|visuals|personality)`) returns 0 today even though the mock is rampant — because the mock is multi-line (`page.route("...identity")` then `route.fulfill()` lines later). If you wire SC-2 as written it'll pass vacuously without you removing a single mock. Use the `page.route("**/api/v1/lisa/marca/{identity,visuals,personality}")` pattern instead (53 `route.fulfill` total, present in the fixture @266+ and 5 specs). Flag this to the architect — it's a validator correctness issue, not just yours to silently fix.

**The FE fix is smaller than it looks.** `marca-voice-api.ts` already has the `userId?` field in `ApiOpts` (line 34) — somebody set up the plumbing and then sent `opts.tenantId` anyway (line 113, with a comment rationalizing it as a "placeholder"). T-2 is basically a one-line swap, but it's load-bearing on T-3b landing first (or the PATCH 422s on a non-UUID).

**No code exists yet** despite `checkpoint phase: BUILD_T3` — clean greenfield, no half-built state to reconcile. The forwarding fixture is genuinely absent.

## 15. Upstream canonical docs references

Fetched 2 of 5 cap (highest signal for this bugfix). Accessed 2026-06-03.

| Framework | URL | Relevant for this PR |
|---|---|---|
| **Playwright** (web-first assertions) | [playwright.dev/docs/test-assertions](https://playwright.dev/docs/test-assertions) | Web-first auto-retrying assertions (`await expect(locator).toBeVisible/toHaveAttribute/toHaveValue`) re-check until met or timeout (default 5s) — use over once-reads (`getAttribute`/`textContent`) which cause flaky tests. `expect.poll()` retries a function return value. **This is the canonical backing for the B3/RN-3 determinism fix** (`getSelectedArchetype` once-read → `waitForSelectedArchetype` web-first). No version visible on page (API stable since 1.x; repo pins `@playwright/test ^1.59.1`). |
| **FastAPI** (Header params) | [fastapi.tiangolo.com/tutorial/header-params](https://fastapi.tiangolo.com/tutorial/header-params/) | Optional header = `Header(default=None, alias="X-User-ID")` (or `Annotated[str \| None, Header(alias="X-User-ID")] = None`). **Without a default → header is REQUIRED** — exactly why `get_prohibited_phrases` returns 422 today (`marca_router.py:584` has no default). The sub-bug #1 fix is a one-line default add. Headers case-insensitive; underscores→hyphens auto. |

**Deferred (architect/builder may fetch on demand — low signal for this bugfix):** sqlalchemy 2.0, pydantic v2, alembic, react-query, clerk, vitest. No schema/migration/new-component/auth-config work in this story → not fetched (avoids Haiku timeout; cap is 5).

## 16. Self-budget snapshot

- Tools used: Read ×11, Bash ×11, Edit ×16, Write ×1, WebFetch ×2.
- Files read (full or partial): ~17 (6 ready-package + 5 BE/FE code reads + 3 module/skill docs + 3 vitalia overlay rules auto-loaded).
- Greps run: ~12 chained Bash blocks (each multi-command).
- Web fetches: 2 (playwright test-assertions, fastapi header-params).
- Brief output size: ~36 KB (~9k tokens) — over the 5-8k target; justified by dual BE+FE surface + caller's explicit "report exact file:line" ask across 7 distinct subsystems.
- Audit log: ~4.2 KB.
- Turns remaining: comfortable headroom (maxTurns 120; used well under half).
