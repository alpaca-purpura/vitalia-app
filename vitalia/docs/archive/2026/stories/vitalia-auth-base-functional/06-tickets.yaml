# 06-tickets.yaml — vitalia-auth-base-functional
# 7 tickets atómicos · gherkin_coverage MANDATORY (post 2026-05-18 cement)
# Owner cost-routing per R23 + production_code per ticket
# DAG: T-1 → T-2 → T-3 (FE chain) ∥ T-4 (BE admin) → T-6.a (local smoke gate) → T-5 (deploy) → T-6.b (LIVE smoke + a11y + mobile + screenshots)
# v2 — 2026-05-18: post Chris ratificación gaps audit (+3 BE integration tests + split T-6 a/b + 3 new specs)

---
story_id: vitalia-auth-base-functional
brand: vitalia
generated_at: 2026-05-18
generated_by: /pm-vitalia
total_tickets: 7
estimated_total_hours: 24
version: v2-post-gaps-audit

# Hot-fix metadata
repro_verified: true                          # /pm-vitalia ran investigation 2026-05-18, root cause confirmed
repro_evidence_ref: checkpoint.md::repro_evidence

# DAG visualization v2:
#
#   T-1 ──→ T-2 ──→ T-3 ──┐
#                          ├──→ T-6.a ──→ T-5 ──→ T-6.b
#                  T-4 ────┘
#
# T-1 + T-4 parallel-able (no dependency)
# T-2 + T-3 secuencial post T-1
# T-6.a (local smoke pre-deploy) bloquea T-5 (gate de calidad)
# T-5 (deploy) bloquea T-6.b (LIVE smoke + a11y + mobile + screenshots)

tickets:

  # ──────────────────────────────────────────────────────────────
  - id: T-1
    title: "FE — Clerk middleware (vitalia/frontend/src/middleware.ts NEW)"
    surface: frontend
    production_code: true
    owner_eligibility: [qwen-opencode, claude-sonnet, claude-opus]
    claude_opus_required: false
    estimated_hours: 1
    blocked_by: []
    blocks: [T-2, T-3, T-6.a]
    decisions_applicable: [D1, D5]
    files_in_scope:
      - vitalia/frontend/src/middleware.ts (NEW)
    description: |
      Crear `middleware.ts` raíz de FE Vitalia con clerkMiddleware + createRouteMatcher.
      Rutas públicas: /sign-in/*, /sign-up/*, /public/*, /api/v1/vitalia/webhooks/*, /api/health.
      Resto protegidas via auth.protect(). Matcher config excluye _next + static assets.
      No redirect manual, no RBAC en middleware.

      TDD: escribir test middleware unit (mock req públicas vs protegidas) ANTES implementación.
    acceptance:
      validator_ids: [nf-fe-tsc, nf-fe-eslint, nf-fe-arch-fitness]
    gherkin_coverage:
      - scenario: "SC-01 — Unauthenticated user accessing dashboard root"
        tests:
          - "vitalia/frontend/e2e/auth/sign-in-redirect.spec.ts::SC-01"
      - scenario: "SC-02 — Public landing route is not protected"
        tests:
          - "vitalia/frontend/e2e/auth/sign-in-redirect.spec.ts::SC-02"

  # ──────────────────────────────────────────────────────────────
  - id: T-2
    title: "FE — Pages Clerk reales (<SignIn /> + <SignUp />) + cleanup step-{1,2,3}"
    surface: frontend
    production_code: true
    owner_eligibility: [qwen-opencode, claude-sonnet, claude-opus]
    claude_opus_required: false
    estimated_hours: 2
    blocked_by: [T-1]
    blocks: [T-6.a]
    decisions_applicable: [D1, D4]
    files_in_scope:
      - vitalia/frontend/src/app/(auth)/sign-in/page.tsx (EDIT — replace placeholder con <SignIn />)
      - vitalia/frontend/src/app/(auth)/sign-up/page.tsx (EDIT — replace placeholder con <SignUp />)
      - vitalia/frontend/src/app/onboarding/step-1/page.tsx (DELETE)
      - vitalia/frontend/src/app/onboarding/step-2/page.tsx (DELETE)
      - vitalia/frontend/src/app/onboarding/step-3/page.tsx (DELETE)
      - vitalia/frontend/src/app/onboarding/step-1/ (DELETE dir)
      - vitalia/frontend/src/app/onboarding/step-2/ (DELETE dir)
      - vitalia/frontend/src/app/onboarding/step-3/ (DELETE dir)
    description: |
      Reemplazar 2 page.tsx placeholders por componentes Clerk reales con appearance
      tokens vitalia. Eliminar 3 stub pages legacy onboarding/step-{1,2,3} (wizard
      unificado vive en /onboarding/wizard).
    acceptance:
      validator_ids: [nf-fe-tsc, nf-fe-eslint]
    gherkin_coverage:
      - scenario: "SC-03 — Sign-in page renders Clerk form"
        tests:
          - "vitalia/frontend/e2e/auth/sign-in-form.spec.ts::SC-03"
      - scenario: "SC-04 — Sign-up page renders Clerk form"
        tests:
          - "vitalia/frontend/e2e/auth/sign-in-form.spec.ts::SC-04"

  # ──────────────────────────────────────────────────────────────
  - id: T-3
    title: "FE — Dashboard `/` mínimo welcome + feature `features/dashboard/`"
    surface: frontend
    production_code: true
    owner_eligibility: [qwen-opencode, claude-sonnet, claude-opus]
    claude_opus_required: false
    estimated_hours: 3
    blocked_by: [T-1]
    blocks: [T-6.a]
    decisions_applicable: [D1, D4]
    files_in_scope:
      - vitalia/frontend/src/app/(dashboard)/page.tsx (EDIT — replace placeholder)
      - vitalia/frontend/src/app/(dashboard)/layout.tsx (NEW if absent — shell con logo + UserButton)
      - vitalia/frontend/src/features/dashboard/index.ts (NEW)
      - vitalia/frontend/src/features/dashboard/components/DashboardWelcome.tsx (NEW Server Component)
      - vitalia/frontend/src/features/dashboard/components/SliceOneStubsRow.tsx (NEW)
      - vitalia/frontend/src/features/dashboard/api/useDashboardData.ts (NEW si requiere fetch client; sino server fetch directo)
      - vitalia/frontend/src/features/dashboard/types/DashboardData.ts (NEW)
      - vitalia/frontend/src/features/dashboard/__tests__/DashboardWelcome.test.tsx (NEW)
    description: |
      Implementar dashboard mínimo welcome state:
        - h1 "Hola, {first_name}"
        - Badge "{clinic_name} · {plan_tier}"
        - CTA "Configurar tu clínica" (visible si !is_onboarded) → /onboarding/wizard
        - SliceOneStubsRow: 5 cards (Inbox/Pipeline/Agenda/Fideliz/Marketing) con label "pronto"
      Fetch user via Clerk auth() server-side. Fetch tenant context via /api/v1/iam/me.
      NO implementar offers/bookings/appointments (siguen placeholder).

      TDD: escribir DashboardWelcome.test.tsx RED ANTES implementación.
    acceptance:
      validator_ids: [nf-fe-tsc, nf-fe-eslint, nf-fe-arch-fitness, fn-fe-dashboard-unit]
    gherkin_coverage:
      - scenario: "SC-06 — Authenticated user lands on welcome dashboard"
        tests:
          - "vitalia/frontend/src/features/dashboard/__tests__/DashboardWelcome.test.tsx::renders_welcome_with_tenant_context"
          - "vitalia/frontend/e2e/dashboard/welcome.spec.ts::SC-06"
      - scenario: "SC-07 — Authenticated user can open onboarding wizard"
        tests:
          - "vitalia/frontend/e2e/dashboard/welcome.spec.ts::SC-07"

  # ──────────────────────────────────────────────────────────────
  - id: T-4
    title: "BE — Admin Streamlit Vitalia + 3 integration tests (webhook + audit_log + cross-tenant)"
    surface: backend
    production_code: true
    owner_eligibility: [qwen-opencode, claude-sonnet, claude-opus]
    claude_opus_required: false
    estimated_hours: 8                              # +2h vs v1 (3 integration tests adicionales)
    blocked_by: []
    blocks: [T-6.a]
    decisions_applicable: [D2, D3, D7, D8]
    files_in_scope:
      # Admin Streamlit core (original v1)
      - vitalia/backend/src/modules/vitalia/admin/__init__.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/app.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/pages/__init__.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/pages/tenants.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/pages/usuarios.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/modules/__init__.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/modules/tenants.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/modules/users.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/_shared/__init__.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/_shared/auth.py (NEW)
      - vitalia/backend/src/modules/vitalia/admin/_shared/db.py (NEW)
      - vitalia/backend/tests/admin/__init__.py (NEW)
      - vitalia/backend/tests/admin/test_admin_contract.py (NEW)
      - vitalia/backend/pyproject.toml (EDIT — add deps streamlit + passlib + clerk-backend-api)
      # ★ NEW v2 — Integration tests (3 archivos) ★
      - vitalia/backend/tests/integration/__init__.py (NEW si absent)
      - vitalia/backend/tests/integration/admin/__init__.py (NEW)
      - vitalia/backend/tests/integration/admin/test_clerk_webhook_integration.py (NEW MUST 2)
      - vitalia/backend/tests/integration/admin/test_audit_log_verify.py (NEW MUST 3)
      - vitalia/backend/tests/integration/admin/test_cross_tenant_isolation.py (NEW SHOULD 7)
      - vitalia/backend/tests/integration/admin/conftest.py (NEW — fixtures fake Clerk webhook, fake admin session)
    description: |
      Bootstrap admin Streamlit Vitalia espejando pattern Nicolify (registry-based
      st.navigation + pages/modules split + _shared utils) PERO scope mínimo:
      SOLO 2 pages (tenants + usuarios). Plus 3 integration tests críticos (v2).

      Admin Streamlit (v1 original):
      - tenants.py: form crear + UUIDv5 determinista + INSERT clinics/tenants/locations + audit_log
      - users.py: form crear + Clerk Backend API + INSERT user_profiles + magic link
      - _shared/auth.py: super-admin gate bcrypt vs VITALIA_ADMIN_PASSWORD_HASH
      - contract test: PAGE_SPECS registry valid + no slug dups + no cross-brand import

      ★ NEW v2 Integration tests críticos ★:
      - test_clerk_webhook_integration.py:
          POST fake user.created webhook valid HMAC → 200 + UserProfile + audit_log creados
          POST fake webhook invalid HMAC → 401 + no row created
          POST fake webhook user.deleted (unsupported event) → 200 noop
          (Cubre SC-05 BE side + SC-12 HMAC validation)
      - test_audit_log_verify.py:
          Invoke admin.tenant.create stub → query vitalia_audit_log → row exists
          Verify action="admin.tenant.create" + actor="super-admin" + resource_id matches
          Verify payload_redacted: contiene clinic_name+country, NO contiene PHI fields
          Idem admin.user.create
          (Cubre SC-13 audit log compliance)
      - test_cross_tenant_isolation.py:
          Admin crea Tenant A + Tenant B
          Query as Tenant A requesting clinic_id of Tenant B → 404 (per dual filter)
          Query as Tenant A header X-Tenant-ID Tenant B → 403
          (Cubre SC-11 HIPAA dual filter runtime)

      TDD: cada integration test escribir RED → implementación BE → GREEN.
    acceptance:
      validator_ids:
        - nf-be-ruff-check
        - nf-be-ruff-format
        - nf-be-arch-fitness
        - nf-anti-duplication-scan
        - fn-be-admin-contract
        - fn-be-webhook-clerk-integration             # NEW v2
        - fn-be-hipaa-audit-log-verify                # NEW v2
        - fn-be-cross-tenant-isolation-admin          # NEW v2
    gherkin_coverage:
      - scenario: "SC-05 — User completes sign-up triggers webhook (BE side: webhook→DB)"
        tests:
          - "vitalia/backend/tests/integration/admin/test_clerk_webhook_integration.py::test_valid_signup_creates_userprofile_and_audit_log"
      - scenario: "SC-08 — Super-admin opens admin app and creates a tenant"
        tests:
          - "vitalia/backend/tests/admin/test_admin_contract.py::test_tenants_page_registered_and_callable"
          - "vitalia/frontend/e2e/admin/tenants-users.spec.ts::SC-08"
      - scenario: "SC-09 — Super-admin creates Clerk user associated to tenant"
        tests:
          - "vitalia/backend/tests/admin/test_admin_contract.py::test_usuarios_page_registered_and_callable"
          - "vitalia/frontend/e2e/admin/tenants-users.spec.ts::SC-09"
      - scenario: "SC-11 — Cross-tenant isolation enforced post admin create (HIPAA dual filter)"
        tests:
          - "vitalia/backend/tests/integration/admin/test_cross_tenant_isolation.py::test_tenant_a_cannot_read_tenant_b_data"
          - "vitalia/backend/tests/integration/admin/test_cross_tenant_isolation.py::test_tenant_header_mismatch_returns_403"
      - scenario: "SC-12 — Webhook HMAC validation rejects invalid signatures"
        tests:
          - "vitalia/backend/tests/integration/admin/test_clerk_webhook_integration.py::test_invalid_hmac_returns_401_no_row"
      - scenario: "SC-13 — Admin audit_log row created post action (HIPAA compliance)"
        tests:
          - "vitalia/backend/tests/integration/admin/test_audit_log_verify.py::test_admin_tenant_create_writes_audit_log"
          - "vitalia/backend/tests/integration/admin/test_audit_log_verify.py::test_admin_user_create_writes_audit_log"
          - "vitalia/backend/tests/integration/admin/test_audit_log_verify.py::test_audit_log_payload_no_phi"

  # ──────────────────────────────────────────────────────────────
  - id: T-6.a                                       # ★ NEW v2 split — local smoke gate pre-deploy ★
    title: "tests — Playwright LOCAL smoke pre-deploy (gate antes T-5 deploy)"
    surface: tests
    production_code: false
    owner_eligibility: [claude-sonnet, claude-opus]
    claude_opus_required: false
    estimated_hours: 2
    blocked_by: [T-1, T-2, T-3, T-4]                # full stack code ready
    blocks: [T-5]                                   # gates deploy
    decisions_applicable: [D6]
    files_in_scope:
      # Specs base (mismas que LIVE pero ejecutadas contra localhost)
      - vitalia/frontend/e2e/auth/sign-in-redirect.spec.ts (NEW)
      - vitalia/frontend/e2e/auth/sign-in-form.spec.ts (NEW)
      - vitalia/frontend/e2e/dashboard/welcome.spec.ts (NEW)
      - vitalia/frontend/e2e/admin/tenants-users.spec.ts (NEW)
    description: |
      Implementar Playwright specs base + correr LOCAL contra make dev-vitalia stack:
        cd vitalia && make dev-vitalia                     # postgres + backend:8002 + frontend:3002
        # Si scope incluye admin Streamlit local también:
        cd vitalia/backend && streamlit run src/modules/vitalia/admin/app.py --server.port=8501 &

        WS=$(git rev-parse --show-toplevel) && bash $WS/scripts/e2e-preflight.sh
        cd vitalia/frontend && \
          E2E_BASE_URL=http://localhost:3002 \
          CLERK_TESTING_TOKEN=$CLERK_TESTING_TOKEN_VITALIA \
          npx playwright test --project=smoke --grep "vitalia-auth-base-functional" --reporter=list

      Specs cubren SC-01..SC-09 baseline (auth + dashboard + admin) — sin a11y/mobile/screenshots
      (esos vienen en T-6.b LIVE).

      Esta validación es **gate de calidad pre-deploy**: si falla local, no se procede a deploy.
      Evita descubrir wire-up FE-BE bugs en producción.

      Specs son **mismos archivos** usados después en T-6.b LIVE — solo cambia E2E_BASE_URL.
    acceptance:
      validator_ids: [vs-playwright-smoke-local-pre-deploy]
    gherkin_coverage:
      - scenario: "SC-01..SC-09 — baseline auth + dashboard + admin scenarios LOCAL"
        tests:
          - "vitalia/frontend/e2e/auth/*.spec.ts (local SC-01..SC-04)"
          - "vitalia/frontend/e2e/dashboard/*.spec.ts (local SC-06, SC-07)"
          - "vitalia/frontend/e2e/admin/*.spec.ts (local SC-08, SC-09 si admin local up)"
      - scenario: "SC-18 — Local smoke pre-deploy ok (gate)"
        tests:
          - "umbrella local smoke GREEN"

  # ──────────────────────────────────────────────────────────────
  - id: T-5
    title: "ops — K8s deploy backend+frontend+admin + secrets verify + seed fixtures + post-deploy smoke"
    surface: ops
    production_code: false                          # config/deploy artifacts, not runtime app code
    owner_eligibility: [qwen-opencode, claude-sonnet, claude-opus]
    claude_opus_required: false
    estimated_hours: 3.5                            # +0.5h vs v1 (post_deploy_smoke.sh)
    blocked_by: [T-6.a]                             # local smoke gate must pass first
    blocks: [T-6.b]
    decisions_applicable: [D7]
    files_in_scope:
      - vitalia/deploy/Dockerfile.admin (NEW)
      - vitalia/deploy/k8s/admin-deployment.yaml (NEW)
      - vitalia/deploy/k8s/admin-service.yaml (NEW)
      - vitalia/deploy/k8s/admin-ingress.yaml (NEW — host vitalia-admin.vitalialat.com)
      - vitalia/deploy/k8s/secrets.template.yaml (EDIT — add VITALIA_ADMIN_PASSWORD_HASH)
      - vitalia/.env.dev.template (EDIT — add VITALIA_ADMIN_PASSWORD_HASH)
      # ★ NEW v2 — post-deploy smoke script + healthcheck endpoints ★
      - vitalia/deploy/scripts/post_deploy_smoke.sh (NEW — automated smoke)
      - vitalia/backend/src/main.py (EDIT — verify /api/health endpoint exists, agregar si no)
    description: |
      Pasos:
      ★ Pre-T-5 (Chris manual checklist — ver 05-guidelines.md § 8):
         Chris verifica Clerk dashboard Vitalia:
         - Webhook endpoint configurado: https://dev-app.vitalialat.com/api/v1/vitalia/webhooks/clerk
         - Evento user.created activo
         - Signing secret obtenido + cargado en K8s secret VITALIA_CLERK_WEBHOOK_SECRET
         (Sin este checklist done, T-5 no proceed.)

      Pasos T-5:
      1. Build container `vitalia/deploy/Dockerfile.admin` + push registry.
      2. Verify K8s vitalia-secrets tiene VITALIA_CLERK_* reales (no REPLACE_ME) — si no, escalate Chris.
      3. Generar `VITALIA_ADMIN_PASSWORD_HASH` con bcrypt + agregar al secret.
      4. `kubectl apply` admin-deployment/service/ingress.
      5. `kubectl rollout restart deployment/vitalia-{backend,admin,frontend}` (force reload).
      6. `kubectl exec deployment/vitalia-backend -- python scripts/seed_fixture_clinics.py --apply`.
      7. Confirmar DNS `vitalia-admin.vitalialat.com` resuelve.
      8. ★ NEW v2: ejecutar `bash vitalia/deploy/scripts/post_deploy_smoke.sh` (auto-verify):
         - kubectl get pods → all Ready
         - curl /api/health → 200
         - curl admin /healthz → 200 (Streamlit native)
         - curl / → 307 to /sign-in
         - curl /sign-in → 200 HTML contains "Iniciar sesión"
         Exit 1 si cualquier check falla → STOP, no proceder a T-6.b.
    acceptance:
      validator_ids: [ops-k8s-healthcheck-deploy-verify]
    gherkin_coverage:
      - scenario: "SC-17 — K8s healthcheck + deploy verify ok"
        tests:
          - "vitalia/deploy/scripts/post_deploy_smoke.sh (exit 0)"

  # ──────────────────────────────────────────────────────────────
  - id: T-6.b                                       # ★ EXPANDED v2 — LIVE + a11y + mobile + screenshots + broader monitor ★
    title: "tests — Playwright LIVE smoke + a11y + mobile + screenshots + broader trace monitor (yo /pm-vitalia ejecuto)"
    surface: tests
    production_code: false
    owner_eligibility: [claude-sonnet, claude-opus]
    claude_opus_required: false
    estimated_hours: 4                              # +1h vs v1 (3 new specs: a11y, mobile, visual)
    blocked_by: [T-5]
    blocks: []                                       # último ticket
    decisions_applicable: [D6]
    files_in_scope:
      # Specs base reusadas de T-6.a (mismos archivos, ahora ejecutados LIVE)
      # ★ NEW v2 — 3 specs adicionales + 1 audit script ★
      - vitalia/frontend/e2e/a11y/a11y-smoke.spec.ts (NEW — a11y axe contra /sign-in, /sign-up, /, /onboarding/wizard)
      - vitalia/frontend/e2e/mobile/mobile-smoke.spec.ts (NEW — viewport iPhone 13)
      - vitalia/frontend/e2e/visual/visual-smoke.spec.ts (NEW — toHaveScreenshot baseline)
      - vitalia/frontend/e2e/visual/screenshots/ (NEW dir — .png baselines git-tracked)
      - vitalia/frontend/playwright.config.ts (EDIT — add project=mobile devices['iPhone 13'])
      - vitalia/frontend/package.json (EDIT — add @axe-core/playwright dep)
      - scripts/playwright_console_network_audit.sh (NEW — parsea trace.zip, exit 1 si console.error|warn o 4xx|5xx no esperado)
    description: |
      Ejecución obligatoria por mí (`/pm-vitalia`) ANTES de declarar story PASS:

      1. Re-correr smoke base LIVE (mismos specs T-6.a, ahora contra dev-app):
         cd vitalia/frontend && \
           E2E_BASE_URL=https://dev-app.vitalialat.com \
           CLERK_TESTING_TOKEN=$CLERK_TESTING_TOKEN_VITALIA \
           VITALIA_ADMIN_PASSWORD=$VITALIA_ADMIN_PASSWORD \
           npx playwright test --project=smoke --grep "vitalia-auth-base-functional" --reporter=list

      2. A11y axe smoke:
         npx playwright test e2e/a11y/ --reporter=list
         (Critical + serious violations = FAIL)

      3. Mobile viewport smoke:
         npx playwright test e2e/mobile/ --project=mobile --reporter=list

      4. Visual screenshot baseline (primer run: establece baselines; runs subsequentes: regression):
         npx playwright test e2e/visual/ --reporter=list
         (Si primer run → commitear .png baselines. Si run subsequente con diff → FAIL hasta investigar.)

      5. Broader trace monitor (post run):
         bash scripts/playwright_console_network_audit.sh vitalia/frontend/playwright-report/
         (Exit 1 si ZERO console.error|warn + ZERO 4xx|5xx no esperado fail)

      Resultado esperado: 18/18 PASS, < 90s, 0 console.error, 0 4xx/5xx no esperados, a11y critical/serious 0, mobile responsive ok, screenshots baseline ok.

      Prerrequisitos:
        - CLERK_TESTING_TOKEN_VITALIA env var (Chris genera en Clerk dashboard Vitalia → Settings → Testing)
        - VITALIA_ADMIN_PASSWORD env var (mismo password cuyo hash está en K8s secret)
        - T-5 deploy completo + post_deploy_smoke.sh GREEN
    acceptance:
      validator_ids:
        - vs-playwright-smoke-live
        - vs-playwright-trace-broader-monitor
        - vs-playwright-a11y-axe
        - vs-playwright-mobile-viewport
        - vs-playwright-screenshot-baseline
    gherkin_coverage:
      - scenario: "SC-01..SC-09 — baseline auth + dashboard + admin scenarios LIVE"
        tests:
          - "vitalia/frontend/e2e/auth/*.spec.ts (LIVE)"
          - "vitalia/frontend/e2e/dashboard/*.spec.ts (LIVE)"
          - "vitalia/frontend/e2e/admin/*.spec.ts (LIVE)"
      - scenario: "SC-10 — Full smoke suite passes against dev-app.vitalialat.com"
        tests:
          - "umbrella LIVE suite GREEN ≥ 18/18 PASS"
      - scenario: "SC-14 — A11y axe smoke contracts pass (sign-in + sign-up + dashboard + wizard)"
        tests:
          - "vitalia/frontend/e2e/a11y/a11y-smoke.spec.ts::all_pages_pass_axe_critical_serious"
      - scenario: "SC-15 — Mobile viewport (iPhone 13) renderable + tappable"
        tests:
          - "vitalia/frontend/e2e/mobile/mobile-smoke.spec.ts::dashboard_renderable_mobile"
          - "vitalia/frontend/e2e/mobile/mobile-smoke.spec.ts::signin_form_tappable_mobile"
      - scenario: "SC-16 — Visual screenshot baseline (regression detection)"
        tests:
          - "vitalia/frontend/e2e/visual/visual-smoke.spec.ts::signin_baseline"
          - "vitalia/frontend/e2e/visual/visual-smoke.spec.ts::dashboard_baseline"
      - scenario: "SC-17 — Broader trace monitor (console + network)"
        tests:
          - "scripts/playwright_console_network_audit.sh exit 0"

# ─── Summary ────────────────────────────────────────────────────
summary:
  fe_tickets: 3              # T-1, T-2, T-3
  be_tickets: 1              # T-4 (expanded +2h con 3 integration tests)
  ops_tickets: 1             # T-5 (expanded +0.5h con post_deploy_smoke.sh)
  test_tickets: 2            # T-6.a local pre-deploy gate + T-6.b LIVE + a11y + mobile + screenshots
  total: 7
  estimated_hours: 23.5
  estimated_dev_days: 3
  parallel_opportunities:
    - "T-1 + T-4 paralelizables (FE middleware + BE admin sin deps)"
    - "T-2 + T-3 secuencial post T-1 (FE chain)"
  critical_path:
    - "T-1 → T-2 → T-3 → T-6.a → T-5 → T-6.b"     # 1+2+3+2+3.5+4 = 15.5h
    - "T-4 → T-6.a → T-5 → T-6.b"                  # alt: 8+2+3.5+4 = 17.5h ← critical path
    - "critical path = 17.5h wall clock"
  cost_routing_total:
    - "0 Opus tickets (no agentic production code)"
    - "7 Sonnet/qwen-opencode tickets (cost-routing eficiente)"
  changes_v2_vs_v1:
    - "T-4 expanded +2h (3 BE integration tests: webhook + audit_log + cross-tenant)"
    - "T-5 expanded +0.5h (post_deploy_smoke.sh auto verify + /api/health endpoint)"
    - "T-6 split T-6.a (local smoke gate) + T-6.b (LIVE expanded a11y + mobile + screenshots)"
    - "T-6.b +1h (3 new specs: a11y axe, mobile viewport, visual screenshot baseline)"
    - "Total +5.5h vs v1 (18h → 23.5h)"
    - "Critical path +5.5h (12h → 17.5h)"
    - "+9 validators (12 → 19)"
    - "+8 Gherkin scenarios (SC-11..SC-18)"
