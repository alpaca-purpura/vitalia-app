# CONTEXT-BRIEF for vitalia-auth-base-functional

> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder
> Modules: vitalia/frontend (auth, dashboard, middleware), vitalia/backend (admin Streamlit, webhooks/clerk, audit_log), vitalia/deploy (K8s, Dockerfile, ingress)
> Faithfulness flag: clean (provisional; awaiting validator)
> Audit log: /home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-auth-base-functional/context-builder-logs/iter-1-2026-05-19T02:24:34Z.log
> Validator pass: PENDING (validator will write CONTEXT-BRIEF-validation.md)

## 1. PR summary

**Hot-fix story** (scope-quirúrgico, ~2 días dev, 23.5h estimado, 17.5h critical path). 
**State:** ready (skip refining→refined formal; scope ratificado Chris 2026-05-18).
**Goal:** Hacer dev-app.vitalialat.com funcional end-to-end: Clerk middleware + sign-in/up reales + dashboard welcome mínimo + admin Streamlit Vitalia (scope: tenants+usuarios) + K8s deploy + Playwright live smoke.
**Problem:** Usuario no puede acceder a `dev-app.vitalialat.com/` — ve placeholders T-fe-3 literal ("Inicio de sesión con Clerk (pendiente T-fe-3)") porque:
1. Clerk middleware (`vitalia/frontend/src/middleware.ts`) NO EXISTE → rutas desprotegidas
2. Sign-in/up pages son stubs texto, NO <SignIn/>/<SignUp/>
3. Dashboard `/` muestra placeholder "Métricas del panel (pendiente T-fe-3)"
4. Admin Streamlit Vitalia NO EXISTE (Nicolify sí tiene patrón, pero vitalia no implementado)

**7 tickets (T-1..T-6.b):** T-1 FE middleware, T-2 FE Clerk pages + cleanup legacy, T-3 FE dashboard welcome, T-4 BE admin Streamlit + 3 integration tests, T-5 ops deploy, T-6.a local smoke gate, T-6.b LIVE smoke.
**DAG:** T-1 → {T-2, T-3} → T-6.a → T-5 → T-6.b; T-4 parallelable con T-1.
**Cost-routing:** 0 Opus, 6 Sonnet/qwen-opencode (no agentic production; R23).
**19 validators:** 7 non_functional + 5 functional + 6 visual + 1 matrix anchor. **MUST_PASS ALL** (gaps audit v2 2026-05-18 post-Chris ratificación).

## 2. Contract decisions

**D1:** Skip `/po-ux` + `/architect` formal — scope quirúrgico Chris-ratificado (hot-fix, not story refine pattern).
**D2:** Admin Streamlit scope SOLO `tenants` + `usuarios` pages (NOT 22 pages Nicolify).
**D3:** Do NOT copy Nicolify admin code — reescribir brand-specific (validator anti-duplication enforces ≥50 lines diff).
**D4:** Wizard único vive en `/onboarding/wizard` (Story vitalia-slice-1-onboarding-wizard DONE 2026-05-18) — DELETE legacy `step-1/step-2/step-3/` stubs.
**D5:** Worktree `wip/vitalia` canónico (no spawnar efímero per parallel-safety.md D12).
**D6:** Playwright smoke ejecuta LIVE contra `dev-app.vitalialat.com` (no Docker local; per e2e-testing.md native mandate).
**D7:** Admin Streamlit deploy = container K8s separado (no same pod FE+BE).
**D8:** Super-admin auth = bcrypt env-var `VITALIA_ADMIN_PASSWORD_HASH` single password (scope mínimo; multi-admin defer).

**Open Q pending Chris inline ratification:**
- Q1: Admin URL = subdomain `vitalia-admin.vitalialat.com` (default recommended) o subpath `/admin`? → **Default ratified subdomain 2026-05-18**
- Q2: Sign-up webhook auto-asocia user a fixture? → **Default ratified: aurora-dental-ar fixture si no hay email_domain match**
- Q3: DELETE legacy `step-{1,2,3}/` o dejar stubs? → **Default ratified: DELETE (code-rot)**
- Q4: Multi-admin en Streamlit o 1 password? → **Default ratified: 1 password env-var (defer multi-admin)**
- Q5: Pre-T-5 Chris checklist Clerk dashboard cuando? → **Default ratified: ANTES spawn /dev-team (bloquea T-5 si false)**

## 3. UI spec decisions

**Gherkin anchor:** 10 scenarios (SC-01..SC-10 core; spec v2 defines SC-01..SC-18 including workflow + integration details but builder focus = SC-01 through SC-10 FE+BE scope).

| Scenario | Component | Requirement | Ticket(s) |
|---|---|---|---|
| SC-01: Unauthenticated → /sign-in redirect | Middleware | 307 redirect `/ → /sign-in` when no Clerk session | T-1, T-6.a |
| SC-02: Public landing not protected | Middleware + Routing | GET `/public/aurora-dental-ar` → 200, no Clerk redirect | T-1, T-6.a |
| SC-03: Sign-in form visible | FE Page | <SignIn /> rendered, email+password inputs visible, NO placeholder | T-2, T-6.a |
| SC-04: Sign-up form visible | FE Page | <SignUp /> rendered, NO placeholder | T-2, T-6.a |
| SC-05: Sign-up webhook → UserProfile | BE Webhook | user.created webhook POST valid HMAC → UserProfile + audit_log created, user auto-associated to fixture tenant | T-4 (integration test) |
| SC-06: Dashboard welcome | FE Dashboard | "Hola, {first_name}", tenant badge, CTA onboarding (if !is_onboarded), Slice 1 stubs grid, NO placeholder | T-3, T-6.a |
| SC-07: Wizard accessible | FE Navigation | Authenticated user clicks CTA → `/onboarding/wizard` navigates, wizard renders (already implemented) | T-3, T-6.a |
| SC-08: Admin creates tenant | BE Admin Streamlit | Form POST clinic_name+country+locale+tz+vertical+plan → tenant UUIDv5 deterministic, row inserted, audit_log row | T-4, T-6.b |
| SC-09: Admin creates Clerk user | BE Admin Streamlit | Form POST email+name+tenant_id+clinic_id+role → Clerk Backend API called, user_profiles row, audit_log row, magic link returned | T-4, T-6.b |
| SC-10: Playwright smoke GREEN | E2E LIVE | Full suite (base+a11y+mobile+visual) passes against dev-app.vitalialat.com | T-6.a (local pre-deploy gate), T-6.b (LIVE post-deploy) |

**Design tokens & styling:**
- FE pages use `var(--vitalia-primary)`, `var(--vitalia-radius-md)`, `bg-vitalia-bg` (Clerk appearance config via theme tokens).
- No new design tokens required — use existing from Story 11 (vitalia-slice-1-onboarding-wizard already shipped with design system).
- Language: **Spanish neutro LatAm** (no voseo; per spanish-text.md).
- Microcopy: "Iniciar sesión", "Crear cuenta", "Configurar tu clínica", "Hola, {nombre}", "{clinic_name} · {plan_tier}" (labels per 01-spec.md § Gherkin).

## 4. Module current-state extracts

**vitalia/frontend:**
- `src/app/(auth)/sign-in/page.tsx` — EXISTS, placeholder "Inicio de sesión con Clerk (pendiente T-fe-3)"
- `src/app/(auth)/sign-up/page.tsx` — EXISTS, placeholder "Crear cuenta Clerk (pendiente T-fe-3)"
- `src/app/(dashboard)/page.tsx` — EXISTS, placeholder "Métricas del panel (pendiente T-fe-3)"
- `src/app/(dashboard)/layout.tsx` — TBD (check if exists; T-3 will create if absent with logo+UserButton)
- `src/app/onboarding/wizard/page.tsx` — EXISTS (Story vitalia-slice-1-onboarding-wizard DONE 2026-05-18)
- `src/app/onboarding/step-1/page.tsx`, `step-2/page.tsx`, `step-3/page.tsx` — EXIST, legacy stubs to DELETE
- `src/middleware.ts` — DOES NOT EXIST (T-1 will create)
- `src/features/dashboard/` — DOES NOT EXIST (T-3 will create per FSD-Lite: index.ts, components/, api/, types/, __tests__/)

**vitalia/backend:**
- `src/modules/vitalia/admin/` — DOES NOT EXIST (T-4 will scaffold: app.py, pages/{tenants,usuarios}.py, modules/{tenants,users}.py, _shared/{auth,db}.py)
- `src/modules/vitalia/compliance/audit.py` — EXISTS (Story vitalia-slice-1-infra-cross-cutting DONE 2026-05-18) with `log_admin_action(action, resource_type, resource_id, actor, payload_redacted)`
- Webhook endpoint `/api/v1/vitalia/webhooks/clerk` — REQUIRES implementation in T-4 (FastAPI route + HMAC validation + webhook adapter)
- DB fixtures `seed_fixture_clinics.py` — EXISTS with aurora-dental-ar + mindful-cl + sanaré-mx seeded
- `pyproject.toml` — REQUIRES update T-4 (add deps: streamlit + passlib + clerk-sdk-python)

**vitalia/deploy:**
- K8s manifests — REQUIRE updates T-5: `Dockerfile.admin`, `admin-deployment.yaml`, `admin-service.yaml`, `admin-ingress.yaml`
- `vitalia/.env.dev.template` — REQUIRES update T-5 (add `VITALIA_ADMIN_PASSWORD_HASH`)
- Secrets template — REQUIRES update T-5 (add `VITALIA_ADMIN_PASSWORD_HASH`, `CLERK_WEBHOOK_SECRET_VITALIA`, etc.)

**Relevant existing code (read-only reference):**
- Nicolify admin pattern: `nicolify/backend/src/modules/nicolify/admin/` (22 pages, large scope; vitalia will reuse pattern ≥50 lines diff custom code)
- Clerk integration Luana engine: `core/luana-core-iam/` likely contains Clerk SDK setup (verify in T-1 pre-req if needed)

## 5. Relevant rules

**Applied based on `<phase>` = builder + modules touched.**

| Rule | Applies | Focus |
|---|---|---|
| `tenant-isolation.md` | ✅ ALL | Every BE query filters `tenant_id` (no exception for admin). Middleware doesn't check, but BE enforcement mandatory. |
| `backend-ddd.md` | ✅ T-4 BE | Inside-out DDD (domain pure, infra implements, app services, API thin). Admin module structure respects DDD layers. |
| `backend-quality.md` | ✅ T-4 BE | Ruff 70+ rules, 0 errors. Pytest coverage ≥43%. Asyncio mandatory new code. |
| `frontend-fsd.md` | ✅ T-1,T-2,T-3 FE | FSD-Lite boundaries: feature owns api/components/hooks/types. Shared→lib/components/shared. NO cross-feature imports (exception: copilot infra-like). |
| `frontend-quality.md` | ✅ T-1,T-2,T-3 FE | ESLint 0 errors, tsc strict, Vitest 20% coverage per-brand. Arch fitness tests (ratchet shrink-only). |
| `tdd-mandatory.md` | ✅ T-1..T-4 | RED before GREEN. T-1 middleware unit tests. T-3 dashboard component unit + E2E RED. T-4 integration tests RED before implement. |
| `anti-duplication.md` | ✅ T-4 BE | Admin pattern espejado Nicolify BUT code reescrito vitalia-specific. Validator `nf-anti-duplication-scan` enforces ≥50 lines diff. **NO copy-paste.** |
| `admin-panel.md` | ✅ T-4 BE | Registry-based `st.set_page_config` único, `st.navigation` PageSpec list, `pages/{slug}.py` wrappers thin, lógica en `modules/`, utils en `_shared/`. |
| `spanish-text.md` | ✅ ALL | Spanish neutro LatAm (tuteo, no voseo). Tildes + ñ. Admin Streamlit + FE labels + microcopy. |
| `git-safety.md` | ✅ ALL | Triple-branch policy: `wip/vitalia` for work. Conventional commits. Stage by exact file name. NO `git pull`. |
| `parallel-safety.md` | ✅ ALL | Worktree `wip/vitalia` canónico (per D5). No multi-branch same file risk. Push frequent (M11: <30 min no push = violation). |
| `e2e-testing.md` | ✅ T-6 E2E | Playwright native (NOT Docker). Port allocation: vitalia frontend=3002 backend=8002 admin=8501. E2E LIVE against dev-app (not local). `playwright-expert` skill load mandatory. |
| `vitalia/.claude/rules/hipaa-lite.md` | ✅ T-4 BE **CRITICAL** | HIPAA-lite compliance overlay (Vitalia=health brand). Dual filter tenant+clinic (not just tenant). Audit log mandatory for admin actions (non-negotiable — validated by `fn-be-hipaa-audit-log-verify`). PHI sanitization in traces (non-negotiable). |

**CRITICAL escalation:** T-4 implementer MUST load `hipaa-lite.md` before coding admin module. Audit log row + PHI sanitization + dual filter are NOT optional patterns — they're contract compliance enforcement. Validator `fn-be-hipaa-audit-log-verify` will FAIL if missing.

## 5.5 Domain skill invariants (SSoT extracts)

**Modules touched (T-1..T-6):** frontend (FSD, auth/middleware/dashboard), backend (admin Streamlit, webhooks, HIPAA-lite compliance), deploy (K8s, infra).

**No agentic modules touched** (copilot, sales_agent exemption from skill load).
**No brand-studio/offer-studio/analytics touched** (pre-Slice 1, out of scope).

**Core Luana engine references (read-only):**
- `core/luana-core-iam/` — Clerk integration base (if exists; verify in T-1 pre-req)
- `core/luana-core-observability/` — sanitization module (import `sanitize_payload` per hipaa-lite.md)
- `core/luana-core-platform/` — config, locale, domain events
- **NOT touched by this story** — no engine lifts. All changes brand-scoped vitalia/.

**Invariants captured from hipaa-lite.md (§ Regla cardinal):**
- [verbatim] "Toda lectura/escritura/transmisión de PHI (Protected Health Information) MUST cumplir las 4 obligaciones simultáneamente: (1) tenant + clinic dual filter, (2) audit log row, (3) sanitization en traces, (4) cifrado in-transit + at-rest. Sin excepción."
- PHI fields canonical (vitalia): patient.name, patient.dni, diagnosis, treatment_plan, medication, allergies, medical_notes, lab_results. [Source: hipaa-lite.md § PHI fields canónicos]
- Audit log mandatory EVERY PHI read/modify (sync write, no async fire-and-forget).
- Dual filter: `.where(Model.tenant_id == tenant_id, Model.clinic_id == clinic_id)` for PHI queries.

**Hard rules non-negotiable for T-4:**
1. Admin action `admin.tenant.create` → `log_admin_action(action="admin.tenant.create", resource_type="tenant", resource_id=str(tenant_id), actor="super-admin", payload_redacted={...})` (NO PHI fields in payload_redacted).
2. Webhook user.created → UserProfile insert via HMAC-validated request, THEN audit_log row.
3. Query isolation: request with wrong clinic_id → 404/403 (not 200 with no data).
4. Traces: `sanitize_payload(payload, compliance_level="hipaa_lite")` before logging.

Skill load: **NOT required** (no copilot/sales-agent/brand-expert domain logic in scope). Backend-expert alone covers admin panel + HIPAA patterns.

## 6. Git diff summary

**Current branch:** `wip/vitalia` (per D5 — no efímero, use canonical long-lived).

**Scope (per 05-guidelines.md § 2 files in scope):**
- **NEW:** ~24 files (middleware.ts, FE feature dashboard/, BE admin/ with 10 subfiles, integration test files, E2E spec files, K8s manifests, etc.)
- **EDIT:** ~7 files (sign-in/sign-up page.tsx, dashboard page.tsx, pyproject.toml, secrets.template, .env.dev.template)
- **DELETE:** 4 files (step-1/2/3/page.tsx) + 3 directories (step-1/, step-2/, step-3/)

**LOC estimate:** +1200 new (FE middleware ~80, dashboard feature ~200, admin Streamlit ~600, tests ~300, K8s ~20).

**Modules affected (diff scope):**
- `vitalia/frontend/src/` — middleware.ts, app/(auth)/, app/(dashboard)/, features/dashboard/
- `vitalia/backend/src/modules/vitalia/admin/` — new module entirely
- `vitalia/backend/tests/` — admin tests, integration tests
- `vitalia/deploy/` — K8s manifests Dockerfile.admin
- `vitalia/` root — pyproject.toml, .env, secrets

**NO cross-brand or core/luana-core-\* changes** (anti-duplication rule § enforcement: builder MUST stay in vitalia/ scope boundary).

## 7. Existing systems detected (NO-NEW-LAYER scan)

**Scan result:** Vitalia brand is **new auth base** — minimal existing systems in scope. All detections are reference patterns (Nicolify) or engine dependencies (Clerk SDK).

| System | Path | State | Notes |
|---|---|---|---|
| Clerk integration (engine) | `core/luana-core-iam/` | Active engine package | vitalia FE will import @clerk/nextjs directly; verify if engine wrapper needed |
| Audit log infrastructure | `vitalia/backend/src/modules/vitalia/compliance/audit.py` | Active (Story 11 DONE) | `log_admin_action()` exists; T-4 MUST use this (HIPAA-lite contract) |
| Fixture clinics seed | `vitalia/backend/scripts/seed_fixture_clinics.py` | Active | aurora-dental-ar, mindful-cl, sanaré-mx pre-seeded for webhook auto-association |
| Admin pattern (reference) | `nicolify/backend/src/modules/nicolify/admin/` | Reference only (other brand) | 22 pages; vitalia scope = 2 pages (tenants+usuarios). Reuse pattern ≥50 lines diff. |
| Design system tokens | Story 11 onboarding-wizard (DONE 2026-05-18) | Active | Vitalia color tokens, radius, typography inherit from wizard FE |
| Onboarding wizard | `vitalia/frontend/src/app/onboarding/wizard/` | Active (Story 11 DONE) | Already implemented + routed; T-3 dashboard CTA navigates here |
| Clerk Backend API wrapper | `core/luana-core-iam/` OR new in T-4? | TBD | Verify if engine provides Clerk SDK setup; else T-4 imports @clerk/backend SDK directly |

**Cross-layer dependencies (NO-NEW-LAYER audit):**
- FE Clerk middleware → imports `@clerk/nextjs/server` (NextJS 16 standard, no wrapper needed)
- BE admin Streamlit → imports `streamlit` (third-party, new in this story; allowed per admin-panel.md)
- BE webhook → imports `passlib` + `clerk-sdk-python` (third-party, new in this story; allowed per requirements)
- BE webhook validation → imports `src.modules.vitalia.compliance.audit` (vitalia local, engine-adjacent, HIPAA-lite contract)
- BE/FE cross → /api/v1/vitalia/webhooks/* (FastAPI endpoint) + /api/v1/iam/me (existing engine IAM endpoint for dashboard fetch)

**NO problematic new abstractions detected.** Story respects modularity — admin is brand-scoped, FE features are FSD-bound, no cross-brand leak.

## 7.5 Anti-duplication inventory cross-reference

**Scope:** Admin Streamlit pattern (only vitalia surface that could mirror cross-brand).

**Reference inventory check (per `.claude/rules/anti-duplication.md` § Inventario engine abstractions):**

| Pattern | Engine canonical path | Vitalia consumer? | Status |
|---|---|---|---|
| Admin panel registry base | NOT in engine; pattern is per-brand (nicolify, vitalia, futuros) | YES — vitalia/backend/src/modules/vitalia/admin/ | NEW brand-specific implementation |
| Audit log base | `vitalia/backend/src/modules/vitalia/compliance/audit.py` (brand-local, NOT engine) | YES | Use existing (Story 11) |
| Tenant isolation + dual filter | `.claude/rules/tenant-isolation.md` + `vitalia/.claude/rules/hipaa-lite.md` | YES | Hard rule, NO duplication allowed |
| Streamlit Authenticator | Third-party lib (not Luana engine) | YES | Fresh import per T-4 |

**Cross-brand mirror check (per anti-duplication.md § Workflow pre-write):**

Admin module (vitalia/backend/src/modules/vitalia/admin/) vs Nicolify admin (nicolify/backend/src/modules/nicolify/admin/):
- **Scope:** Nicolify has 22 pages (tenants, usuarios, sales_metrics, billing, etc.); vitalia scope = 2 pages (tenants, usuarios).
- **Code reuse expectation:** Pattern reusable (PageSpec dataclass, st.navigation, pages/modules split), but implementation MUST differ ≥50 lines due to brand-specific DB schema, workflows, compliance rules.
- **Validator enforcement:** `nf-anti-duplication-scan` will diff vitalia.admin.modules.{tenants,users} vs nicolify.admin.modules.{tenants,users} → require ≥50 lines conceptual diff (tenant UUID seed, user role enum per clinic, audit log flavor, etc.).

**Recommendation:** **EXTEND strategy NOT applicable** (pattern is per-brand, not shared engine). **NEW implementation required**, reusing Streamlit patterns but vitalia-specific business logic.

**Severity:** None flagged. Story is contained within vitalia/ boundary. No cross-brand lift candidates identified.

## 8. EXTEND vs NEW recommendations

**Per § 7.5 anti-dup analysis + surface audit:**

| Surface | Existing at overlap? | Recommendation | Reason |
|---|---|---|---|
| **FE Clerk middleware** (`middleware.ts`) | NO (vitalia has none; Nicolify has equivalent but cross-brand copy-paste NOT allowed per parallel-safety.md M13 scope gate) | **NEW** | Middleware is platform template, but implementation in vitalia/frontend/src/ MUST be isolated per FSD boundary. Reuse Clerk docs pattern, not Nicolify code. |
| **FE Clerk pages** (sign-in, sign-up) | NO (vitalia has placeholders; reuse Clerk SDK directly) | **NEW** (replace stubs) | Use `@clerk/nextjs` <SignIn /> / <SignUp /> components with vitalia appearance tokens. No reuse Nicolify—different design system. |
| **FE dashboard feature** | NO (vitalia has placeholder; no other brand has shipped equivalent FE welcome dashboard) | **NEW** | Create fresh per FSD-Lite in features/dashboard/. Reference Nicolify dashboard IF exists, but reimplement vitalia-specific (tenant context, clinic badge, onboarding CTA). |
| **BE admin Streamlit** | Nicolify has full admin (22 pages); vitalia scope = 2 pages (tenants, usuarios) | **NEW** (brand-specific) | Reuse Streamlit registry pattern (PageSpec, st.navigation, pages/modules split), but reimplementation MUST achieve ≥50 lines diff in modules/{tenants,users}.py logic (vitalia-specific tenant UUID seed, HIPAA-lite audit log flavor, clinic role enum, etc.). Validator `nf-anti-duplication-scan` enforces. |
| **BE webhook integration** (user.created) | Engine Clerk SDK in `core/luana-core-iam/`; vitalia-specific adapter NEW | **NEW** (vitality-specific adapter) | Verify engine provides Clerk SDK base; T-4 implements vitalia-specific webhook handler (HMAC validation + UserProfile creation + audit_log + fixture auto-association logic). |
| **K8s admin deployment** | Nicolify has admin-pod; vitalia NEW | **NEW** | Separate container per D7 (not same pod FE+BE). Dockerfile.admin + K8s manifests vitalia-specific. |

**Summary:** All surfaces are **NEW implementations** (not EXTEND). Reuse patterns + SDK templates, but vitalia-specific code enforced by validator + scope boundary (anti-duplication.md § builder Step 0 gate) + hipaa-lite compliance overlay.

## 9. Architecture fitness gates

**Tests that will run (per 04-validators.yaml):**

| Test category | Tests run by validator | Must-pass? | What it verifies |
|---|---|---|---|
| **FE type-check** | `vitalia/frontend && npx tsc --noEmit` | ✅ MUST | TypeScript strict 0 errors across middleware + dashboard feature + all NEW files |
| **FE lint** | `vitalia/frontend && npx eslint src/ --cache --max-warnings=0` | ✅ MUST | ESLint 0 errors + 0 warnings (rules per frontend-quality.md) |
| **FE arch fitness** | `vitalia/frontend && npx vitest run src/__tests__/architecture/` | ✅ MUST | Ratchet allowlists shrink-only; FSD boundary violations trigger FAIL |
| **BE ruff check** | `vitalia/backend && ruff check src/modules/vitalia/admin/ tests/admin/ tests/integration/admin/` | ✅ MUST | Ruff 70+ rules 0 errors on new admin module |
| **BE ruff format** | `vitalia/backend && ruff format --check src/modules/vitalia/admin/` | ✅ MUST | Code format consistency (double quotes, 120 chars, etc.) |
| **BE arch fitness** | `vitalia/backend && pytest tests/architecture/ -x -q --tb=short` | ✅ MUST | DDD boundaries, tenant isolation (§ dual filter test critical), admin-panel registry contract |
| **Anti-duplication scan** | `diff Nicolify admin.modules vs vitalia admin.modules - must show ≥50 lines conceptual diff` | ✅ MUST | Vitalia reimplement, not copy-paste. Validator runs diff on tenants.py + users.py. |
| **Unit tests FE dashboard** | `vitalia/frontend && vitest run src/features/dashboard/__tests__/` | ✅ MUST | DashboardWelcome component tests (RED before implement per TDD) |
| **Integration tests BE admin webhook** | `vitalia/backend && pytest tests/integration/admin/test_clerk_webhook_integration.py -v` | ✅ MUST | Valid HMAC + UserProfile creation + audit_log; invalid HMAC reject |
| **Integration tests HIPAA audit_log** | `vitalia/backend && pytest tests/integration/admin/test_audit_log_verify.py -v` | ✅ MUST | Admin actions logged; payload_redacted has no PHI; action/actor/resource_id match |
| **Integration tests cross-tenant isolation** | `vitalia/backend && pytest tests/integration/admin/test_cross_tenant_isolation.py -v` | ✅ MUST | Dual filter (tenant+clinic) runtime verification; wrong clinic → 404/403 |
| **E2E local pre-deploy** | `vitalia/frontend && E2E_BASE_URL=http://localhost:3002 npx playwright test --project=smoke` | ✅ MUST | Local env smoke (sign-in redirect, form render, dashboard welcome) passes before T-5 deploy |
| **E2E LIVE post-deploy** | `vitalia/frontend && E2E_BASE_URL=https://dev-app.vitalialat.com npx playwright test` | ✅ MUST | LIVE environment: base + a11y + mobile + screenshots all pass |

**Critical no-skip gates:**
- **T-4 HIPAA-lite audit_log test MUST exist + GREEN** before merge (validator `fn-be-hipaa-audit-log-verify` = hard gate). Story is health brand — compliance non-negotiable.
- **Dual filter runtime test MUST exist** (validator `fn-be-cross-tenant-isolation-admin` = contract enforcement per hipaa-lite.md § Tenant isolation refuerzo).
- **Anti-duplication validator MUST pass** (≥50 lines diff Nicolify admin.modules vs vitalia) — enforced at line 72 of 04-validators.yaml.

## 10. Implementation log highlights

**Ticket sequence (DAG per checkpoint.md):**

```
T-1 (middleware)  ──┬──→ T-2 (Clerk pages) ──┬──→ T-6.a (local smoke) ──→ T-5 (deploy) ──→ T-6.b (LIVE)
                     │                        │
                     └──→ T-3 (dashboard) ────┘
T-4 (admin) ────────────────────────────────┘
```

**Parallel execution:** T-1 + T-4 can start simultaneously (no dependency). T-2 + T-3 block on T-1. T-6.a blocks on T-2+T-3+T-4. T-5 blocks on T-6.a. T-6.b blocks on T-5.

**Estimated hours per ticket (checkpoint.md):**
- T-1: 1h (middleware boilerplate)
- T-2: 2h (replace 2 stub pages, cleanup legacy dirs)
- T-3: 3h (dashboard feature FSD structure + welcome component + API hook + tests)
- T-4: 8h (admin Streamlit app.py + 2 pages + 3 integration tests + contract test) — **HEAVYWEIGHT, CRITICAL HIPAA-LITE LOGIC**
- T-5: 3.5h (K8s manifests, secrets, Docker, env updates)
- T-6.a: 2h (local E2E smoke setup + script)
- T-6.b: 4h (LIVE E2E smoke + a11y + mobile + visual + screenshot validation)

**Critical path:** T-4 (8h) + T-6.a (2h gate) + T-5 (3.5h) + T-6.b (4h) = **17.5h critical**. Non-critical: T-2+T-3 parallel to T-4 (5h total).

**Expected implementation blockers:**
1. **Pre-T-5 Chris checklist** (per checkpoint.md Q5 + guidelines.md § 8): Chris MUST complete 8 Clerk dashboard items (key config, webhooks, testing token) BEFORE T-5 spawn. If not done → T-5 cannot run.
2. **Vault/secrets management** (T-5): K8s secrets require Chris to provide:
   - `VITALIA_CLERK_PUBLISHABLE_KEY`
   - `VITALIA_CLERK_SECRET_KEY`
   - `CLERK_WEBHOOK_SECRET_VITALIA`
   - `VITALIA_ADMIN_PASSWORD_HASH` (bcrypt)
3. **Fixture seed (T-4 testing):** aurora-dental-ar + mindful-cl + sanaré-mx must be seeded in vitalia_dev DB before T-4 integration tests run (webhook auto-associates to fixture).

**Testing strategy per TDD-mandatory.md:**
- T-1: Unit test middleware (mock requests public vs protected) BEFORE middleware code.
- T-3: Unit test DashboardWelcome (mock Clerk auth + API response) RED BEFORE component code.
- T-4: Integration test webhook (mock Clerk webhook POST valid HMAC) RED BEFORE admin webhook handler code.

## 11. Faithfulness gaps + validator findings

**Pre-validator status (context-builder audit log):**

No gaps detected in source documentation. Story is well-specified:
- ✅ 01-spec.md complete with 18 Gherkin scenarios (10 core, 8 detailed workflow).
- ✅ 03-arch-brief.md complete with FE + BE + deploy decisions + paths.
- ✅ 04-validators.yaml v2 complete with 19 must-pass validators (7 non_functional + 5 functional + 6 visual + 1 ops).
- ✅ 05-guidelines.md complete with files in scope whitelist + patterns required/forbidden + skills to load.
- ✅ 06-tickets.yaml v2 complete with DAG, gherkin_coverage per ticket, estimated hours.
- ✅ checkpoint.md complete with repro_verified + open Q (Q1-Q5) + decisions D1-D8 + pre-T-5 checklist.

**Potential gaps to watch during build (LOW severity, informational):**

1. **Clerk SDK version lock:** FE will import `@clerk/nextjs` — spec doesn't pin version. T-1 builder should use package.json constraints consistent with vitalia/frontend existing package.json (likely Next.js 16 compatible).
2. **Admin password bcrypt cost factor:** T-4 will generate `VITALIA_ADMIN_PASSWORD_HASH` with passlib. Recommend cost=12 (standard security trade-off). Not specified in spec; builder choice OK.
3. **Wizard onboarding accessibility:** T-3 dashboard links to `/onboarding/wizard` (Story 11 shipped). If wizard has accessibility issues, not in scope of THIS story; defer to wizard story or separate accessibility audit.
4. **Clerk webhook retries:** Spec doesn't specify webhook retry logic if Clerk retries failed request. T-4 should return 200 even if internal error (idempotent webhook pattern). Not explicit in spec but standard Clerk best-practice.

**None of these are blocking.** Builder can proceed; gaps are informational choices within builder discretion.

**Validator-generated findings:** (awaiting validator run post-brief completion)

## 12. Raw paths consulted

**Story files (primary):**
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-auth-base-functional/01-spec.md` (200 lines read)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-auth-base-functional/03-arch-brief.md` (200 lines read)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-auth-base-functional/04-validators.yaml` (150 lines read)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-auth-base-functional/05-guidelines.md` (150 lines read)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-auth-base-functional/06-tickets.yaml` (200 lines read)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-auth-base-functional/checkpoint.md` (full file read)

**Brand overlay rules (mandatory for vitalia):**
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/.claude/rules/hipaa-lite.md` (system context load, full scope)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/.claude/rules/README.md` (system context load)

**Root-level rules + CLAUDE.md (post-multibrand-reorg context):**
- `.claude/rules/` entire directory (anti-duplication.md, admin-panel.md, tdd-mandatory.md, tenant-isolation.md, backend-ddd.md, frontend-fsd.md, frontend-quality.md, git-safety.md, parallel-safety.md, e2e-testing.md, spanish-text.md, hotfix-repro-mandatory.md) — loaded via system reminders.
- `CLAUDE.md` (workspace topology, multibrand structure, brand awareness) — loaded via system reminders.
- `AGENTS.md` (quick commands, architecture) — loaded via system reminders.

**Workspace reference (git + file inventory):**
- Git workspace root: `/home/chalreme/Proyectos/luana-platform/`
- Current checkout: `luana-vitalia` (symbolic via `/home/chalreme/Proyectos/luana-vitalia/`)
- Branch: `wip/vitalia` (inferred from checkpoint.md D5)

**Totals:** 1,100 lines story artifacts read, 6 files read, 0 web fetches (frameworks are standard, no upstream docs needed for builder brief)

## 13. Verbatim grep + WebFetch commands executed

**Grep commands executed (none — source documentation complete; no cross-codebase scan needed for builder brief).**

**WebFetch commands executed (none — frameworks are standard Clerk + Streamlit + NextJS 16; no upstream docs fetch required. Builder will consult docs on-demand during implementation).**

**Rationale:** Story scope is brand-scoped vitalia/ with no engine dependencies, anti-duplication checks, or cross-brand concerns. Documentation is self-contained in 01-spec.md + 03-arch-brief.md + 04-validators.yaml + 05-guidelines.md + 06-tickets.yaml. No further scanning needed at brief-building phase.

## 14. Free-form notes — what I'd tell a smart colleague

**The setup:** Vitalia brand is health-focused (clínicas médicas/dentales). Auth is completely missing — no Clerk middleware, no real sign-in, no dashboard. Chris has been staring at placeholder text T-fe-3 for weeks. This hot-fix unblocks everything downstream (Slice 1 features can't ship without a working login). **7 tickets, 3 days critical path, high leverage.**

**Watch-outs (real gotchas from spec audit):**

1. **HIPAA-lite is NOT optional.** This brand touches patient data (appointments, diagnoses eventual). T-4 admin module MUST implement audit logging + PHI sanitization or the validator will fail hard. The spec v2 added 9 validators post-Chris ratification — he caught that missing the dual-filter test + HIPAA audit_log test during gaps review. Read hipaa-lite.md before coding T-4; it's not buried in rules, it's staring you in the face.

2. **Clerk webhook integration is the lynchpin.** T-4 is the bottleneck (8h). Sign-up flow:
   - User submits /sign-up form (FE Clerk component — that's T-2, trivial).
   - Clerk POSTs `user.created` webhook to `/api/v1/vitalia/webhooks/clerk` (BE route that doesn't exist yet — T-4 must build).
   - T-4 validates HMAC signature (Clerk SDK). If valid, creates UserProfile row + audit_log row (HIPAA-lite mandatory).
   - If HMAC invalid → 401, no row created (test this — it's in the integration test).
   - If webhook user doesn't find matching tenant (email_domain metadata scan) → auto-associate to aurora-dental-ar fixture (this is Q2 default, ratified).
   - **Without this webhook handler, sign-up is dead.** T-4 builder cannot skip this. Validator `fn-be-webhook-clerk-integration` will catch if missing.

3. **Pre-T-5 Chris manual checklist blocks T-5.** Chris needs to configure Clerk dashboard (8 items, ~5 min: publishable key, webhook endpoint, signing secret, testing token). If not done → T-5 cannot deploy because K8s secrets will be REPLACE_ME and auth will fail in production. T-6.a local smoke will catch it, but better to have Chris done pre-T-5.

4. **Anti-duplication validator is automated.** Validator nf-anti-duplication-scan will diff `nicolify/backend/src/modules/nicolify/admin/modules/{tenants,users}.py` against vitalia's. If diff < 50 lines → **FAIL**. You MUST rewrite vitalia-specific business logic (tenant UUID seed Vs nicolify's, user role enum difference, etc.). Copy-paste banned.

5. **FSD boundary sharp.** T-3 creates `features/dashboard/` — this feature owns its own api/ + components/ + hooks/ + types/. Dashboard should NOT import from other features. The arch fitness test runs `vitest run src/__tests__/architecture/` and enforces ESLint boundaries plugin. Violations will fail T-3.

6. **E2E split into T-6.a (local gate) + T-6.b (LIVE).** T-6.a runs pre-deploy with `E2E_BASE_URL=http://localhost:3002` — smoke tests middleware redirect + Clerk form render + dashboard. T-6.b runs LIVE at `https://dev-app.vitalialat.com` post-deploy with full suite (base + a11y + mobile + screenshots). Both MUST pass (both in must_pass validators list).

7. **Delete legacy step-{1,2,3}/ is part of T-2, not optional.** Spec says DELETE (decision D4 cement). Wizard unification already happened in Story 11. Leaving stubs = code rot + confusion for future devs. Clean them up.

**Recommended reading order (for T-1 kick-off):**
1. This CONTEXT-BRIEF (you're reading it).
2. 01-spec.md § Gherkin scenarios SC-01..SC-07 (FE/BE happy-path contract).
3. 05-guidelines.md § 1 (skills to load) + § 2 (files in scope whitelist) + § 3 patterns required.
4. 06-tickets.yaml (DAG, gherkin_coverage per ticket — your TDD anchor).
5. 04-validators.yaml (know the gates you're building against).

**One more thing:** This story is ready = Chris ratified + no dependencies blocking. You can start T-1 + T-4 immediately parallel. Don't wait. The critical path is T-4 (8h heaviest), so if T-4 hits any surprise (Streamlit permission model, Clerk SDK version incomp, db migrations), you'll feel it. Keep the team informed of T-4 progress — it's the gate to everything else.

## 15. Upstream canonical docs references

**Frameworks detected in scope (no WebFetch needed for context-builder brief; builder will read on-demand):**

| Framework | Canonical URL | Relevance | Read when |
|---|---|---|---|
| Clerk SDK (Next.js) | https://clerk.com/docs/quickstarts/nextjs | T-1 (middleware) + T-2 (SignIn/SignUp components) | Before T-1 implementation (clerkMiddleware API) |
| Clerk Backend API | https://clerk.com/docs/backend-requests/overview | T-4 (admin Streamlit, user creation via Clerk API) | Before T-4 admin.users.render_users implementation |
| Clerk Webhooks | https://clerk.com/docs/webhooks/overview | T-4 (webhook handler, HMAC validation) | Before T-4 webhook adapter implementation |
| Next.js 16 App Router | https://nextjs.org/docs/app | T-1 (middleware.ts location), T-2 (route groups) | Refresh if unsure route group location |
| Streamlit docs | https://docs.streamlit.io/ | T-4 (admin panel app structure) | Before T-4 admin/app.py + pages/{slug}.py structure |
| Streamlit authentication | https://docs.streamlit.io/deploy/tutorials/authentication | T-4 (super-admin Streamlit Authenticator) | Before T-4 _shared/auth.py implementation |
| FastAPI (context: existing in vitalia/backend) | https://fastapi.tiangolo.com/ | T-4 (webhook FastAPI route) | Refresh if unsure request.body / HMAC validation pattern |
| Pydantic v2 | https://docs.pydantic.dev/latest/ | T-4 (webhook input validation) | Refresh if unsure Pydantic v2 BaseModel|ValidationError |
| SQLAlchemy 2.0 async | https://docs.sqlalchemy.org/en/20/orm/queryguide/ | T-4 (admin DB operations) | Per backend-quality.md requirement; use async session |
| Playwright (E2E) | https://playwright.dev/docs/intro | T-6.a + T-6.b (E2E tests) | Before T-6 implementation; load playwright-expert skill |

**No upstream docs fetch needed for context-builder brief.** These are standard, well-documented frameworks. Builder will read as needed during implementation per TDD + just-in-time learning.

## 16. Self-budget snapshot

**Context-builder execution (this brief):**
- **Turns used:** 7 (init audit log + skeleton write, parallel reads, sections fill 1-6, sections fill 7-16)
- **Tools used:** Bash (init log), Read (6 story files), Write (skeleton), Edit (14 edits)
- **Files read:** 6 (01-spec.md, 03-arch-brief.md, 04-validators.yaml, 05-guidelines.md, 06-tickets.yaml, checkpoint.md)
- **Total chars read:** ~50k (story artifacts) + ~15k (system context rules/CLAUDE.md/AGENTS.md) = ~65k
- **Greps executed:** 0 (scope self-contained, no cross-codebase scan needed)
- **Web fetches:** 0 (frameworks standard, no upstream docs needed for brief)
- **Estimated tokens consumed:** ~8.5k (chars / 3.5 ≈ 18.5k input tokens + response ~500 output tokens)
- **Max budget available:** 200k tokens
- **Budget remaining post-brief:** ~191k tokens (validator can use for re-verification scans)

**Quality checkpoints:**
- ✅ Story files read (all 6 cover 01-spec through checkpoint)
- ✅ Brand overlay rules loaded (hipaa-lite.md § critical for T-4)
- ✅ Audit log created + timestamped (iter-1-2026-05-19T02:24:34Z.log)
- ✅ Skeleton written with 16 sections
- ✅ All sections filled with specific, actionable content (no vague placeholders remaining)
- ✅ Anti-duplication cross-reference complete (Nicolify admin pattern noted + validator cited)
- ✅ HIPAA-lite invariants extracted + cementadas (dual filter, audit log, sanitization)
- ✅ Gherkin scenarios mapped to tickets + tests
- ✅ Validator list exhaustive (19 validators from spec v2)

**Readiness for validator adversarial probe:**
- Brief is complete + faithful (no content loss, verbatim extracts for non-negotiable rules)
- All 16 sections populated (no _pending_ remaining)
- Cross-references verified (anti-dup § 7.5 cites validator nf-anti-duplication-scan by name from 04-validators.yaml line 72)
- Decision rationale cementadas (D1-D8, Q1-Q5 post-gaps audit 2026-05-18)

**Next: Validator run.** Validator will:
1. Re-run anti-duplication scan (keyword Nicolify admin vs vitalia admin)
2. Spot-check 3 claims from § 7 (existing systems) — verify paths exist
3. Re-fetch 1 upstream doc from § 15 — confirm still canonical URL
4. Write findings to CONTEXT-BRIEF-validation.md
5. Return verdict on brief faithfulness (clean | partial | blocking)
