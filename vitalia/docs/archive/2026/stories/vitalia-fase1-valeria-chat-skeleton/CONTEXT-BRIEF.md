<!-- voseo-allowed: context brief cita glosario voseo verbatim per R25 (.claude/rules/spanish-text.md § Magic comment escape) — referencia técnica de qué CORREGIR ("Empezá" → "Empieza"), no copy user-facing -->
# CONTEXT-BRIEF for vitalia-fase1-valeria-chat-skeleton (F1-S6)
> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder (Conv 2 autonomous build — ready package sealed architect_iter=1)
> Modules: shell-organism
> Faithfulness flag: clean (16/16 sections complete, zero blocking findings)
> Audit log: vitalia/docs/product/stories/vitalia-fase1-valeria-chat-skeleton/context-builder-logs/iter-1-2026-05-25T00:43:44Z.log
> Validator pass: R24 step_12 in progress (adversarial probe spawned or queued)

## 1. PR summary

**Story F1-S6 vitalia-fase1-valeria-chat-skeleton** (ready package closed 2026-05-24): FE-only organismo `ValeriaChat` para shell-organism Vitalia. Reemplaza placeholder `ValeriaChatSlot` (F1-S5) con componente real: estructura `ChatHeader` (evoluciona header slot con Mode Pill) + `ChatMessages` (role="log" aria-live, 6 mensajes mock, empty state branch) + `ChatComposer` (Textarea auto-resize, 3 icon stubs, Send button, Enter/Shift+Enter handlers). Zustand store separado `chat-store.ts` gestiona estado ephemeral (mock sendMessage, 800ms thinking delay, MOCK_RESPONSES rotativa determinista). Agent catalog SSoT (`lib/agent-catalog.ts`) define 6 agentes Vitalia (Lisa, Valeria, Adrián, Lucas, Camila, Mateo) consumidos cross-shell. **Zero engine touch, zero BE, zero agentic, zero cross-brand imports.** Mockup HTML ratificado Chris 2026-05-24. Deliverables: 10 NEW componentes + test files (T-1..T-6), 7 E2E specs Playwright + 4 visual goldens (T-7..T-9), 9h estimated via TDD strict.

## 2. Contract decisions

**Decisiones cardinales cementadas spec § 0 + 03-arch.md § 0:**

| ID | Decisión | Justificación | Status |
|---|---|---|---|
| **D1** | `ChatHeader` evoluciona del slot F1-S5: preserva avatar+name+status+dot, agrega Mode Pill "🤖 Modo agente" right-aligned + status text `"En línea · Tu secretaria virtual"` | Sin disruptión visual. Pill identifica modo agéntico para futuras stories (e.g., "📊 Modo informe" F2-S*). | ✅ Ratified Chris 2026-05-24 |
| **D2** | Mock send: user msg → 800ms `thinking` → canned bot response rotatita determinista (NO random). Responses `MOCK_RESPONSES[messageCount % len]` — predecible Playwright snapshots. | Determinismo crítico tests visuales. Variantes rotación por contador (18 combinaciones × 6 agentes futuros). | ✅ Spec § 5.4 ratified |
| **D3** | Composer adornments 📎/🎙️/⚡ stubs visuales (title="próximamente", NO disabled, sin handlers). Cobertura visual MVP completa. | Evita "shell vacío" demo. F2-S* implementa sin remock. | ✅ Ratified visual |
| **D4** | `DelegateMarker` formato: italic centered "→ delegando a [pill Camila] (modo Mantener)". "Mantener" = 1 de 3 modos Camila (Mantener/Reactivar/Multiplicar). | Context narrativo handoff sin tooltip. Coherencia voz Valeria. | ✅ Spec § 0 D4 |
| **D5** | `TypingIndicator` rico: bubble con texto `"{Agent} está {acción}"` + 3 dots animados (e.g., "Camila está abriendo Voz del paciente ..."). | Indica progresión substantiva, NO solo "wait". Pattern Slack/Telegram observable. | ✅ Spec § 0 D5 |
| **D6** | Spanish neutro hardcoded `MOCK_MESSAGES` (tuteo `"Tienes 8 turnos"`). F2-S11 (camila-voz) integrará compilador voz tenant; F1-S6 entrega esqueleto chrome mock. Excepción documentada sales-agent-brand-voice.md. | Mock ≠ output real agente. Fase 2 reemplaza con tenant voice. | ✅ Spec § 0 D6 |
| **D7** | Timestamps `HH:MM` debajo bubble (NO dentro), font `text-[10px] text-muted-foreground`. Agent name + timestamp bot ("Valeria · 09:01"); user solo time. | Mockup ratificado F1-S5 showroom. Coherencia. | ✅ Spec § 0 D7 |
| **D8** | Visual gate único: `mockups/valeria-chat-sample.html` com 2 variantes (6-msg chat + empty state) en mismo file. | Empty state cobertura v4.1 obligatoria. Un file SSoT visual. | ✅ Mockup HTML ratified |
| **D9** | `ChatHeader` + `MessageBubble` parametrizados `agent: AgentSlug` día 1 (NO hardcoded Valeria). Catalog 6 agentes. F1-S6 NO implementa selector; UI default Valeria. Arquitectura preparada futuro selector sin re-trabajo. | Chris ratifica futuro UX (Valeria default, cambiar a Lucas growth, volver Valeria). Design ahora = zero-cost; selector F1-S6 sería scope-creep. | ✅ Spec § 0 D9 ratified |

**Output contractual:** 03-arch.md § 2 FE Architecture + 04-validators.yaml § 7 test_construction_plan + 06-tickets.yaml 9 atomic tickets DAG.

## 3. UI spec decisions

**7 Gherkin scenarios 100% coverage (01-spec.md § 1 + 04-validators.yaml scenario_coverage):**

| Scenario | Type | Coverage | Key assertion |
|---|---|---|---|
| **SC-1** | happy | E2E + visual (populated light+dark) | 6 mocks render, ChatHeader visible, status "En línea · Tu secretaria virtual", Mode Pill |
| **SC-2** | happy | E2E (sendMessage flow) | user bubble + 800ms thinking + bot canned + ariа-live announce |
| **SC-3** | edge | E2E (keyboard) | Shift+Enter inserts newline (preservado whitespace-pre-wrap), Enter solo envía |
| **SC-4** | adversarial | E2E (XSS) | `<script>alert('xss')</script>` renders plaintext escaped, no dialog fires, DOM inspeccion confirma |
| **SC-5** | empty_state | E2E + visual (empty light+dark) | messages=[] → EmptyState ilustración + "Empieza una conversación" heading (corrected tuteo) + "Pregúntale" (tilde+clítico) subtexto |
| **SC-6** | accessibility | E2E + axe wcag2aa | Tab order, sr-only label, aria-live polite, contrast ≥4.5:1, zero violations |
| **SC-7** | i18n | E2E (Spanish neutro) | 30+ strings regex no voseo, tildes/eñes OK, cero PHI real patterns, aria-labels correcto |

**Visual variantes:** 4 PNG golden snapshots (populated/empty × light/dark @ 1280×800). Mockup-per-component protocol (vitalia/.claude/rules/shell-mockup-per-component.md) enforced — ratificación Chris pre-arch MANDATORY. Ratified 2026-05-24 via `valeria-chat-sample.html`.

## 4. Module current-state extracts

**Module:** `shell-organism` (brand-local Vitalia frontend chrome, UI-only, NO cross-brand).

Módulo doc: `vitalia/docs/product/modules/shell-organism.md` (auto-list block tracks capabilities, updated post-merge via `scripts/reconcile_capabilities.py --brand vitalia`).

**Current-state post F1-S5 done (2026-05-24):**
- Componentes cementados: TopBarGlobal, LogoMark, TenantSwitcher, ThemeToggle, ShellOrganismLayoutClient (50/50 grid), ValeriaSidebar (rail+history wrapper), ValeriaChatSlot (placeholder)
- Hooks cementados: useKeyboardShortcuts (Cmd+K focus)
- Stores cementados: shell-store.ts (valeriaState={collapsed,rail,full} + valeriaMode={agent,...})
- **F1-S6 agregará:** ValeriaChat organismo, ChatHeader/ChatMessages/ChatComposer moléculas, MessageBubble/TypingIndicator/DelegateMarker átomos, chat-store zustand NEW (separado shell-store SoC), agent-catalog.ts SSoT (6 agentes)

**NO modificaciones shell-store.ts, NO modificaciones heredadas F1-S1..F1-S5, solo puntual swap ValeriaChatSlot → ValeriaChat en ValeriaSidebar.**

Arch test heredado `test-shell-store-schema-readonly-f1-s5.test.ts` verifica invariante.

## 5. Relevant rules

**Root rules mandatory (apply all brands):**
- `.claude/rules/frontend-fsd.md` — FSD-Lite: shell-organism en `components/shared/`, agent-catalog en `lib/`, chat-store en `stores/`. NO cross-feature imports. NEW lib/agent-catalog.ts consumido cross-shell OK.
- `.claude/rules/frontend-quality.md` — ESLint 60+ rules, TS strict, Vitest 20% coverage threshold. val-fe-tsc + val-fe-lint + val-fe-prettier mandatory per ticket.
- `.claude/rules/spanish-text.md` — Spanish neutro: 30+ microcopy sin voseo. Spec § 6 correcciones "Empezá"→"Empieza", "Preguntale"→"Pregúntale" tilde obligatorias (repo code, no mockup copy).
- `.claude/rules/anti-duplication.md` — cross-brand mirror scan executed clean (0 matches ValeriaChat/ChatHeader/etc in nicolify/comunify/lupulo). LIFT CANDIDATE docs post-merge cuando 2do brand consumer.
- `.claude/rules/tdd-mandatory.md` — RED tests primero per capa (catalog+token → store+mock → átomos → composer → organism → integration). T-1..T-6 production code todos TDD.
- `.claude/rules/e2e-testing.md` — Playwright NATIVE Linux (no Docker), port 3002 vitalia, preflight obligatorio. T-7..T-9 E2E suite.
- `.claude/rules/tenant-isolation.md` — URL [tenantId] propaga Next routing; middleware Clerk upstream gates. F1-S6 NO API calls, only mock.

**Brand overlay rules (apply vitalia only):**
- `vitalia/.claude/rules/hipaa-lite.md` — PHI scope: `not_applicable` (shell chrome UI sin datos médicos reales). Mock Marina Pérez + Dr. Juan García son nombres ficticios mockup ratificados, NO PHI.
- `vitalia/.claude/rules/shell-mockup-per-component.md` — Protocolo bloqueante visual ratificación Chris PRE-architect. F1-S6 ratified 2026-05-24 vía valeria-chat-sample.html.

**Critical enforce:** architect REFUSE sin `checkpoint.md::ratified_visual_by_chris == true`.

## 5.5 Domain skill invariants (SSoT extracts)

**Skills MUST load (05-guidelines.md § 1):**

| Skill | Critical hard rules + SSoT (extract) | Enforced by |
|---|---|---|
| `frontend-expert` | Server-First default; `'use client'` solo en hojas con state/hooks. FSD-Lite: `shared/shell-organism/` cross-feature OK, `lib/` canonical para registries (agent-catalog SSoT). useEffect deps complete, no stale closures (sendMessage zustand action, NOT useEffect). Hydration safe. | test_server_first.test.ts allowlist extend shrink-only |
| `tessl__react-patterns` | React 19 `e.isComposing` IME guard (composer onKeyDown). `useRef<HTMLTextAreaElement>` auto-resize setup. No stale closures en setTimeout callbacks (store action handles 800ms delay, NOT inline setMessage effect). | ChatComposer.test.tsx spec handlers |
| `tessl__shadcn-ui` | REUSE F1-S0 installed: Textarea (rows=1 manual auto-resize), Button (variant default), Badge (variant outline Mode Pill). Versiones cementadas — NO upgrade per story. | no edits ui/ primitives |
| `tessl__tailwind` | Semantic tokens ONLY: `bg-card`, `border-border`, `text-foreground`, `text-muted-foreground`, `bg-agent-{slug}`, `bg-agent-{slug}-soft`, `bg-vitalia-success`. NO hex literals componentes (except agent-catalog.ts hex metadata field). NO arbitrary values salvo `max-h-[100px]`. | test_no_hardcoded_colors.test.ts |
| `tessl__vitest` | RTL + `@testing-library/jest-dom`. Mock `useChatStore` via `setState({})` + `getState().clearMessages()` afterEach. Time mocking `vi.useFakeTimers()` + `vi.advanceTimersByTime(800)` sendMessage 800ms. NO `vi.mock('@/stores/chat-store')`. | T-1..T-6 unit tests |
| `playwright-expert` | POM en `e2e/shell-organism/poms/` (F1-S6 new path vs F1-S5 legacy `e2e/pages/`). Fixtures shared (shell-theme REUSE, clerk-auth REUSE, chat-store-seed NEW). `page.addInitScript` seed (`window.__chatStoreSeed__`). axe-playwright wcag2aa ruleset. Visual goldens side-by-side mockup iter 1 post Chris. | T-7..T-9 E2E |

**No cargar:** backend-expert, copilot-expert, sales-agent-expert, brand-expert, offer-expert, etc. (FE-only chrome, no BE/agentic/domain).

## 6. Git diff summary

**Current branch:** `wip/vitalia` (story-specific branch does not yet exist; `/dev-team` will create `wip/vitalia-valeria-chat-skeleton` per `scripts/git/new-session.sh`).

**Post-merge diff expected (git diff main..developed):**
- **NEW files (28 paths):** agent-catalog.ts + test, chat-store.ts + test, _mock-messages.ts, ValeriaChat + ChatHeader + ChatMessages + ChatComposer + MessageBubble + TypingIndicator + DelegateMarker (7 tsx + 3 test files), test-agent-catalog-ssot.test.ts (NEW arch test), 7 E2E specs (happy/send/keys/xss/empty/a11y/i18n/visual), POM + fixture, 4 PNG golden snapshots
- **MODIFY files (4 paths):** ValeriaSidebar.tsx (swap slot 2 occurrences), globals.css (add --agent-mateo-soft + .typing-dot keyframes), test-no-cross-brand-shell-mirror.test.ts (extend names), test_server_first.test.ts (extend allowlist)
- **DELETE files (2 paths):** ValeriaChatSlot.tsx + .test.tsx (legacy F1-S5 placeholder)
- **Total estimated:** ~2150 LOC new (1500 production, 650 tests), ~50 LOC modified, ~40 LOC deleted
- **R23 production_code applicability:** T-1..T-6 production code (FE only, Sonnet/opencode eligible, zero Opus). T-7..T-9 tests-only (fast-tracked Sonnet).

Per `.claude/rules/git-safety.md` triple-branch: story branches as wip/{slug}, squash-merge to main per story-closure-gate.md Fase F after auditor APPROVED.

## 7. Existing systems detected (NO-NEW-LAYER scan)

**Cross-brand anti-duplication scan (03-arch.md § 0.1 audit):**

Grep executed for ValeriaChat/ChatHeader/ChatMessages/ChatComposer/MessageBubble/TypingIndicator/DelegateMarker/useChatStore/AGENT_CATALOG across nicolify/comunify/lupulo backends/frontends. **Result: 0 exact name matches** (cross-brand HARD BAN satisfied per anti-duplication.md).

**Nicolify reference pattern (read-only):** CopilotSidebar exists at `nicolify/frontend/src/features/copilot/components/CopilotSidebar.tsx`. Conceptually similar chat-bubbles pattern BUT scope distinct (Nicolify copilot floating tooltip ≠ Vitalia agentic shell-panel organism). **NOT IMPORTED** per HARD BAN. Lift candidate after 2nd brand consumer.

**Existing Vitalia systems:**
- `useShellStore` zustand (F1-S4/S5 done) — **NOT CONSUMED** by F1-S6. Chat uses separate store by SoC. Arch test `test-shell-store-schema-readonly-f1-s5.test.ts` invariant heredado bloquea modificación.
- `ValeriaChatSlot` placeholder (F1-S5) — **DELETE**, reemplazado `ValeriaChat` real F1-S6
- `ValeriaSidebar` organism (F1-S5) — **MODIFY puntual**: import swap + 2 JSX swaps solo. NADA más.
- Shadcn primitives (F1-S0) — **REUSE ONLY** Textarea/Button/Badge (copy-paste local, NO edit).
- Agent thumbnails `public/agents/{slug}/{thumbnail,transparent}` (×6) — **REUSE READ-ONLY**, sync verificado md5 2026-05-24.
- CSS tokens (F1-S1) — **VERIFY + MODIFY D9**: `--agent-mateo-soft` light + dark + `.typing-dot` keyframes missing.

**Cross-codebase scope (engine + brands): 0 matches.**

## 7.5 Anti-duplication inventory cross-reference

**Reading `.claude/rules/anti-duplication.md` Inventory SSoT (engine shared abstractions):** subsystem chat-organism pattern — **NOT listed in inventory** (engine packages focus observability/LLM/events/billing/extraction, not UI). Pattern first-ocurrence Vitalia F1-S6 = **NEW correctly**.

Future cross-brand lift (#2 consumer) → promotion candidate `/pm-luana` proposal `core/@luana/shell-chat-organism/` + `core/@luana/agent-catalog/` (post-merge learning doc).

| Subsystem in F1-S6 | Listed in anti-dup inventory? | Canonical path | Recommendation |
|---|---|---|---|
| `ValeriaChat` organismo + moléculas | NO | N/A (first occurrence Vitalia) | NEW correctly. LIFT CANDIDATE post 2° brand consumer. |
| `agent-catalog` 6-agent registry | NO | N/A (first occurrence Vitalia) | NEW correctly. Future core-lift candidate. |
| `chat-store` zustand ephemeral | NO | N/A (first occurrence Vitalia) | NEW correctly. No cross-brand duplication risk (agentic patterns). |
| Shadcn `Textarea`/`Button`/`Badge` | N/A (UI primitives, not subsystem) | `vitalia/frontend/src/components/ui/` | REUSE installed F1-S0. |

**Severity:** clean (0 HIGH findings). Candidate lift cases documented for future escalation, not blocking F1-S6.

## 8. EXTEND vs NEW recommendations

Per `anti-duplication.md` decision tree:

| Surface F1-S6 proposes | Existing system ≥80% overlap (§7) | In anti-dup inventory? (§7.5) | Recommendation | Reason |
|---|---|---|---|---|
| `ValeriaChat` organism + children | NO existing match (first Vitalia UI chat shell) | NO | **NEW** — first occurrence, zero cross-brand conflict. LIFT CANDIDATE post 2° brand. | Zero existing subsystem, spec § 13 deliverable list cites NEW. |
| `agent-catalog.ts` 6-agent registry | NO existing match (Nicolify CopilotSidebar has no equivalent registry) | NO | **NEW** — first occurrence SSoT. Future cross-brand lift candidate. | Catalog pattern new-to-vitalia, no prior art cross-brands Luana. |
| `chat-store.ts` zustand | NO existing match (shell-store separate per SoC) | NO | **NEW** — separate store by design (shell ≠ chat lifecycle). | Shell-store immutable (F1-S5 invariant). Chat ephemeral per Spec § 5.5. |
| `ChatComposer` textarea auto-resize | Shadcn Textarea reusable (F1-S0 installed) | N/A (primitive reuse) | **EXTEND** Textarea wrapper component (ChatComposer) — composition, not mirror | Shadcn primitive consumed, not duplicated. |

**No REPLACE cases identified. ALL NEW correctly justified.**

## 9. Architecture fitness gates

**Tests corre post-build (all per-brand vitalia frontend):**

```bash
cd vitalia/frontend

# Non-functional gates (val-fe-*)
npx tsc --noEmit                        # val-fe-tsc
npx eslint src/components/shared/shell-organism src/lib src/stores src/__tests__/architecture --cache
npx prettier --check '...'             # val-fe-prettier
npx vitest run src/components/... --coverage --coverage.thresholds.lines=20   # val-fe-vitest-unit

# Architectural tests (val-fe-arch-*)
npx vitest run src/__tests__/architecture/test_fsd_boundaries.test.ts          # FSD: no cross-feature imports
npx vitest run src/__tests__/architecture/test-no-cross-brand-shell-mirror.test.ts  # 9 NEW names no matches nicolify/comunify/lupulo
npx vitest run src/__tests__/architecture/test-shell-store-schema-readonly-f1-s5.test.ts  # chat-store separate
npx vitest run src/__tests__/architecture/test-agent-catalog-ssot.test.ts    # NEW: no hardcoded agent names/colors fuera catalog
npx vitest run src/__tests__/architecture/test_server_first.test.ts          # 'use client' allowlist extend
npx vitest run src/__tests__/architecture/test_no_hardcoded_colors.test.ts   # NO hex literals (except metadata)
npx vitest run src/__tests__/architecture/test-no-vt-classes-in-new-features.test.ts  # NO legacy .vt-* 
npx vitest run src/__tests__/architecture/test-vitalia-ui-strings-no-voseo.test.ts   # Spanish neutro regex

# E2E gates (val-fe-e2e-*, val-fe-visual-*, val-fe-axe)
E2E_BASE_URL=http://localhost:3002 npx playwright test shell-organism/valeria-chat-*.spec.ts  # 8 specs
E2E_BASE_URL=http://localhost:3002 npx playwright test --project=visual shell-organism/valeria-chat-visual.spec.ts --update-snapshots  # 4 PNG post Chris ratify iter 1
```

**Shrink-only ratchet:** architecture allowlists only grow (NEW files added), never shrink without justification (commit body cite removals).

## 10. Implementation log highlights

**Per 06-tickets.yaml 9 atomic tickets T-1..T-9:**

- **T-1** (1.5h, 120 LOC): agent-catalog.ts SSoT 6 agentes + globals.css --agent-mateo-soft light+dark + .typing-dot keyframes + optional tailwind.config.ts verify mateo entries. RED agent-catalog.test.ts cases → GREEN. Arch gates: tsc, lint, vitest, arch-fsd, arch-no-hex.

- **T-2** (2h, 180 LOC): chat-store.ts zustand (messages, activeAgent, status, sendMessage mock 800ms flow, clearMessages, setActiveAgent) + _mock-messages.ts (6 MOCK_MESSAGES stable IDs + MOCK_RESPONSES_BY_AGENT Valeria 4 canned). RED chat-store.test.ts determinism/idempotency cases → GREEN. Depends T-1 (AgentSlug type). Arch gates: tsc, lint, vitest, arch-fsd, arch-no-voseo.

- **T-3** (3h, 280 LOC): MessageBubble (bot/user variants, XSS text-children, onError avatar fallback) + ChatHeader (avatar+name+status dot+Mode Pill, parametrized agent) + TypingIndicator (bubble+texto+3 animated dots) + DelegateMarker (italic centered pill). RED per componente → GREEN unit tests. Depends T-1. Arch gates: tsc, lint, vitest, arch-fsd, arch-no-hex, arch-no-voseo, arch-no-vt, arch-server-first.

- **T-4** (2.5h, 180 LOC): ChatComposer (Textarea rows=1 auto-resize useEffect, 3 icon stubs emoji, Send button, onKeyDown Enter/Shift+Enter/isComposing guards, sendMessage hook). RED ChatComposer.test.tsx keyboard+idempotency → GREEN. Depends T-2 (useChatStore). Arch gates: tsc, lint, vitest, arch-fsd, arch-no-hex, arch-no-voseo, arch-no-vt, arch-server-first.

- **T-5** (3.5h, 280 LOC): ValeriaChat organism (role="region" aria-label, grid grid-rows-[auto_1fr_auto], ChatHeader+ChatMessages+ChatComposer) + ChatMessages (role="log" aria-live="polite", branch messages.length === 0 vs populated, auto-scroll sentinel useEffect [messages.length], switch role → render MessageBubble|DelegateMarker|TypingIndicator). RED ValeriaChat.test.tsx integration cases → GREEN. Depends T-2, T-3, T-4. Arch gates: tsc, lint, vitest, arch-fsd, arch-no-hex, arch-no-voseo, arch-no-vt, arch-server-first.

- **T-6** (1.5h, 150 LOC): MODIFY ValeriaSidebar.tsx (import swap, 2 JSX occurrences swap, NADA más) + DELETE ValeriaChatSlot{,.tsx} legacy + extend test-no-cross-brand-shell-mirror.test.ts (9 NEW names) + extend test_server_first.test.ts allowlist + NEW test-agent-catalog-ssot.test.ts (invariant: no hardcoded agent strings fuera catalog + _mock-messages allowlist). Depends T-5. Arch gates: tsc, lint, vitest, arch-fsd, arch-no-cross-brand-mirror, arch-shell-store-readonly-heredado, arch-agent-catalog-ssot, arch-server-first, arch-skip-link.

- **T-7** (2.5h, 220 LOC): POM ValeriaChatPage (methods: goto, seedChatStore/Empty, getMessage, getDelegateMarker, getThinkingIndicator, getChatHeader, getModePill, getComposer, getSendButton, sendMessage, clearMessages, getEmptyState, setViewport, setTheme) + fixtures chat-store-seed.fixture.ts (addInitScript window.__chatStoreSeed__ MOCK_MESSAGES or []). Depends T-6. Arch gates: tsc, lint, prettier (tests-only, no unit tests).

- **T-8** (3h, 360 LOC): 4 E2E behavior specs (SC-1 happy render, SC-2 send mock flow, SC-3 Shift+Enter keys, SC-4 XSS adversarial). Depends T-7. Arch gates: val-fe-e2e-{happy,send,keys,xss}.

- **T-9** (3.5h, 380 LOC): valeria-chat-{empty,a11y,i18n,visual}.spec.ts + 4 PNG golden snapshots (populated/empty × light/dark). SC-5 empty state, SC-6 a11y + axe wcag2aa, SC-7 i18n regex no voseo, visual goldens side-by-side mockup iter 1 POST CHRIS RATIFY. Depends T-8. Arch gates: val-fe-e2e-{empty,a11y,axe,i18n} + val-fe-visual-{populated,empty}-{light,dark}.

**Total 23h, 2150 LOC, DAG parallelizable (Wave 1: T-1+T-2; Wave 2: T-3 after T-1, T-4 after T-2; Wave 3: T-5; Wave 4: T-6; Wave 5: T-7; Wave 6: T-8; Wave 7: T-9 post Chris).**

## 11. Faithfulness gaps + validator findings

**Audit log scan (iter-1 2026-05-25T00:43:44Z):**
- All 5 priority files read in parallel ✅ (01-spec.md, 03-arch.md, 04-validators.yaml, 05-guidelines.md, 06-tickets.yaml)
- Cross-brand anti-duplication grep clean ✅ (0 matches 9 NEW component names)
- Mockup HTML ratification verified ✅ (valeria-chat-sample.html Chris 2026-05-24)
- Skill loads enumerated ✅ (6 must-load, 8 must-NOT-load per 05-guidelines § 1)
- No architect-run conflict (ready package sealed; post-build validator will catch errors)
- Milestone PRE-CONDITION: Chris ratify visual goldens iter 1 BEFORE `--update-snapshots` (§10 T-9 notes)

**Zero blocking findings. Partial coverage: §15 upstream canonical docs deferred to builder (fetch React 19 + Next.js 16 + Zustand + Tailwind v4 + Playwright official docs on-demand per skill load).**

## 12. Raw paths consulted

File list (audit log ref):
1. `vitalia/docs/product/stories/vitalia-fase1-valeria-chat-skeleton/01-spec.md` (556 lines, v3 ratified)
2. `vitalia/docs/product/stories/vitalia-fase1-valeria-chat-skeleton/03-arch.md` (633 lines consolidado FE)
3. `vitalia/docs/product/stories/vitalia-fase1-valeria-chat-skeleton/04-validators.yaml` (367 lines v4.1 schema)
4. `vitalia/docs/product/stories/vitalia-fase1-valeria-chat-skeleton/05-guidelines.md` (264 lines must-load/forbidden)
5. `vitalia/docs/product/stories/vitalia-fase1-valeria-chat-skeleton/06-tickets.yaml` (569 lines DAG)
6. `vitalia/docs/architecture/SHELL-DESIGN-CONTRACT.md` (referenced § 2.4/3.3, NOT read detail — used in 03-arch § 0.1 audit)
7. `vitalia/.claude/rules/hipaa-lite.md` (referenced § 4 PHI scope)
8. `vitalia/.claude/rules/shell-mockup-per-component.md` (referenced § 2 visual gate protocol)
9. `.claude/rules/frontend-fsd.md`, `.claude/rules/frontend-quality.md`, `.claude/rules/spanish-text.md`, etc. (referenced § 5 rules table, inline extracts)
10. `vitalia/frontend/public/agents/{slug}/*` (referenced § 5.2 asset sync, NOT downloaded)

Cross-brand paths checked (grep, not read):
- `nicolify/frontend/src/features/copilot/components/CopilotSidebar.tsx` (reference pattern scan)
- `comunify/frontend/src`, `lupulo/frontend/src` (anti-duplication sweep, 0 matches)
- `core/luana-core-*/` (engine touch scan, 0 matches — FE-only story)

## 13. Verbatim grep + WebFetch commands executed

**Anti-duplication cross-brand mirror scan (03-arch.md § 0.1 audit commands):**
```bash
for b in nicolify comunify lupulo; do
  for name in ValeriaChat ChatHeader ChatMessages ChatComposer MessageBubble TypingIndicator DelegateMarker useChatStore AGENT_CATALOG; do
    matches=$(grep -rln "$name" ${WS}/$b/frontend/src 2>/dev/null | wc -l)
    [ $matches -gt 0 ] && echo "$b::$name → $matches matches" || true
  done
done
# Result: 0 matches all brands (Nicolify CopilotSidebar exists but NO exact name match)
```

**Nicolify pattern scan:**
```bash
ls ${WS}/nicolify/frontend/src/features/copilot/components/CopilotSidebar.tsx
# Result: EXIST (read-only reference, NO IMPORT)
```

**Upstream canonical docs (framework detection):** Deferred to builder per skill loads (React 19, Next.js 16, Zustand 5, Tailwind v4, Playwright, Vitest docs loads on-demand per 03-arch.md § 0 architect note "library currency verified live 2026-05-24").

**WebFetch:** NOT EXECUTED (no web fetch needed; mockup HTML is local SSoT; upstream docs canonicals are well-known).

## 14. Free-form notes — what I'd tell a smart colleague

**Architecture is crystalline.** F1-S6 is the purest FE-only story in the series: zero BE, zero agentic, zero engine, zero cross-brand. It's a 2150-LOC complement of the shell organism spec finalized F1-S5 (the placeholder chat slot felt like a placeholder because it WAS—just a day old test wrapper). Now F1-S6 delivers the real organism structure with proper component contracts.

**Mock data is deterministic, which matters.** The `MOCK_RESPONSES[count % len]` pattern ensures Playwright golden snapshots won't flake. Chris ratified the mockup HTML 2026-05-24, so visual ratchet is locked. Builders will confirm parity once components mount.

**Zustand store separation is surgical SoC.** Chat lifecycle (ephemeral, user-driven) is orthogonal to shell layout state (persistent, UI chrome). No bleeding. The store-per-concern pattern keeps future Phases 2+ wiring WebSocket + sales_agent backend clean.

**Agent catalog is first-occurrence lift candidate.** Once Nicolify needs a chat-with-agents pattern (unlikely P2, but possible), this SSoT becomes shareable. Codified in learning doc post-merge. No blocker for F1-S6.

**Spanish neutro corrections are non-trivial.** The spec § 6 explicitly marks "Empezá" → "Empieza" and "Preguntale" → "Pregúntale". The mockup HTML references the VOSEO versions but builders MUST use the corrected tuteo. Arch test catches drift.

**Gateway: Chris visual ratification T-9 iter 1.** The critical path bottleneck is that visual goldens (4 PNG snapshots) cannot be generated until mockup is side-by-side validated with actual React components post-T-8. Wave 7 (T-9) cannot start in earnest until that gate opens. /pm-vitalia coordinates timing.

**No gotchas seen.** Ready package is airtight. Tests are comprehensive. Validators are enforceable. Builders can trust the spec and run T-1..T-9 DAG.

## 15. Upstream canonical docs references

**Frameworks detected + canonical URLs (per 03-arch.md § 0 architect note—library currency verified 2026-05-24):**

| Framework | Canonical URL | Relevance to F1-S6 |
|---|---|---|
| React 19 | https://react.dev/reference (App Router + Server Components, `use client` boundary rules) | `useRef`, `useEffect` deps, hooks contract, hydration |
| Next.js 16 App Router | https://nextjs.org/docs/app (static asset serving, dynamic routes, Server Components) | `public/agents/{slug}/` paths, no new routes, boundary decisions |
| Zustand 5 | https://github.com/pmndrs/zustand (create, getState, setState selectors) | chat-store contract, mock patterns, no persist middleware |
| Tailwind v4 | https://tailwindcss.com/docs (semantic tokens via CSS vars, JIT safelist, dynamic classes) | `bg-agent-{slug}` class name handling, NO template literals |
| Shadcn UI | https://ui.shadcn.com/docs (primitives: Textarea, Button, Badge) | copy-paste local, no upgrade, onError patterns |
| Playwright | https://playwright.dev/docs (POM, fixtures, axe-core integration) | E2E E2E suite architecture, visual goldens iter 1 protocol |
| Vitest | https://vitest.dev/guide (RTL, mocking, fake timers) | mock `useChatStore`, `vi.advanceTimersByTime(800)` sendMessage flow |
| Axe-core WCAG 2.1 | https://github.com/dequelabs/axe-core (wcag2aa ruleset, contrast rules) | a11y gate val-fe-axe, zero violations target |

Detalle extraído de skills SSoT (frontend-expert, tessl__* variants, playwright-expert) en 03-arch.md § 0 architect note — canonical docs loaded on-demand per builder skill bootstrap step.

## 16. Self-budget snapshot

**Haiku 4.5 context budget usage (iter-1 2026-05-25):**
- **Turns used:** ~3 (init + parallel reads + audit log + edits)
- **Files read:** 5 primary (01-spec, 03-arch, 04-validators, 05-guidelines, 06-tickets) + ~12 reference (rules, related specs, prior stories)
- **Total bytes parsed:** ~250kb (primary ~180kb + rules/references ~70kb)
- **Greps executed:** 9 anti-duplication cross-brand scans + engine touch scan = 10 bash commands (logged)
- **Web fetches:** 0 (upstream canonical docs deferred to builder; mockup HTML local)
- **Output artifacts:** CONTEXT-BRIEF.md (16 sections filled), audit log (iter-1 timestamp)
- **Estimated tokens consumed:** ~65k (reads + edit + log writes)
- **Remaining budget:** ~135k (healthy headroom for validator adversarial probe + context sealing)

**Completion state:** Skeleton → sections 1-16 FILLED (no _pending_ markers remain). Ready for validator-runner spawn.
