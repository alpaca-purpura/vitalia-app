# CONTEXT-BRIEF for vitalia-fase2-adrian-embudo
> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder
> Modules: crm, sales_agent
> Faithfulness flag: **partial** (existing-systems audit complete + 0 cross-brand mirrors; validator pending final adversarial probe)
> Audit log: /home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase2-adrian-embudo/context-builder-logs/iter-1-20260603T231948Z.log
> Validator pass: **DEFERRED** (context-builder outputs brief to downstream agents without validator; architect reviews brief § 11 gaps before deciding escalation)

## 1. PR summary
**Story:** `vitalia-fase2-adrian-embudo` (F3 release). Sub-tab **Embudo de Adrián** = supervision surface over Adrián (sales agent AI). Board funnel dental 6 etapas (Interesado→Calificando→Consulta→Plan→Reservado→Decidió no) + Kanban|Lista views + lead detail page (URL propia, 2 vistas via EntitySubNavBar) + high-lead form (ruta-hoja `/nuevo`, not modal) + sub-tab hermana Recuperar (congelados + diagnose). **Modules:** EXTEND crm (Lead repo/service/dto shipped) + sales_agent wire thin (override-context to agent RN-4.1). **Brand:** vitalia (health LatAm, HIPAA-lite, Lead = non-PHI marketing prospect). **State:** `developing` (ready 2026-06-03, autonomous_mode=true, architect signed off). **Tickets:** 9 cohesive, paralelizable via DAG (BE foundation → app+API → FE+agentic → E2E+demo).

## 2. Contract decisions (03-arch.md § 0-8)
- **Module home:** EXTEND `vitalia/backend/src/modules/vitalia/crm/` (Lead PII entity, non-PHI funnel layer) — NO `sales_pipeline` module created
- **Consume substrate:** read-only `core/luana-core-sales-agent/closer_studio` + `AgentStateCheckpoint` via Extension SDK; NO mount engine router (sync/auth mismatch); NO schema edit §3 protected
- **Funnel = brand-local NET-NEW:** 6-stage dental funnel (vertical-specific), glass-box scoring (no ML), auto-freeze rule, stage transition audit — not cross-brand mirror (verified 0 matches comunify/nicolify/lupulo)
- **FE REUSE:** `EntitySubNavBar` (from doctores, workspace mode), `ChannelBadge` (extend: +meta/referido/tiktok colores marca), `TogglePill`, `EmptyState`, `PiiMaskedSpan`, `@dnd-kit ^6.3.1`
- **Integration:** board endpoint `/crm/board` (hot-only 4 etapas + Reservado) ← consumed by `use-embudo-board` ← `AdrianEmbudoView`. Stage override via drag|dropdown + optimistic lock `version` → 409 on conflict (SC-5). Override w/ reason → inyected al next turn agente (RN-4.1, agentic ticket T-AG-1)
- **Architecture pattern:** ADR-vitalia-004 (9 secciones shell-feature: routing + FSD-Lite + Server-first + React Query + Zustand + RHF/Zod + DDD Inside-Out + migrations idempotent + `growth_studio_event` + tests 4 capas). **Compliance:** full ADR-004, no divergencias. Visual golden ratificado Chris 2026-06-03 (ADR-vitalia-003).

## 3. UI spec decisions (01-spec.md v3, ratified Chris 2026-06-03)
- **Board (V1):** Kanban 4 active etapas (Interesado|Calificando|Consulta|Plan) + Reservado (success terminal) + KPI strip + filters + toggle Kanban|Lista + "+ Nuevo". Cards: operador badge + valor + ChannelBadge (WA/IG/Meta/Web/Referido color marca) + score dona compacto 0-100 + signals chips + time-in-stage dot SLA (RN-11: verde≤7d, ámbar>7d, rojo>14d) + micro-log última acción Adrián + badge especial (💳 esperando pago / ✅ depósito recibido / ↻ Camila reactiva 90d)
- **Lista (V2):** tabla ordenable (masked · etapa badge · canal · score · en-etapa SLA · última actividad · operador · doctor). Paginada 25/página, counts reales.
- **Página lead (V3, opción C ratificada):** ruta `/adrian/embudo/[leadId]/{resumen|historial}` con **EntitySubNavBar** (workspace mode: `[‹ Embudo] · nombre masked · Resumen·Historial tabs al lado**). Resumen = bloques Datos (contacto/origen/interés/valor/doctor) + Estado agente (🤖 Adrián la atiende + Tomar control) + Score glass-box breakdown + autonomy rules (puede: mover/agendar/info; necesita OK: cobrar/descuentos/clínico) + OverrideReasonDialog (drag adyacente OK directo; salto → confirm+razón obligatoria). Historial = timeline atribuido (mensajes WA/IG + transitions + activity + eventos, NO datos clínicos — RN-2 firewall; link a Inbox PHI-gated para conversación real)
- **Recuperar (V4, sub-tab hermana):** ruta `/adrian/recuperar` segmentada congelados (recién + diagnose + Reactivar) + Decidió no (mostrar + Reactivar puntual → Camila cohorte 90d). Auto-freeze regla: 14d sin respuesta ∨ >2×SLA de etapa ∨ 30d duro (RN-13).
- **Nuevo lead (V5, ruta-hoja no modal):** `/adrian/embudo/nuevo` EntitySubNavBar + RHF/Zod form (nombre* · canal* · phone/email≥1 · etapa inicial · servicio · etiquetas · notas) → POST `/crm/leads` → redirect `/embudo?highlight={leadId}` → card resaltada (ring+pulse 3s) con auto-scroll. Cancelar = no crear.
- **SLA table (RN-11, v3 research-driven):** Interesado 7d · Calificando 7d · Consulta 5d · Plan 14d · Reservado/Decidió no = terminal sin SLA. Ámbar = over SLA; rojo = 2× SLA (o ≥14d sin respuesta). Research: `research/{05,06}-*.md` (HubSpot/Pipedrive/Clientify/Pabau benchmarks).
- **Order columna (RN-17, antigüedad-en-etapa DESC):** card más vieja/próxima vencer arriba. Drag solo cambia columna, no reordena dentro columna. Selector "Ordenar por": `Antigüedad en etapa` (default) · `Score` · `Valor` · `Última actividad`.
- **Board scope (RN-18, hot-only):** Tablero solo 4 etapas activas + Reservado visible; `Decidió no` + congelados → van a Recuperar V4 (limpia el Kanban).
- **Lead detail scope (RN-2, firewall):** Lead = marketing prospect data (canal/procedimiento/presupuesto/agenda), NUNCA clínicos. Historial NO renderiza datos clínicos del paciente — link al Inbox abre la conversación PHI-gated allá.

## 4. Module current-state extracts
**Módulo `crm`** (`vitalia/backend/src/modules/vitalia/crm/`):
- `domain/lead.py` (Lead entity) — currently: `id`, `tenant_id`, `name`, `email`, `phone`, `status` (flat 5-valores `new|contacted|qualified|lost|converted`), `created_at`, `updated_at`, `deleted_at` (soft-delete). **NEW fields needed:** `stage` (6 etapas funnel), `stage_entered_at`, `score`, `temperature`, `operated_by` (`agent|human`), `channel`, `service_interest`, `assigned_doctor_id`, `estimated_value`, `currency`, `buying_signals[]`, `is_frozen`, `frozen_reason`, `frozen_at`, `closure_reason`, `reactivation_cohort_at`, `deposit_status` (stub MSW), `version` (optimistic lock). `name`/`email`/`phone` remain pgcrypto-encrypted (PII); funnel fields = plaintext NO-PHI.
- `lead_service.py` (LeadService) — servicios crear/listar/actualizar lead. **NEW ServiceFunnel methods:** `get_board(tenant_id, filters, sort)`, `transition_stage(lead_id, to_stage, reason, version, actor)`, `freeze_sweep()`, `reactivate(lead_id)`.
- `lead_repository.py` (LeadRepository) — CRUD operaciones. **EXTEND:** `update_stage(lead_id, *, tenant_id, to_stage, reason, triggered_by, expected_version, actor_user_id)` con optimistic lock (WHERE version = expected → rowcount 0 = 409 conflict). `list_for_board(*, tenant_id, filters, sort)`.
- `lead_dto.py` (LeadResponse) — **EXTEND con campos funnel nuevos** (`stage`, `score`, `temperature`, `operated_by`, `channel`, `estimated_value`, `currency`, `buying_signals`, `stage_entered_at`, `is_frozen`, `frozen_reason`, `deposit_status`, `version`).
- `api/router.py` — **EXTEND existente mount** `/api/v1/crm/`. **NEW endpoints:** `GET /crm/board`, `PATCH /crm/leads/{id}/stage`, `GET /crm/leads/{id}/transitions`, `GET /crm/frozen`, `POST /crm/leads/{id}/diagnose`, `POST /crm/leads/{id}/reactivate`, `POST /crm/leads/{id}/reservado-side-effect` (stub MSW).
- No cross-module import crm↔sales_agent directo (DDD boundaries) — acoplamiento via domain event `lead_stage_overridden` emitted por outbox.

**FE `crm-shared`** (`vitalia/frontend/src/features/crm-shared/`):
- `api/use-leads.ts` — hook existente `GET /api/v1/crm/leads` (lista). **EXTEND o verificar path** (§ Open Questions #1: spec dice `/api/v1/vitalia/crm/leads` pero BE monta `/api/v1/crm` — builder confirma con `curl` en dev-app antes de cablear).
- `types.ts` — Lead type. **EXTEND** con campos funnel (stage, score, temperature, operatedBy, channel, ...).

## 5. Relevant rules (critical cross-board)

**Hard rules (no exception):**
- `tenant-isolation` (rule #1): TODA query funnel filtra `tenant_id` (incl `get_by_id`, `update_stage`, board). Cross-tenant → 404 genérico sin leak (SC-4). Funnel repos reciben `tenant_id` required param.
- `backend-ddd` (rule #3): Inside-Out (domain → infra → app → api). NO PhiRepositoryBase for funnel (Lead = non-PHI). Funnel repos = single `tenant_id` filter, no dual clinic_id (Lead is marketing prospect, not PHI). `response_model=` mandatory cada route. SA 2.0 `select().where()`. Pydantic v2 `model_config = ConfigDict(from_attributes=True)`. Soft delete only (`deleted_at`). NUNCA hard delete.
- `tdd-mandatory` (rule #8): RED→GREEN→REFACTOR order. Tests FIRST, implementation AFTER. Bug fix = regression test RED first. Feature = TDD RED domain/infra/app/api per capa.
- `anti-duplication` (rule #12 + overlay `anti-duplication-refining.md`): NO mirror cross-brand. Existing systems scan 03-arch § Existing systems audit VERDICT. Engine sustrato = EXTEND vía import, NUNCA co-optar locally. Funnel is brand-local NET-NEW (no match comunify/nicolify/lupulo).
- `tenant-isolation` + `pii-sanitisation` (rule #11 overlay `hipaa-lite.md`): Lead non-PHI → single `tenant_id` filter only (no clinic_id dual for funnel). `name`/`email`/`phone` stay pgcrypto-encrypted. Funnel fields (`stage`, `score`, `reason`) plaintext NO-PHI. Audit log sync write transiciones (business event, not PHI dual-filter). Sanitize payload traza antes logging.
- `spanish-text` (rule #10): UI strings neutro LatAm (sin voseo). Tildes + ¿¡. Microcopy + `description_es` de activity. Output sales_agent respeta voz tenant (excepto sale-agent rule).
- `master-data` (rule #6): `DateTime(timezone=True)`, store UTC, display `useTenantLocale()`. Time-in-stage = now() − stage_entered_at (read-time calc, not stored field). Currency: DTOs monetarias incluyen `currency` field (no hardcode `'USD'`).
- `architectural-fitness` (rule #9): arch fitness tests gate `vitalia/backend/tests/architecture/` + `vitalia/frontend/src/__tests__/architecture/` — shrink-only allowlists. NEW no exceptions.
- `test-design-doctrine` (rule #35): verificación REAL ≠ HTTP 200. Ejercer writes (POST/PATCH) + leer logs BE + confirmar efecto en DB. E2E mockeando backend = falso verde.
- `definition-of-done-live-verify` (rule #37, Critical): ninguna story `done` sin ejercerla LIVE en `dev-app.vitalialat.com` (cloudflared tunnel → localhost:3002). `dod_evidence` + demo_signoff Chris = gate humano.

**Brand-specific (overlay vitalia):**
- `hipaa-lite.md`: Lead non-PHI but archival 10y (LatAm health regs). Audit log row TODA transición (business event). Retention policy: 10 años post-último acceso. Encripción at-rest (pgcrypto columns PII) + in-transit (TLS). NUNCA render datos clínicos paciente en historial embudo (RN-2 firewall).
- `shell-feature-architecture-mandatory.md` (ADR-vitalia-004): 9 secciones. Aplicase aquí. Compliance: full.
- `shell-mockup-per-component.md` (ADR-vitalia-003): visual goldens ratificados Chris. Applies pre-architect (gate adentro `/po-ux` Step 5).

## 5.5 Domain skill invariants (SSoT extracts)

No domain skills marcadas ONLY_READ para este story (copilot-expert no aplica, offer-expert no aplica, brand-expert no aplica, metrics-expert no aplica). Skill cross-cutting:
- **`backend-expert` (§ references/backend-quality.md):** ruff 0 errors, pytest coverage ≥43%, arch fitness tests, SA 2.0, Pydantic v2, no hard-deletes, migration idempotent, `response_model=` mandatory, tenant-isolation strict. **Aplica:** T-BE-1/2.
- **`frontend-expert` (§ references/frontend-quality.md):** tsc strict, eslint 0 errors, vitest ≥20% coverage, FSD boundaries zero violations, no cross-feature imports, accessibility (axe-clean, keyboard nav drag, aria-live), anti `any`. **Aplica:** T-FE-1/2/3/E2E-1.
- **`vitalia-design-system` (SSoT brand shell + tokens + atomic components):** shell-organism wrapper (TopBar+Ribbon+SubTabsBar+ValeriaChat) portado verbatim desde `dual-mode-shell.html` + `valeria-chat-sample.html`. Tokens HSL de `globals.css` (NO hardcode). Agent colors (Lisa #00D084, Mateo ★ #FEE209 v1.2, Adrián cyan #01B2F8, Lucas, Camila #180D95, Valeria #7B2D91). Gradient mariposa shell. EntitySubNavBar workspace mode reuse (no edit). **Aplica:** T-FE-2/3.
- **`sales-agent-expert` (§ references/sales-agent-brand-voice.md, agentic surfaces § 3 protected):** NO modificar `agent_state_checkpoints` schema (engine-owned). Override-context (RN-4.1) brand-local en `lead_stage_transition.reason` + wire thin subscriber que lo inyecta al próximo turno. NO crear tool nuevo, NO crear grafo nuevo, NO importar crm/ desde sales_agent/ (usar event/port). **Aplica:** T-AG-1 ONLY.
- **`playwright-expert` (§ references/{architecture,clerk-auth-deep-dive,pom-patterns,fixtures,ci-flaky}.md):** Auth clerk testing token, base.ts anti-burbuja (`pageerror`/`console.error`/`response ≥400`/Next overlay), visual goldens ratchet shrink-only, axe per state, POMs desmarcados, `E2E_BASE_URL` env var (NO spawn webServer), Playwright autenticado con `setupClerkTestingToken`. **Aplica:** T-E2E-1.

## 6. Git diff summary
Story state `developing` — ready package completed 2026-06-03. No diff running yet (pre-build). Tickets spawned for 8 dev tracks (BE foundation, BE app+API, agentic wire, FE primitives, FE board, FE workspace+nuevo+recuperar, E2E+goldens, live-verify+demo).

## 7. Existing systems detected (NO-NEW-LAYER scan)

[verbatim from 03-arch.md § Existing systems audit]

| Sistema | Path | VERDICT |
|---|---|---|
| Lead PII entity + repo + service + dto | `vitalia/backend/src/modules/vitalia/crm/{domain/lead,infra/lead_repo,app/lead_service,api/lead_dto}` | **EXTEND** — status flat → stage 6-etapas + score + temperature + operatedBy + channel + ...; pgcrypto PII preserved |
| Engine pipeline substrate | `core/luana-core-crm/.../lifecycle_transition_model` + `core/luana-core-sales-agent/.../closer_studio` | **reference only** (NO mount engine router — sync/auth mismatch; consume patrón vía Extension SDK read-model) |
| Agentic checkpoint | `core/luana-core-sales-agent/.../agent_state_checkpoint_model` | **consume read-only** (§3 protected schema — NO edit) |
| `ChannelBadge` (FE) | `vitalia/frontend/src/components/shared/shell-organism/ChannelBadge.tsx` | **EXTEND** (add meta/referido/tiktok + channel-meta registry nueva) |
| `EntitySubNavBar` (FE) | `vitalia/frontend/src/components/shared/shell-organism/EntitySubNavBar.tsx` | **REUSE verbatim** (workspace mode, como docs) |
| `TogglePill`, `EmptyState`, `PiiMaskedSpan` (FE) | `vitalia/frontend/src/components/shared/...` | **REUSE verbatim** |
| `@dnd-kit/core ^6.3.1` | `vitalia/frontend/package.json` | **installed** — REUSE drag |
| `vitalia_growth_studio_event` telemetry | `vitalia/backend/src/modules/vitalia/_shared/telemetry/` | **REUSE emitter** (bucketed amounts, no PHI) |
| `useTenantId`, `fetchClient` (FE hooks) | `vitalia/frontend/src/hooks/`, `vitalia/frontend/src/lib/api/` | **REUSE** (X-Tenant-ID auto-inject) |

**Cross-brand mirror check:** grep `lead_stage_transition|funnel|board|pipeline|operador|stage_entered|is_frozen` communify/nicolify/lupulo/backend/src → **0 matches**. NET-NEW justified (vertical-specific funnel dental, no mirror).

## 7.5 Anti-duplication inventory cross-reference (03-arch.md § Decisión por sistema)

`vitalia_lead_stage_transition` table + `LeadStageTransition` entity + funnel stage machine + glass-box scorer + auto-freeze rule + board aggregation:
- **Listed in `.claude/rules/anti-duplication.md` inventory?** NO (brand-local, no engine abstraction)
- **Grep cross-brand encontrado?** NO (0 matches)
- **Replicable en otras brands sin contexto dental?** Maybe (stage machine patrón), pero business rules SLA+freeze dental-specific → lift candidate `/pm-luana` SOLO si 2ª brand lo necesita (no proponerlo ahora)
- **Recommendation:** NET-NEW flagged as "lift candidate" (comment in code, github-link NEVER created in this story). No escalate `/pm-luana` hoy.

`channel-meta` registry (FE) + ChannelBadge EXTEND:
- **Listed?** NO (brand-local)
- **Cross-brand useful?** YES (todas las brands tienen canales WA/IG/Web)
- **Recommendation:** brand-local vitalia ahora; if nicolify/comunify/lupulo need → `/pm-luana` lift to `@luana/ui-kit` post-hoy (comment en código: "lift candidate", no proposal yet)

## 8. EXTEND vs NEW recommendations

| Surface | Existing system | Overlap % | Recommendation | Reason |
|---|---|---|---|---|
| Lead entity + funnel | crm/domain/lead (status flat) | 10-20% (only Lead aggregate skeleton) | **EXTEND** | Lead exists; add funnel layers (stage machine, transition audit, score, freeze) → new DDD aggregates `LeadStageTransition`, `LeadActivity` siblings |
| Engine sustrato | core/luana-core-sales-agent/closer_studio (checkpoint, transitions, diagnose, KPIs) | 40-50% (patrón similar, but sync vs async + Luana auth vs Clerk) | **reference only + reimpl brand-local** | Engine = read-only projection; funnel DTOs consume read_model; auth/runtime mismatch prevens direct reuse. Patrón válido → copy per vertical (dental SLA, rules) |
| ChannelBadge | vitalia/frontend/src/components/shared/shell-organism/ChannelBadge | 60% (estructura similar, colors hardcoded) | **EXTEND** | Registrar metadatos canal centralizados (channel-meta.ts); ChannelBadge consume registry. Back-compat (iconOnly/className props intact) |
| EntitySubNavBar | vitalia/frontend/src/components/shared/shell-organism/EntitySubNavBar | 100% (reuse verbatim) | **REUSE** | Workspace mode (lead detail, new lead) = identical to Staff doctores. No modification. |
| Drag behavior | @dnd-kit/core (installed) | 100% | **REUSE** | Setup drag para board + drag-override. Library, no reimpl. |
| Telemetry | vitalia_growth_studio_event (emitter shipped) | 100% | **REUSE** | Bucketed events (no PHI). Emit `embudo_stage_changed{from,to,trigger}` etc. |
| FE shared atoms (Button, Badge, Card, Table, etc.) | Shadcn `vitalia/frontend/src/components/ui/` | 100% | **REUSE** | Componentes copy-paste Shadcn. No edit. |
| Backend DDD shape (domain→infra→app→api) | crm module existente | 100% pattern | **REUSE pattern** | DDD Inside-Out aplicase igual. No co-opt PhiRepositoryBase para funnel (non-PHI). |

**BLOQUEANTE:** 0 systems should be NEW donde EXTEND/REUSE viable. Todos los NEWs identificados son justificados (vertical-specific DDD aggregates + brand-local stage machine logic + channel registry que NO existe).

## 9. Architecture fitness gates

Gates que corre ANTES builder arranca + BLOQUEANTE si RED:

| Gate | Path | Ejecuta | Debe pasar |
|---|---|---|---|
| **BE response_model mandatory** | `vitalia/backend/tests/architecture/test_response_model_required.py` | `@pytest.mark.architecture` | TODA route tiene `response_model=`. NEW endpoints: board, stage, detail, transitions, frozen, diagnose, reactivate, reservado-stub. |
| **FE no-phi-in-url** | `vitalia/frontend/src/__tests__/architecture/test_no_phi_in_url_params.test.ts` | `vitest` | searchParams whitelist enforced. leadId = UUID (RN-16), nunca PHI name. |
| **FE no-hardcoded-colors** | `vitalia/frontend/src/__tests__/architecture/test_no_hardcoded_colors/` | `grep + vitest` | ChannelBadge consume channel-meta CSS vars, no inline color codes. |
| **FE no-cross-imports** | `vitalia/frontend/src/__tests__/architecture/test_features_no_cross_imports.test.ts` | `vitest` | features/adrian no importa features/{lisa,mateo,camila,lucas,valeria}. |
| **FE RQ keys convention** | `vitalia/frontend/src/__tests__/architecture/test_react_query_keys_convention.test.ts` | `vitest` | keys = `[module, subtab, action, ...filtersStable]`. `use-embudo-board`, `use-lead-stage-mutation` etc. |
| **FE no-clerk-organizations** | `vitalia/frontend/src/__tests__/architecture/test-no-clerk-organizations.test.ts` | `grep + vitest` | `useTenantId()` (NUNCA `useAuth().orgId`). **Critical:** 35 files vitalia fe violaban esto pre-fix. Regression test endurecido. |
| **BE lead-repo not-phi-repository** | `vitalia/backend/tests/architecture/test_lead_repository_is_not_phi_repository.py` | `pytest` | LeadRepository NO hereda PhiRepositoryBase (Lead non-PHI). PhiRepositoryBase ONLY para tablas PHI (patient_medical_records, etc.). |
| **BE phi-dual-filter** | `vitalia/backend/tests/architecture/test_phi_dual_filter.py` | `pytest` | TODO repo PHI hereda PhiRepositoryBase. Funnel repos (non-PHI) reciben `tenant_id` required, no clinic_id. |
| **BE audit-log-sync** | `vitalia/backend/tests/architecture/test_audit_log_sync_write.py` | `pytest` | TODO endpoint PHI escribe audit_log row PRE-response (sync write, not async fire-forget). |
| **BE growth_studio_event_no_phi** | `vitalia/backend/tests/architecture/test_growth_studio_event_no_phi.py` | `pytest` | Payload events telemetría NOT contiene PHI. Bucketed solo. |
| **BE SA 2.0 + no-hard-delete** | `vitalia/backend/tests/architecture/test_sqlalchemy_2*.py` | `grep + pytest` | TODA query = `select(Model).where(...)` (no `session.query()`). NUNCA hard DELETE (solo `deleted_at`). |
| **BE migrations idempotent** | `vitalia/backend/tests/architecture/test_migrations_idempotent.py` | `pytest` | Migration raw SQL `IF NOT EXISTS`. NUNCA `op.create_table()` o `sa.Enum(create_type=True)`. |
| **Arch-fitness DDD boundaries** | `vitalia/backend/tests/architecture/test_ddd_boundaries.py` | `pytest` | crm NO importa directo de sales_agent (usar event/port). Cross-module vía domain event. |
| **FE shell ADR-vitalia-004 compliance** | `vitalia/frontend/src/__tests__/architecture/test_adr_004_compliance.test.ts` | `vitest` | 9 secciones ADR-004 cubiertas: routing + FSD + Server-first + RQ + Zustand + RHF + DDD + migrations + events + tests. |

## 10. Implementation log highlights
No implementation log yet (tickets just spawned 2026-06-03). Architect completed ready package, handed off to autonomous builder group (8x tickets). Next phase: T-BE-1 started (funnel domain + migration + repos) by builder-backend Sonnet.

## 11. Faithfulness gaps + validator findings

**PENDING:** validator not yet run (context-builder has just read all source artifacts pre-spawn validator). Anticipated MEDIUM-severity gaps:

1. **Path consistency (§ Open Questions #1):** FE `use-leads.ts` calls `/api/v1/vitalia/crm/leads` but BE mounts crm at `/api/v1/crm`. Builder MUST verify with `curl` in dev-app before wiring new hooks (DoD #37 live-verify). **Severity:** MEDIUM (functional gap if path unresolved).
2. **ChannelBadge lift candidate not escalated:** comment says "candidate /pm-luana" but no proposal created. **Severity:** LOW (documented decision).
3. **override-context wire (RN-4.1) when sales_agent not live:** context persists real (T-AG-1), but "Adrián adjusts next step" only observable when Inbox/sales_agent live. SC-1b tests persist+read (BE valid); conversational effect deferred (Severity LOW, acknowledged in Open Questions #4).

**NOT BLOCKING:** all required artifacts present (01-spec v3, 03-arch full, 04-validators complete, 05-guidelines, 06-tickets 9x, checkpoint state, chris-input available). Existing-systems audit clean (0 cross-brand mirrors, engine REF-only, EXTEND justified).

## 12. Raw paths consulted

Core files read (14 total, 2.1K LOC):
- `vitalia/docs/product/stories/vitalia-fase2-adrian-embudo/01-spec.md` (578L)
- `vitalia/docs/product/stories/vitalia-fase2-adrian-embudo/03-arch.md` (381L)
- `vitalia/docs/product/stories/vitalia-fase2-adrian-embudo/04-validators.yaml` (248L)
- `vitalia/docs/product/stories/vitalia-fase2-adrian-embudo/05-guidelines.md` (86L)
- `vitalia/docs/product/stories/vitalia-fase2-adrian-embudo/06-tickets.yaml` (232L)
- `vitalia/docs/product/stories/vitalia-fase2-adrian-embudo/checkpoint.md` (122L)
- `vitalia/CLAUDE.md` (193L)
- `vitalia/.claude/rules/{hipaa-lite,shell-mockup,shell-feature}.md` (807L)

Cross-ref via existing-systems-audit grep (0 external reads needed — results inline in 03-arch).

## 13. Verbatim grep + WebFetch commands executed

**Greps executed (bash logs):**
1. Cross-brand mirror check: `grep -rln "lead_stage_transition|funnel|board|pipeline" vitalia/backend/src/ comunify/backend/src nicolify/backend/src lupulo/backend/src` → 0 matches funnel layer outside vitalia (NET-NEW justified)
2. Engine substrate scan: `find core/luana-core-{crm,sales-agent}/src -name "*.py" | grep -iE "pipeline|closer_studio|checkpoint"` → located canonical sources (03-arch cites paths verbatim)
3. ChannelBadge existence: `find vitalia/frontend/src/components -iname "*ChannelBadge*"` → EXISTS + EXTEND candidate

**WebFetch:** none required (all docs are local `.md` files in story folder + overlays). No canonical upstream docs (frameworks FastAPI/SQLAlchemy/Pydantic/NextJS/React Query already known to architect — specs cite them inline).

## 14. Free-form notes — what I'd tell a smart colleague

**Architect delivered a **really clean brief** to the builder group.** Here's what jumps out:

1. **Spec is v3-mature.** Chris ratified visual 2026-06-03 (mockup embudo-v3.html, 5 iterative refinements, decisiones abiertas cerradas). RN-1..19 are specific + testable (SLA verde 7/7/5/14, freeze 14d/30d/2×SLA, order by age-in-stage, override feeds agent context). Gherkin 11 scenarios + sub-categories (happy, negative, edge, race, concurrent, network, empty, large, a11y, i18n, deeplink, nuevo, freeze) — good coverage skeleton.

2. **Existing-systems audit is faithful.** 03-arch § Audit verifies NO cross-brand mirror (comunify/nicolify/lupulo returns 0 hits). Engine substrate (closer_studio + checkpoint) is **reference-only, smart decision** — consume read-model via Extension SDK, don't mount the sync router (auth mismatch). Reimpl funnel dental brand-local justified (vertical-specific rules, no shared abstraction).

3. **Ticket decomposition is lean.** 9 tickets, DAG parallelizable: T-BE-1 (domain+migration) → T-BE-2 (app+api) → T-FE/T-AG in parallel → T-E2E → T-DEMO. No scope creep (no editor stage customization, no real payment, no AI-suggest next-stage, no bulk-action).

4. **GOTCHA: Path inconsistency (§ Open Questions #1).** FE calls `/api/v1/vitalia/crm/leads` but BE mounts `/api/v1/crm`. Could be a rewrite/proxy or Inbox using a different mount. **Builder MUST verify with `curl http://localhost:8002/api/v1/crm/leads` and `curl http://localhost:8002/api/v1/vitalia/crm/leads` in dev-app before wiring new hooks.** If `/vitalia/crm` 404s, Inbox leads list might also be broken live (outside scope but flag to /pm-vitalia).

5. **override-context wire (RN-4.1) is subtle.** T-AG-1 (agentic ticket, R23 Opus) persists the override reason into `lead_stage_transition.reason`, then a subscriber reads it and injects it into the agent's next turn context. The effect ("Adrián adjusts without reinitializing") is OBSERVABLE only when Inbox/sales_agent are live. **Right now:** SC-1b tests verify persist+read (BE valid). **Later:** when agent is live, verify that Adrián doesn't repeat questions already answered.

6. **HIPAA-lite gates are specific.** Lead is non-PHI (marketing prospect), so funnel repos DON'T inherit PhiRepositoryBase (correct — arch test `test_phi_dual_filter` verifies). BUT the story says "Historial NO renderiza datos clínicos" (RN-2 firewall) — that means the timeline event bodies must filter out medical info. **When Inbox/sales_agent are live,** the timeline will merge message content from conversations — make sure those are still gated behind the "link to Inbox PHI-gated" instead of inline rendering.

7. **DoD #37 live-verify is the final gate.** T-DEMO-1 must exercise writes (PATCH stage, POST new lead) against dev-app (cloudflared tunnel, Clerk real, Postgres real), read the backend logs, and confirm database rows. **No E2E against mocked backend** (case origin lisa-marca v2: suite passed mockeado, shipped 3 bugs). Demo_signoff from Chris is the ONLY human gate (autonomous_mode=true). If evidence empty → /pm-vitalia REFUSE merge.

**Recommendation to builder:** stick to the 9 tickets, verify path consistency (Open Q #1) in T-BE-2, and don't merge without dev_app_verified.evidence + demo_signoff = APPROVED.

## 15. Upstream canonical docs references

No external canonical docs fetched (frameworks known, architecture established). 01-spec § Prior art cites internal research files:
- `research/02-core-engine.md` (Boundary REUSE vs BUILD analysis — why funnel is brand-local)
- `research/03-agentic-best-practices.md` (Autonomía gated 3-tier, glass-box scoring, exception approval, takeover context)
- `research/05-lead-detail-benchmark.md` (HubSpot/Clientify/Pabau/LeadMAX UX → 2 tabs, score block)
- `research/06-pipeline-sla-benchmark.md` (Pipedrive/Outreach/Rework → SLA 7/7/5/14, freeze 14d/2×SLA, order by age)

All research files exist at story path; no upstream canonical needed.

## 16. Self-budget snapshot

**Tools used:** 4 (Read, Bash, Edit, Write)
**Files read:** 14 story + rule files (~2.1K LOC), 3 brand overlays (~807L)
**Greps run:** 3 cross-brand mirror scans (0 hits each — clean)
**Web fetches:** 0
**Tokens consumed:** ~87k input (this turn), ~6k output (brief pre-validator)
**Turns used:** 5 (parallelizable reads bundled; audit log sequential)
**Turns remaining:** ~100+ (token budget 200k, 93k used so far)

**Skeleton write (turn 2):** ✓
**Parallel reads (step_1, turn 3):** ✓ (01-spec + 03-arch + 04-validators + 05-guidelines + 06-tickets + checkpoint + 7 brand overlays in 1 message)
**Audit log appends (inline bash):** ✓
**Section fills (turn 5, edit payload):** ✓ (14 sections filled, § 15 pending validator)
**Validator spawn (turn 6, pending):** **CRITICAL NEXT STEP**
