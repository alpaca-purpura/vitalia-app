# CONTEXT-BRIEF for vitalia-shell-core-hardening
> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder
> Modules: shell, clinics, crm
> Faithfulness flag: clean  (clean | partial | blocking)
> Audit log: vitalia/docs/product/stories/vitalia-shell-core-hardening/context-builder-logs/iter-1-2026-06-10T23-09-19Z.log
> Validator pass: CONTEXT-BRIEF-validation.md (0 HIGH, 0 MEDIUM, 1 LOW — sealed clean; probe ran inline, no Agent tool in subagent — see validation §Provenance)

## 1. PR summary

**Umbrella de hardening del chrome del shell-organism** (ui-story, state `developing` phase `BUILD_T1`, autonomous_mode `true` ratificado Chris 2026-06-10). FE-only. Release F3, programa cross-brand `design-system-homologation` (ADR-014, owner /pm-luana).

Consolida 5 trabajos abiertos del chrome en UN vehículo para una sola revisión + lift-durante a `core/@luana/ui-kit`:
1. **Responsive backbone** (firmado, parcialmente construido — baseline 614bfbd5 + 29451ef6): puntos 1-7 del backbone `vitalia-bugfix-shell-valeria-responsive`.
2. **Race-fix** (folded): drag inmediato post-hidratación respeta clamps.
3. **B1 soft-nav hang** (root cause `ssr:false` + soft-nav → "Rendered more hooks" Next 16.2.3).
4. **Dark token-audit** (BUG #2, ahora EN scope ratificado Chris 2026-06-10): hardcoded→token en sub-tabs shipped.
5. **U3 splitter** (trazado a SC-1/SC-4, resuelto por default 30/70).

**Scope nuclear:** refactor de la máquina de estados de Valeria (de `collapsed/rail/full` legacy → `closed|chat` + `historyOpen` aditivo), eliminar modo web (`shellMode`/`ShellModeToggle`), tira-avatar estado A, "+" nueva conversación, migrar N3 list/detail (staff + embudo) a `@luana/ui-kit`, dark consistente, soft-nav confiable.

**Cross-module:** `[shell, clinics, crm]`. `cap_target: null` (chrome transversal, zona Infraestructura → caja plataforma-técnica). `cap_change_type: fix`.

**14 AC** (AC-1..11 backbone heredados firmados + AC-12 dark + AC-13 soft-nav + AC-14 race). **16 RN** (RN-1..13 backbone + RN-14/15/16 umbrella). **22 SC** (SC-1..19 backbone + SC-20 dark + SC-21 soft-nav + SC-22 race). **8 tickets** FE (builder-frontend/sonnet, ~$4.35 / ~5h estimado).

**Verification:** `funcional` → demo Chris (G chris_verify.signoff) + anti-burbuja base.ts + live-verify dev-app gate #37 OBLIGATORIO antes del merge (autonomous NO relaja DoD).

## 2. Contract decisions

> 3 decisiones que Chris delegó al architect, cerradas con código real (03-arch.md § Architecture Decisions).

**Decisión A — soft-nav confiable (RN-14 / B1):**
- Root cause confirmado en código: `ShellOrganismLayout.tsx` usa `dynamic(..., {ssr:false})` porque `react-resizable-panels v4.11.1` tiene `storage: n = localStorage` bare-name default param que crashea el SSR pass de Next (ReferenceError pre-`typeof` guard). El `ssr:false` es **NECESARIO y NO se elimina**.
- Solución ELEGIDA (opción 2): **edge-redirect en `proxy.ts` (307) + revertir band-aid hard-nav del chip `frozen-kpi-badge` → soft-nav (`next/link`)**. El 307 fuerza fetch fresco → Router monta limpio. El proxy ya existe (`bareTenantLandingRedirect`) — se EXTIENDE.
- Tradeoff: workaround del trigger, NO fix del framework. Verificado con SC-21 loop ×15 real. T-4 debe chequear (WebSearch + nextjs.org) si hay fix upstream Next del "Rendered more hooks" ANTES de más edge-redirects, PERO **NO bumpear Next sin gate de regresión completo** → bump = escalate Chris.

**Decisión B — N3 lift sequencing (lift-durante acotado):**
- HALLAZGO CRÍTICO que reencuadra: `core/@luana/ui-kit` **v0.3.0 YA SHIPPED** el N3 (`EntityWorkspaceLayout` + `EntitySubNavBar` + `EntityInfoCard`) + page-primitives + archetypes (`core-ds-foundation`, accepted). El N3 NO se porta de nicolify — ya vive en core.
- Acción esta story: **CONSUMIR** — migrar `lisa/staff` + `adrian/embudo` a importar `EntityWorkspaceLayout` de `@luana/ui-kit` + **RETIRAR** la copia brand-local `vitalia/.../shell-organism/EntitySubNavBar.tsx` (mata el mirror; encoge allowlist `test-no-cross-brand-shell-mirror`).
- El **chrome del shell** (ShellOrganismLayout/Ribbon/ValeriaSidebar) **NO se liftea acá** — es el outcome platform de 1-2 semanas de `2026-06-01-lift-shell-organism-to-core` (handoff a /pm-luana). Se modifica brand.
- Edit a `@luana/ui-kit`: **additivo-mínimo SOLO si falta una pieza N3**. Cualquier edit NO-additivo (refactor chrome, breaking) → **STOP, escalate /pm-luana** (pause-point dispatch-plan).

**Decisión C — dark token-audit (RN-15 / BUG #2 · reduce deuda):**
- HALLAZGO: los tokens core `--vitalia-{bg,surface,text,border,...}` YA tienen override `[data-theme="dark"]` (globals.css ~248-272). BUG #2 NO es "vt-* sin dark wholesale" — es (1) colores **hardcodeados** en componentes de feature (ej. inbox `ContactSidebar` blanco `rgb(255,255,255)`, cards) + (2) `--vitalia-*-soft` derivados sin variante dark.
- Estrategia: **migrar el color hardcodeado al token semántico que YA tiene dark** (`bg-card`/`bg-background`/`vt-bg-surface`/`text-foreground`) — PREFERIR esto sobre crear variante `[data-theme=dark]` nueva por clase (reduce el sistema paralelo). Completar variante dark SOLO para los `--vitalia-*-soft` que realmente falten.
- Scope acotado: **SHIPPED-only** (lisa/marca, adrian/inbox, adrian/embudo, lisa/staff, mateo/agenda + wrapper). NO migra los 368 arbitrary-values de ADR-014 (otra story).

**Surfaces:** FE-only confirmado. **No hay surface BE. No hay surface AGENTIC** (R23 N/A — el chat de Valeria se toca como chrome/layout, NUNCA comportamiento del agente). El "+" nueva-conversación es estado UI-local (Zustand mock store), NO turn del copilot ni persistencia BE. **GUARD HARD:** si el build descubre que "+"/historial deben persistir conversaciones reales → STOP, escalate /pm-vitalia (scope nuevo).

**No hay entidades/modelos/DTOs/rutas/repos/services nuevos** (FE-only). Sin migración DB. Cambio de shape persistido en localStorage → migrate legacy obligatorio (`collapsed`→`closed`, `rail`/`full`→`chat`, corrupto→fallback+warn, SC-18 cubre).

## 3. UI spec decisions

**Máquina de estados nueva (shell-store.ts refactor — 03-arch-fe.md § 1):**
```ts
type ValeriaOpen = "closed" | "chat";   // A = closed (tira-avatar 44px) · B = chat (split 30/70)
interface ShellState {
  valeriaOpen: ValeriaOpen;              // persisted
  historyOpen: boolean;                  // NOT persisted (RN-5/11: nunca reabre con historial)
  valeriaPct: number | null;             // split % persisted (null = default 30)
  mobileDrawerOpen: boolean;             // persisted, slice independiente
}
```
- Estado A (closed): tira-avatar 44px; agente 100%; historyOpen forzado false.
- Estado B (chat): split 30/70 default resizable (persiste).
- Estado C (chat + historial): historial **empuja** 260px FIJO + chat resizable; agente angosta.
- ELIMINAR: `shellMode` ('agentic'|'web') + `ShellModeToggle` + grid web-mode + rail 60px legacy.

**Componentes (reuse > new):**
- `TopBarGlobal.tsx` MODIFY — cluster derecho orden `[ThemeToggle][TenantSwitcher]` (switcher al extremo); sin chip; skeleton variant conservado.
- `ShellModeToggle.tsx` RETIRE — AC-1 grep `shellMode`/`ShellModeToggle` = 0.
- `ValeriaCollapsedStrip.tsx` NEW (o transformar `ValeriaRail.tsx`) — tira 44px + **avatar real de Valeria** (`public/agents/valeria/thumbnail.png`, NUNCA placeholder "V") + dot + label; clic reabre B; `aria-label="Abrir a Valeria"`.
- `ValeriaRail.tsx` RETIRE/TRANSFORM (rail 60px se va).
- `ChatHeader.tsx`/`ValeriaSidebar.tsx` MODIFY — botones cabecera `[+][◷][⟨]` aria-labels neutro; "+" limpia chat + archiva conv actual (UI-local).
- `ValeriaHistory.tsx` MODIFY — 260px FIJO empuja.
- `useViewportGuard.ts` MODIFY — clamp 320; drawer 1024; eliminar 620/rail. **Gate desktop↔mobile = CSS, NUNCA JS** (montar/desmontar `<Group>` condicional → "Rendered more hooks"; `<Group>` se monta siempre, Valeria panel `hidden md:flex`).

**Responsive:** <1024 drawer/overlay (role=dialog, focus-trap, backdrop, Esc) · [1024,1280) clamp min 320px · ≥1280 30/70 + historial empuja 260 fijo.

**Behavior-fi HARD:** el mockup `shell-valeria-states.html` (firmado, en `vitalia-bugfix-shell-valeria-responsive/mockups/`) comunica COMPORTAMIENTO; el ESTILO sale del design system vigente (canon + tokens). UI actual NO retrocede (AC-10/SC-19 regresión cero). Avatar Valeria = asset catálogo. El toggle dark del mockup ES la referencia de comportamiento esperado para AC-12.

**N3 canon signature (core v0.3.0):** `EntityWorkspaceLayout`: `{entity|null, leaves[], rootHref, rootLabel, isLoading?, placeholder?, onAddAffordance?, children}`. Directory mode (`entity=null`): leafs disabled (aria-disabled, tabIndex=-1, opacity ~.45). La copia brand vieja tenía signatura distinta (`{rootHref, rootLabel, entity, leaves, activeLeaf, className}` con back-link separado) — adaptar call-sites a la signatura core (root-pill + identity unificados).

**Microcopy neutro LatAm:** "Nueva conversación", "Historial", "Abrir a Valeria", "Colapsar a Valeria", "Mostrar/Ocultar historial". Sin voseo (chrome del shell, no output agéntico).

## 4. Module current-state extracts

### shell-organism (`vitalia/docs/product/modules/shell-organism.md`)
Brand-local module, NO cross-brand consumers, UI chrome only (no PHI). Capabilities LIVE:
- `shell.empty-states` · `shell.layout-5050` (root split, story F1-S4) · `shell.ribbon` (5 RibbonTabs + ConfigTab) · `shell.routing` (proxy.ts clerkMiddleware + [agent]/[subtab] dynamic) · `shell.sub-tabs` (22 sub-tabs) · `shell.valeria-chat` (chat-store zustand mock) · `shell.valeria-sidebar` (full/rail/collapsed modes — ValeriaRail 60px + ValeriaHistory + ValeriaChatSlot).

Decisiones cardinales relevantes al build:
- **react-resizable-panels v4 SSR workaround:** `useDefaultLayout` bare-name `localStorage` default param crashea SSR → split `ShellOrganismLayout` (wrapper) + `ShellOrganismLayoutClient` via `next/dynamic({ssr:false})`. [Esto confirma Decisión A — NO eliminar ssr:false.]
- **minSize PERCENT (no pixels):** v4 trata numeric minSize como pixels; pasamos string `"${pct}%"`. ResizeObserver recalcula percent (MIN_VALERIA_PX 620 full / 360 rail · MIN_APP_PX 480). [El build CAMBIA estos: clamp 320, sin 620.]
- **Snap-up Fix A via useGroupRef:** useEffect post-ResizeObserver detecta layout persisted < minSize → setLayout imperativo. [Relacionado con SC-22 race/AC-14.]

### Current `shell-store.ts` (LEGACY shape — el T-1 lo refactoriza) ★ load-bearing
El store ACTUAL tiene la forma vieja que el build DEBE migrar:
```ts
type ValeriaState = "collapsed" | "rail" | "full";   // → nuevo: ValeriaOpen "closed"|"chat" + historyOpen
type ShellMode = "agentic" | "web";                  // → ELIMINAR (AC-1)
SHELL_STORAGE_KEY = "vitalia-shell-state"
PersistedState = { valeriaState, shellMode, mobileDrawerOpen }
default valeriaState: "rail" (Chris 2026-06-04)  ·  cycleValeriaState: rail ↔ full
```
Consume `createSsrSafePersistedStore` de `@luana/hooks/create-ssr-safe-persisted-store` (ADR-vitalia-006, skipHydration + setItem NO-OP pre-hydration — NO recrear). El builder DEBE escribir un `migrate`/fallback: `collapsed`→`closed`; `rail`/`full`→`chat`+`historyOpen=false`; corrupto→fallback+warn. Mantener `mobileDrawerOpen` slice independiente. Storage key se conserva (mismo key = shapes viejas en localStorage de usuarios reales → migrate obligatorio, no opcional).

### shell-organism/ dir actual (componentes a tocar — confirmados existen)
`ShellOrganismLayout.tsx` (3.8K, wrapper ssr:false) · `ShellOrganismLayoutClient.tsx` (16.7K, real impl) · `TopBarGlobal.tsx`* · `ShellModeToggle.tsx` (2.5K → RETIRE) · `ChatHeader.tsx` · `SubSubTabsBar.tsx` (N3-static, NO es el list/detail) · `EntitySubNavBar.tsx` (9.3K brand-local → RETIRE tras migrar) · `Ribbon.tsx` · `SubTabsBar.tsx` · `HistoryGroup/HistoryItem.tsx`.
> ✅ CONFIRMADO (ls completo): TODOS los componentes a tocar existen en `vitalia/frontend/src/components/shared/shell-organism/`: `ValeriaSidebar.tsx` · `ValeriaChat.tsx` · `ValeriaHistory.tsx` · `ValeriaRail.tsx` · `useViewportGuard.ts` · `TopBarGlobal.tsx` · `TenantSwitcher.tsx` · `TenantBadge.tsx` · `ThemeToggle.tsx`. También confirmados: `vitalia/frontend/src/proxy.ts` (Decisión A) · `vitalia/frontend/src/features/adrian/components/embudo/EmbudoMetrics.tsx` (band-aid revert) · `vitalia/frontend/e2e/regression/vitalia-fase1-shell-layout-5050/resize-and-state.spec.ts` (reactivar race, AC-14). Cada componente tiene su `.test.tsx` co-ubicado (baseline para TDD).

### crm + clinics
- `crm.md` existe en `vitalia/docs/product/modules/`. El N3 de embudo (adrian) vive en módulo crm. El embudo está `developed` + `defer_audit: true` — su board/writes live-verified NO deben romperse (AC-10 regression).
- clinics = lisa/staff N3. `vitalia-fase2-lisa-doctores` parked (keystone regresión /lisa/staff VIVA, bug de feature Lisa NO chrome). No hay `clinics.md` en modules/ (n/a — el módulo lisa cubre staff).

## 4.5 Cap pointers (cap-as-locator · HB-43)

**`cap_target: null`** (confirmado en checkpoint.md L13 + L14 `cap_change_type: fix`). Chrome transversal del shell-organism sin agente/cap user-visible. Resolver `scripts/resolve_cap.py` NO aplica (gate del step solo corre con cap_target no-null). Sin §4.5 locator — el build apunta directamente a los paths en §4 + el File map de 03-arch-fe.md. El lift a `@luana/ui-kit` es cap-work de /pm-luana (core), fuera de esta story (gate HB-34 no exige YAML para fix).

## 5. Relevant rules

Builder-phase + FE rules (todas en mi contexto auto-load). Hard 1-liners load-bearing para este build:

| Rule | Hard rules / No-skip relevantes a este build |
|---|---|
| `tdd-mandatory` | Tests PRIMERO por capa (vitest hook/component/store → playwright). RED antes de implementar. Bug fix = regression test RED primero. El `test.skip(true)` de race se REACTIVA (AC-14). |
| `frontend-fsd` | FSD-Lite. Server Components default; `"use client"` solo en hojas con state/effects. NO `any`, NO default exports (excepto Next pages). `cn()` de lib/utils. **Import de `@luana/ui-kit` PERMITIDO** (cross-brand vía `@luana/*`). `shared` nunca importa de `features`. |
| `frontend-quality` | ESLint 0 errors · tsc strict 0 errors · vitest 20% coverage per-brand · arch fitness ratchet shrink-only. `// eslint-disable` solo con justificación. |
| `frontend-visual-fidelity` | **D1 Design System Canon binding HARD:** list/detail = `EntityWorkspaceLayout` (NUNCA cablear a mano) · franja N3 full-bleed `bg-card`+border-bottom+sticky (NUNCA card redondeada) · NO `<select>` nativo · NO arbitrary-values (todo de tokens). D2 mockup adherence (behavior-fi). D3 scope discipline (solo scenarios + deliverables del ticket). |
| `tenant-isolation` | FE `tenant_id` SIEMPRE de `useTenantId()` — **NUNCA `useAuth().orgId`/Clerk org** (arch-test `test-no-clerk-organizations` HARD). N3 `[entityId]` es UUID, NUNCA PHI en URL. |
| `spanish-text` | Microcopy chrome neutro LatAm SIN voseo. Aplica a UI user-facing (no al output agéntico). |
| `definition-of-done-live-verify` (#37) | Verde ≠ done. Cada ticket FE cierra con live-verify dev-app + `dod_evidence` (≥1 write/acción real + logs + efecto). NUNCA declarar "funciona" por suite verde o GET 200. `dev-app.vitalialat.com` (Chrome DevTools MCP) o localhost:3002 fallback. |
| `anti-duplication` | grep-before-write. El N3 brand-local DUPLICA el de core → RETIRAR (mata mirror). Consumir `@luana/ui-kit`, NUNCA recrear. `createSsrSafePersistedStore` consumir de `@luana/hooks`, no recrear. |
| `parallel-safety` (M14/M15) | Bucket locks `code:{shell,clinics,crm}` al build. `parallel_safe: false` (toca el shell). Commit por pathspec (índice compartido), NUNCA `git add .`/`-A`. M15 sweep-guard activo. |
| `git-safety` | Conventional Commits. NO `git pull`/`--force`/`--no-verify`/`git add .`. Stage por nombre exacto. |
| `test-design-doctrine` | Verificación REAL ≠ HTTP 200. Batería por naturaleza del ticket. jscpd + arch-fitness first-class (rojo = NO verde). e2e que mockea el backend del surface bajo prueba = falso verde. |
| `architectural-fitness` | Arch tests FE en `vitalia/frontend/src/__tests__/architecture/`. Ratchet KNOWN_* shrink-only. Retirar `EntitySubNavBar` brand-local ENCOGE `test-no-cross-brand-shell-mirror` allowlist (OK). |

**Vitalia overlay rules (auto-load en `vitalia/`):**
| Rule | Relevancia |
|---|---|
| `shell-feature-architecture-mandatory` (ADR-vitalia-004) | Story cita `architecture_pattern: ADR-vitalia-004`. `adr_004_compliance: partial-with-rationale` (N3 consume @luana/ui-kit en vez de cablear a mano; chrome cita ADR-vitalia-006). **N3 anti-pattern:** NO Shadcn `Tabs` internas para sub-secciones (es N3-static/SubSubTabsBar). |
| `hipaa-lite` | Chrome NO toca PHI (no-phi-scope). NO PHI en localStorage (shell-store no-phi). N3 `[entityId]` UUID no PHI. No aplica dual-filter (sin queries). |
| `shell-mockup-per-component` (ADR-vitalia-003) | Mockup firmado heredado (`shell-valeria-states.html`). Wrapper fidelity: portar verbatim, NO reinventar simplificado (grises en vez de tokens, 50/50 hardcoded, chat inventado). Avatar Valeria = asset catálogo. |

## 5.5 Domain skill invariants (SSoT extracts)

> **NO se tocan módulos de dominio agentic** (copilot/sales_agent/brand/offer/analytics) → esos skills (copilot-expert etc.) son N/A. Surface = FE chrome puro. Los skills relevantes son `frontend-expert` + `vitalia-design-system` + `playwright-expert` + `chrome-devtools-verify` (must_load_skills del ready package).

| Skill | Hard rules / SSoT highlights | Source |
|---|---|---|
| `frontend-expert` | • Anti-alucinación: componente/hook/tipo que no aparece al explorar código real → **no existe** (docs de dominio = orientación negocio, NUNCA inventario técnico).<br>• Server-First: todo Server Component por default; `"use client"` solo en hojas con state/effects.<br>• Runtime quality checklist OBLIGATORIO antes de commit + spawn auditor (useEffect deps, stale closures, routing tenantId, mock anti-patterns, live-verify gate).<br>• Live-verify via `chrome-devtools-verify` antes de shipped (FE PR ≥ M). | SKILL.md:1-90 |
| `vitalia-design-system` | • **Tokens SSoT = `vitalia/frontend/src/app/globals.css`** (`:root` + `.dark`). ⚠️ TRAMPA: `core/@luana/design-tokens` SOLO exporta `Z_INDEX` — NO colores. Buscar color en el engine → reinventa = el bug.<br>• Agent-color JIT-safe lookup: `shell-organism/_agent-tw-classes.ts` (evita purga JIT).<br>• Primario cian `#01B2F8` · accent púrpura `#7B2D91` · agent-lisa `#00D084` · agent-mateo `#FEE209` · agent-adrian `#01B2F8`. Gradiente mariposa para logo/CTA.<br>• 19 átomos Shadcn en `components/ui/` — NUNCA reinventar. `tabs` Shadcn ≠ N3-static (anti-pattern usar Tabs internas para sub-secciones).<br>• **Modelo nav cardinal:** 3 niveles de tab GUÍAN hasta la HOJA (contenido = lo único que cambia); una hoja NUNCA contiene tabs.<br>• Wrapper fidelity = portar VERBATIM (causa #1 de pérdida): TopBar+Ribbon+SubTabsBar+ValeriaSidebar/chat de fuentes canónicas, NO reinventar simplificado.<br>• `components/shared/shell/` (AppShell/Sidebar/TopBar) = LEGACY en deprecación, NO usar.<br>• PHI nunca en URL/searchParams. | SKILL.md:1-90 |
| `@luana/ui-kit` (v0.3.0 — lift target inventory) | **CONSUMIR estos exports (no recrear):** `EntityWorkspaceLayout` · `EntitySubNavBar` · `EntityInfoCard` · `EntityPicker` · `archetypes` (List/Detail/Form/Dashboard PageScaffold) · `layout` (PageContainer/page/toolbar/states/skeletons/pagination). Átomos Shadcn duplicados (button/card/dialog/select/...). Consume `@luana/hooks` + `@luana/design-tokens` + `@luana/format`. | src/index.ts + EntityWorkspaceLayout.tsx + EntitySubNavBar.tsx |

**★ N3 canon signatures VERBATIM (load-bearing — el builder consume, NO recrea):**
```ts
// @luana/ui-kit EntityWorkspaceLayout
interface EntityWorkspaceLayoutProps {
  entity: EntitySubNavEntity | null;   // null = SSR skeleton / master mode / loading
  leaves: EntitySubNavLeaf[];          // content leaves + optional add affordance
  rootHref: string;                    // master grid href, ej. /{tenantId}/{agent}/{collection}
  rootLabel: string;                   // ej. "Staff"
  isLoading?: boolean;                 // store-free skeleton (G2)
  placeholder?: string;                // master mode placeholder
  onAddAffordance?: () => void;        // fired on add-leaf click (mutation sin routing literal)
  children: ReactNode;                 // active leaf page content
  className?: string;
}
// activeLeaf = URL-derived (useParams [leaf]) — NUNCA stored. router.push entre leaves = soft nav.
// "use client" + STORE-FREE (G2: subscribe a NINGÚN store). Named export (NO default).

// EntitySubNavBar leaf:
interface EntitySubNavLeaf {
  id: string; label: string; href: string;   // href pre-built por el caller (tenantId+entityId)
  isAddAffordance?: boolean; prefixEmoji?: string;
  avatarBgClass?: string;  // full static Tailwind class (JIT-safe — NO template literals)
  isPrimary?: boolean; isRootLeaf?: boolean;  // isRootLeaf interno, set por el componente
}
interface EntitySubNavEntity { id: string; name: string; avatarUrl?: string|null; icon?: string; }
```
- Master mode (`entity=null`): solo root leaf, activo. Workspace mode: root leaf inactivo (clickable → rootHref) + entity identity + content leaves + add affordance.
- role=tablist + roving tabindex + flechas Left/Right/Home/End (a11y heredado, SC-16).
- Full-bleed ribbon: `sticky top-0 z-20 bg-card border-b border-border rounded-none`.
- testids: `entity-workspace-layout`, `entity-sub-nav-bar`, `entity-sub-nav-tablist`, `entity-leaf-root`, `entity-leaf-add-affordance`, `entity-leaf-{id}`, `entity-sub-nav-skeleton`.
- **Adaptación de signatura:** la copia brand-local vieja usaba `{rootHref, rootLabel, entity, leaves, activeLeaf, className}` con back-link separado; el core unifica root-pill (`‹ {rootLabel}` peer leaf) + identity. Adaptar call-sites de staff/embudo.

## 6. Git diff summary

`git diff main..HEAD` = **14 files, +1643/-7, DOCS ONLY** (el ready package + checkpoints). **CERO production code** — el build NO ha empezado (story recién en `developing` phase `BUILD_T1`).

Branch `wip/vitalia`. Commits del story en branch:
- `283fa5ff` docs(vitalia): shell-core-hardening READY — ready package /architect (6 artifacts)
- `abf89364` docs(vitalia): shell-core-hardening REFINED — spec unificado v1 + firma reconciliación
- `8c128aa6` docs(vitalia): umbrella vitalia-shell-core-hardening — consolidación chrome shell + gate ejecutado

Implicación builder: empezar from clean code state. Todos los cambios FE de los 8 tickets son net del build. El baseline construido del backbone (614bfbd5 + 29451ef6) ya está en `main`/branch base (no en este diff).

## 7. Existing systems detected (NO-NEW-LAYER scan)

> Scope: `vitalia/frontend/src/` + `core/@luana/`. Evidencia, no veredicto. Severidad de duplicación: el N3 brand-local es un mirror ACTIVO del core (resuelto por la story = RETIRE).

| Sistema | Path | Estado | Evidencia |
|---|---|---|---|
| **N3 canon (core)** | `core/@luana/ui-kit/src/{EntityWorkspaceLayout,EntitySubNavBar,EntityInfoCard,EntityPicker}.tsx` | active v0.3.0 | Exportado en `src/index.ts`. Signatures verbatim en §5.5. Es el target del consumo. |
| **N3 brand-local (MIRROR)** | `vitalia/frontend/src/components/shared/shell-organism/EntitySubNavBar.tsx` (9.3K) + `.test.tsx` | active (legacy, signatura vieja) | DUPLICA el de core. **Consumido HOY por:** `features/lisa/components/staff/workspace/StaffWorkspaceShell.tsx:24` + `features/adrian/components/embudo/lead/LeadWorkspace.tsx:28` (ambos importan `@/components/shared/shell-organism/EntitySubNavBar`, NO `@luana/ui-kit`). NO existe brand-local `EntityWorkspaceLayout` (solo `EntitySubNavBar`). → RETIRE tras migrar (AC-9). |
| **shell-store SSR-safe** | `vitalia/frontend/src/stores/shell-store.ts` | active (LEGACY shape) | `valeriaState: collapsed/rail/full` + `shellMode` + `cycleValeriaState`. Consume `createSsrSafePersistedStore`. Refactor T-1 + migrate. |
| **Store SSR-safe factory** | `@luana/hooks/create-ssr-safe-persisted-store` | active | Consumido por shell-store + tenant-store + chat-store. CONSUMIR, NO recrear. |
| **shellMode footprint** | ~13 archivos (store, `ShellModeToggle.tsx`, `ValeriaSidebar.tsx`, `ShellOrganismLayoutClient.tsx`, arch tests `test-shell-store-schema*.test.ts`) | active | AC-1 exige grep `shellMode`/`ShellModeToggle` = 0 post-build. Los arch tests `test-shell-store-schema*` referencian el shape viejo → el builder debe actualizarlos. |
| **Edge-redirect proxy** | `vitalia/frontend/src/proxy.ts` | active (YA tiene 307) | `bareTenantLandingRedirect` (L28 import de `@/lib/shell-routes`) + `NextResponse.redirect` 307 (L65-67) + matcher (L74). Decisión A = verificar/extender, NO crear de cero. |
| **frozen-kpi-badge band-aid** | `vitalia/frontend/src/features/adrian/components/embudo/EmbudoMetrics.tsx:80-92` | active (band-aid) | `<a href>` hard-nav intencional (comment L13 cita learning 2026-06-03). `data-testid="frozen-kpi-badge"` L87. Decisión A = revertir a `next/link`. |
| **dark tokens** | `vitalia/frontend/src/app/globals.css` | active (mayormente dark-aware) | 155 matches `data-theme="dark"`/`vt-*`/`--vitalia-*`. Solo **2 archivos** con hex/rgb hardcodeado en shipped subtabs (inbox + staff/mateo) → BUG#2 es NARROW (confirma estrategia Decisión C: hardcoded→token, no wholesale). `ContactSidebar.tsx` (inbox + shared) es el caso canónico a auditar. |
| **race test** | `vitalia/frontend/e2e/regression/vitalia-fase1-shell-layout-5050/resize-and-state.spec.ts` | active | NO contiene `test.skip(true)` literal — tiene 6 tests SC-3 activos (drag clamp, persist, reload survive, full→rail no-shrink, rail→full snap-up). El "test omitido" del race (AC-14) es un assert comentado/diferido que el builder debe LOCALIZAR + reactivar contra la máquina nueva (03-arch-fe.md § 10 lo advierte explícitamente). |

**§7-cross-brand (HIGH severity — flag para architect, NO enumerado):** `nicolify/frontend/src/components/shared/shell-organism/EntitySubNavBar.tsx` también existe (mirror cross-brand del mismo N3). NO se toca en esta story (forbidden). Convergencia nicolify→core = otra story /pm-luana post-merge. El builder NO debe scanear ni tocar nicolify.

## 7.5 Anti-duplication inventory cross-reference

`anti-duplication.md` SSoT (engine abstractions) es Python-centric (`{engine_prefix.python_glob}`); el inventario concreto vive en `docs/rules-detail/anti-duplication.md` (no cargado — stack-specific). Para esta story FE, el inventario relevante es `core/@luana/*` (TS engine):

| Subsystem en PR scope | En inventario engine (`@luana/*`)? | Canonical path | Recomendación |
|---|---|---|---|
| N3 list/detail (`EntityWorkspaceLayout`/`EntitySubNavBar`) | YES (`@luana/ui-kit` v0.3.0) | `core/@luana/ui-kit/src/EntityWorkspaceLayout.tsx` + `EntitySubNavBar.tsx` | **CONSUMIR (EXTEND from canonical)** — migrar consumers, retirar brand-local mirror. Hard rule: mirror banned. |
| Store SSR-safe factory | YES (`@luana/hooks`) | `@luana/hooks/create-ssr-safe-persisted-store` | **CONSUMIR** — ya consumido, conservar. |
| page-primitives / archetypes | YES (`@luana/ui-kit`) | `core/@luana/ui-kit/src/{layout,archetypes}` | CONSUMIR si se necesita (el master grid de staff puede adoptar `EntityInfoCard` pero AC-9 solo exige migrar el WORKSPACE — scope-discipline). |
| shell chrome (ShellOrganismLayout/Ribbon/ValeriaSidebar/...) | proposal accepted (`2026-06-01-lift-shell-organism-to-core`) pero NO shipped a core aún | brand `vitalia/.../shell-organism/` (hoy) | **MODIFICAR brand** — NO liftear acá (es outcome platform 1-2 sem). Handoff /pm-luana. |
| dark tokens `--vitalia-*` | NO (brand-specific; `@luana/design-tokens` solo Z_INDEX) | `vitalia/.../globals.css` | brand-specific, NO liftear. |

**Escalaciones §11:** ninguna HIGH del lado de la story (el mirror N3 es resuelto-por-diseño = RETIRE, no una propuesta NEW indebida). El nicolify cross-brand mirror = MEDIUM (post-merge /pm-luana, ya en plan).

## 8. EXTEND vs NEW recommendations

| Surface PR propone | Existing system ≥80% overlap (§7) | En inventario (§7.5)? | Recomendación | Razón |
|---|---|---|---|---|
| N3 workspace para staff/embudo | `@luana/ui-kit` EntityWorkspaceLayout (100%) | YES | **EXTEND/CONSUMIR** | Canon shipped v0.3.0; migrar imports staff:24/embudo:28 de brand-local → `@luana/ui-kit`. |
| Retirar `EntitySubNavBar` brand-local | mirror exacto del core | YES | **REMOVE (post-migrate)** | Mata mirror; encoge `test-no-cross-brand-shell-mirror` allowlist (shrink-only). |
| shell-store máquina nueva | `shell-store.ts` legacy (refactor) | factory YES | **EXTEND** | Refactor del store existente + migrate legacy; consume factory `@luana/hooks`. NO recrear store. |
| `ValeriaCollapsedStrip` (tira-avatar A) | ninguno (afford. de reapertura nuevo) | NO | **NEW** (justificado) | RN-12 net-new; transformar `ValeriaRail.tsx` (rail 60px se va). Candidato lift cross-brand. |
| edge-redirect soft-nav | `proxy.ts bareTenantLandingRedirect` (307 ya existe) | n/a | **EXTEND** | Verificar/extender matcher; NO crear de cero. |
| revertir band-aid hard-nav | `EmbudoMetrics.tsx:80-92 <a href>` | n/a | **MODIFY** | `<a href>` → `next/link` soft-nav (AC-13). |
| dark hardcoded→token | 2 archivos shipped con hex/rgb | tokens dark ya existen | **MODIFY** (migrar a token) | Decisión C: PREFERIR migrar al token semántico dark-aware sobre crear variante nueva. |
| máquina de estados limpia (RN-5/6/7/13) | legacy collapsed/rail/full | NO | **NEW model** (justificado) | Net-new para ambas marcas; supera el conflated legacy. Candidato lift.

**Decision rule aplicado:** todo lo que está en `@luana/*` inventory → EXTEND/CONSUMIR (hard). Lo brand-specific (tira-avatar, máquina nueva) → NEW justificado documentado. CERO `REPLACE` recomendado.

## 9. Architecture fitness gates

Gates que corren contra el diff (NATIVE host, per 04-validators.yaml):
```bash
cd vitalia/frontend && npx tsc --noEmit
cd vitalia/frontend && npx eslint src/ --cache
cd vitalia/frontend && npx eslint src/ --cache --rule 'no-arbitrary-value: error' || true  # advisory
cd vitalia/frontend && npx vitest run src/__tests__/architecture/
cd vitalia/frontend && npx vitest run src/components/shared/shell-organism/ src/stores/
# e2e (real-backend, base.ts, NUNCA make e2e* — OOM):
cd vitalia/frontend && E2E_BASE_URL=http://localhost:3002 npx playwright test e2e/regression/vitalia-fase1-shell-layout-5050/ --project=smoke
cd vitalia/frontend && E2E_BASE_URL=http://localhost:3002 npx playwright test e2e/regression/shell-core-hardening/ --project=smoke
cd vitalia/frontend && E2E_BASE_URL=http://localhost:3002 npx playwright test --project=a11y --grep shell
# si se toca ui-kit (default = consumir, no editar):
cd core/@luana/ui-kit && pnpm --filter @luana/ui-kit typecheck && pnpm --filter @luana/ui-kit test
```

**Arch tests que corren contra este diff (confirmados existen en `vitalia/frontend/src/__tests__/architecture/`):**
- `test-no-cross-brand-shell-mirror.test.ts` — el allowlist puede ENCOGER al retirar `EntitySubNavBar` brand-local (shrink-only OK; verificar que no rompa al migrar consumers).
- `test-no-clerk-organizations.test.ts` — debe seguir verde (shell no introduce org).
- `test-shell-store-schema.test.ts` + `test-shell-store-schema-readonly-f1-s5.test.ts` — ★ referencian el shape viejo del store (`valeriaState collapsed/rail/full` + `shellMode`); el T-1 refactor DEBE actualizarlos (sino rompen).
- FSD boundaries (`boundaries/dependencies`) — import de `@luana/ui-kit` permitido (cross-brand vía `@luana/*`).
- no-arbitrary-values / no-hardcoded-color (si activos por ADR-014) — el dark token-audit los hace ENCOGER.

**Allowlist plan:** shrink-only. NO crecer ningún allowlist sin justificación en commit.

**Naturaleza de verificación (DoD #37):** `funcional` → gates automáticos + anti-burbuja base.ts + cobertura por regla de negocio (gherkin @rule) + **demo Chris (G chris_verify.signoff)** + live-verify dev-app (`dod_live_verified: true` + `dod_evidence`). mutation_gate Stryker enabled (líneas nuevas shell-store + ValeriaCollapsedStrip; degrada advisory si falta tool).

## 10. Implementation log highlights

No `T-{n}-impl-log.md` ni `06-audit/` existen aún — story recién entró a `developing` phase `BUILD_T1` (2026-06-10). El build NO ha empezado.

**8 tickets DAG (06-tickets.yaml + dispatch-plan.md):**
```
T-1 (store máquina) ─┬─ T-2 (chrome layout: topbar/clamp/drawer/quitar web/historial)
                     └─ T-3 (Valeria: tira-avatar + cabecera +/historial/colapsar)
T-4 (proxy edge-redirect + revertir band-aid) ── paralelo (depends nada)
T-5 (N3 → @luana/ui-kit) ── depende T-2
T-6 (dark token-audit shipped) ── depende T-2,T-3,T-5
T-7 (e2e SC-1..22 + reactivar race) ── depende T-1..T-6
T-8 (SHELL-DESIGN-CONTRACT + handoff /pm-luana + live-verify) ── depende T-1..T-7
```
Orden serial recomendado single-builder: T-1 → T-2 → T-3 → T-4 → T-5 → T-6 → T-7 → T-8. Todos `builder-frontend` / sonnet. Bucket locks `code:{shell,clinics,crm}` al arrancar build. `parallel_safe: false` (toca el shell mismo).

**Pause-points (dispatch-plan.md, NO bloqueantes pero el chain DEBE respetarlos):**
- T-5: edit ui-kit NO-additivo → STOP /pm-luana.
- T-4: bump Next → escalate Chris.
- Demo Chris (G) NO se relaja por autonomous: `chris_verify.signoff` HARD antes del merge.

**Build preconditions ejecutadas (checkpoint, ratificado Chris):**
- `vitalia-fase2-adrian-embudo` → `defer_audit: true` (developed + dod_live_verified preservados; auditor post-hardening evita re-audit por rebase del chrome crm).
- `vitalia-fase2-lisa-doctores` → parked (keystone regresión /lisa/staff VIVA — bug feature Lisa, NO chrome).
- `vitalia-fase2-lisa-servicios` → parked (su N3 depende del contrato EntityWorkspaceLayout que cambia acá).

## 11. Faithfulness gaps + validator findings

**MEDIUM:**
- **M1 — nicolify cross-brand mirror del N3.** `nicolify/.../shell-organism/EntitySubNavBar.tsx` también existe (mirror del N3 que esta story consolida en core para vitalia). NO se toca acá (forbidden, otra brand). Convergencia nicolify→core es story /pm-luana post-merge — YA en plan (checkpoint `cross_brand_consumers: [vitalia, nicolify]`). Flag para architect/auditor, no acción del builder.

**LOW:**
- **L1 — race "test omitido" no es `test.skip(true)` literal.** El grep confirmó que `resize-and-state.spec.ts` tiene 6 tests SC-3 activos pero NO un `.skip(true)` literal. El spec/arch-fe ya lo advierte (03-arch-fe §10: "el grep actual no lo muestra como `.skip` literal; el builder localiza el assert omitido/comentado del race"). AC-14 = el builder debe LOCALIZAR el assert diferido + reactivarlo. No es discrepancia del brief — es un known-unknown que el builder resuelve en T-7.
- **L2 — docs de framework no fetcheadas (4 de 7).** Solo nextjs.org se fetcheó. react-resizable-panels/zustand/playwright/vitest/tailwind/react son referencias (§15). Mitigado: el architect ya delegó la verificación upstream del "Rendered more hooks" al T-4 builder + las versiones están confirmadas en package.json.
- **L3 — `anti-duplication.md` inventario detallado (docs/rules-detail) no cargado.** Es Python-centric (engine packages); para esta story FE el inventario relevante es `@luana/*` (TS), cubierto en §7.5 directamente. No afecta faithfulness.
- **L4 (validator-surfaced) — theme provider location.** El adversarial scan reveló que el provider `next-themes` vive en `vitalia/frontend/src/app/providers.tsx`. El brief cubre el wiring dark conceptualmente (§2 Decisión C "wiring next-themes se conserva") pero no nombraba el path. Informativo; el builder lo encuentra por grep. NO conserva el wiring incorrectamente.

**Validator findings (CONTEXT-BRIEF-validation.md):** 0 HIGH, 0 MEDIUM, 1 LOW (L4 arriba). Synonym re-scan clean (cero sistemas missed). 3/3 claims aleatorios verificados EXACTOS. §15 fetch accurate. → flag sellado en **clean**.

**Sin entradas HIGH.** PR.md (01-spec) presente + completo. Scope decidible (3 decisiones cerradas, ready package completo 6 artifacts + dispatch-plan). Skills SSoT cargadas (frontend-expert + vitalia-design-system + ui-kit inventory). N3 dup resuelto-por-diseño (no propuesta NEW indebida).

**Provisional flag: clean** (cero HIGH, solo MEDIUM/LOW informativos sin acción de corrección de inputs). → validator puede escalar.

## 12. Raw paths consulted

```
# PR folder
vitalia/docs/product/stories/vitalia-shell-core-hardening/{01-spec,03-arch,03-arch-fe,05-guidelines}.md
vitalia/docs/product/stories/vitalia-shell-core-hardening/{04-validators,06-tickets}.yaml
vitalia/docs/product/stories/vitalia-shell-core-hardening/{dispatch-plan,checkpoint}.md
# backbone (cross-referenced)
vitalia/docs/product/stories/vitalia-bugfix-shell-valeria-responsive/01-spec.md
# lift target (ui-kit)
core/@luana/ui-kit/{package.json, src/index.ts, src/EntityWorkspaceLayout.tsx, src/EntitySubNavBar.tsx}
core/@luana/ui-kit/src/ (tree: archetypes/, layout/, examples/, __tests__/)
# module state
vitalia/docs/product/modules/shell-organism.md (+ crm.md confirmed exists)
vitalia/frontend/src/stores/shell-store.ts (LEGACY shape)
vitalia/frontend/src/components/shared/shell-organism/ (full ls — all Valeria*/Tenant*/Theme*/useViewportGuard confirmed)
vitalia/frontend/src/proxy.ts · features/adrian/components/embudo/EmbudoMetrics.tsx
vitalia/frontend/e2e/regression/vitalia-fase1-shell-layout-5050/resize-and-state.spec.ts
# skills
.claude/skills/frontend-expert/SKILL.md · .claude/skills/vitalia-design-system/SKILL.md
# rules: auto-loaded in context (frontend-fsd/quality/visual-fidelity, tdd, tenant-isolation, spanish-text,
#   dod-live-verify, anti-duplication, parallel-safety, git-safety, architectural-fitness, test-design-doctrine,
#   + vitalia overlay: hipaa-lite, shell-mockup-per-component, shell-feature-architecture-mandatory)
```

## 13. Verbatim grep + WebFetch commands executed

```bash
WS=/home/chalreme/Proyectos/luana-vitalia; FE=$WS/vitalia/frontend/src; UIK=$WS/core/@luana
# N3 dup (core vs brand-local) + consumers
grep -rln "EntityWorkspaceLayout\|EntitySubNavBar" "$UIK" | grep -v node_modules | grep -v __tests__
grep -rln "EntityWorkspaceLayout\|EntitySubNavBar" "$FE"
grep -rn "import.*EntitySubNavBar\|from \"@luana/ui-kit\"" \
  "$FE/features/lisa/components/staff/workspace/StaffWorkspaceShell.tsx" \
  "$FE/features/adrian/components/embudo/lead/LeadWorkspace.tsx"
# shellMode footprint (AC-1 target = 0)
grep -rln "shellMode\|ShellModeToggle" "$FE"
# store factory consumers
grep -rln "createSsrSafePersistedStore\|useShellStore\|shell-store" "$FE"
# dark hardcoded in shipped subtabs
grep -rlE "#[0-9a-fA-F]{6}|rgb\(|bg-\[#" "$FE/features/adrian/components/inbox/" \
  "$FE/features/lisa/components/staff/" "$FE/features/mateo/"
grep -cE "data-theme=\"dark\"|\.vt-|--vitalia-" "$FE/app/globals.css"
# proxy edge-redirect (Decisión A)
grep -n "bareTenantLandingRedirect\|307\|NextResponse.redirect\|matcher" "$FE/proxy.ts"
# band-aid revert target
sed -n '78,95p' "$FE/features/adrian/components/embudo/EmbudoMetrics.tsx"
# race spec
grep -n "test.skip\|test(\|drag\|immediate" "$WS/vitalia/frontend/e2e/regression/vitalia-fase1-shell-layout-5050/resize-and-state.spec.ts"
# git diff (story branch)
git diff main..HEAD --stat
# versions
grep -E "\"(next|react-resizable-panels|zustand)\"" "$WS/vitalia/frontend/package.json"
```
WebFetch: `https://nextjs.org/docs/app/building-your-application` → resolvió a Getting Started, version 16.2.9, lastUpdated 2025-05-02.

## 14. Free-form notes — what I'd tell a smart colleague

Esta story es chrome FE puro — cero BE, cero agentic. No te dejes asustar por el tamaño del spec: la mitad es herencia firmada del backbone (`vitalia-bugfix-shell-valeria-responsive`). Lo NUEVO de la umbrella es: dark + soft-nav + race generalizado. Leé AMBOS specs (umbrella manda en conflicto).

El hallazgo que más te ahorra tiempo: **el N3 (`EntityWorkspaceLayout`/`EntitySubNavBar`) YA está shipped en `@luana/ui-kit` v0.3.0** — NO lo portes de nicolify. Tu trabajo es CONSUMIR: cambiar 2 imports (StaffWorkspaceShell.tsx:24 + LeadWorkspace.tsx:28 hoy apuntan a `@/components/shared/shell-organism/EntitySubNavBar`) a `@luana/ui-kit`, adaptar la signatura (root-pill + identity unificados, el back-link ya no es separado), y BORRAR la copia brand-local. Las firmas verbatim están en §5.5 — el `onAddAffordance` y el `isRootLeaf` interno son los detalles que confunden.

Tres trampas confirmadas en código:
1. **NO toques `ssr:false`** — es necesario por react-resizable-panels v4.11.1 (bare-name `localStorage` default param crashea SSR). El soft-nav se arregla en `proxy.ts` (el 307 YA existe — solo verificá/extendé) + revertir el `<a href>` band-aid en `EmbudoMetrics.tsx:80-92` a `next/link`.
2. **El "test omitido" del race NO es un `test.skip(true)` literal** — el spec lo advierte (03-arch-fe §10). Tenés que LOCALIZAR el assert comentado/diferido del drag inmediato y reactivarlo contra los clamps nuevos (320, no 580/620).
3. **El store actual tiene la forma LEGACY** (`collapsed/rail/full` + `shellMode`). El migrate no es opcional: usuarios reales tienen el shape viejo en `vitalia-shell-state` localStorage (mismo key). Sin migrate → crash en hidratación (SC-18). Y ojo: los arch tests `test-shell-store-schema*.test.ts` referencian el shape viejo → actualizalos o rompen.

Dark es más chico de lo que suena: solo 2 archivos shipped tienen hex/rgb hardcodeado (los tokens `--vitalia-*` YA tienen variante dark). Migrá el hardcoded al token semántico, NO crees variantes nuevas por clase (Decisión C reduce deuda). El caso canónico es `ContactSidebar.tsx` (inbox).

Gate #37 es HARD: ni autonomous ni suite verde te salvan — tenés que ejercer la acción real en dev-app.vitalialat.com (Chrome DevTools MCP) + leer logs + `dod_evidence`. Demo Chris (G chris_verify.signoff) antes del merge, sin excepción. Y AC-10: el board/writes del embudo (developed, defer_audit) NO se pueden romper por el rebase del chrome crm.

## 15. Upstream canonical docs references

| Framework (versión repo) | Canonical URL | Relevancia para este PR |
|---|---|---|
| **Next.js 16.2.3** (docs @ 16.2.9, 2025-05-02) | `https://nextjs.org/docs/app/getting-started/linking-and-navigating` | Soft-nav (`Link`/`router.push` + prefetch + client-side nav) vs hard-nav. Core del bug B1 (soft-nav intra-route-group → layout `ssr:false` → "Rendered more hooks", flaky ~40%, Next 16.2.3). T-4 debe WebSearch `"Next.js Router Rendered more hooks fix"` + chequear si hay fix upstream consumible ANTES de más edge-redirects (NO bumpear Next sin gate completo). |
| **react-resizable-panels ^4.11.1** | `https://github.com/bvaughn/react-resizable-panels` | El `storage` default param bare-name `localStorage` crashea SSR (causa documentada del `ssr:false`). minSize numeric = pixels (usar string `"${pct}%"`). NO eliminar ssr:false. |
| **Zustand ^5.0.5** | `https://zustand.docs.pmnd.rs/` | persist middleware + `skipHydration` + `_hasHydrated` + setItem NO-OP pre-hydration (ADR-vitalia-006). El refactor de la máquina CONSERVA este patrón vía `createSsrSafePersistedStore`. |
| **React 19** | `https://react.dev/reference/react` | "Rendered more hooks than during the previous render" = hooks condicionales / montar-desmontar componentes condicional. Por eso gate desktop↔mobile = CSS, NUNCA JS (montar/desmontar `<Group>` lo trippea). |
| **Tailwind v4** | `https://tailwindcss.com/docs` | dark mode via `[data-theme="dark"]` selector (wiring existente se conserva). Arbitrary-values prohibidos (canon). JIT: clases dinámicas necesitan strings estáticos completos (`avatarBgClass`). |
| **Playwright** | `https://playwright.dev/docs/intro` | e2e real-backend + base.ts anti-burbuja (pageerror/console-error/api≥400/Next overlay). NUNCA `@playwright/test` directo. SC-20/21/22 real-backend. |
| **Vitest** | `https://vitest.dev/guide/` | unit/component RED-first (shell-store máquina, ValeriaCollapsedStrip, etc). 20% coverage per-brand. |

> Solo se fetcheó Next.js (1 fetch) — los demás son referencias. El architect ya delegó la verificación upstream del "Rendered more hooks" al T-4 builder (Research Notes 03-arch.md § 15).

## 16. Self-budget snapshot

- **Tools usados:** ~22 (Read ×13 incluyendo backbone, Bash ×7, Write ×1 skeleton, Edit ×9, WebFetch ×1).
- **Files leídos:** 17 (9 PR files + backbone spec + 4 ui-kit + shell-store + shell-organism.md + 2 skills). Total ~210 KB.
- **Greps corridos:** ~8 chained Bash calls (cubren N3 dup, shellMode, store, dark, proxy, band-aid, race, git diff, versions).
- **Web fetches:** 1 (nextjs.org).
- **Tokens estimados consumidos:** ~75-85k (chars / 3.5 — specs grandes + auto-loaded rules en contexto).
- **Turns restantes:** ~95 de 120 (amplio headroom).
