# CONTEXT-BRIEF for vitalia-fase2-lisa-doctores — DELTA v3 (regenerado 2026-06-11)

> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder
> Modules: clinics, shell, assets
> FOCUS: SOLO delta v3 (spec § "★ Delta v3" → end + 03-arch-delta.md + checkpoint § scope_extension_2026_06_11). Build original (T-BE-1..7, T-FE-1..3, T-E2E) YA HECHO — este brief comprime lo necesario para los 12 tickets delta.
> Faithfulness flag: **partial** (MEDIUM: validator in-process por toolset + F-1 download greenfield — ver §11; cero HIGH)
> Audit log: vitalia/docs/product/stories/vitalia-fase2-lisa-doctores/context-builder-logs/iter-2-2026-06-11T23:05:00Z.log
> Validator pass: vitalia/docs/product/stories/vitalia-fase2-lisa-doctores/CONTEXT-BRIEF-validation.md (in-process adversarial — 7 probes, 3 re-checks, 1 web re-check; sin discrepancias HIGH)

## 1. PR summary

**Delta v3 de `vitalia-fase2-lisa-doctores`** (F2-S8, `state: developing`, REANUDADA 2026-06-11 post shell-core-hardening, cap `lisa.doctores`). El build original (T-BE-1..7 + T-FE-1..3 + T-E2E, 2026-05-31) está HECHO y live-verified — NO re-arquitecturar. Este delta = **6 workstreams / 12 tickets** ratificados Chris (FIRMA 1 funcional + FIRMA 2 mockup `doctores.html` v3.2, ambas TRUE en checkpoint):

| WS | Qué | Tickets |
|---|---|---|
| D3-A | Entity switcher N3: `EntityPicker` (core, YA construido) entra a `EntitySubNavBar` vía prop opcional `entityIdentitySlot` + cableado doctores (preserva hoja al cambiar) | T-CORE-picker-slot (lift gated /pm-luana) → T-FE-switcher-wire |
| D3-B | Bio docs FUNCIONAL: subida real a R2 (kind `bio_doc` nuevo), tabla `vitalia_doctor_bio_files` (mig 040), list/delete/download + audit + homologación canon (átomos reales, matar emoji-autosave inline) | T-BE-bio-docs → T-FE-bio-docs |
| D3-C | FIX occurrences (repro Chris: occurrences=2 → repite indefinido). Root cause CONFIRMADO: `AvailabilityCalendar.tsx::recurrentBlockVisibleInWeek` ignora occurrences/open_ended (render FE; BE rrule CORRECTO). Fix = NEW endpoint range `availability-occurrences` + FE consume proyección BE (muere expansión client-side) | T-BE-occurrences-endpoint (RED first) → T-FE-occurrences-consume |
| D3-D | Página pública doctor `/d/{clinica-slug}/{doctor-slug}` (mini-Doctoralia mobile, SIN CTA, SIN stats, anti-enumeración) + perfil ESTRUCTURADO (supersede BioPublic 3-blobs, mig 041) + 4ª hoja N3 "Página" + generación estado-driven + OG tags | T-BE-pagina-publica → T-FE-pagina-publica |
| D3-E | Vista MES calendario (NEW, 6×7, chips, consume MISMO endpoint occurrences, read+nav only) | T-FE-vista-mes |
| D3-F | Editor recurrencia clon Google Calendar: `days_of_week[]` + `interval` (mig 042 compat), chips L-D, Termina nunca/fecha/N, resumen humano single-source | T-BE-recurrencia-domain → T-FE-recurrencia-editor |
| x-cut | E2E delta real-backend + visual goldens (ratifica Chris en G) | T-E2E-delta |

**Mandato modelo (checkpoint):** Fable 5 para builders + auditor del delta (NO Opus; R23 N/A — cero agentic). `autonomous_mode: true` con 2 gates NO-autonomous: merge T-CORE (lift /pm-luana — proposal YA accepted, OQ-1) + goldens (Chris en G). DAG/orden: 03-arch-delta + 06-tickets `delta_dag` (D3-C BE antes que D3-F BE — ambos editan `availability_projection_service.py`, serializa `code:clinics` M14).

## 2. Contract decisions

SSoT = `03-arch-delta.md` (351 líneas, Fable 5, 2026-06-12). Resumen load-bearing:

**Endpoints nuevos (todos bajo `doctors_router` existente salvo público, todos `response_model=` + RBAC `_STAFF_MUTATION_ROLES` {owner, admin_clinic}):**
- `POST/GET/DELETE /{doctor_id}/bio-files(+/{file_id})` + `GET .../download` — DTOs `BioFileDTO {id, filename, size_bytes→sizeBytes, content_type, uploaded_at→uploadedAt}` · audit `doctor.bio_file_{added,deleted,downloaded}` · soft delete · RN-D3B-1 bio snapshot intocable
- `GET /{doctor_id}/availability-occurrences?from=&to=` (max 62 días) → `AvailabilityOccurrenceDTO {block_id, occurrence_date, start_time, end_time, kind, freq, pattern_summary}` — colapsa slots a 1 occurrence por (block, date); REUSA `AvailabilityProjectionService`
- `GET /api/public/clinic/{tenant_slug}/doctors/{doctor_slug}` (NO auth) → `PublicDoctorProfileDTO` allow-list (secciones nullable RN-D3D-6, idiomas solo si len>1 RN-D3D-5, NO stats, NO CTA). Toggle OFF / slug desconocido / cross-tenant → 404 genérico IDÉNTICO (anti-enumeración RN-D3D-9)

**Dominio/DTO extends:** `DoctorDetailDTO` += `bio_files[]` + `profile_state {generated_at, material_new, material_new_count}` (detección server: `max(insumos.updated_at) > bio_generated_at` — RN-D3D-4). `DoctorPublicProfile` VO estructurado (sobre_mi · formacion[] · experiencia[] · tratamientos[] · certificaciones[] · idiomas[]) en JSONB `vitalia_doctors.public_profile`. `AvailabilityBlockDTO` += `daysOfWeek: number[]`, `interval: number` (legacy single-day coerced).

**Migraciones (chain 039→040→041→042, raw SQL `IF NOT EXISTS`):** 040 tabla `vitalia_doctor_bio_files` (tenant_id+clinic_id dual-filter, 2 índices) · 041 `vitalia_doctors` += `public_profile JSONB, bio_generated_at TIMESTAMPTZ, public_slug TEXT` + unique idx `(tenant_id, clinic_id, public_slug)` + backfill desde `bio_public` + slugify · 042 `vitalia_availability_blocks` += `days_of_week JSONB, interval INTEGER` + backfill `[day_of_week]` / `freq=='biweekly'?2:1` (RN-D3F-3 legacy proyecta idéntico).

**Recurrencia:** `rrule(WEEKLY, interval=interval, byweekday=days_of_week, count|until)` — `count` = ocurrencias TOTALES cross-weekday (semántica Google, RN-D3F-2; rrule nativo ya lo hace). `format_recurrence_summary(block)` BE NUEVO en `clinics/application/recurrence_summary.py` = single source del resumen humano (RN-D3F-1), consumido por `pattern_summary` + editor FE (`formatRecurrenceSummary` espeja semántica).

**OQ resueltas (03-arch-delta § 16):** OQ-1 proposal lift `2026-06-12-ui-kit-entity-subnavbar-picker-slot.md` **accepted** → T-CORE desbloqueado · OQ-2 ruta pública excluida de Clerk (ver §14 gotcha: el archivo real es `src/proxy.ts`, lista `isPublicRoute`) · OQ-3 `clinics.slug` EXISTE (`clinic_model.py:39`, unique per-tenant) — guard: lookup >1 clínica → 404 genérico + structlog warning; creación nueva valida unicidad global service-level (sin migración de constraint).

**Env:** `NEXT_PUBLIC_APP_BASE_URL` (FE) + `APP_BASE_URL` (BE) — dev `dev-app.vitalialat.com` · test `test-app.vitalialat.com` · prod `app.vitalialat.com`. NUNCA hardcodear.

## 3. UI spec decisions

Fuente: `01-spec.md § ★ Delta v3` (L465-673) + mockup ratificado `mockups/doctores.html` (FIRMA 2) + canon `docs/architecture/luana-platform/design-system-canon.md`.

- **D3-A switcher:** trigger `▾` en franja N3 abre `EntityPicker` (search server-side debounced + cursor 20 + windowed + ✓ activo). Seleccionar → navega `/lisa/staff/[nuevo-id]/{hoja-actual}` PRESERVANDO hoja (derivar leaf de `usePathname()`, último segmento ∈ {perfil, horarios, servicios, pagina}). Master mode (entity=null) sin picker. Inactivos NO aparecen (`active=true`).
- **D3-B:** filas de docs con icono por tipo + nombre truncado + tamaño + fecha + descargar + eliminar-con-confirm. Estados: subiendo (progreso por archivo) / error (reintentar/quitar) / empty suave. PDF/JPG/PNG/DOCX ≤10MB múltiple. Rechazo → feedback inline, jamás silencioso (RN-D3B-2). Homologación: `Textarea`/`Button` átomos (matar crudos), links Zod URL chips, **eliminar emojis autosave inline** → única `FloatingAutosaveIndicator` por página (canon §2.6), card borde + header `agent-lisa`.
- **D3-C:** CERO cambio visual del calendario semana — solo qué semanas pinta (proyección BE).
- **D3-D Página pública (v3.2 D3-D.1):** INFORMATIVA pura — **SIN botón WhatsApp/contacto/reserva** (RN-D3D-3 REEMPLAZADA), **SIN stats de venta**. Estructura Doctoralia: header sobrio (foto·nombre·especialidad·badge "✓ Nro. colegiatura CMP verificado") → Sobre mí → Formación → Experiencia → Tratamientos (chips) → Certificaciones (✓) → Idiomas (SOLO si >1, posición discreta — RN-D3D-5) → Consultorio. Mobile-first 390×844; desktop centered max-width. Sin foto → avatar iniciales (RN-D3D-8). OG tags vía `generateMetadata()` (RN-D3D-7, sin PHI). Workspace hoja "Página": pill Publicada/Borrador + Copiar link + Ver página + toggle visible + material privado + editor estructurado inline+autosave + preview frame teléfono.
- **Generación estado-driven (mata "regenerar porque sí"):** nunca-generado → único CTA "✨ Generar perfil" · generado-quieto → "Última generación {fecha}" + ⋮ Regenerar (secundario), SIN CTA primario · material-nuevo (server) → banner "⚡ {N} nuevos" + CTA "✨ Actualizar perfil". Overwrite con ediciones manuales → modal confirm (RN-D3B-3).
- **D3-E mes:** toggle pills Semana|Mes (default Semana; drag-create solo semana). Grilla 6×7, chips compactos hora inicio-fin tint `agent-lisa`, overflow "+N más". Click día → vista semana de esa semana. Nav ‹ · hoy · ›. a11y grid + aria por día. Read+nav only (RN-D3E-2).
- **D3-F editor:** Select "Repetir" CANÓNICO §2.5 (NO `<select>` nativo — gate `test-no-native-select`): No se repite / Todos los días / Cada semana el {día} / Cada 2 semanas el {día} / Personalizado…. Sub-editor custom: "Repetir cada [N] semana(s)" + chips días L M X J V S D multi-select (teclado, SC-D3F-5) + Termina: Nunca/El [fecha]/Después de [N] repeticiones. Resumen humano SIEMPRE visible ("Se repite cada 2 semanas los lunes y jueves, 8 veces"). `CalendarBlock` muestra resumen corto del patrón.
- **Gherkin delta:** 37 SC (`SC-D3A-1..4 · D3B-1..5 · D3C-1..8 · D3D-1..15 · D3E-1..4 · D3F-1..5`) — matriz RN→SC sin huecos ni huérfanos (spec L626 + L672). Todos los funcionales FE `playwright_required: true` real-backend (auth.fixture, sin mock del surface).
- **Spanish neutro LatAm** en todo copy nuevo (SC-D3D-14).

## 4. Module current-state extracts

**BE `vitalia/backend/src/modules/vitalia/clinics/` (verificado en disco):**
- `api/`: `doctors_router.py` · `assets_proxy_router.py` (`_VALID_KINDS={avatar, credential_doc}` L59 · `_CREDENTIAL_DOC_ALLOWED_CONTENT_TYPES` L49 · ⚠️ mensaje error L70 hardcodea los 2 kinds — actualizar texto al agregar `bio_doc`) · `public_doctors_router.py` · `account_router.py` · `dtos.py` · `decorators.py`
- `application/`: `availability_projection_service.py` (docstring L9-13: weekly interval=1 / biweekly interval=2 / `count=occurrences` / `until=end_date` / open_ended hoy+90d rolling; `from dateutil.rrule import WEEKLY, rrule` L35; HOY single `day_of_week`) · `availability_block_service.py` · `bio_generation_service.py` (determinista, D-4) · `public_doctor_serializer.py` · `doctor_service.py` · `credential_validator.py`
- `infrastructure/repositories/doctor_repository.py`: **raw SQL con listas de columnas EXPLÍCITAS** (incl `bio_public` en L147/231/316/370/443/556) — mig 041 agrega 3 columnas → tocar CADA SELECT/INSERT/UPDATE (ver §14)
- Migración más reciente: `039_vitalia_config_cuenta_fields.py` → chain delta 040/041/042 correcta

**FE `vitalia/frontend/src/features/lisa/`:**
- `api/staff.ts` (`useAvatarUpload` L371-378 — patrón espejo para `useBioFileUpload`; `API_BASE=''` relativo en client post bug#2; `buildStaffMutationHeaders` manda `X-User-Role`+`X-User-ID` post bug#3) · `staff-server.ts` (SSR, base absoluta OK)
- `components/staff/workspace/`: `StaffWorkspaceShell.tsx` (L25 `import { EntityWorkspaceLayout } from "@luana/ui-kit"` — `LEAF_DEFS` vive acá, agregar 4ª hoja "Página") · `perfil/BioRepoInputs.tsx` (dropzone NO-OP a reescribir) · `horarios/AvailabilityCalendar.tsx` (root cause L99-115 + call site L294-296) · `horarios/BloquePopover.tsx` (a reescribir D3-F)
- `src/proxy.ts` (Next 16 — NO existe `middleware.ts`): `isPublicRoute = createRouteMatcher([...])` L33-50 con `/sign-in`, `/public(.*)`, `/marketing(.*)`, `/showcase(.*)`, etc. — **agregar `"/d/(.*)"` acá** (OQ-2)

**Core `core/@luana/ui-kit/src/`:** `EntityPicker.tsx` + `EntitySubNavBar.tsx` + `EntityWorkspaceLayout.tsx` + `__tests__/{los 3}.test.tsx` — todos activos. `entityIdentitySlot` NO existe aún (grep 0 hits = greenfield T-CORE). Consumers existentes a NO romper: `StaffWorkspaceShell`, `adrian/{NewLeadPage, LeadWorkspace, LeadSummaryHeader}`, `staff/[doctor-id]/layout.tsx`, showcase `EntitySection`, `archetypes/DetailPageScaffold.tsx`.

## 4.5 Cap pointers (cap-as-locator · HB-43)

`resolve_cap.py vitalia lisa.doctores --extract`:
- cap: `vitalia/docs/product/capabilities/clinics/lisa-doctores.yaml` · slug `lisa-doctores` · status **partial**
- package_path: `vitalia/backend/src/modules/vitalia/clinics/` + `vitalia/frontend/src/features/lisa/components/doctores/`
- dev_preview.main_component: `…/features/lisa/components/doctores/DoctorsDirectoryView.tsx` · route `/lisa/staff`
- scenarios (5): crear-doctor-horarios · credencial-invalida-422 · doctor-desactivado-preserva-citas · cross-tenant-doctor-404 · keyboard-a11y-tabs-workspace
- ⚠️ **Pointer STALE:** el código real vive en `features/lisa/components/staff/` (`StaffDirectoryView.tsx`), no `components/doctores/`. Post-merge F.3 corregir `dev_preview` + agregar SC-D3* + RN-D3* (ya listado en 03-arch-delta § 13).

## 5. Relevant rules

| Rule | No-skip para este delta |
|---|---|
| `vitalia/.claude/rules/hipaa-lite.md` | Dual filter `tenant_id+clinic_id` en TODA query PHI/staff (incl `get_by_id` bio_files) · audit log SYNC pre-response (bio_file add/delete/download) · PHI nunca en URLs/GET params · repos heredan `PhiRepositoryBase` |
| `tenant-isolation.md` | FE `useTenantId()` NUNCA Clerk orgId · ruta pública resuelve tenant por slug (sin header) — serializer allow-list = boundary |
| `backend-ddd.md` | Inside-Out domain→infra→app→api · DTOs thin · `response_model=` en TODO endpoint (incl público) · soft deletes · SQLA 2.0 — pero OJO: doctor_repository usa raw SQL (patrón existente, seguirlo) |
| `backend-migrations.md` | Raw SQL `IF NOT EXISTS` SIEMPRE · NUNCA `op.create_table()`/`sa.Enum()` · 040/041/042 prod-clone test |
| `tdd-mandatory.md` + `hotfix-repro-mandatory.md` | D3-C/D3-F: regression RED PRIMERO (SC-D3C-1 weekly×2→2 fechas; SC-D3F-4 legacy idéntico). `repro_evidence` YA anclado (trace: code-inspection + chris-live, checkpoint + ticket T-BE-occurrences) |
| `anti-duplication.md` | EntityPicker se CONSUME (cero picker nuevo) · `sanitize_payload` shared si toca traces · mirror cross-brand ban |
| `frontend-fsd.md` (Tier-2) | features no cross-import · public API via index.ts · componentes delta en `features/lisa/components/staff/workspace/{pagina,horarios}/` |
| `frontend-visual-fidelity.md` + ADR-vitalia-003 | Mockup `doctores.html` ratificado = contrato visual · goldens `maxDiffPixelRatio 0.001` · wrapper shell verbatim, NUNCA reinventar |
| ADR-vitalia-004 (shell-feature-architecture) | 9 secciones: route group + FSD + Server-first + RQ/Zustand sin mezclar + RHF/Zod + PhiRepositoryBase + migrations idempotent + tests 4 capas. Hoja "Página" = 4ª leaf en `LEAF_DEFS` (N3-dyn existente, NO Shadcn Tabs) |
| `definition-of-done-live-verify.md` (#37) | CADA ticket FE exit = live-verify dev-app (write real + logs + efecto) + `dod_evidence` — "NUNCA tests verdes". GET 200 ≠ verificado |
| `e2e-testing.md` + `test-design-doctrine.md` | preflight SIEMPRE · native host `E2E_BASE_URL=http://localhost:3002` · specs via `auth.fixture`/`fixtures/base.ts` (runtime_error_gate), NUNCA `@playwright/test` directo · pública: spec SIN auth + slug-fuzz |
| `spanish-text.md` | Tuteo neutro en todo copy UI nuevo (página pública incluida) |
| `parallel-safety.md` M14/M15 | BE serializa `code:clinics` · FE `code:lisa` · core bucket aparte · commit por pathspec SIEMPRE (hub compartido) |
| `git-safety.md` | Stage por nombre exacto · NUNCA `git add .` · Conventional Commits |
| `anti-default-flip-audit.md` | N/A confirmado (03-arch-delta § 10.5: cero flag flip — todo aditivo) |
| `pii-sanitisation.md` | `response_model=` whitelist en endpoints nuevos · OG tags sin PHI |

## 5.5 Domain skill invariants (SSoT extracts)

| Skill | Hard rules / SSoT highlights | Fuente |
|---|---|---|
| `backend-expert` | Anti-alucinación: si no aparece en código real, NO existe · sin `Any`/dicts mágicos · sin lógica en `api/` · soft delete only · **leer `references/runtime-quality-checklist.md` ANTES de commit y de spawn auditor** · arch fitness nativo `${WS}/.venv/bin/pytest tests/architecture/` | SKILL.md L94-105 |
| `frontend-expert` | Server-First (`"use client"` solo en hojas con estado/handlers) · `cn()` de shared, no helpers dup · ruta nueva → smoke e2e ANTES de la página · **runtime-quality-checklist obligatorio pre-commit** (useEffect deps — ya mordió acá: NuevoIntegranteModal loop) · live verification via `chrome-devtools-verify` | SKILL.md L87-97 |
| `vitalia-design-system` | Tokens SSoT = `globals.css` (⚠️ `core/@luana/design-tokens` solo exporta Z_INDEX, NO colores) · 19 átomos `components/ui/` NUNCA reinventar · hoja NUNCA contiene tabs (N3-static, no Shadcn Tabs) · agent-lisa `#00D084` · wrapper shell portar verbatim · PHI UI → `PiiMaskedSpan`+`RequireRole` · checklist 8 pasos §8 | SKILL.md §1-8 |
| canon `design-system-canon.md` §2.4 | EntityPicker: search server-side debounced + paginado cursor ~20 + windowed + ❌ PROHIBIDO cargar toda la colección al cliente · a11y combobox listbox/option ↑↓ Enter Esc focus-on-open | canon L63-71 |
| canon §2.5 | Select canónico Shadcn-style: ❌ PROHIBIDO `<select>` nativo en producto (gate `test-no-native-select`) — aplica al editor D3-F | canon L73-79 |
| canon §2.6 | UNA sola `FloatingAutosaveIndicator` por página (sticky abajo-centro, role=status) · sin badge por-grupo · autosave 600ms + coalescing + flush() — D3-B mata emojis inline por esto | canon L81-87 |
| canon §6.3-6.4 | `EntitySubNavBar` = root-pill ‹ + EntityPicker + leaves (composición de referencia del slot) · EntityPicker reference impl: useInfiniteQuery + Virtuoso + Popover (el core real es query-lib-agnostic vía `searchFn` prop) | canon L183-222 |

## 6. Git diff summary

Branch `wip/vitalia` vs `main`: **52 commits · 354 files · +12009/−36860** — pero es el branch-hub multi-story (config-cuenta done, cockpit alpaca purge, embudo archive, CIL). **Cero código delta v3 escrito aún** (verificado: `entityIdentitySlot`=0 hits, `availability-occurrences`=0, `bio_doc`=0, `format_recurrence_summary`=0 en código). Los 12 tickets son greenfield sobre superficies existentes. Files clinics tocados recientemente en el branch (por config-cuenta, NO por este delta): `account_router.py`, `dtos.py`, `clinic_account_service.py`, `clinic_model.py`, `clinic{_config,}_repository.py` — al editar `dtos.py` leer el estado actual primero (M8 extend-no-destroy).

## 7. Existing systems detected (NO-NEW-LAYER scan)

Scan propio (brand-scoped vitalia + core/@luana) ejecutado 2026-06-11; CONFIRMA el self-scan del 03-arch-delta § 1 con evidencia línea-exacta:

| Sistema | Path | Estado | Evidencia |
|---|---|---|---|
| `EntityPicker` | `core/@luana/ui-kit/src/EntityPicker.tsx` (+test) | active | searchFn-agnostic; canon §6.4. REUSE as-is (forbidden_to_touch en T-CORE) |
| `EntitySubNavBar` | `core/@luana/ui-kit/src/EntitySubNavBar.tsx` (+test) | active | `entityIdentitySlot` 0 hits → prop a AGREGAR (T-CORE). Consumers: StaffWorkspaceShell, adrian×3, DetailPageScaffold, showcase |
| `EntityWorkspaceLayout` | `core/@luana/ui-kit/src/EntityWorkspaceLayout.tsx` (+test) | active | StaffWorkspaceShell L25 importa de `@luana/ui-kit` (lift 3cb9d5a0 confirmado — la ubicación brand-local del 03-arch-fe original es STALE, reconciliado en 03-arch-delta § 8) |
| Root cause D3-C | `…/horarios/AvailabilityCalendar.tsx` L99 `recurrentBlockVisibleInWeek` · L109 solo chequea `endConditionKind === "end_date"` · L120 `oneOffBlockVisibleInWeek` · L294-296 call site | active (BUGGY) | A ELIMINAR en T-FE-occurrences-consume (validator V-D3C-NODUP: grep=0 post-fix) |
| `AvailabilityProjectionService` | `…/clinics/application/availability_projection_service.py` | active, CORRECTO | rrule L35; weekly/biweekly via interval; `count=occurrences`; open_ended hoy+90d. EXTEND (D3-C consume / D3-F byweekday-list) |
| Assets proxy R2 | `…/clinics/api/assets_proxy_router.py` | active | `_VALID_KINDS` L59 = {avatar, credential_doc} → += `bio_doc`. Allow-list contenido L49 reutilizable. Error msg L70 hardcodea kinds (actualizar) |
| Public doctors | `…/api/public_doctors_router.py` + `application/public_doctor_serializer.py` | active | EXTEND con ruta per-doctor + DTO estructurado (NO capa pública nueva) |
| `BioPublic` 3-blobs | `…/clinics/domain/bio.py` + persistido en `doctor_repository.py` (raw SQL, cols explícitas L147+) | active, a SUPERSEDER | mig 041 backfill; `bio_public` queda read-only legacy |
| `BioGenerationService` | `…/application/bio_generation_service.py` | active, determinista | REUSE para perfil estructurado (D-4: NO agentic, R23 N/A) |
| RQ hooks staff | `…/features/lisa/api/staff.ts` (`useAvatarUpload` L371) | active | patrón espejo para `useBioFileUpload`; `buildStaffMutationHeaders` ya manda X-User-Role/ID |
| Clerk public routes | `vitalia/frontend/src/proxy.ts` `isPublicRoute` L33-50 | active | `/d/(.*)` NO está → agregar (T-FE-pagina-publica). ⚠️ archivo = `proxy.ts`, NO `middleware.ts` |
| `clinics.slug` | `…/infrastructure/models/clinic_model.py:39` | active | `Column(String, nullable=False, index=True)` — unique per-tenant (no global) → guard OQ-3 |
| NO existen aún (greenfield confirmado) | `format_recurrence_summary` · `availability-occurrences` endpoint · `useAvailabilityOccurrences` · `MonthCalendar` · ruta `/d/` · tabla bio_files | — | grep 0 hits en código (solo docs delta) |

## 7.5 Anti-duplication inventory cross-reference

| Subsystem en scope | ¿En inventario `anti-duplication` (engine)? | Canonical path | Recomendación |
|---|---|---|---|
| EntityPicker / EntitySubNavBar / EntityWorkspaceLayout | SÍ (post-lift shell-core-hardening — `@luana/ui-kit` es shared core TS) | `core/@luana/ui-kit/src/` | EXTEND core vía prop aditiva + promotion proposal (YA accepted, OQ-1). Mirror en vitalia PROHIBIDO |
| Recurrencia (rrule) | NO engine — decisión D-2 original: brand-local `dateutil.rrule` (NO `luana-core-commercial-calendar`) | `clinics/application/availability_projection_service.py` | EXTEND brand-local (forbidden_to_touch: commercial-calendar) |
| Upload R2 | NO inventario engine python — proxy assets es brand-local (consume core assets infra) | `clinics/api/assets_proxy_router.py` | EXTEND allow-list (kind nuevo), cero path de upload nuevo |
| PII sanitization (si toca traces) | SÍ | `luana_core_observability…/sanitization.py::sanitize_payload` | CONSUMIR import — pero el delta NO escribe traces nuevos |
| Serialización pública | NO (brand-local, PHI boundary vitalia) | `public_doctor_serializer.py` | EXTEND allow-list + arch test nuevo |

## 8. EXTEND vs NEW recommendations

| Superficie propuesta | Overlap existente (§7) | Inventario | Recomendación |
|---|---|---|---|
| Prop `entityIdentitySlot` core | EntitySubNavBar/EWLayout | SÍ | **EXTEND** core (gated lift — proposal accepted) |
| searchFn adapter page↔cursor + picker wiring | staff.ts hooks | n/a | **NEW** chico en `features/lisa/api/staff.ts` (patrón canon, sin picker nuevo) |
| kind `bio_doc` + endpoints bio-files | assets proxy + doctors_router | n/a | **EXTEND** router/proxy + **NEW** tabla/repo/domain (sin capa nueva — arq ratificada) |
| Endpoint occurrences + `recurrence_summary.py` | AvailabilityProjectionService | n/a | **EXTEND** service vía endpoint nuevo que lo REUSA; summary = NEW file compartido D3-C/D3-F |
| Perfil estructurado + ruta pública per-doctor | BioPublic + public_doctors_router | n/a | **EXTEND** router/serializer + supersede VO con backfill compat |
| Vista mes + editor recurrencia + hoja Página | AvailabilityCalendar/BloquePopover/LEAF_DEFS | n/a | **NEW** componentes en `features/lisa/…` consumiendo átomos canon (Select canónico, NO nativo) |
| NUNCA | — | — | ❌ picker nuevo · ❌ expansión recurrencia client-side (muere) · ❌ capa pública paralela · ❌ mirror core en vitalia

## 9. Architecture fitness gates

Deben seguir verdes (03-arch-delta § 0/§ 12) + 1 NUEVO:
- BE (`vitalia/backend/tests/architecture/`): `test_phi_dual_filter.py` · `test_response_model_required.py` · `test_migrations_idempotent.py` · `test_clinics_domain_no_engine_imports.py` · `test_public_doctors_allowlist.py` (EXTEND) · **NEW `test_public_doctor_profile_allowlist.py`** (V-D3D-ARCH: PublicDoctorProfileDTO cero campos PHI) · `test_audit_log_sync_write.py` · `test_doctor_mutation_rbac.py` (10/10 post bug#3)
- FE (`vitalia/frontend/src/__tests__/architecture/`): `test-no-div-layout` · **`test-no-native-select`** (gate D3-F) · `test-ribbon-no-shadcn-tabs` · `test_fsd_boundaries` · `test-no-clerk-organizations` · voseo
- Core: `@luana/ui-kit` unit suite back-compat ratchet (EntitySubNavBar/EWLayout sin slot → render idéntico)
- Validators v5 (04-validators BACKFILLED): `verification_nature: ambas` · `demo_required: true` (G aplica pese a autonomous) · mutation diff-scoped HARD sobre projection/serializer/migration-backfill · `regression_guard` (suite original + ui-kit) · `runtime_error_gate` fixtures/base.ts en cada spec · jscpd (no re-expandir client-side)

## 10. Implementation log highlights

- Build original COMPLETO (2026-05-31): T-BE-1..7 + T-FE-1..3 + T-E2E results en story-folder. T-BE-7 = R2 provisioning `chris_manual_action` (bloquea solo E2E live-R2 — relevante para T-FE-bio-docs live-verify si R2 dev no está armado).
- T-FIX-1/T-FIX-2 (06-01): doctors-500 keystone + tenant-resolution.
- Regresiones 06-06 TODAS resueltas (checkpoint): bug#1 NuevoIntegranteModal useEffect loop ✅ · bug#2 client base absoluta→CORS (fix: `API_BASE=''` relativo en client; SSR mantiene absoluto — patrón a respetar en hooks nuevos) ✅ · bug#3 mutations 403 (fix: `buildStaffMutationHeaders` + RBAC widened {owner, admin_clinic}) ✅ · bug#4 useCurrentUser global-role (opción B targeted) ✅.
- Patrón "contrato imaginado" ya mordió 4 veces en esta story — D3-C es la 4ª instancia (FE duplicando lógica BE). Por eso el delta exige consumo BE-SSoT + contract real.

## 11. Faithfulness gaps + validator findings

| Sev | Item |
|---|---|
| MEDIUM | Validator ejecutado IN-PROCESS (este entorno no expone Agent tool — spawn imposible). Probes adversariales sí corrieron (keywords distintos, 7 probes + 3 re-checks + 1 web re-check) — ver `CONTEXT-BRIEF-validation.md`. Independencia reducida → flag `partial`, no `clean` |
| MEDIUM | **F-1 download bio-files es GREENFIELD total:** assets proxy = upload-only (`POST /upload` única ruta); D-3 in-file: "presigned upload does NOT exist in luana-core-assets". El arch (§3.1) asume "presigned/proxy URL via AssetsService" — builder T-BE-bio-docs debe verificar la superficie real de `AssetsService` (engine, consume read-only) y construir el mecanismo (GET proxy stream boto3 `get_object` o presigned-GET brand-local). No hay precedente de lectura de assets en clinics |
| LOW | F-2: `slugify` BE no existe (util nuevo para backfill 041) · F-3: prior art OG/no-auth en `app/public/[clinic-slug]/{page,booking}` (generateMetadata + `/public(.*)` ya público — copiar patrón para `/d/**`) |
| LOW | Cap `lisa.doctores` dev_preview STALE (apunta a `components/doctores/` inexistente — §4.5) · Tickets dicen "middleware.ts", archivo real = `src/proxy.ts` (§14.1) |
| LOW | §6 git diff solo resumido (354 files = branch multi-story; el delta no tiene código aún — verificado por greps 0-hit) · 01-spec L1-464 (build original) NO re-leído este run (FOCUS delta; el brief original 05-31 lo cubría y fue sobrescrito — para contexto del build original usar 03-arch.md/-be/-fe directos) |

## 12. Raw paths consulted

Story-folder: `01-spec.md` (L465-673 delta) · `03-arch-delta.md` (completo) · `checkpoint.md` (L1-170: frontmatter + scope_extension + regresiones) · `06-tickets.yaml` (L295-643 delta) · `04-validators.yaml` (greps schema v5 + delta_v3) · `dispatch-plan.md` (completo) · `mockups/` (listing: doctores.html + _shared.css).
Código: `core/@luana/ui-kit/src/` (listing + greps) · `clinics/{api,application}/` (listing) · `assets_proxy_router.py` · `availability_projection_service.py` · `AvailabilityCalendar.tsx` · `staff.ts` · `doctor_repository.py` (greps) · `proxy.ts` (L30-110) · `clinic_model.py:39` · `alembic/versions/` (tail).
Harness: canon `design-system-canon.md` (L63-102, L183-222) · skills `{backend-expert,frontend-expert,vitalia-design-system}/SKILL.md` (completos) · rules quality/fsd/visual-fidelity (headers) · overlay vitalia (hipaa-lite, ADR-003, ADR-004 — auto-cargados).
Tooling: `scripts/resolve_cap.py vitalia lisa.doctores --extract`.

## 13. Verbatim grep + WebFetch commands executed

```bash
WS=/home/chalreme/Proyectos/luana-vitalia
# §7 keyword scan (brand-scoped vitalia + core/@luana — cero cross-brand)
grep -rln "EntityPicker\|EntitySubNavBar\|EntityWorkspaceLayout" $WS/core/@luana/ui-kit/src/ $WS/vitalia/frontend/src/
grep -rn "entityIdentitySlot" $WS/core/@luana/ui-kit/src/ $WS/vitalia/frontend/src/        # 0 hits
grep -n "recurrentBlockVisibleInWeek\|endConditionKind" $WS/.../horarios/AvailabilityCalendar.tsx
grep -n "rrule\|byweekday\|interval\|count=\|until=" $WS/.../availability_projection_service.py
grep -n "_VALID_KINDS\|credential_doc\|bio_doc" $WS/.../assets_proxy_router.py
grep -n "useAvatarUpload\|useBioFile\|bioFiles" $WS/.../features/lisa/api/staff.ts
grep -rn "bio_public\|BioPublic\|public_profile\|public_slug\|bio_generated_at" $WS/.../clinics/
grep -rn "format_recurrence_summary\|formatRecurrenceSummary" $WS/vitalia/ $WS/core/       # solo docs
grep -rn "availability-occurrences\|useAvailabilityOccurrences" $WS/vitalia/               # 0 en código
grep -rn "matcher\|isPublicRoute" $WS/vitalia/frontend/src/proxy.ts
ls $WS/vitalia/backend/alembic/versions/ | sort | tail -5                                  # 039 latest
git log --oneline main..HEAD | wc -l && git diff main..HEAD --stat | tail -3
$WS/.venv/bin/python $WS/scripts/resolve_cap.py vitalia "lisa.doctores" --extract
# WebFetch: https://dateutil.readthedocs.io/en/stable/rrule.html (count/interval/until semantics)
```

## 14. Free-form notes — what I'd tell a smart colleague

1. **`middleware.ts` NO EXISTE — es `src/proxy.ts`** (Next 16 renombró middleware→proxy; el header del archivo lo documenta). El deliverable de T-FE-pagina-publica que dice "middleware.ts matcher EXCLUDE /d/**" se ejecuta agregando `"/d/(.*)"` al array `isPublicRoute = createRouteMatcher([...])` (~L33-50 de proxy.ts), NO tocando `config.matcher` (ese atrapa todo por diseño). Patrón a imitar: `"/showcase(.*)"`.
2. **`doctor_repository.py` usa raw SQL con listas de columnas explícitas** (≥6 statements citan `bio_public` y columnas hermanas). La migración 041 agrega `public_profile`, `bio_generated_at`, `public_slug` → hay que tocar CADA SELECT/INSERT/UPDATE del repo o las columnas nuevas jamás se leen/persisten. Es el lugar más fácil para un verde-falso (unit con mock no lo caza — usar test repo real, learning 2026-06-11 mocked-service-tests).
3. **`assets_proxy_router.py` L70**: el mensaje de error 422 hardcodea "kind debe ser 'avatar' o 'credential_doc'" — actualizarlo al agregar `bio_doc` (string user-facing → Spanish neutro).
4. **dateutil confirma la semántica que el arch asume** (§15): `count` cuenta ocurrencias TOTALES cross-weekday (RN-D3F-2 OK nativo) y `until` es INCLUSIVO por igualdad — pero `until` compara datetime: para SC-D3C-3 (end_date inclusivo TZ tenant) el builder debe construir el datetime límite correcto (fin de día / borde TZ), no pasar date-medianoche a secas.
5. **Open_ended hoy es horizonte rolling +90d materializado** (docstring del service). El endpoint range (max 62d) debe proyectar dentro de `[from,to]` sin depender de qué se materializó — leer `project_block(reference_date=from)` con cuidado.
6. **CORS/base relativa**: hooks client nuevos (`useBioFileUpload`, `useAvailabilityOccurrences`) usan `API_BASE=''` relativo (patrón post bug#2); SSR (`staff-server.ts`, y el fetch del Server Component público `/d/`) usa base absoluta server-to-server. La página pública es Server Component → fetch directo al BE absoluto está OK ahí.
7. **Mutaciones staff requieren `buildStaffMutationHeaders`** (X-User-Role + X-User-ID) — bug#3. Los endpoints bio-files nuevos van a exigirlo igual.
8. **Orden BE obligatorio**: T-BE-occurrences-endpoint ANTES de T-BE-recurrencia-domain (ambos editan `availability_projection_service.py`; bucket `code:clinics` serializa). T-CORE puede ir paralelo (bucket core).
9. **T-CORE merge gated** pero la proposal YA está accepted (03-arch-delta § 16 OQ-1) — el wire FE puede buildear contra el core local y mergear después del lift.
10. **R2 live**: T-BE-7 original era provisioning manual Chris — si el dev stack no tiene R2 armado, el live-verify de T-FE-bio-docs escala (dispatch-plan caps: escalate si R2 creds faltan).
11. **Cap pointer stale** (§4.5): `dev_preview` apunta a `components/doctores/` inexistente — no confundirse buscando ahí; el código vive en `components/staff/`.
12. **Download de assets NO existe hoy** (finding validator F-1): el proxy assets es upload-only. Antes de implementar `GET .../bio-files/{id}/download`, inspeccionar `luana_core_assets.application.assets_service.AssetsService` real (import L175 del proxy como referencia de consumo) y decidir: stream proxy (get_object) vs presigned-GET brand-local. NUNCA editar el engine.
13. **Prior art página pública**: `app/public/[clinic-slug]/{page,booking/page}.tsx` ya hacen Server Component público + `generateMetadata` (y `/public(.*)` ya está en isPublicRoute) — copiar ese patrón para `/d/**` en vez de inventar.

## 15. Upstream canonical docs references

| Framework | URL | Relevancia para el delta |
|---|---|---|
| dateutil.rrule | https://dateutil.readthedocs.io/en/stable/rrule.html | **Fetched 2026-06-11.** Confirma: (1) `count` con `byweekday` múltiple limita ocurrencias TOTALES cross-weekday (ej. doc: Tue+Thu `count=10` → 10 fechas alternadas) — RN-D3F-2 es semántica nativa, sin código extra; (2) `interval=2` + WEEKLY = cada 2 semanas; (3) `until` INCLUSIVO ("greatest datetime less than or equal to until") — SC-D3C-3 depende de construir bien el datetime límite en TZ |
| Next.js `generateMetadata` (OG tags D3-D.2) | https://nextjs.org/docs/app/api-reference/functions/generate-metadata | Deferred — patrón estándar Server Component; builder consulta si duda (og:image absoluta requiere metadataBase o URL completa con APP_BASE_URL) |
| FastAPI / SQLAlchemy 2.0 / Pydantic v2 / React Query | (tabla canónica del agente) | Deferred — cero patrón novel (03-arch-delta § 15: todo reusa mecanismos in-tree verificados) |

## 16. Self-budget snapshot

- Tool calls: ~14 (6 Bash, 9 Read, 1 WebFetch, 10 Edit, 1 Write al cierre)
- Files leídos: 13 (≈95KB) · Greps: ~25 (todos brand-scoped) · Web fetches: 1
- Tokens consumidos estimados: ~55-65k input acumulado · brief ≈7k
- Turnos restantes: amplios (>100)

