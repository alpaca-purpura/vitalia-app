# T-1 result — npm install next-themes

**Ticket:** T-1
**Story:** vitalia-fase1-design-tokens-theme (F1-S1)
**State:** done

## Summary
Installed `next-themes` via `pnpm add next-themes --filter @luana/vitalia-web`.
Package added: `"next-themes": "^0.4.6"` (satisfies spec `^0.3.0` constraint).
`pnpm-lock.yaml` updated at workspace root.

## Validators GREEN
- `package.json` contains `next-themes` ✅
- No peer-dep conflicts blocking install (React 19 warnings are pre-existing workspace-wide) ✅

## Files modified
- `vitalia/frontend/package.json`
- `pnpm-lock.yaml`
