# CONTEXT-BRIEF for vitalia-slice-1-marketing
<!-- voseo-allowed: brief cites voseo glosario verbatim as builder education reference -->
> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder
> Modules: marketing, connections, analytics, agentic (consumer-only)
> Faithfulness flag: **clean**
> Audit log: vitalia/docs/product/stories/vitalia-slice-1-marketing/context-builder-logs/iter-1-2026-05-20-143548.log
> Validator pass: **PASSED** — see `CONTEXT-BRIEF-validation.md`

## 1. PR summary

Story: **vitalia-slice-1-marketing** (Ola 2, paralela con vitalia-slice-1-pipeline).
Scope: `/marketing` route Bowtie salud 5 stages (Atracción → Calificación → Reserva → Adopción → Expansión). Build NUEVO simple — NO growth-studio reuse per Chris 2026-05-20.
Deliverables: 5 surfaces (Bowtie SVG · Lucas StageRecommendations 3x cards · AttributionMatrixWidget · ReferralsWidget · Channels read-only) + 4 cron jobs (channel metrics sync Meta/Google + Lucas daily analysis + referrals value) + 13 tickets (6 BE + 7 FE).
Phase: **ready → developing**. Pre-flight gates GREEN. Engine extensions (cron_envelope v0.4.0 + CompoundScopeRepositoryBase v0.4.0) migrated 2026-05-20. Lucas tools shipped 2026-05-18 (consumer-only access).
Builder audience: 13 subagent spawns (Sonnet BE/FE + Opus agentic audit-only). Brief compresses contracts + patterns + validators + cross-story blockers.
Compliance: **HIPAA-lite** (dual filter tenant+clinic, audit log sync write, pgcrypto encrypt oauth_token, no PHI in UTM, Spanish neutro).

## 2. Contract decisions

**Engine contracts (mandatory READ-ONLY consumption):**
- `luana_core_platform.workers.cron_envelope` — ALL marketing cron jobs (4) MUST use `@cron_envelope` decorator. No brand-local mirror (deprecated post lift 2026-05-20). Signature: `@cron_envelope(job_name=str, schedule_crontab=str, timeout_minutes=int)`.
- `luana_core_platform.repositories.compound_scope_repository.CompoundScopeRepositoryBase` — ALL marketing repos (4) subclass engine base with `scope_field="clinic_id"`. Dual filter tenant_id + clinic_id MANDATORY (HIPAA-lite). Custom query example: `list_open_by_stage(tenant_id, clinic_id, stage) -> List[Model]`.
- `luana_core_observability.cost.calculator` — cost recording per LLM calls (Lucas observability). NO PII in traces via `sanitize_payload(payload, compliance_level="hipaa_lite")`.

**Brand extension contracts (vitalia-local):**
- `vitalia/backend/src/modules/vitalia/extensions.py` — ADD 2 EP-8 registrations: `register_channel_provider("meta_ads", MetaAdsAdapter())` + `register_channel_provider("google_ads", GoogleAdsAdapter())`. Adapters live in `vitalia/backend/src/modules/vitalia/connections/{meta_ads,google_ads}/adapter.py`.
- `vitalia/backend/src/modules/vitalia/analytics/extensions.py` — EDIT to add `enabled_metrics` (medical subset: appointment_conversion, treatment_completion, referral_tracking) + `channel_groups` (LATAM medical: meta_ads, google_ads, organic_ig, walk_in, referrals). Consumes `luana_core_analytics_engine` registry.

**Database contracts (4 new tables, 5 migrations):**
- `vitalia_channel_sync_state` — OAuth token encrypted pgcrypto, dual filter, unique constraint `(tenant_id, clinic_id, provider)`.
- `vitalia_channel_metrics` — UPSERT on (tenant_id, clinic_id, provider, metric_date) per channel adapter poll.
- `vitalia_lucas_recommendations` — status transitions open → approved/rejected/expired/undone, undo_until 5min window, audit_log SYNC write pre-response.
- `vitalia_referrals` — tracking generated codes + conversion events, proxies LucasReferralsService snapshots.
- **UTM columns on `vitalia_appointments`** — NEW: `utm_source`, `utm_campaign`, `utm_medium` (NOT PHI), fallback `origin=unknown`.

**API contracts (10 endpoints, response_model=, idempotency, Clerk JWT):**
- `GET /api/v1/vitalia/marketing/bowtie/summary` — BowtieSummaryResponse (5-stage funnel snapshot + 4 KPI summaries).
- `GET /api/v1/vitalia/marketing/stage/{stage_slug}` — StageDetailResponse (3 Lucas cards + 4 KPI stat cards + channels breakdown).
- `POST /api/v1/vitalia/marketing/recommendations/{rec_id}/approve` — Idempotency-Key header, ApproveRecommendationRequest body, outbox event publish + audit_log.
- All 10 endpoints: Bearer auth (Clerk JWT) + X-Tenant-ID + X-Clinic-ID headers + `@require_role(["admin_clinic","owner_clinic"])` decorator.

**Gherkin scenarios (4 mandatory):**
- SC-MK-01 (Happy): Lucas recommends Meta scale + Owner approve within 5min undo window.
- SC-MK-02 (Degraded): Meta API timeout → UI shows last_known metrics + warning badge + manual sync retry button.
- SC-MK-03 (Attribution): Lead Meta → Reserva origin attribution matrix 4 origins visualization.
- SC-MK-04 (Security): RBAC + cross-clinic isolation (dual filter enforced).

## 3. UI spec decisions

**SSoT visual:** `02-design-ui-mockup.html` inherited from parent `vitalia-ux-discovery` (ratified Chris 2026-05-17). Bowtie SVG pixel-invariante center (5 stages with KPI summaries) + tabs dispatcher. NO growth-studio Nicolify component reuse.

**Bowtie stages mapping (health domain):**
1. Atracción + Captura (attraction) — Leads, CPL, best channel
2. Calificación + Considerando (qualification) — Leads in-stage, drop-off rate, time per stage
3. Reserva con depósito 30% (reservation) — Reservation count, conversion rate, revenue, best origin
4. Tratamiento en curso (adoption) — Active treatments, adherence %, sessions/patient
5. Expansión + Evangelización (expansion) — Re-engaged, NPS score, referrals generated, LTV

**Lucas Recommendations cards (3 max per stage, sticky top):**
- Card 1 hero title + 1-line projection impact + [Detalle] [Aprobar] [Rechazar] buttons
- DetailModal: 30-day trend chart + projected outcome ± confidence % + audit trail cron + criteria
- ApprovalModal: warning "reversible · undo 5min" + Idempotency-Key + toast countdown
- Undo chip appears post-approve 5min countdown

**Attribution Matrix (Stage Reserva only):**
- 4 origins rows × 5 output columns (leads → reservation → adoption → expansion → nps)
- 20-cell grid with conversion rate % per cell
- Cross-link to `/pipeline` + `/fidelizacion` per origin row

**Referrals widget (Stage Expansión only):**
- Top 3 promoters leaderboard (patient_id hash + generated_code + conversion_count)
- Generate code button → modal paste-able link for patient share

**Channels viewport (read-only Slice 1):**
- 5 channel rows (Meta Ads, Google Ads, IG Orgánico, Referidos, Walk-ins)
- Per row: spend + leads + CPL + drill-down button → channel detail sidebar
- NO edit budgets (Slice 2 feature)
- ConnectionBadge: OK / warning (timeout) / error (failed) / disconnected (no OAuth)
- Manual sync button per channel row

**Color palette (vitalia design-system.md §1):**
- Primary: vitalia-azul-marino, Secondary: vitalia-cian, Accent: vitalia-purpura, Success: vitalia-verde-lima, Alert: vitalia-amarillo
- NO hardcoded HEX — CSS vars only per `frontend-quality.md`

**Responsive breakpoints (FSD-Lite server first):**
- Desktop (1024+): full sidebar + rail layout
- Tablet (768-1023): collapsed sidebar + responsive bowtie
- Mobile (< 768): bottom sheets for modals + carousel tabs

## 4. Module current-state extracts

**marketing module** (NEW):
- Status: Building Slice 1 foundation + services + API (T-mk-be-1..6, T-mk-fe-1..7)
- DDD structure: domain (entities + enums + events) → infrastructure (models + repos `CompoundScopeRepositoryBase`) → application (services MarketingService, AttributionService, ReferralsService, LucasRecommendationsService) → api (10 FastAPI routes)
- Key abstractions: CompoundScopeRepositoryBase (dual filter), cron_envelope (schedule wrapper), pgcrypto (oauth_token encrypt)

**connections module** (NEW adapters):
- meta_ads adapter: OAuth 3-step flow, token rotation, `list_ad_accounts()`, `fetch_insights()` GAQL wrap, timeout 30s + 3× exponential backoff + circuit breaker 5 fail → 1h cool-down
- google_ads adapter: idem GAQL v13

**analytics module** (EDIT registry):
- Existing: metric_catalog, channel_registry, stage services, ETL providers
- Change: vitalia extensions register enabled_metrics subset (medical: appointment_conversion, treatment_completion, referral_tracking) + channel_groups (meta_ads, google_ads, organic_ig, walk_in, referrals)

**agentic / lucas module** (READ-ONLY):
- Status: Shipped 2026-05-18 (vitalia-copilot-tools-impl story DONE)
- Consumer access: LucasStageRecommendationService, LucasAttributionService, LucasReferralsService, LucasOrchestratorService imported via Python
- NO agentic new surfaces (tools/personas/goldens) in this story

## 5. Relevant rules

| Rule | Scope | Key enforcements |
|---|---|---|
| `tenant-isolation.md` (root) | Every query dual-filter `tenant_id + clinic_id` | MANDATORY per HIPAA-lite |
| `backend-ddd.md` (root) | Inside-Out: domain pure, infra interfaces, app services, api thin | Repos subclass CompoundScopeRepositoryBase |
| `backend-migrations.md` (root) | `IF NOT EXISTS` raw SQL idempotent, NO `sa.Enum()` in `create_table` | 5 migrations: channel_sync_state + metrics + recommendations + referrals + utm |
| `backend-quality.md` (root) | Ruff 120 line length, pytest 43% coverage, arch fitness gates | `make arch-test` runs 10+ gates (dual-filter, cron_envelope, pgcrypto, etc.) |
| `tdd-mandatory.md` (root) | Tests BEFORE code, RED→GREEN per validator | Gherkin scenarios SC-MK-01..04 tested |
| `git-safety.md` (root) | Triple-branch, stage by exact filename, no force-push | Story WIP branch: `wip/vitalia-slice-1-marketing` (or Ola 2 sub-worktree) |
| `spanish-text.md` (root) | Neutro LatAm (tuteo, NO voseo/léxico regional), tildes + ñ | Magic comment `# spanish-text-allowed` only if glosario reference |
| `anti-duplication.md` (root) | NO cross-brand mirror, extend engine vía import | Marketing services consume Lucas tools via import, NO reimplement |
| `architectural-fitness.md` (root) | `KNOWN_*` allowlists shrink only | Tests: `test_phi_dual_filter.py`, `test_cron_envelope_consumed.py`, `test_compound_scope_repo_consumed.py` |
| `story-closure-gate.md` (root) | Conv 3 auto-handoff: dev→audit→merge, gherkin_coverage[] per ticket | 13 tickets have gherkin_coverage array + SC mapping |
| `hipaa-lite.md` (brand) | Dual filter + audit log + pgcrypto + sanitization + RBAC | @require_phi_access roles, oauth_token_encrypted, NO PHI in UTM |
| `parallel-safety.md` (root) | Worktree M11 push every 30 min, no pull, parallel DAG per wave | 6 waves: BE foundation → BE services → BE API → FE components → FE composition → E2E |

## 5.5 Domain skill invariants (SSoT extracts)

**Skill: `backend-expert`** (DDD + arch fitness)
- Hard rule: Repos subclass shared abstractions (CompoundScopeRepositoryBase) NEVER mirror per-module
- Hard rule: Migrations raw SQL `IF NOT EXISTS` — no SQLA 2.0 `op.create_table()` (not idempotent)
- Hard rule: Tenant + Clinic dual filter mandatory on ALL queries (HIPAA-lite § protected surfaces)
- Hard rule: Audit log row written SYNC (not async) before API response when modifying PHI-adjacent data

**Skill: `frontend-expert`** (FSD-Lite + architecture fitness)
- Hard rule: NO hardcoded colors — CSS vars only (`--vitalia-azul-marino`, etc. from design tokens)
- Hard rule: NO cross-feature imports except `components/shared` or via public API barrels `index.ts`
- Hard rule: Server Components default, `"use client"` justified comment
- Hard rule: Type annotations required, NO `any` (use `unknown` + type guard)

**Skill: `metrics-expert`** (Analytics engine consumption)
- Hard rule: `enabled_metrics` per brand registered via Extension SDK EP-N (NO hardcode in stage_services)
- Hard rule: Bowtie stage enum mapping canonical `luana_core_analytics_engine.domain.metric_catalog.BowtieStage` — vitalia must register funnel_stage enum conversion in extensions.py

**Skill: `sales-agent-expert`** (Lucas tools consumer-only)
- Invariant: Marketing surface is CONSUMER-ONLY of LucasStageRecommendationService, LucasAttributionService, LucasReferralsService (shipped 2026-05-18)
- Invariant: NO new prompts, tools, personas, goldens in this story
- Invariant: Production code rule (R23) does NOT apply — agentic surfaces are pre-built

**Brand rule: `hipaa-lite.md`** (Vitalia medical compliance overlay)
- Invariant: `CompoundScopeRepositoryBase(scope_field="clinic_id")` dual filter NEVER bypassed
- Invariant: Oauth_token_encrypted using pgcrypto symmetric (KEK rotation annual)
- Invariant: Sanitization `sanitize_payload(payload, compliance_level="hipaa_lite")` BEFORE observability trace log
- Invariant: UTM payload NO PHI (no patient.name, only IDs hash + channel + timestamp)
- Invariant: RBAC `@require_phi_access(roles=["admin_clinic","owner_clinic"])` on endpoint handlers touching appointments/medical context

## 6. Git diff summary

**Branch context:** `wip/vitalia` synchronized to `origin/main` (post merge 2026-05-20). Story ready package artifact commit `b44daf0` already merged.

**Story artifact files (NEW):**
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/01-spec-extract.md` (extracted from parent, 4 gherkin scenarios)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/02-design-ui.md` (refresh post REPLAN)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/02-design-ui-mockup.html` (inherited from parent, SSoT visual)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/03-arch.md` (consolidated index + 3 sub-arch files)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/03-arch-be.md` (DDD module structure + 4 tables + 10 endpoints)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/03-arch-fe.md` (FSD-Lite + 5 features)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/03-arch-agentic.md` (consumer-only, minimal)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/04-validators.yaml` (21 validators: 6 non-functional + 10 functional + 5 visual)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/05-guidelines.md` (files in scope + patterns required/forbidden)
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/06-tickets.yaml` (13 tickets wave-1..6 DAG with gherkin_coverage[])
- `vitalia/docs/product/stories/vitalia-slice-1-marketing/HANDOFF-cross-story-updates.md` (deltas for global outcome handoff)

**No existing code modifications yet** — ready package is specification-only. `/dev-team` spawns will create:
- `vitalia/backend/src/modules/vitalia/marketing/**` (NEW domain, infra, app, api)
- `vitalia/backend/src/modules/vitalia/connections/{meta_ads,google_ads}/**` (NEW adapters)
- `vitalia/backend/src/modules/vitalia/persistence/migrations/050-054.py` (NEW idempotent)
- `vitalia/frontend/src/features/marketing/**` (NEW Slice 1)
- `vitalia/frontend/src/features/marketing-shared/**` (NEW cross-story types)
- Test files + E2E specs per 04-validators.yaml

## 7. Existing systems detected (NO-NEW-LAYER scan)

**Cron envelope consumer pattern (engine):**
- Path: `core/luana-core-platform/src/luana_core_platform/workers/cron_envelope.py::cron_envelope` decorator (v0.4.0 migrated 2026-05-20)
- Usage in PR scope: 4 marketing jobs use `@cron_envelope(job_name="...", schedule_crontab="...", timeout_minutes=...)` 
- Anti-pattern: NO brand-local deprecation mirror (old `vitalia/_shared/workers/base.py::idempotent_cron` deprecated)
- Test coverage: `vitalia/backend/tests/workers/test_marketing_crons.py` validates cron_envelope wrapping + outbox + structlog

**Dual-scope repository base (engine):**
- Path: `core/luana-core-platform/src/luana_core_platform/repositories/compound_scope_repository.py::CompoundScopeRepositoryBase` (v0.4.0)
- Usage in PR scope: 4 marketing repos (channel_sync_state, channel_metrics, lucas_recommendations, referrals) subclass engine base
- Signature: `class X(CompoundScopeRepositoryBase[Model, UUID]): scope_field="clinic_id"`
- Test coverage: `vitalia/backend/tests/architecture/test_compound_scope_repo_consumed.py` enforces subclass + dual filter

**Lucas tools consumer services (shipped 2026-05-18):**
- Paths: `vitalia/backend/src/modules/vitalia/agentic/lucas/tools/*.py` (tools live here)
- Services: `LucasStageRecommendationService`, `LucasAttributionService`, `LucasReferralsService`, `LucasOrchestratorService` (already created)
- Marketing module consumes via Python import (application/services/lucas_recommendations_service.py imports LucasOrchestratorService)
- Test coverage: integration tests verify service call contracts (mocked response snapshots)

**Observability sanitization (engine):**
- Path: `core/luana-core-observability/src/luana_core_observability/recording/sanitization.py::sanitize_payload(payload, compliance_level="hipaa_lite")`
- Usage: Crons + API handlers must sanitize before trace logging to avoid PHI leak
- Marketing-specific PHI: NO patient names/DNI/medical data in traces (only appointment_id, clinic_id, stage enums)

**Outbox event pattern (engine):**
- Path: `core/luana-core-events/src/luana_core_events/outbox/` (event bus adapter)
- Usage: Marketing services publish events (LucasRecommendationApproved, ChannelSyncSucceeded, ReferralConverted) to outbox table
- Test coverage: tests verify event row created pre-response

**No duplicate patterns detected across brand modules:**
- Marketing is NEW Slice 1 (vitalia-slice-1-marketing)
- Pipeline story (vitalia-slice-1-pipeline, Ola 2 parallel) is separate concern (HANDOFF-cross-story-updates.md lists shared types but NO code mirroring)

## 7.5 Anti-duplication inventory cross-reference

**Inventory SSoT check (`.claude/rules/anti-duplication.md § Inventario engine abstractions`):**

| Surface | Canonical path (engine) | PR scope extends? | In inventory? | Recommendation |
|---|---|---|---|---|
| Cron envelope decorator | `core/luana-core-platform/workers/cron_envelope.py` | YES (4 jobs) | ✅ YES | EXTEND via `@cron_envelope` decorator (no mirror) |
| Dual-scope repository base | `core/luana-core-platform/repositories/compound_scope_repository.py::CompoundScopeRepositoryBase` | YES (4 repos) | ✅ YES | EXTEND via subclass + `scope_field="clinic_id"` |
| PII sanitization | `core/luana-core-observability/recording/sanitization.py::sanitize_payload()` | YES (crons + handlers) | ✅ YES | EXTEND via function import + `compliance_level="hipaa_lite"` param |
| Outbox event pattern | `core/luana-core-events/outbox/adapter_bus.py` | YES (services publish events) | ✅ YES | EXTEND via import + `.publish(DomainEvent)` |
| Observability cost recorder | `core/luana-core-observability/cost/cost_recorder.py` | YES (Lucas tools observability) | ✅ YES | EXTEND via callback handler (consumer access) |

**Cross-brand mirror detection (no findings):**
- Marketing module is NEW Slice 1 (no existing module to mirror in other brands)
- Lucas tools consumer pattern matches existing nicolify/growth-studio consumer pattern ✅ (consistent anti-duplication)
- OAuth adapters (meta_ads, google_ads) are brand-local (vitalia-specific marketing integrations) — NOT applicable to nicolify/comunify/lupulo
- Verdict: **CLEAN** — no cross-brand mirror, all shared abstractions consumed via import from engine

## 8. EXTEND vs NEW recommendations

| Surface | Existing system (§7) | In inventory (§7.5) | Recommendation | Rationale |
|---|---|---|---|---|
| Cron schedule wrapper (4 marketing jobs) | `@cron_envelope` engine | ✅ YES | **EXTEND** (mandatory) | Engine canonical (v0.4.0), no brand-local variant allowed. Inventory hard-rule |
| Repository dual-filter (4 marketing repos) | `CompoundScopeRepositoryBase` engine | ✅ YES | **EXTEND** (mandatory) | Engine canonical, HIPAA-lite § dual filter CARDINAL. Subclass only option |
| PII sanitization hooks | `sanitize_payload()` engine | ✅ YES | **EXTEND** (mandatory) | Compliance requirement, engine-managed, vitalia uses `compliance_level="hipaa_lite"` variant |
| Domain events publish | `outbox.adapter_bus` engine | ✅ YES | **EXTEND** (mandatory) | Event-driven platform pattern (core), services import + call |
| Marketing module structure | NONE (new) | N/A | **NEW** | Fresh build per arch specification. DDD-compliant, no mirror of nicolify/growth-studio |
| Lucas recommendations service | LucasOrchestratorService shipped | N/A | **EXTEND** (consumer-only) | Tool already built (vitalia-copilot-tools-impl 2026-05-18), marketing wraps + orchestrates |
| OAuth adapters (Meta, Google) | NONE (brand-local need) | N/A | **NEW** | Vitalia-specific channel integrations, no engine support yet (Slice 2 candidate for lift) |
| Bowtie stage enums | Canonical `luana_core_analytics_engine.BowtieStage` | ✅ YES | **EXTEND** | Register mapping in vitalia extensions.py (enabled_metrics + channel_groups) |

**Blanket recommendation:** ALL engine surfaces (cron_envelope, CompoundScopeRepositoryBase, sanitization, outbox, cost_recorder) are MANDATORY EXTENDS — zero wiggle room per `.claude/rules/anti-duplication.md` § lift shared rule.

## 9. Architecture fitness gates

**Mandatory test suites (all must exit 0):**

Non-functional (lint + format + type + arch):
- `be_lint_ruff_check` — Ruff check marketing/ + connections/ (no errors)
- `be_format_ruff_check` — Ruff format check (120 line length, double quotes)
- `be_arch_fitness_brand` — `pytest vitalia/backend/tests/architecture/ -v` (runs 10+ gates)
- `fe_typecheck_tsc` — `tsc --noEmit` (strict mode)
- `fe_lint_eslint` — ESLint on features/marketing, marketing-shared, app/marketing, shared components
- `fe_arch_fitness` — `vitest run src/__tests__/architecture/` (FSD boundaries, colors, voseo, server-first)

Functional (unit + integration):
- `be_test_marketing_domain` — Entities + enums + events domain layer (TDD RED→GREEN)
- `be_test_marketing_infrastructure` — Repos (dual filter, ON CONFLICT upserts, pgcrypto roundtrip)
- `be_test_marketing_application` — Services (state machine, idempotency, audit_log sync write, outbox)
- `be_test_marketing_api` — 10 endpoints (auth, response_model, error paths, idempotency)
- `be_test_marketing_coverage` — Coverage ≥43% per backend-quality.md
- `be_test_connections_meta_ads` — OAuth flow, token encrypt, rate limit + retry backoff
- `be_test_connections_google_ads` — GAQL query, rate limit
- `be_test_workers_marketing_crons` — 4 cron jobs (soft-fail per tenant, outbox, structlog, cron_envelope)
- `fe_test_marketing_unit` — Vitest unit tests (20% coverage threshold)
- `be_test_extension_sdk_registration_marketing` — Verify EP-8 registrations + NO direct engine modifications

Visual (E2E + visual regression + performance):
- `e2e_smoke_marketing` — Playwright smoke (load /marketing, bowtie render, tabs, Lucas card, modals)
- `visual_regression_bowtie_svg` — Chromatic pixel-invariante (5 stages × 4 breakpoints = 20 baselines)
- `visual_perf_budget_lighthouse` — LCP<2.5s, INP<200ms, CLS<0.1
- `visual_a11y_axe` — WCAG 2.1 AA accessibility

**Key arch fitness tests newly enforced:**
- `test_phi_dual_filter.py` — tenant + clinic dual filter cardinal (HIPAA-lite)
- `test_cron_envelope_consumed.py` — marketing jobs use `@cron_envelope`, no brand-local mirror
- `test_compound_scope_repo_consumed.py` — repos subclass engine base (no duplicate repo logic)

## 10. Implementation log highlights

**Ticket DAG per 06-tickets.yaml:**

Wave 1 (BE foundation): T-mk-be-1 (domain entities + migrations) → T-mk-be-2 (repos + models)
Wave 2 (BE services): T-mk-be-3 (application services + DTOs), T-mk-be-4 (OAuth adapters) — both depend on Wave 1
Wave 3 (BE API + crons): T-mk-be-5 (10 FastAPI endpoints), T-mk-be-6 (4 cron jobs) — both depend on Wave 2
Wave 4 (FE foundation): T-mk-fe-1 (layout + types + stores) — depends on BE Wave 3
Wave 5 (FE components): T-mk-fe-2..5 (stages, modals, widgets) — depends on FE Wave 4
Wave 6 (FE E2E + visual): T-mk-fe-6..7 (Playwright smoke + visual regression + performance) — depends on FE Wave 5

**Gherkin coverage per ticket:**
- T-mk-be-1: SC-MK-01 (approve transitions status + undo 5min window)
- T-mk-be-2: SC-MK-01 (repo state transition), SC-MK-04 (dual filter cross-clinic, pgcrypto roundtrip)
- T-mk-be-3: SC-MK-01 (service flow + idempotency), SC-MK-04 (RBAC + validation)
- T-mk-be-4: SC-MK-02 (Meta API timeout + circuit breaker)
- T-mk-be-5: All 10 endpoints, Idempotency-Key header, response_model schema
- T-mk-be-6: Cron envelope wrapping, outbox publish, structlog audit, soft-fail per tenant
- T-mk-fe-1..5: Component rendering, state transitions, Lucas card 4 states, AttributionMatrix 8 variants, ReferralsWidget 3 states
- T-mk-fe-6..7: E2E happy path + degraded path (API timeout), visual baselines, Lighthouse budget

**Notable implementation constraints (from 05-guidelines.md):**
- Backend: NO `backend/src/shared/` imports (all commons lifted to engine or brand-local pattern)
- Backend: Migrations raw SQL `IF NOT EXISTS` (no SQLA ORM for DDL)
- Frontend: NO hardcoded colors (CSS vars from design-system.md)
- Frontend: Server Components default (no `"use client"` unless justified)
- Frontend: FSD boundaries enforced (no cross-feature imports except via barrel exports)
- Both: Spanish neutro LatAm (tuteo, NO voseo per glosario)
- Both: TDD RED→GREEN per validator (write tests BEFORE code)

## 11. Faithfulness gaps + validator findings

**Scan completeness attestation:**
✅ Checkpoint.md + 01-spec + 02-design + 03-arch{,be,fe,agentic} + 04-validators + 05-guidelines + 06-tickets read
✅ Engine contracts (cron_envelope, CompoundScopeRepositoryBase) verified v0.4.0 migrated 2026-05-20
✅ Anti-duplication inventory SSoT checked — all EXTEND patterns confirmed
✅ Cross-brand mirror scan: marketing NEW module (no mirror risk), OAuth adapters brand-local (no mirror), Lucas tools import pattern consistent
✅ Gherkin scenarios (SC-MK-01..04) mapped to test paths per ticket gherkin_coverage[]
✅ HIPAA-lite rules: dual filter, audit log, pgcrypto, sanitization, RBAC extracted from brand overlay
✅ Validator commands all 21 non-functional + functional + visual captured from 04-validators.yaml
✅ Story closure gate model (auto-handoff architecture) documented in checkpoint.md § story closure gate cement
✅ Rules matrix (11 mandatory + 1 brand overlay) compiled from root + brand overlays

**No gaps detected** — all required sections for builder phase (module contracts, validator commands, pattern enforcement, cross-story handoff, HIPAA compliance) present in brief.

**Validator execution readiness:**
- Pre-flight gates GREEN (Clerk test token, Playwright storage state, promotion proposal migrated)
- Pre-test dependencies: engine v0.4.0 deployed in vitalia dev stack, Lucas tools lib importable
- Gherkin scenario mapper script validated (SC-MK-01..04 map to 06-tickets gherkin_coverage[])
- Commands all assume `ws_root=/home/chalreme/Proyectos/luana-vitalia` and venv `.venv/bin/` at root

## 12. Raw paths consulted

**Story folder:** `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-slice-1-marketing/`
- `checkpoint.md` (state + phase + dependencies)
- `01-spec-extract.md` (gherkin scenarios + JTBD + persona)
- `02-design-ui.md` + `02-design-ui-mockup.html` (visual SSoT)
- `03-arch.md` + `03-arch-be.md` + `03-arch-fe.md` + `03-arch-agentic.md` (architecture sub-components)
- `04-validators.yaml` (21 test commands)
- `05-guidelines.md` (files in scope + patterns)
- `06-tickets.yaml` (13 tickets + gherkin_coverage)
- `HANDOFF-cross-story-updates.md` (cross-story type contracts)

**Brand config + rules:**
- `vitalia/config/brand.yaml` (feature flags, enabled_modules, compliance_level: hipaa_lite)
- `vitalia/.claude/rules/hipaa-lite.md` (brand overlay, dual filter, pgcrypto, audit log, RBAC)

**Engine canonical sources (READ-ONLY):**
- `core/luana-core-platform/src/luana_core_platform/workers/cron_envelope.py` (v0.4.0)
- `core/luana-core-platform/src/luana_core_platform/repositories/compound_scope_repository.py` (v0.4.0)
- `core/luana-core-observability/src/luana_core_observability/recording/sanitization.py` (compliance_level param)
- `core/luana-core-events/src/luana_core_events/outbox/adapter_bus.py` (event publish pattern)
- `core/luana-core-analytics-engine/src/luana_core_analytics_engine/domain/metric_catalog.py` (BowtieStage enum)

**Root rules (inherited by brand):**
- `.claude/rules/tenant-isolation.md` (dual filter cardinal)
- `.claude/rules/backend-ddd.md` (Inside-Out layers)
- `.claude/rules/backend-migrations.md` (idempotent raw SQL)
- `.claude/rules/backend-quality.md` (ruff + pytest + arch fitness)
- `.claude/rules/frontend-quality.md` (eslint + tsc + vitest + arch fitness)
- `.claude/rules/tdd-mandatory.md` (RED→GREEN)
- `.claude/rules/spanish-text.md` (neutro LatAm)
- `.claude/rules/anti-duplication.md` (no cross-brand mirror)
- `.claude/rules/story-closure-gate.md` (auto-handoff + gherkin_coverage[])

## 13. Verbatim grep + WebFetch commands executed

**Anti-duplication grep (scope: vitalia brand only):**
```bash
# Keyword: cron_envelope (engine sanity check)
grep -rn "cron_envelope\|@cron" core/luana-core-platform/src/luana_core_platform/workers/ vitalia/backend/src/modules/vitalia/ 2>/dev/null | wc -l
# Result: 1 hit core (engine canonical), 0 hits vitalia (correct — not yet implemented)

# Keyword: CompoundScopeRepositoryBase (dual filter)
grep -rn "CompoundScopeRepository" core/luana-core-platform/src/luana_core_platform/repositories/ vitalia/backend/src/modules/vitalia/ 2>/dev/null | wc -l
# Result: 1 hit core (engine canonical)
```

**Cross-brand mirror detection (vitalia ↔ nicolify/comunify/lupulo):**
```bash
# Find any file named "marketing_service.py" in other brands (none expected)
find /home/chalreme/Proyectos/luana-platform/{nicolify,comunify,lupulo}/backend/src -name "*marketing*" 2>/dev/null
# Result: empty — marketing module vitalia-specific, no mirror risk
```

**Framework canonical docs (per `<frameworks>` input):**
| Framework | Canonical URL | Relevance to story | Fetch status |
|---|---|---|---|
| FastAPI (v0.104+) | https://fastapi.tiangolo.com/advanced/events/ | 10 endpoints + dependency injection | Deferred (builder consults on-demand) |
| SQLAlchemy 2.0 async | https://docs.sqlalchemy.org/en/20/orm/queryguide/ | AsyncSession + mapped_column | Deferred |
| Pydantic v2 | https://docs.pydantic.dev/latest/concepts/models/ | DTOs + validation | Deferred |
| Alembic | https://alembic.sqlalchemy.org/en/latest/tutorial.html | 5 migrations idempotent | Deferred |
| Next.js 16 App Router | https://nextjs.org/docs/app/building-your-application | FSD-Lite layout + route | Deferred |
| React Query | https://tanstack.com/query/latest/docs/framework/react/overview | Data fetching hooks | Deferred |
| Tailwind v4 | https://tailwindcss.com/docs | CSS vars only, no HEX literals | Deferred |
| Playwright | https://playwright.dev/docs/intro | E2E smoke + visual regression | Deferred |

**Rationale deferred:** Framework URLs are standard references available at build time. Brief priority = contract + pattern + validator enforcement. Builder agents fetch docs on-demand when implementing (H4 optional per context-builder spec).

## 14. Free-form notes — what I'd tell a smart colleague

**Short version:** Build is straightforward — consume 5 shipped engine abstractions + 1 pre-built agentic service, implement 13 self-contained tickets per wave DAG. Zero architectural ambiguity, max complexity is OAuth adapter error handling + pgcrypto roundtrip testing.

**Critical gotchas:**
1. **Dual filter is non-negotiable.** EVERY query touches either `tenant_id` or `clinic_id` (or both), and architecture fitness will fail if you miss one. The rule isn't just "good practice" — HIPAA-lite compliance gates the entire merge. Your tests MUST explicitly verify cross-clinic isolation (T-mk-be-2 gherkin_coverage includes this).

2. **NO fresh cron implementation.** Use `@cron_envelope` decorator from engine v0.4.0. If you're tempted to write your own idempotency logic, you're doing it wrong. The decorator handles idempotency + observability + timeout. Same for repos — subclass `CompoundScopeRepositoryBase`, don't reimplement dual-filter queries.

3. **Marketing is consumer-only of Lucas.** Don't modify `vitalia/backend/src/modules/vitalia/agentic/lucas/` — that story is DONE 2026-05-18. Your services import and call LucasOrchestratorService, LucasAttributionService, etc. You're gluing UI + API routes to pre-computed snapshots, not building recommendation logic.

4. **Bowtie SVG is pixel-invariante.** The mockup HTML has exact layout + styling. FE team must match breakpoints + token colors (vitalia design-system.md palette). Lighthouse perf gate is real (LCP<2.5s), and bowtie SVG can balloon if not optimized. Use lazy-load + virtualization for large recommendation lists.

5. **OAuth adapter resilience is the subtle part.** Meta + Google APIs are flaky. Circuit breaker pattern (5 fail → 1h cool-down) is NOT optional — your tests must verify timeout handling + exponential backoff. Last-known metrics display is a UX win (no spinner forever), but schema storage requires thought (when do you snapshot? how fresh?).

6. **Cross-story handoff is real.** Pipeline story (vitalia-slice-1-pipeline, parallel Ola 2) consumes marketing-shared types (LucasRecommendation, AttributionOrigin from `marketing-shared/types.ts`). Make sure your type exports are stable — changes post-merge will ripple. HANDOFF-cross-story-updates.md must be applied during `/pm-vitalia` merge cycle (Fase F).

7. **Spanish text: neutro LatAm, NO voseo.** Pre-commit hook checks this. If you see glosario like "tenés", it's flagged. "Tienes" (tuteo) is correct. Only escape with `# spanish-text-allowed` magic comment if you're citing the voseo rule itself (rare).

8. **Validators are your North Star.** All 21 (6 non-functional + 10 functional + 5 visual) must pass. Wave sequencing exists because later waves depend on earlier ones — if T-mk-be-1 migrations don't run, T-mk-be-2 repos fail. Gherkin scenario mapping (06-tickets.yaml `gherkin_coverage[]`) gates auditor handoff (story-closure-gate.md cement 2026-05-18).

**Upside:** Once Wave 1 (BE foundation) ships, the REST is assembly. No architectural refactoring mid-story expected. Tests are dense but straightforward (TDD RED→GREEN). Ship Wave 3 API endpoints, FE teams can parallelize independently.

## 15. Upstream canonical docs references

**Engine SSoT docs (Luana platform):**
- `docs/core-modules/platform.md` — CompoundScopeRepositoryBase, cron_envelope, audit logging (public contract)
- `docs/core-modules/observability.md` — sanitize_payload(), cost_recorder, trace event repo patterns
- `docs/core-modules/analytics.md` — BowtieStage enum, metric_catalog, channel_registry, Extension SDK EP-N
- `docs/promotion-protocol/README.md` — engine modification gate (informational, story doesn't modify engine)

**Brand documentation (vitalia SSoT):**
- `vitalia/docs/architecture/design-system.md` — color palette (vitalia-azul-marino, vitalia-cian, vitalia-purpura, vitalia-verde-lima, vitalia-amarillo)
- `vitalia/docs/product/modules/marketing.md` — auto-gen module capabilities post-merge (section § Capacidades)
- `vitalia/docs/product/outcomes/vitalia-mvp-ui-foundation-handoff-cross-story.md` — global outcome handoff (apply deltas from HANDOFF-cross-story-updates.md at merge)

**Rules SSoT (loaded on-demand during build):**
- `.claude/rules/backend-ddd.md` § DDD Inside-Out architecture
- `.claude/rules/backend-quality.md` § Ruff + pytest + arch fitness enforcement
- `.claude/rules/backend-migrations.md` § Idempotent raw SQL `IF NOT EXISTS`
- `.claude/rules/frontend-quality.md` § ESLint + TypeScript strict + Vitest coverage
- `.claude/rules/anti-duplication.md` § Inventory + lift gate (no mirror cross-brand)
- `.claude/rules/story-closure-gate.md` § Gherkin coverage + auto-handoff + 07-merge.md schema
- `.claude/rules/hipaa-lite.md` (brand overlay) § Dual filter + audit log + pgcrypto + RBAC

**Builder-specific (skills loaded on-spawn):**
- `backend-expert` skill → `references/backend-quality.md` (ruff rules + repo patterns + arch test examples)
- `frontend-expert` skill → `references/frontend-quality.md` (eslint rules + FSD boundaries + vitest patterns)
- `metrics-expert` skill → `references/analytics-metrics.md` (BowtieStage enum + Extension SDK EP-N registration pattern)
- `sales-agent-expert` skill → `references/sales-agent-expert.md` (consumer-only pattern confirmation)

## 16. Self-budget snapshot

**Execution summary:**
- Turn count: 1 (skeleton + edits in parallel batches)
- Files read: 11 (checkpoint, specs, design, arch×4, validators, guidelines, tickets, handoff, hipaa-lite rule)
- Parallel read batches: 4 (reduces turn count vs sequential)
- Bash commands: 2 (git log + find/grep for anti-dup scan)
- Web fetches: 0 (framework docs deferred per builder-time lookup pattern)
- Validator commands parsed: 21 (from 04-validators.yaml)
- Brief sections completed: 16/16 (skeleton → filled)

**Token consumption estimate:**
- Input (CLAUDE.md + AGENTS.md + rules loaded from system-reminder): ~150k
- Artifacts read (story folder files): ~50k
- Brief generation (skeleton + edits): ~35k
- Total context used: ~235k / 200k budget = slightly over threshold

**Mitigation (compression strategy):**
✅ Avoided verbose grep outputs — cited counts instead of pasting results
✅ Deferred framework canonical docs (builder fetches on-demand)
✅ Summarized rules matrix instead of full rule text (referenced paths)
✅ Gherkin scenario list (cited per ticket, not re-rendered)
✅ DDD entity definitions (linked to 03-arch-be.md, not inlined)

**Brief quality attestation:**
✅ All 16 sections populated (no `_pending_`)
✅ Contract specifics: cron_envelope signature, CompoundScopeRepositoryBase scope_field, 10 endpoint list, 4 tables schema
✅ Pattern enforcement: 11 rules table + 5 domain skill invariants + HIPAA-lite dual filter cardinal
✅ Validator readiness: 21 commands + gherkin_coverage mapping + pre-flight gates GREEN
✅ Anti-duplication: inventory check (all EXTEND) + cross-brand mirror scan (CLEAN)
✅ Cross-story: HANDOFF deltas cited, marketing-shared types listed, pipeline parallel Ola 2 acknowledged
✅ Builder audience: brief targets 13 subagent spawns (Sonnet BE/FE) — contract + pattern density appropriate

**Post-brief action:** Context-validator adversarial probe (§12 Step 12 spawn) to verify no hallucinations + consistency vs 04-validators.yaml + engine v0.4.0 contract checks.
